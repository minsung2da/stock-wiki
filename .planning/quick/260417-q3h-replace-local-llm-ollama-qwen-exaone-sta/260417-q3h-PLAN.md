---
phase: quick
plan: 260417-q3h
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - CLAUDE.md
  - .env.example
autonomous: true
requirements:
  - INGEST-02
  - INGEST-03
  - INGEST-04
  - INGEST-05
  - INGEST-06
  - INGEST-07
  - INGEST-10
must_haves:
  truths:
    - "Roadmap Phase 5 reflects Claude-schedule-driven enrichment (no Ollama/Qwen/EXAONE)"
    - "REQUIREMENTS.md INGEST-02/-03/-04/-10 describe Claude schedule + sentence-transformers, not Ollama"
    - "PROJECT.md and CLAUDE.md no longer prescribe local LLMs or Ollama for ingest"
    - ".env.example contains no OLLAMA_* key"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 title + goal + success criteria rewritten"
      contains: "Claude Schedule"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INGEST-02/03/04/10 rewritten; traceability intact"
    - path: ".planning/PROJECT.md"
      provides: "Constraints Cost line + Key Decisions table updated"
    - path: "CLAUDE.md"
      provides: "Technology Stack table + §5 rewritten; Ollama block removed"
    - path: ".env.example"
      provides: "No OLLAMA_* keys present"
  key_links:
    - from: "REQUIREMENTS.md INGEST-10"
      to: "bge-m3 via sentence-transformers"
      via: "text update"
      pattern: "sentence-transformers"
    - from: "ROADMAP.md Phase 5"
      to: "Claude schedule extraction"
      via: "goal + success criteria rewrite"
      pattern: "Claude [Ss]chedule"
---

<objective>
Replace the local-LLM (Ollama/Qwen2.5/EXAONE) ingest stack with Claude-schedule-driven enrichment across all planning documents, and decouple bge-m3 embeddings from Ollama (route via sentence-transformers directly). Preserves the Phase 1 `anthropic`-ban in the ingest venv because Claude schedule runs as a separate agent via git round-trip, not as an API call from the ingest venv.

Purpose: User is a Claude Max subscriber. Paying separate Anthropic API bills for local-model substitutes contradicts the cost model. Local LLMs add GPU/Ollama infra burden with no value when Claude schedule already has the capacity.

Output: ROADMAP, REQUIREMENTS, PROJECT, CLAUDE, and .env.example all aligned with the Claude-schedule architecture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
@.env.example

**Architectural principle (locked):**
- systemd.timer → collectors write `vault/raw/**/*.md` (no LLM)
- Claude Schedule (RemoteTrigger, git round-trip) reads raw docs, writes `_derived` frontmatter block, commits back
- Local python (ingest venv) runs sentence-transformers directly for bge-m3 (1024-d) embeddings → Postgres
- Ingest venv still forbids `anthropic`/`openai` imports (Claude schedule is a separate agent outside the venv)
- No Ollama anywhere in the ingest path

**Scope:** documentation correction only. No new requirements, no code changes beyond `.env.example`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ROADMAP.md Phase 5 for Claude-schedule enrichment</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit .planning/ROADMAP.md:

1. In the phase summary list (around line 19), replace:
   `- [ ] **Phase 5: Local-LLM Enrichment with Korean Number Safety** - Ollama+Qwen/EXAONE extract _derived attributes, DART financials bypass the LLM, narrative numbers pass regex-LLM-Pydantic-checksum`
   WITH:
   `- [ ] **Phase 5: Claude-Schedule Enrichment with Korean Number Safety** - Claude schedule (git round-trip) extracts _derived attributes, DART financials bypass the LLM, narrative numbers pass regex-LLM-Pydantic-checksum`

2. Replace the entire Phase 5 detail block (starts at `### Phase 5: Local-LLM Enrichment with Korean Number Safety`, currently lines 81-92) with:

```
### Phase 5: Claude-Schedule Enrichment with Korean Number Safety
**Goal**: The ingest worker extracts `_derived` attributes (tickers, event_type, catalysts, sentiment, numeric_facts, summary) via a Claude Schedule agent that runs outside the ingest venv and commits enriched frontmatter back through git — not via local Ollama models. The ingest venv's `anthropic` ban is preserved because the schedule agent is a separate process. Korean financial numbers are kept out of free-form LLM extraction — DART financials go through `dart-fss` structured accessors; narrative numbers go through regex-LLM-Pydantic-checksum. Embeddings (bge-m3, 1024-d) are computed locally via sentence-transformers directly, with no Ollama dependency.
**Depends on**: Phase 4
**Requirements**: INGEST-02, INGEST-03, INGEST-04, INGEST-05, INGEST-06, INGEST-07
**Success Criteria** (what must be TRUE):
  1. A Claude Schedule agent (defined outside the ingest venv) polls `vault/raw/` for documents missing a `_derived` block, extracts attributes, and commits the enriched frontmatter back via git; the ingest venv itself contains no `anthropic`/`openai` imports and the CI guard (COLL-07) still passes
  2. The schedule agent writes only the `_derived` zone of frontmatter (provenance and ingest-state zones are write-protected against schedule writes)
  3. DART financial-statement numbers appearing in `_derived.numeric_facts` match values pulled directly from dart-fss structured accessors (no LLM involvement in those specific fields) on a 10-filing golden set
  4. News/report narrative numbers pass the four-stage pipeline (regex candidate extraction → Claude picks → Pydantic validates → digit-checksum compares to source); disagreements flag the doc for review instead of silent acceptance
  5. Re-running the schedule on the same unchanged document produces byte-identical `_derived` blocks; the three frontmatter zones remain non-overlapping
**Plans**: TBD
**Research flag**: NEEDS RESEARCH — Korean BM25 tokenizer benchmark on equity vocabulary (mecab-ko vs soynlp vs kiwipiepy), bge-m3 chunking strategy for long DART 사업보고서, VectorChord-BM25 Docker image availability (composite vs custom Dockerfile), Claude Schedule RemoteTrigger git round-trip latency for daily batch volumes
```

3. In the Progress table (around line 155), replace:
   `| 5. Local-LLM Enrichment with Korean Number Safety | 0/TBD | Not started | - |`
   WITH:
   `| 5. Claude-Schedule Enrichment with Korean Number Safety | 0/TBD | Not started | - |`
  </action>
  <verify>
    <automated>grep -c "Local-LLM Enrichment" .planning/ROADMAP.md | grep -qx 0 && grep -c "Ollama\|Qwen\|EXAONE" .planning/ROADMAP.md | grep -qx 0 && grep -q "Claude-Schedule Enrichment" .planning/ROADMAP.md && grep -q "sentence-transformers" .planning/ROADMAP.md</automated>
  </verify>
  <done>
ROADMAP.md contains "Claude-Schedule Enrichment" (phase title + progress row); contains zero occurrences of "Ollama", "Qwen", "EXAONE", "Local-LLM Enrichment"; Phase 5 success criteria reference Claude Schedule and sentence-transformers; requirement IDs (INGEST-02..07) preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite REQUIREMENTS.md INGEST-02/-03/-04/-10</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit .planning/REQUIREMENTS.md:

1. Replace INGEST-02 (currently at line 40):
   FROM: `- [ ] **INGEST-02**: 단일 \`llm_client.py\` 추상화가 Ollama 엔드포인트를 기본으로 하고 \`ALLOW_CLOUD_LLM=1\` + \`MAX_CLOUD_USD\` 가드 하에서만 Claude Haiku 4.5 폴백을 허용한다`
   TO: `- [ ] **INGEST-02**: \`_derived\` 추출은 ingest venv 내부가 아닌 별도 Claude Schedule 에이전트가 수행한다 — vault raw 문서를 git round-trip으로 읽고 enriched frontmatter를 커밋해 돌려놓는 방식. ingest venv에서는 \`anthropic\`/\`openai\` import가 여전히 금지(COLL-07 CI 가드 유지).`

2. Replace INGEST-03 (currently at line 41):
   FROM: `- [ ] **INGEST-03**: Ollama + Qwen2.5-14B-Instruct (Q4_K_M)가 기본 추출 모델로 동작한다`
   TO: `- [ ] **INGEST-03**: Claude Schedule 에이전트는 \`vault/raw/\` 하위에서 \`_derived\` 블록이 없는 문서를 주기적으로 감지하고 처리한다 (멱등: 이미 \`_derived\`가 있고 원문 content-hash가 동일하면 건너뜀).`

3. Replace INGEST-04 (currently at line 42):
   FROM: `- [ ] **INGEST-04**: EXAONE-3.5-7.8B가 한국어 특화 문서(뉴스 본문·리포트)에 사용된다`
   TO: `- [ ] **INGEST-04**: Claude Schedule 에이전트는 \`_derived\` 구역에만 쓰기 권한이 있으며 provenance·ingest-state 구역을 수정하면 ingest doctor가 drift로 리포트한다 (STORE-06 구역 무결성 유지).`

4. Replace INGEST-10 (currently at line 48):
   FROM: `- [ ] **INGEST-10**: bge-m3 임베딩(1024차원)이 Ollama를 통해 생성되고 Postgres \`chunks\` 테이블에 저장된다`
   TO: `- [ ] **INGEST-10**: bge-m3 임베딩(1024차원)이 sentence-transformers 라이브러리로 로컬에서 직접 생성(No Ollama)되고 Postgres \`chunks\` 테이블에 저장된다`

5. Leave the traceability table (Phase 5 mapping) unchanged — INGEST-02/-03/-04 stay mapped to Phase 5, INGEST-10 stays mapped to Phase 3.

6. Do NOT modify any other requirement IDs, any v2 section, or the "Last updated" footer (append a second footer line: `*Updated: 2026-04-17 — INGEST-02/03/04/10 rewritten for Claude Schedule + sentence-transformers*`).
  </action>
  <verify>
    <automated>grep -c "Ollama\|Qwen2\|EXAONE" .planning/REQUIREMENTS.md | grep -qx 0 && grep -q "Claude Schedule 에이전트" .planning/REQUIREMENTS.md && grep -q "sentence-transformers" .planning/REQUIREMENTS.md && grep -q "INGEST-02" .planning/REQUIREMENTS.md && grep -q "INGEST-10" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
REQUIREMENTS.md: zero occurrences of "Ollama", "Qwen2", "EXAONE"; INGEST-02/-03/-04 describe the Claude Schedule agent; INGEST-10 mentions sentence-transformers and "No Ollama"; traceability table still maps INGEST-02/03/04 to Phase 5 and INGEST-10 to Phase 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PROJECT.md and CLAUDE.md (Constraints + Key Decisions + Tech Stack + §5)</name>
  <files>.planning/PROJECT.md, CLAUDE.md</files>
  <action>
**Part A — .planning/PROJECT.md:**

1. Replace the Cost constraint line (line 67):
   FROM: `- **Cost**: 수집에 LLM 토큰 0. 인제스트는 로컬 모델 우선, 필요 시 Haiku 같은 저가 모델만 사용. Claude 메인 API 호출은 Claude Code 세션 내부에서만 발생`
   TO: `- **Cost**: 수집에 LLM 토큰 0. \`_derived\` 추출은 Claude Max 구독 기반 Claude Schedule 에이전트가 수행(별도 API 과금 없음). 로컬 Ollama/Qwen/EXAONE는 사용하지 않음. 임베딩은 sentence-transformers로 로컬 직접 계산.`

2. Replace the 7th row of the Key Decisions table (around line 83):
   FROM: `| 인제스트 LLM은 로컬 우선(Ollama+Qwen2.5/Llama3.3 + bge-m3 임베딩), 필요 시 Haiku 폴백 | 비용 최소화, 민감 데이터 로컬 처리, 야간 배치에 적합 | — Pending |`
   TO: `| 인제스트 \`_derived\` 추출은 Claude Schedule 에이전트(git round-trip)가 수행, 임베딩은 sentence-transformers 로컬 직접 (No Ollama) | 사용자 Claude Max 구독 기반이라 별도 API 과금 없음, 로컬 GPU·Ollama 인프라 부담 제거, ingest venv \`anthropic\` 금지 원칙 유지 | Decided 2026-04-17 |`

**Part B — CLAUDE.md (the one at /mnt/c/Users/minsu/workspace/stock/CLAUDE.md):**

1. In the TL;DR table, DELETE these two rows entirely:
   - `| Embeddings | **bge-m3** (via Ollama) | bge-m3:latest | HIGH |`
   - `| Local LLM (ingest) | **Qwen2.5-14B-Instruct** Q4_K_M (primary) + **EXAONE-3.5-7.8B** (Korean-heavy docs) | Ollama tags | HIGH |`
   - `| Ingest fallback | **Claude Haiku 4.5** | via Anthropic SDK | HIGH |`
   REPLACE with these two rows at the same location:
   ```
   | Embeddings | **bge-m3** via sentence-transformers (local, no Ollama) | BAAI/bge-m3 | HIGH |
   | Ingest extraction | **Claude Schedule** (git round-trip, Claude Max subscription) | n/a | HIGH |
   ```

2. DELETE the entire `## 4. Embeddings — Pick: bge-m3 via Ollama` section and replace with:
   ```
   ## 4. Embeddings — Pick: `bge-m3` via sentence-transformers (local)
   - **Multilingual top-tier.** Ranked #1 average on MIRACL (nDCG@10=70.0). Korean falls squarely within its training distribution.
   - **Route via sentence-transformers directly.** `from sentence_transformers import SentenceTransformer; model = SentenceTransformer("BAAI/bge-m3")`. No Ollama dependency, no separate embedding server process. Ingest venv imports and runs the model directly.
   - **8192-token context.** Handles full 공시 텍스트 without aggressive chunking.
   - **VRAM:** ~2GB at fp16, ~1GB at q4. CPU acceptable (~200ms/doc on x86, faster on Apple Silicon).
   - **Model version tracked in `chunks.embedding_model` column** so a future model swap triggers re-indexing.
   ```
   (Keep the comparison table of embedding alternatives as-is if present, but remove any "via Ollama" language from it.)

3. DELETE the entire `## 5. Local LLM for Ingest — Pick: Qwen2.5-14B-Instruct (primary) + EXAONE-3.5-7.8B (Korean-heavy)` section including all subsections 5.1 through 5.5 (hardware guidance, cost crossover with Haiku, etc.). Replace with:
   ```
   ## 5. Ingest Extraction — Pick: Claude Schedule (no local LLM)
   - **Why no local LLM.** User has a Claude Max subscription; routing ingest through a separate Anthropic API key would double-bill. Local Ollama/Qwen/EXAONE adds GPU + model-management burden for no quality gain over Claude.
   - **Architecture.** A Claude Schedule agent (RemoteTrigger) polls the repo on a cron, reads `vault/raw/**/*.md` documents lacking a `_derived` frontmatter block, extracts attributes (tickers, event_type, catalysts, sentiment, numeric_facts, summary), writes them back as frontmatter, and commits via git. The ingest venv never imports `anthropic` — the schedule agent is a separate process.
   - **Korean number safety unchanged.** DART financials bypass the LLM entirely (dart-fss structured accessors). Narrative numbers in news/reports still go through regex → LLM → Pydantic → digit-checksum.
   - **Cost.** $0 marginal per document for subscribers (subject to Claude Max schedule quotas). Compared to Haiku 4.5 (~$0.0035/doc) that's 100 docs/day × 365 = ~$130/year saved.
   - **Hard rule retained.** `ingest/` and `collectors/` directories must not import `anthropic`/`openai` (CI guard COLL-07). Any Claude-facing code lives outside the ingest venv.
   ```

4. In the "Alternatives Considered" table, DELETE rows referencing `Local LLM` (Qwen/EXAONE) and `Cloud LLM fallback` (Haiku/Haiku 3.5).

5. In the "What NOT to Use" table, DELETE the row `| Llama-3.3-70B local | 40GB+ VRAM needed | Qwen2.5-14B Q4 (or Haiku via API) |`. Leave the `text-embedding-ada-002` row.

6. In "Version Compatibility Notes", DELETE rows for `Qwen2.5-14B Q4_K_M` and modify the `bge-m3` row to read:
   `| bge-m3 | PyTorch >= 2.0 via sentence-transformers | 8192 context needs recent transformers |`

7. In "Confidence Summary", DELETE the row `| Qwen2.5-14B / EXAONE | ... |` and the row `| Claude Haiku pricing | ... |`. Replace with:
   `| Claude Schedule for ingest extraction | **HIGH** on direction; **MEDIUM** on quota fit | RemoteTrigger latency at daily batch volumes needs empirical verification |`

8. In the Installation Summary, DELETE the `# Ollama: LLMs + embeddings` line and the `# Install Ollama: https://ollama.com/download` line.

**Do NOT touch** other sections (DART, KRX, News, Scheduler, Storage, MCP, Obsidian, graphify, Git) — those are unrelated to this correction.
  </action>
  <verify>
    <automated>grep -c "Ollama\|OLLAMA\|ollama\|Qwen\|EXAONE" CLAUDE.md | grep -qx 0 && grep -q "Claude Schedule" CLAUDE.md && grep -q "sentence-transformers" CLAUDE.md && grep -c "Ollama\|Qwen\|EXAONE" .planning/PROJECT.md | grep -qx 0 && grep -q "Claude Schedule 에이전트" .planning/PROJECT.md</automated>
  </verify>
  <done>
PROJECT.md: Cost constraint rewritten, Key Decisions row 7 rewritten, zero mentions of Ollama/Qwen/EXAONE. CLAUDE.md: zero mentions of Ollama/OLLAMA/ollama/Qwen/EXAONE; §4 rewritten for sentence-transformers; §5 entirely replaced with Claude Schedule section; stack table + alternatives + veto + version + confidence tables cleaned.
  </done>
</task>

<task type="auto">
  <name>Task 4: Patch .env.example (confirm no OLLAMA_* keys, add comment)</name>
  <files>.env.example</files>
  <action>
The current `.env.example` does NOT contain an `OLLAMA_HOST` key (verified via grep). The user constraint assumed it did. Action:

1. Confirm by grep — if no OLLAMA keys exist, skip removal and only add a clarifying comment.

2. Append a new comment block above the `# Cloud LLM fallback` block (around line 17) clarifying that no Ollama config exists because ingest extraction goes through Claude Schedule, not a local Ollama server:

Insert these lines immediately before the existing `# Cloud LLM fallback (only for MCP server, NEVER for ingest)` line:

```
# Ingest extraction: handled by Claude Schedule agent (git round-trip),
# not a local Ollama server. No OLLAMA_HOST / OLLAMA_MODEL keys are needed.
# Embeddings run in-process via sentence-transformers (BAAI/bge-m3).

```

3. If (contrary to the initial grep) an `OLLAMA_HOST=` or `OLLAMA_*=` line is found, delete it.

Do NOT touch any other env key (DART, Postgres, FRED, ECOS, Anthropic fallback).
  </action>
  <verify>
    <automated>grep -ic "OLLAMA" .env.example | grep -qx 0 && grep -q "Claude Schedule agent" .env.example && grep -q "sentence-transformers" .env.example && grep -q "OPEN_DART_API_KEY" .env.example</automated>
  </verify>
  <done>
.env.example: zero case-insensitive occurrences of "OLLAMA"; includes new Claude Schedule comment block; sentence-transformers mentioned; pre-existing keys (OPEN_DART_API_KEY, DATABASE_URL, FRED_API_KEY, ECOS_API_KEY, ANTHROPIC_API_KEY) preserved.
  </done>
</task>

</tasks>

<verification>
Run all four automated checks from task `<verify>` blocks. Additionally:

```bash
# Cross-file sanity: no stray Ollama/Qwen/EXAONE left in planning + CLAUDE.md + .env*
grep -rE "Ollama|Qwen|EXAONE" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md CLAUDE.md .env.example 2>/dev/null | wc -l
# Expected: 0

# Claude Schedule wiring is visible in all four docs
grep -l "Claude Schedule" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/PROJECT.md CLAUDE.md
# Expected: 4 files

# sentence-transformers is the new embedding path in CLAUDE.md + REQUIREMENTS.md
grep -l "sentence-transformers" .planning/REQUIREMENTS.md CLAUDE.md .env.example
# Expected: 3 files
```
</verification>

<success_criteria>
1. Every file in `files_modified` passes its grep verification.
2. Cross-file sanity grep returns 0 occurrences of Ollama/Qwen/EXAONE across the five touched files.
3. Traceability table in REQUIREMENTS.md still maps all v1 requirements to exactly one phase (71/71 preserved).
4. Phase 1 CI guard (COLL-07: `anthropic`/`openai` forbidden in `ingest/`+`collectors/`) is explicitly reaffirmed in rewritten INGEST-02 and in CLAUDE.md §5.
5. `.env.example` loads without ambiguity about ingest extraction (Claude Schedule path is explicit).
</success_criteria>

<output>
After completion, create `.planning/quick/260417-q3h-replace-local-llm-ollama-qwen-exaone-sta/260417-q3h-SUMMARY.md` summarizing the five files changed, the architectural shift recorded, and any deviations from the plan.
</output>
