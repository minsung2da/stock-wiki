---
phase: quick-260924-ax5
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collectors/fundamentals/dart_roe.py
  - src/collectors/fundamentals/db_writer.py
  - src/collectors/fundamentals/__init__.py
  - src/db/entity_models.py
  - src/db/migrations/versions/0012_fundamentals_roe_provenance.py
  - src/db/explorer.py
  - src/db/explorer.html
  - tests/collectors/fundamentals/test_roe.py
  - tests/collectors/fundamentals/test_db_writer.py
  - tests/collectors/fundamentals/test_collect_fundamentals.py
  - tests/db/test_explorer.py
  - docs/collection.md
autonomous: true
requirements: [QUICK-SOURCED-ROE]
must_haves:
  truths:
    - "ROE is collected from official DART indices even when KRX fails or returns no data."
    - "Stored ROE records identify their annual period, source and observation timestamp."
    - "User sees ROE as a percentage and can sort it numerically in the explorer."
    - "ROE enrichment preserves existing PER, PBR, EPS, BPS, dividend yield and DPS values."
  artifacts:
    - path: src/collectors/fundamentals/dart_roe.py
      provides: Official DART annual ROE accessor with provenance
    - path: src/collectors/fundamentals/db_writer.py
      provides: ROE-only upsert preserving other stored metrics
    - path: src/db/migrations/versions/0012_fundamentals_roe_provenance.py
      provides: Additive nullable provenance fields
    - path: src/db/explorer.html
      provides: ROE percentage rendering and annual period display
  key_links:
    - from: src/collectors/fundamentals/__init__.py
      to: src/collectors/fundamentals/dart_roe.py
      via: ROE retrieval independently of the KRX result
    - from: src/collectors/fundamentals/db_writer.py
      to: fundamentals
      via: ROE-only typed NUMERIC update with provenance
---

<objective>Collect official sourced ROE for portfolio companies independently of KRX availability, retain its annual reporting context, and show sortable ROE percentages in the existing explorer.</objective>

<context>
@AGENTS.md
@.planning/research/redesign-2026-05.md
@src/collectors/fundamentals/roe.py
@src/collectors/fundamentals/db_writer.py
@src/collectors/fundamentals/__init__.py
@src/db/entity_models.py
@src/db/explorer.py
Parent-verified official API: fnlttSinglIndx.json, corp_code, bsns_year=2025, reprt_code=11011, idx_cl_code=M210000; ROE is idx_code=M211550, idx_val in percent. Samsung sample 10.783 percent maps to stored fraction 0.10783. Existing roe NUMERIC(10,6) and downstream contracts use fraction. Explicit reporting policy is preceding completed calendar year annual report (2025 for 2026 collection), not trailing-twelve-month or locally derived approximation. Migration head before this task is 0011_jev_reviews.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add official ROE source contract and provenance-preserving storage</name>
  <files>src/collectors/fundamentals/dart_roe.py, src/collectors/fundamentals/db_writer.py, src/db/entity_models.py, src/db/migrations/versions/0012_fundamentals_roe_provenance.py, tests/collectors/fundamentals/test_roe.py, tests/collectors/fundamentals/test_db_writer.py</files>
  <behavior>
    - Official ROE 10.783 percent becomes fraction 0.10783 without replacing it with a local income/equity calculation.
    - Missing official records/values remain unavailable; malformed data and request failures are distinguishable.
    - ROE-only writes preserve every pre-existing non-ROE metric and its collection metadata.
    - Repeated ROE enrichment is idempotent by ticker and fdate and retains correct period/source context.
  </behavior>
  <action>Parent owns this contract/storage task. Add fetch_annual_roe(corp_code, year) returning RoeObservation or None; observation fields are value (Decimal fraction), period_end (date), source (str), fetched_at (datetime). Keep compute_roe as a legacy helper but remove it from the active collector path. Read the existing DART key securely; use bounded HTTP timeout and validate status, index identity and finite numeric values. Select the prior completed year explicitly, using 11011 annual report and M211550 ROE only. Add nullable roe_source TEXT, roe_period_end DATE and roe_fetched_at TIMESTAMPTZ through an additive migration and matching ORM declarations; do not fabricate provenance for older computed values. Implement upsert_roe(engine, *, ticker, fdate, corp_code, observation) returning inserted, updated or skipped: change only ROE and its metadata on existing rows; new ROE-only rows use source dart_roe. Preserve PER/PBR/EPS/BPS/dividend_yield/DPS and existing KRX metadata. A valid absent result must not write a made-up zero. Tests use API fixtures and disposable Postgres.</action>
  <verify><automated>.\.venv\Scripts\python.exe -m pytest tests/collectors/fundamentals/test_roe.py tests/collectors/fundamentals/test_db_writer.py -q</automated></verify>
  <done>Official source and safe independent persistence are covered by tests; migration adds provenance without corrupting existing metric values.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Run ROE independently for each scoped company</name>
  <files>src/collectors/fundamentals/__init__.py, tests/collectors/fundamentals/test_collect_fundamentals.py, docs/collection.md</files>
  <behavior>
    - A KRX exception or empty frame still allows an official ROE write.
    - A DART failure or missing official ROE does not discard valid KRX metrics.
    - Missing entity codes and unavailable annual values are reported separately from successful ROE writes.
    - Collector statistics account for both independent paths without double-counting the scoped ticker total.
  </behavior>
  <action>Collector executor owns this task after consuming task 1 contracts. Separate KRX and DART retrieval/write paths inside each scoped ticker iteration so neither early continue nor exception prevents the other. Resolve corp_code before invoking official ROE retrieval; use the prior-year annual policy and existing fdate identity consistently. Report ROE success, unavailable and failure counts explicitly alongside existing collector stats. Do not synthesize ROE from stored EPS/BPS or historical income/equity. Preserve per-ticker failure isolation, existing metric collection and run reporting. Update tests and document annual reporting policy, fractional storage, percentage display, missing-data semantics and independent KRX behavior.</action>
  <verify><automated>.\.venv\Scripts\python.exe -m pytest tests/collectors/fundamentals/test_collect_fundamentals.py -q</automated></verify>
  <done>Regression tests prove ROE collection survives KRX errors and empty results, while valid KRX data survives DART failure.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Display sourced ROE and verify portfolio enrichment</name>
  <files>src/db/explorer.py, src/db/explorer.html, tests/db/test_explorer.py</files>
  <behavior>
    - Stored fraction 0.10783 displays as ROE 10.783% with its 2025 annual period.
    - Null ROE remains visibly missing; server-side asc/desc numeric sorting still places nulls last.
    - Detail view exposes source and observation timestamp without changing the stored fraction contract.
  </behavior>
  <action>UI worker owns explorer descriptors/rendering/tests; parent owns actual enrichment and final verification. Add ROE and roe_period_end to scalar sortable preview columns and expose roe_source and roe_fetched_at provenance. Format fractional ROE into percentage explicitly without accidentally converting dividend yield, which already uses percentage units. Preserve existing sorting/filter/pagination behavior. Parent applies tested additive migration, snapshots all existing non-ROE metrics, enriches the actual 198-company portfolio scope using the ROE-only path, and compares snapshots afterward. Existing snapshot rows are enriched with currently observed official annual values and their explicit observation timestamp; do not claim point-in-time historical reconstruction or arbitrary historical API availability. Record counts of successful official values, unavailable data and errors accurately; complete coverage means all scoped companies were attempted, not that every company necessarily exposes ROE. Verify table percentage, period and sorting with Aside's own CLI/MCP/REPL; no Orca or alternative browser-control fallback. If Aside cannot complete a check, identify the exact limitation and report that check unverified.</action>
  <verify><automated>.\.venv\Scripts\python.exe -m pytest tests/db/test_explorer.py -q</automated><human-check>Parent uses Aside directly to inspect ROE percent, period and sorting after actual enrichment, and verifies database snapshots show unchanged other metrics.</human-check></verify>
  <done>Explorer exposes correctly scaled, sourced annual ROE; portfolio attempt outcomes and preservation evidence are recorded.</done>
</task>
</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| Wrong index, year or unit | mitigate | Exact official index/report validation, explicit prior-year policy, percent-to-fraction tests |
| Financial data overwritten during enrichment | mitigate | Dedicated ROE-only writer and pre/post non-ROE snapshot comparison |
| API key disclosure or unbounded requests | mitigate | Secure existing environment key, bounded timeout, sanitized failures |
| Misleading unavailable values | mitigate | No synthetic zero, explicit missing/error statistics and nullable display |
</threat_model>

<source_audit>
GOAL/REQ QUICK-SOURCED-ROE: collected ROE and sortable table — tasks 1–3. RESEARCH: official endpoint evidence supplied by parent, typed financial storage and provenance — task 1. CONTEXT: preceding-year annual policy, independent KRX errors/empty data, preservation, 198-company attempt, ROE percent/period — tasks 1–3. User browser constraint: Aside-only verification — task 3. No deferred features or package installs.
</source_audit>

<success_criteria>Official sourced annual ROE is independently collected; storage keeps fractional units and provenance; existing unrelated financial values remain unchanged; explorer shows percentage/period and correct sorting; tests and live attempt counts are documented.</success_criteria>
<output>Create 260924-ax5-SUMMARY.md alongside this plan with migration, tests, portfolio outcomes, preservation comparison and Aside verification evidence.</output>
