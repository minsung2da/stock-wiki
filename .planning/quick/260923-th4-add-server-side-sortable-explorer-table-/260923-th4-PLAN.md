---
phase: quick-260923-th4
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [src/db/explorer.py, src/db/explorer.html, tests/db/test_explorer.py, docs/database-explorer.md]
autonomous: true
requirements: [QUICK-TABLE-SORT]
must_haves:
  truths:
    - "User sorts PER and PBR numerically ascending or descending across all matching records."
    - "Sorting preserves filters, returns to page one, and keeps null values last in either direction."
    - "Only explicitly allowed scalar columns and directions reach SQL ORDER BY."
  artifacts:
    - path: src/db/explorer.py
      provides: Validated server-side sort before pagination
    - path: src/db/explorer.html
      provides: Accessible sortable headers with direction indicators
    - path: tests/db/test_explorer.py
      provides: Numeric, null, tie, pagination and invalid-input regression coverage
  key_links:
    - from: src/db/explorer.html
      to: src/db/explorer.py
      via: sort_by and sort_dir query parameters
---

<objective>Add table-header sorting to the existing database explorer, including numeric PER/PBR ordering over the full filtered result set, without changing stored records or adding dependencies.</objective>

<context>
@AGENTS.md
@.planning/research/redesign-2026-05.md
@src/db/explorer.py
@src/db/explorer.html
@tests/db/test_explorer.py
Existing contracts: Dataset.preview supplies displayed column keys; Filters is a Pydantic model with extra forbidden; Explorer.inventory emits column metadata; Explorer.search applies conditions then ORDER BY before LIMIT/OFFSET. Current default is date descending plus descending stable keys. Numeric serialization deliberately returns Decimal as strings: sorting must use original typed database columns, never formatted browser text. UI uses applied filters, page, queryVersion and search() to protect against stale responses.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add validated server-side scalar sorting and regression tests</name>
  <files>src/db/explorer.py, tests/db/test_explorer.py</files>
  <behavior>
    - PER/PBR values such as 2, 10 and 100 sort numerically, with null last for both directions.
    - Sorting is applied before pagination and retains keyword/ticker/date filters.
    - Equal selected values retain deterministic order using dataset primary/composite keys.
    - Invalid fields, SQL-looking identifiers, array/JSON fields and invalid directions are rejected.
    - Requests without sort parameters preserve existing default order.
  </behavior>
  <action>Add an explicit dataset scalar sort allowlist restricted to preview columns, excluding JSON and array values; expose per-column sortable metadata in inventory. Extend Filters with optional sort_by and Literal asc/desc sort_dir, default asc for a newly chosen column. Validate dataset-specific eligibility and reject direction-only input when no field is supplied. Build ORDER BY from trusted descriptor identifiers and validated direction only, with NULLS LAST in both directions and nonduplicated stable key tie breakers. Preserve the current order when no custom field is chosen. Do not cast numeric fields to text for sorting. Add seeded database tests for both PER and PBR, multiple pages, duplicate values, nulls, combined filters and invalid inputs, plus HTTP rejection coverage.</action>
  <verify><automated>.\.venv\Scripts\python.exe -m pytest tests/db/test_explorer.py -q</automated></verify>
  <done>All supported scalar fields sort globally and deterministically; numeric/null cases and unsafe requests pass focused tests.</done>
</task>
<task type="auto">
  <name>Task 2: Connect accessible table headers and verify browser behavior</name>
  <files>src/db/explorer.html, docs/database-explorer.md</files>
  <action>Add independent current sort state. Render keyboard-operable header buttons only when inventory marks the column sortable, with visible arrow indicators and appropriate aria-sort on th. First click uses ascending, repeat toggles descending; changing column resets to ascending. Every sort change resets page to one, preserves applied filters, clears stale detail and uses the existing queryVersion guard. Include sort parameters on pagination, page-size and refresh queries; reset sorting on dataset changes and explicit filter reset. Leave unsortable headings and the detail column as plain labels. Briefly document header sorting and empty values last. Verify with browser automation that PER/PBR clicks issue expected requests, change displayed ordering and ARIA state, and reset page while retaining filters. Use disposable test records or intercepted controlled responses where live fundamentals are empty; never seed production merely for a screenshot.</action>
  <verify><automated>.\.venv\Scripts\python.exe -m pytest tests/db/test_explorer.py -q</automated><human-check>Parent runs available Playwright/Edge browser checks against the local explorer, including ascending/descending toggles and retained filters.</human-check></verify>
  <done>Sortable headings communicate and apply server-side ordering; browser evidence confirms interaction and existing filter/pagination behavior remains usable.</done>
</task>
</tasks>

<threat_model>
| Threat | Disposition | Mitigation |
|---|---|---|
| SQL injection through column or direction | mitigate | Explicit descriptor allowlist plus strict Pydantic direction validation before SQL composition |
| Incorrect financial ordering | mitigate | Order by typed numeric DB columns before pagination, verified with 2/10/100 and null fixtures |
| Stale browser results | mitigate | Preserve existing queryVersion cancellation guard for sorting requests |
</threat_model>

<source_audit>
GOAL and REQ QUICK-TABLE-SORT: tasks 1–2 cover user PER/PBR asc/desc sorting. RESEARCH: existing Postgres canonical/typed-number/no-arbitrary-SQL rules covered in task 1. CONTEXT: parent requires scalar allowlist, nulls last, stable ties, filter preservation and accessible headers; all covered in tasks 1–2. No deferred ideas or new packages.
</source_audit>

<success_criteria>Focused tests pass and browser verifies header toggling, ordering state, page reset and preserved filters; no schema/data changes.</success_criteria>
<output>Create 260923-th4-SUMMARY.md alongside this plan with test and browser evidence.</output>
