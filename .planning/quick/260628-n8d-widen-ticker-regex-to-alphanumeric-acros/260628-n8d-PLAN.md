---
phase: quick-260628-n8d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cards/models.py
  - src/mcp_v2/models.py
  - src/mcp_v2/tools/market.py
  - src/mcp_v2/errors.py
  - src/shared/frontmatter.py
  - src/shared/portfolio.py
  - tests/cards/test_models.py
  - tests/mcp_v2/test_market.py
  - tests/test_frontmatter_news_fields.py
  - tests/test_portfolio.py
autonomous: true
requirements: [QUICK-260628-n8d]

must_haves:
  truths:
    - "A stored alphanumeric-ticker company (e.g. 0001A0 = 덕양에너젠) is READABLE end-to-end: its ticker validates in DecisionCard, the mcp_v2 read models (OhlcvRange/FlowRange/PortfolioHolding), the ohlcv_range/flow_range tool guard, news frontmatter TickerRef, and portfolio.md."
    - "Every read/analysis/shared ticker guard accepts 0001A0 and still REJECTS 5/7-char, lowercase, SQL/path metachars, and non-ASCII (superscript) shapes."
    - "corp_code guards stay digit-only (^[0-9]{8}$) — UNCHANGED."
    - "No validation behavior changes beyond the ticker widening; SQL stays parameterized (Veto #7)."
  artifacts:
    - path: "src/cards/models.py"
      provides: "DecisionCard.ticker widened to ^[0-9A-Z]{6}$"
      contains: "[0-9A-Z]{6}"
    - path: "src/mcp_v2/models.py"
      provides: "_TICKER_PATTERN widened to ^[0-9A-Z]{6}$"
      contains: "[0-9A-Z]{6}"
    - path: "src/mcp_v2/tools/market.py"
      provides: "_TICKER_RE widened to ^[0-9A-Z]{6}$"
      contains: "[0-9A-Z]{6}"
    - path: "src/shared/frontmatter.py"
      provides: "TickerRef.ticker widened to ^[0-9A-Z]{6}$"
      contains: "[0-9A-Z]{6}"
    - path: "src/shared/portfolio.py"
      provides: "_TICKER_RE widened to ^[0-9A-Z]{6}$"
      contains: "[0-9A-Z]{6}"
  key_links:
    - from: "tests/test_ticker_shape.py (write-path matrix, mh9)"
      to: "read/analysis/shared guards"
      via: "identical widened literal ^[0-9A-Z]{6}$"
      pattern: "\\[0-9A-Z\\]\\{6\\}"
---

<objective>
Finish the alphanumeric-KRX-ticker rollout started by quick task 260628-mh9. mh9
widened the six WRITE-PATH guards (`^[0-9]{6}$` → `^[0-9A-Z]{6}$`) in the
dart/krx/news/kind/fundamentals `db_writer.py` files plus `db/entity.py`. A
company with a new-style 6-char alphanumeric short code (e.g. "0001A0" =
덕양에너젠) can now be WRITTEN — but the READ / analysis / shared layers still
carry the old digit-only `^[0-9]{6}$` ticker guard, so the same row would be
REJECTED on the way back out (write/read inconsistency).

This plan widens the remaining FIVE ticker code sites and syncs the surrounding
documentation so a stored alphanumeric ticker round-trips end-to-end.

Purpose: eliminate the write-accepts / read-rejects split for new-style KRX tickers.
Output: 5 widened ticker guards + accuracy-synced docstrings + unit tests proving
each widened guard accepts "0001A0" and still rejects malformed shapes.

SCOPE GUARDS (do NOT violate):
- Change `[0-9]{6}` → `[0-9A-Z]{6}` for the TICKER pattern ONLY.
- Leave EVERY `^[0-9]{8}$` corp_code pattern UNCHANGED (corp_code is always 8 digits).
- Do NOT re-touch the six mh9 write-path files (already done): src/collectors/{dart,krx,news,kind,fundamentals}/db_writer.py, src/db/entity.py.
- Uppercase-only decision (matches mh9): KRX issues UPPERCASE short codes; lowercase stays rejected. The added class is `A-Z` only — no SQL/path metacharacters, so the pre-bind allowlist guard stays injection-safe (Veto #7 / D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260628-mh9-fix-two-dart-collector-bugs-alphanumeric/260628-mh9-SUMMARY.md

<interfaces>
<!-- The five ticker code sites to widen. ASCII-confirmed by grep — this is the COMPLETE set of remaining ^[0-9]{6}$ ticker guards in src/. -->

src/cards/models.py:80  — DecisionCard (Pydantic Field pattern):
  ticker: str = Field(pattern=r"^[0-9]{6}$")
  (corp_code on line 79 is r"^[0-9]{8}$" — LEAVE IT.)

src/mcp_v2/models.py:47 — module-level string constant:
  _TICKER_PATTERN = r"^[0-9]{6}$"
  (_CORP_CODE_PATTERN on line 48 is r"^[0-9]{8}$" — LEAVE IT.)
  models.py:13 module docstring mentions r"^[0-9]{6}$" (ticker) — sync prose.

src/mcp_v2/tools/market.py:44 — compiled regex:
  _TICKER_RE = re.compile(r"^[0-9]{6}$")
  (_CORP_CODE_RE on line 45 is r"^[0-9]{8}$" — LEAVE IT.)
  market.py also has prose to sync (see Task 1 action).

src/shared/frontmatter.py:32 — TickerRef (Pydantic Field pattern):
  ticker: str = Field(pattern=r"^[0-9]{6}$")
  (corp_code on line 33 is r"^[0-9]{8}$" — LEAVE IT.)

src/shared/portfolio.py:21 — compiled regex (used by Holding + watchlist validators):
  _TICKER_RE = re.compile(r"^[0-9]{6}$")

src/mcp_v2/errors.py:20,55 — docstrings mention ^[0-9]{6}$ — sync prose.
</interfaces>

<test_homes>
<!-- Reuse existing test files; each guard tested in its semantic home. -->
- DecisionCard.ticker        → tests/cards/test_models.py (fixture: decision_card_yaml; override "ticker")
- mcp_v2 _TICKER_PATTERN + market _TICKER_RE → tests/mcp_v2/test_market.py (DB-free model/regex tests)
- frontmatter TickerRef      → tests/test_frontmatter_news_fields.py
- portfolio Holding/watchlist → tests/test_portfolio.py
- Write-path matrix already lives in tests/test_ticker_shape.py (mh9) — DO NOT need to edit it.
</test_homes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the five read/analysis/shared ticker guards + sync prose</name>
  <files>src/cards/models.py, src/mcp_v2/models.py, src/mcp_v2/tools/market.py, src/mcp_v2/errors.py, src/shared/frontmatter.py, src/shared/portfolio.py</files>
  <action>
For each of the FIVE code sites, change the TICKER regex literal `^[0-9]{6}$` →
`^[0-9A-Z]{6}$` (surgical per-file edit, no other logic change):
  1. src/cards/models.py:80 — DecisionCard.ticker Field(pattern=...).
  2. src/mcp_v2/models.py:47 — _TICKER_PATTERN constant.
  3. src/mcp_v2/tools/market.py:44 — _TICKER_RE = re.compile(...).
  4. src/shared/frontmatter.py:32 — TickerRef.ticker Field(pattern=...).
  5. src/shared/portfolio.py:21 — _TICKER_RE = re.compile(...).

Do NOT touch any `^[0-9]{8}$` corp_code pattern (cards/models.py:79,
mcp_v2/models.py:48, market.py:45, frontmatter.py:33).

Sync the surrounding documentation/messages for accuracy (prose only — these
already describe the ticker shape and would otherwise be misleading after the
widening; no test asserts on these strings, so this is a documentation fix, not
a validation-behavior change). Use the mh9 wording convention "uppercase
alphanumeric":
  - src/mcp_v2/models.py module docstring (~line 13): the `r"^[0-9]{6}$"` (ticker) mention.
  - src/mcp_v2/errors.py docstrings (~lines 20, 55): the `^[0-9]{6}$` ticker mentions.
  - src/mcp_v2/tools/market.py: module docstring "ticker (6 ASCII digits)" (~line 21-23 / the `_TICKER_RE` comment ~line 42-43); the ohlcv_range/flow_range function docstrings ("the 6-digit KRX ticker", "ticker is not 6 ASCII digits"); and the two `InvalidArgument("ticker must be 6 ASCII digits")` message strings (~lines 166, 207). Reword to reflect the 6 uppercase-alphanumeric shape (^[0-9A-Z]{6}$).
  - src/cards/models.py and src/shared/portfolio.py / frontmatter.py: update any inline comment / validator ValueError text (e.g. portfolio.py "ticker must be 6 ASCII digits, got {v!r}" and watchlist equivalent) to the alphanumeric wording.

Keep the ASCII rationale note ("str.isdigit accepts superscripts" / non-ASCII
rejection) where it exists — it is still true (the guard remains ASCII-class).
  </action>
  <verify>
    <automated>grep -rIn "\[0-9\]{6}" src/cards/models.py src/mcp_v2/ src/shared/frontmatter.py src/shared/portfolio.py</automated>
    Expected: ZERO matches (no ticker `[0-9]{6}` literal remains anywhere — code OR prose — in these files; corp_code's `[0-9]{8}` literal does not contain `[0-9]{6}` so it is not matched and stays intact). Then confirm the widened literal is present: `grep -rIn "\[0-9A-Z\]{6}" src/cards/models.py src/mcp_v2/ src/shared/frontmatter.py src/shared/portfolio.py` returns 5+ matches. Then confirm corp_code untouched: `grep -rIn "\[0-9\]{8}" src/cards/models.py src/mcp_v2/models.py src/mcp_v2/tools/market.py src/shared/frontmatter.py` still returns its matches.
  </verify>
  <done>All five ticker code sites use `^[0-9A-Z]{6}$`; all `^[0-9]{8}$` corp_code patterns unchanged; no stale `[0-9]{6}` ticker prose remains in the touched files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests proving each widened guard accepts 0001A0 + rejects bad shapes; run regression</name>
  <files>tests/cards/test_models.py, tests/mcp_v2/test_market.py, tests/test_frontmatter_news_fields.py, tests/test_portfolio.py</files>
  <behavior>
Accept matrix (every widened guard MUST accept): "0001A0", "005930", "AAAAAA", "0A0A0A".
Reject matrix (every widened guard MUST reject): "00593" (5-char), "0059300" (7-char),
"0001a0" (lowercase), "0001A!" (metachar), "0;DROP" (SQL metachar), "²²²²²²" (non-ASCII).

  - cards.DecisionCard.ticker: model_validate with ticker="0001A0" succeeds; each reject value raises ValidationError.
  - mcp_v2.models._TICKER_PATTERN: re.compile(_TICKER_PATTERN).match("0001A0") is not None; OhlcvRange(ticker="0001A0") constructs; reject values raise/None.
  - mcp_v2.tools.market._TICKER_RE: .match("0001A0") is not None; each reject value .match(...) is None.
  - shared.frontmatter.TickerRef: TickerRef(ticker="0001A0").ticker == "0001A0"; lowercase/wrong-length raise ValidationError.
  - shared.portfolio.Holding + watchlist: Holding(ticker="0001A0", qty=1, avg_cost=1) constructs; Portfolio(watchlist=["0001A0"]) constructs; "0001a0" raises ValidationError.
  </behavior>
  <action>
Add tests to the four existing files (reuse, do not create new files):

  1. tests/cards/test_models.py — add `test_alphanumeric_ticker_accepted` (build
     `{**decision_card_yaml, "ticker": "0001A0"}` → DecisionCard.model_validate ok)
     and a parametrized `test_bad_ticker_shape_rejected` over the reject matrix
     (pytest.raises(ValidationError)). Reuse the existing `decision_card_yaml` fixture.

  2. tests/mcp_v2/test_market.py — add DB-FREE tests (no seeded_engine fixture):
     import `_TICKER_RE` from mcp_v2.tools.market and `_TICKER_PATTERN, OhlcvRange`
     from mcp_v2.models (add `import re`). Parametrize accept/reject over BOTH
     `_TICKER_RE.match(...)` and `re.compile(_TICKER_PATTERN).match(...)`; assert
     OhlcvRange(ticker="0001A0").ticker == "0001A0". (Leave the existing
     test_*_bad_ticker_raises_invalid_argument tests — "12AB"/"abc" are still rejected.)

  3. tests/test_frontmatter_news_fields.py — add `test_alphanumeric_ticker_ref_accepted`
     (TickerRef(ticker="0001A0")) and `test_lowercase_ticker_ref_rejected`
     (pytest.raises(ValidationError) for "0001a0"). The existing
     test_invalid_ticker_in_tickers_raises ("5930") stays valid.

  4. tests/test_portfolio.py — add `test_alphanumeric_holding_and_watchlist_accepted`
     (Holding(ticker="0001A0", qty=1, avg_cost=1) and a portfolio yaml with
     watchlist ["0001A0"] loads) and `test_lowercase_ticker_rejected`
     (pytest.raises(ValidationError)). The existing
     test_invalid_5digit_ticker_raises_validation_error stays valid.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/cards/test_models.py tests/mcp_v2/test_market.py tests/test_frontmatter_news_fields.py tests/test_portfolio.py tests/test_ticker_shape.py -q</automated>
    Then run the full regression and RECORD EXACT COUNTS: `.venv/Scripts/python.exe -m pytest tests/mcp_v2 tests/cards tests/shared tests/db -q` must stay green (Postgres is up in Docker; tests/db uses testcontainers/pg_clean, never the live DB).
  </verify>
  <done>New tests pass; `tests/mcp_v2 tests/cards tests/shared tests/db` all green with counts recorded in the SUMMARY; no previously-passing test regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → read tools / models | ticker arg crosses into Pydantic guards + pre-bind regex, then parameterized SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-N8D-01 | Tampering/Injection | widened ticker guards `^[0-9A-Z]{6}$` (cards/models, mcp_v2/models, market._TICKER_RE, frontmatter.TickerRef, portfolio._TICKER_RE) | mitigate | added class is `A-Z` only — NO SQL/path metacharacters; guards stay pre-bind allowlists and all DB reads keep using SQLAlchemy bind params / FIXED text() constants (Veto #7, SC#3 AST guard untouched). Reject matrix tests prove lowercase/metachar/non-ASCII/wrong-length still rejected. |
| T-N8D-02 | Spoofing (identity confusion) | corp_code guards | accept | corp_code patterns left UNCHANGED at `^[0-9]{8}$` (always 8 digits); no widening, no new surface. |
</threat_model>

<verification>
- Old ticker literal `[0-9]{6}` fully gone from the 5 touched source files (grep, Task 1).
- Widened literal `^[0-9A-Z]{6}$` present at all 5 code sites.
- All `^[0-9]{8}$` corp_code patterns intact.
- Accept "0001A0" / reject malformed shapes proven for every widened guard (Task 2).
- Regression dirs `tests/mcp_v2 tests/cards tests/shared tests/db` green, counts recorded.
</verification>

<success_criteria>
- A stored alphanumeric ticker (e.g. "0001A0") validates through DecisionCard, the
  mcp_v2 read models + ohlcv_range/flow_range tool guard, news frontmatter TickerRef,
  and portfolio.md — closing the mh9 write/read split.
- Lowercase, wrong-length, metachar, and non-ASCII shapes still rejected everywhere.
- corp_code validation unchanged.
- No regression in tests/mcp_v2, tests/cards, tests/shared, tests/db.
</success_criteria>

<output>
Create `.planning/quick/260628-n8d-widen-ticker-regex-to-alphanumeric-acros/260628-n8d-SUMMARY.md` when done.
Record: exact regression test counts, the commit hash(es), and confirm corp_code guards were left untouched.
</output>
