---
phase: 03-one-company-walking-skeleton
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/ingest/injection_defense.py
  - src/ingest/tokenizer.py
  - src/ingest/embedder.py
  - src/ingest/chunking.py
  - src/ingest/parsers/__init__.py
  - src/ingest/parsers/dart.py
  - tests/test_injection_defense.py
  - tests/test_bm25_tokenizer.py
  - tests/test_embedder.py
  - tests/test_parsers.py
autonomous: true
requirements: [INGEST-08, INGEST-09, INGEST-10, INGEST-11, INGEST-12]
tags: [ingest, embedder, tokenizer, injection-defense, parsers, security]

must_haves:
  truths:
    - "wrap_untrusted emits the D-16 XML delimiter format verbatim with validated attributes"
    - "detect_injection_patterns returns list of matches covering all 6 seeded pattern families from D-18"
    - "is_adversarial returns True only for trust_level='adversarial' (INGEST-09 gate helper)"
    - "tokenize_ko returns stable int32 IDs for Korean content tokens (NNG/NNP/SL/SN) — same function for index and query (D-12)"
    - "Embedder loads BAAI/bge-m3 and encodes batches to normalized 1024-d vectors; EMBEDDING_MODEL_VERSION constant exported (INGEST-10/INGEST-12)"
    - "chunk_document splits section-first, then 512/64 windows when a section exceeds 1500 bge-m3 tokens (D-05)"
    - "parse_sections(body, source='dart') returns at least 1 Section with (title, path, text, order)"
  artifacts:
    - path: "src/ingest/injection_defense.py"
      provides: "wrap_untrusted + detect_injection_patterns + PATTERNS + is_adversarial"
      exports: ["wrap_untrusted", "detect_injection_patterns", "PATTERNS", "is_adversarial"]
    - path: "src/ingest/tokenizer.py"
      provides: "tokenize_ko(text) -> list[int] mecab-ko content POS + hash-to-int32"
      exports: ["tokenize_ko"]
    - path: "src/ingest/embedder.py"
      provides: "Embedder class + EMBEDDING_MODEL_VERSION constant + encode_query LRU"
      exports: ["Embedder", "EMBEDDING_MODEL_VERSION", "encode_query", "get_default_embedder"]
    - path: "src/ingest/chunking.py"
      provides: "Chunk dataclass + chunk_document(sections, max_tokens, win, overlap)"
      exports: ["Chunk", "chunk_document"]
    - path: "src/ingest/parsers/dart.py"
      provides: "parse_sections(body, source) -> list[Section] for DART"
      exports: ["parse_sections", "Section"]
  key_links:
    - from: "src/ingest/tokenizer.py"
      to: "mecab.MeCab"
      via: "module-level _mc singleton"
      pattern: "MeCab\\(\\)"
    - from: "src/ingest/embedder.py"
      to: "sentence_transformers.SentenceTransformer"
      via: "Embedder.__init__ loads 'BAAI/bge-m3'"
      pattern: "SentenceTransformer"
    - from: "src/ingest/chunking.py"
      to: "transformers.AutoTokenizer"
      via: "module-level _tok = AutoTokenizer.from_pretrained('BAAI/bge-m3')"
      pattern: "AutoTokenizer\\.from_pretrained"
---

<objective>
Ship the ingest worker's leaf utilities: injection defense scaffolding (Layers 2+3 of D-15), mecab-ko Korean BM25 tokenizer, bge-m3 embedder, section-aware chunker, and DART section parser. Each module is self-contained and imports no DB. Plan 04 composes them.

Purpose: Fulfills INGEST-08/09/10/11/12 + chunking D-05/D-06/D-07. Parallel-safe with Plan 02 (different files).
Output: 5 leaf modules + 4 test files, ~25 unit tests covering pattern prefilter, tokenization, embedding shape/norm, chunking boundaries, DART TOC parse.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md
@.planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md
@.planning/phases/03-one-company-walking-skeleton/03-01-SUMMARY.md

<interfaces>
Interfaces defined by this plan and consumed by Plan 04 (worker) and Plan 05 (MCP search tool):

```python
# src/ingest/injection_defense.py
PATTERNS: list[tuple[str, re.Pattern[str]]]
def wrap_untrusted(body: str, source: str, trust_level: str, doc_id: str) -> str: ...
def detect_injection_patterns(body: str) -> list[dict]: ...
def is_adversarial(trust_level: str) -> bool: ...

# src/ingest/tokenizer.py
def tokenize_ko(text: str) -> list[int]: ...

# src/ingest/embedder.py
EMBEDDING_MODEL_VERSION: str = "BAAI/bge-m3@v1"
class Embedder:
    def __init__(self, device: str = "cpu") -> None: ...
    def encode(self, texts: list[str]) -> list[list[float]]: ...
def encode_query(q: str) -> tuple[float, ...]: ...  # @lru_cache(maxsize=256)

# src/ingest/chunking.py
@dataclass
class Chunk:
    text: str
    chunk_index: int
    section_path: str | None
    section_index: int | None
def chunk_document(sections, *, max_tokens=1500, win=512, overlap=64) -> list[Chunk]: ...

# src/ingest/parsers/dart.py
@dataclass
class Section:
    title: str
    path: str
    text: str
    order: int
def parse_sections(body: str, source: str) -> list[Section]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: injection_defense.py with pattern table, wrapper, detector — RED→GREEN</name>
  <files>
    src/ingest/injection_defense.py,
    tests/test_injection_defense.py
  </files>
  <read_first>
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-15, D-16, D-17, D-18, D-19
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pitfall 3 + §Security Domain
  </read_first>
  <behavior>
    - T1 test_wrap_xml_format — wrap returns the exact D-16 delimiter template
    - T2 test_wrap_rejects_unsafe_attrs — source/trust_level with chars outside [A-Za-z0-9_-] raise ValueError
    - T3-T8 test_pattern_match_* — each of the 6 seeded pattern families (D-18) matches a canonical attack string
    - T9 test_clean_text_matches_nothing — benign Korean disclosure snippet produces []
    - T10 test_multiple_matches_ordered_by_span — two injection patterns in one body return list sorted by span[0]
    - T11 test_is_adversarial_trust_level — returns True only for "adversarial"
    - T12 test_pattern_ids_stable — the ordered list of PATTERN[0] IDs matches the snapshot exactly
  </behavior>
  <action>
    1. Create src/ingest/injection_defense.py with:
       - Module docstring citing D-15/16/17/18/19, INGEST-08, INGEST-09, threat T-3-01.
       - Constant PATTERNS (ordered 6 entries):
         EN_IGNORE_PREV: regex for "ignore (all )?(previous|prior|above) (instructions|prompts|rules)" case-insensitive.
         FAKE_SYSTEM_TAG: regex for open/close tags of system/assistant/user/instructions/prompt (case-insensitive).
         DAN_MODE: regex for "DAN mode", "jailbreak", "developer mode" (case-insensitive).
         ROLEPLAY_ADMIN: regex for "role-play as" OR "pretend (you are|to be) (admin|root|system)".
         KO_IGNORE_PREV: regex for "이전\s*지시\s*무시" / "이전\s*프롬프트\s*무시".
         KO_ADMIN_MODE: regex for "관리자\s*모드" / "시스템\s*프롬프트\s*출력".
       - _SAFE_ATTR_RE = re.compile(r"^[A-Za-z0-9_-]+$"); _SAFE_DOC_ID_RE = re.compile(r"^[a-f0-9]{1,32}$").
       - wrap_untrusted(body, source, trust_level, doc_id):
         Validate source/trust_level/doc_id against their regexes; on mismatch raise ValueError with a generic message (do not include the offending value — info-disclosure guard).
         Return exactly: the open tag + "\n" + body + "\n" + close tag.
       - detect_injection_patterns(body): for each (pid, rx), for each rx.finditer(body), collect {"pattern_id": pid, "match": m.group(0)[:80], "span": (m.start(), m.end())}, return sorted by span[0].
       - is_adversarial(trust_level): return trust_level == "adversarial".
    2. Create tests/test_injection_defense.py with 12 tests above. Use literal UTF-8 Korean. Include pattern-exercising attack strings for the 6 families; include a clean benign snippet; include a two-pattern combined sample for T10.
  </action>
  <verify>
    <automated>uv run pytest tests/test_injection_defense.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'EN_IGNORE_PREV\|FAKE_SYSTEM_TAG\|DAN_MODE\|ROLEPLAY_ADMIN\|KO_IGNORE_PREV\|KO_ADMIN_MODE' src/ingest/injection_defense.py == 6
    - grep -c 'def wrap_untrusted\|def detect_injection_patterns\|def is_adversarial' src/ingest/injection_defense.py == 3
    - grep -c 'def test_' tests/test_injection_defense.py >= 12
    - All 12 tests pass
  </acceptance_criteria>
  <done>
    Injection defense module ships PATTERNS table + 3 functions with 12 green tests. Scaffolding ready for worker/MCP delimiter wrap.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tokenizer.py (mecab-ko) + embedder.py (bge-m3) — RED→GREEN</name>
  <files>
    src/ingest/tokenizer.py,
    src/ingest/embedder.py,
    tests/test_bm25_tokenizer.py,
    tests/test_embedder.py
  </files>
  <read_first>
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 3, §Pattern 4, §Code Examples bge-m3 + mecab-ko
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-11, D-12
  </read_first>
  <behavior>
    - T1 test_tokenize_ko_emits_int_list — all IDs are non-negative int < 2^31
    - T2 test_tokenize_ko_deterministic — same input → same list
    - T3 test_tokenize_ko_content_only — Korean josa/endings dropped
    - T4 test_tokenize_ko_empty_returns_empty — "" → []
    - T5 test_tokenize_ko_query_matches_index (D-12) — same pipeline for index and query
    - E1 (slow) test_embedder_shape_and_norm — (1, 1024); L2 norm ≈ 1.0
    - E2 (slow) test_embedder_batch_encode — (3, 1024)
    - E3 test_embedding_model_version_constant — EMBEDDING_MODEL_VERSION == "BAAI/bge-m3@v1"
    - E4 (slow) test_encode_query_lru_cache_hit — second call hits cache (cache_info().hits >= 1)
  </behavior>
  <action>
    1. Create src/ingest/tokenizer.py:
       - Module docstring citing D-12, INGEST-11, Pattern 4.
       - import mecab at module top (python-mecab-ko ships wheels, no system deps).
       - _mc = mecab.MeCab() module singleton.
       - _CONTENT_POS = frozenset({"NNG", "NNP", "SL", "SN"}).
       - _token_id(surface): blake2s digest_size=4 → int32 positive via & 0x7FFFFFFF.
       - tokenize_ko(text): empty → []; iterate _mc.parse(text); for each token with feature.pos in _CONTENT_POS, append _token_id(surface.lower()); return list.
    2. Create src/ingest/embedder.py:
       - Module docstring citing INGEST-10, INGEST-12, Pattern 3, D-11, CLAUDE.md §4.
       - EMBEDDING_MODEL_VERSION = "BAAI/bge-m3@v1" (module-level).
       - class Embedder: __init__(device="cpu") lazy-imports SentenceTransformer and loads "BAAI/bge-m3"; encode(texts) returns model.encode(texts, batch_size=16, normalize_embeddings=True).tolist().
       - _default_embedder = None; get_default_embedder() lazy singleton.
       - @functools.lru_cache(maxsize=256) encode_query(q) → tuple(get_default_embedder().encode([q])[0]).
       - __all__ listing the 4 exports.
    3. Create tests/test_bm25_tokenizer.py with T1-T5. No slow markers.
    4. Create tests/test_embedder.py with E1-E4. Mark E1/E2/E4 with pytest.mark.slow. Skip if HF_HUB_OFFLINE env is set. Use a session-scoped warm fixture that pre-loads BAAI/bge-m3 once.
  </action>
  <verify>
    <automated>uv run --group ingest --group dev pytest tests/test_bm25_tokenizer.py -xvs &amp;&amp; uv run --group ingest --group dev pytest tests/test_embedder.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - grep -n 'EMBEDDING_MODEL_VERSION = "BAAI/bge-m3@v1"' src/ingest/embedder.py prints one line
    - grep -n 'SentenceTransformer("BAAI/bge-m3"' src/ingest/embedder.py prints one line
    - grep -n 'lru_cache(maxsize=256)' src/ingest/embedder.py prints one line (D-11)
    - grep -n 'blake2s' src/ingest/tokenizer.py prints one line
    - grep -n '_CONTENT_POS' src/ingest/tokenizer.py >= 2 matches
    - tests/test_bm25_tokenizer.py: all 5 tests pass
    - tests/test_embedder.py: E3 passes without model load; E1/E2/E4 pass under slow marker (or skip with clear message if HF offline)
  </acceptance_criteria>
  <done>
    Tokenizer + embedder modules with stable versioning. Tests green, fast subset (<2s) for CI; slow subset runs on demand.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chunking.py + parsers/dart.py section-aware chunker — RED→GREEN</name>
  <files>
    src/ingest/chunking.py,
    src/ingest/parsers/__init__.py,
    src/ingest/parsers/dart.py,
    tests/test_parsers.py
  </files>
  <read_first>
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 2 (chunking), §Open Questions Q4 (section_index semantics)
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-05, D-06, D-07, D-08
    - .planning/phases/03-one-company-walking-skeleton/probe-findings.md (DART Report shape from Plan 01)
  </read_first>
  <behavior>
    - C1 test_chunk_single_short_section — one Section with 200 tokens produces 1 Chunk; section_index=0; chunk_index=0
    - C2 test_chunk_long_section_splits — one Section with 3000 tokens (max_tokens=1500, win=512, overlap=64) produces >= 5 chunks with increasing section_index starting at 0; first chunk chunk_index=0, last chunk_index increments monotonically
    - C3 test_multiple_sections_chunk_index_monotonic — three sections (500, 2000, 800 tokens); chunk_index is monotonic per document (0, 1, 2, 3, ... without reset) per Q4 decision; section_index resets to 0 at each new section
    - C4 test_section_path_preserved — Chunk.section_path equals Section.path for every chunk derived from that section
    - P1 test_dart_major_event_single_section — parse_sections(short_body, 'dart') where body lacks a TOC returns exactly one Section with title='(root)' and path='(root)', order=0
    - P2 test_dart_regular_report_splits_by_toc — a fabricated body containing "I. 회사의 개요\n1. 회사의 개요\n...\nII. 사업의 내용\n..." is split into >= 2 Sections; Section.path contains the hierarchical title joined by "/"
    - P3 test_parse_unknown_source_raises — parse_sections(body, 'unsupported_source') raises ValueError
  </behavior>
  <action>
    1. Create src/ingest/chunking.py:
       - Module docstring citing D-05, D-08, Pattern 2, Open Question Q4 (chunk_index monotonic per doc; section_index resets per section).
       - Lazy module-level _tok singleton: from transformers import AutoTokenizer; _tok = None; def _get_tok(): load lazily on first call (BAAI/bge-m3 tokenizer for token counting).
       - @dataclass Chunk: text: str; chunk_index: int; section_path: str | None; section_index: int | None.
       - def chunk_document(sections, *, max_tokens=1500, win=512, overlap=64) -> list[Chunk]:
         - chunks: list[Chunk] = []
         - For each sec in sections:
             ids = _get_tok().encode(sec.text, add_special_tokens=False)
             If len(ids) <= max_tokens: chunks.append(Chunk(sec.text, len(chunks), sec.path, 0)); continue.
             Else, for i, start in enumerate(range(0, len(ids), win - overlap)):
                 piece = _get_tok().decode(ids[start:start+win])
                 chunks.append(Chunk(piece, len(chunks), sec.path, i))
         - return chunks
    2. Create src/ingest/parsers/__init__.py:
       - def parse_sections(body: str, source: str) -> list[Section]:
         - if source == "dart": from .dart import parse_sections as _parse; return _parse(body, source)
         - else: raise ValueError(f"unsupported source for section parsing") — do NOT interpolate the source value (info-disclosure hardening).
    3. Create src/ingest/parsers/dart.py:
       - @dataclass Section: title: str; path: str; text: str; order: int.
       - def parse_sections(body: str, source: str) -> list[Section]:
         - Case A: body contains Korean-numeral TOC markers matching r"^(?:[IVX]+\.|[0-9]+\.)\s+\S" → split by heading. Use a regex scanner over lines; accumulate current section title stack; emit Section when a new heading is found or at EOF. Path joins the accumulated ancestor titles with "/".
         - Case B: no headings → return [Section(title="(root)", path="(root)", text=body, order=0)].
         - Robustness fallback: if Case A splitting yields empty titles or zero sections, return Case B.
         - D-07: formally this should read dart-fss TOC via filing.report structure, but at this layer we receive plain text. The collector in Plan 02 stored only body text. Discretionary: this regex-based heading split is adequate for Phase 3 (documented as Assumption A4 follow-up).
    4. Create tests/test_parsers.py with 7 tests (4 chunking + 3 parser). C2 test may require that _get_tok() has already loaded BAAI/bge-m3 — mark as slow OR use a fake tokenizer via monkeypatch that counts bytes. Prefer the monkeypatch approach: create a dummy_tok object with encode(text, add_special_tokens) → list(range(len(text))) and decode(ids) → " ".join([str(i) for i in ids])[:original_length]. Monkeypatch chunking._get_tok to return dummy_tok for C1/C2/C3/C4 to keep tests fast.
  </action>
  <verify>
    <automated>uv run --group ingest --group dev pytest tests/test_parsers.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'def chunk_document\|def parse_sections\|@dataclass' src/ingest/chunking.py src/ingest/parsers/dart.py >= 4
    - grep -n 'win - overlap\|win-overlap' src/ingest/chunking.py prints one line (D-05 sliding window)
    - grep -n 'max_tokens: int = 1500' src/ingest/chunking.py prints one line (D-05)
    - grep -n '"(root)"' src/ingest/parsers/dart.py prints at least one line (Case B fallback)
    - grep -c 'def test_' tests/test_parsers.py >= 7
    - All 7 tests pass (with monkeypatched tokenizer for chunking tests)
  </acceptance_criteria>
  <done>
    Chunker and DART parser implement D-05/D-07/D-08. Chunk.chunk_index monotonic per document; section_index resets per section; parser falls back to single-root Section for bodies without TOC.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw document body → ingest | Untrusted text crosses here (pre-empts Phase 5 LLM exposure) |
| mecab parser → Python code | Library parses attacker-controlled text; library is memory-safe (Rust backend of python-mecab-ko) |
| HuggingFace model download | Supply-chain surface on first run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (prompt injection) | injection_defense | mitigate | D-16/D-18 delimiter wrap + 6-pattern regex prefilter; full unit test coverage; pattern IDs stable so downstream frontmatter flags remain queryable |
| T-3-11 | Supply chain (HF weights) | embedder | accept | BAAI/bge-m3 is a widely mirrored open model; weights cached under default HF_HOME; documented pre-download step |
| T-3-14 | Resource exhaustion (huge body) | chunking | mitigate | bge-m3 tokenizer handles up to 8192 tokens; D-05 splits anything larger into 512-window pieces so memory stays bounded per chunk; no unbounded accumulation |
| T-3-15 | Info disclosure via error messages | parsers/__init__.py | mitigate | ValueError for unsupported source uses a generic message; does not interpolate user-supplied source value |
</threat_model>

<verification>
- uv run pytest tests/test_injection_defense.py tests/test_bm25_tokenizer.py tests/test_parsers.py -xvs green
- uv run pytest tests/test_embedder.py -m 'not slow' -xvs green (version constant only)
- uv run pytest tests/test_embedder.py -m slow -xvs green when HF cache is warm
- grep -rE '(import|from) (anthropic|openai)' src/ingest/ prints nothing
</verification>

<success_criteria>
- INGEST-08: injection_defense module ships wrap_untrusted + detect_injection_patterns with 6 pattern families + unit tests
- INGEST-09: is_adversarial helper exported; trust_level='adversarial' → True (gate in Plan 04 worker)
- INGEST-10: Embedder loads bge-m3, encodes normalized 1024-d vectors
- INGEST-11: tokenize_ko returns stable int32 IDs via mecab-ko content POS filter
- INGEST-12: EMBEDDING_MODEL_VERSION constant exported for chunks.embedding_model
- Chunking respects D-05 size/overlap bounds; DART parser handles TOC + single-root fallback
</success_criteria>

<output>
After completion, create `.planning/phases/03-one-company-walking-skeleton/03-03-SUMMARY.md` with the exact EMBEDDING_MODEL_VERSION string (for Plan 04 to reference), the tokenizer content-POS set, the chunker's section_index vs chunk_index rule, and the PATTERNS ID snapshot (for downstream frontmatter queries).
</output>
