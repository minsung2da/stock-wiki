---
phase: 03-one-company-walking-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/db/migrations/versions/0002_phase03_chunking_columns.py
  - tests/conftest.py
  - tests/test_migration_0002.py
  - tests/test_api_probes.py
  - .env.example
autonomous: true
requirements: [STORE-03, STORE-04, INGEST-12]
tags: [migration, foundation, probes, ingest, db]

must_haves:
  truths:
    - "Alembic migration 0002 upgrades cleanly on the running stock-postgres container"
    - "chunks table has section_path TEXT, section_index INT, bm25_tokens INT[] columns"
    - "documents table has corp_code CHAR(8) NULL column with ix_documents_corp_code index (enables RET-02 ticker filter)"
    - "HNSW index over chunks.embedding exists using vector_cosine_ops"
    - "VectorChord-BM25 index over chunks.bm25_tokens exists using bm25_catalog.bm25_ops"
    - "dart-fss probe against corp 00126380 documents Report body accessor shape (A4 de-risk)"
    - "vchord_bm25 array cast probe confirms _vchord_bm25_cast_array_to_bm25vector accepts INT[] (A5 de-risk)"
  artifacts:
    - path: "src/db/migrations/versions/0002_phase03_chunking_columns.py"
      provides: "Phase 3 chunks schema extension (section_path/section_index/bm25_tokens) + documents.corp_code + HNSW + BM25 indexes"
      contains: "revision = \"0002\""
    - path: "tests/test_migration_0002.py"
      provides: "Migration verification via information_schema.columns + pg_indexes"
    - path: "tests/test_api_probes.py"
      provides: "dart-fss + vchord_bm25 one-shot sanity probes"
  key_links:
    - from: "src/db/migrations/versions/0002_phase03_chunking_columns.py"
      to: "src/db/migrations/versions/0001_phase02_initial_schema.py"
      via: "down_revision = \"0001\""
      pattern: "down_revision\\s*=\\s*\"0001\""
    - from: "tests/conftest.py"
      to: "alembic head"
      via: "pg_engine fixture runs command.upgrade(cfg, \"head\") → includes 0002"
      pattern: "command\\.upgrade"
---

<objective>
Ship the Phase 3 schema extension and de-risk the two load-bearing unknowns (dart-fss body shape, vchord_bm25 INT[] cast) before any collector or ingest code is written. This is Wave 1 and BLOCKS every downstream plan that writes to chunks.

Purpose: Migration 0002 adds `section_path`, `section_index`, `bm25_tokens` columns plus HNSW + VectorChord-BM25 indexes. Probes validate RESEARCH.md assumptions A4/A5 at cost ~5 minutes before committing the worker.
Output: Applied migration on live Postgres, two green probe tests, updated pyproject.toml dep groups, .env.example entry for DART_API_KEY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md
@.planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md
@.planning/phases/02-canonical-entity-identity/02-03-SUMMARY.md
@src/db/migrations/versions/0001_phase02_initial_schema.py
@tests/conftest.py
@docker-compose.yml
@pyproject.toml

<interfaces>
<!-- From 0001 migration — chunks table shape this migration extends. -->
```python
# chunks: id BIGINT PK, document_id CHAR(64) FK, ord INT, text TEXT,
#         embedding_model TEXT NULL, embedding vector(1024)
# ix_chunks_document_id on (document_id)
```

<!-- Verified live in stock-postgres 2026-04-17 per RESEARCH.md §Environment Availability: -->
<!-- vector 0.8.2, vchord_bm25 0.3.0, pg_trgm 1.6. bm25_catalog._vchord_bm25_cast_array_to_bm25vector exists. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0002 adds section/bm25 columns + HNSW + BM25 indexes (RED→GREEN)</name>
  <files>
    src/db/migrations/versions/0002_phase03_chunking_columns.py,
    tests/test_migration_0002.py,
    tests/conftest.py,
    pyproject.toml
  </files>
  <read_first>
    - src/db/migrations/versions/0001_phase02_initial_schema.py (establishes chunks shape + import pattern)
    - tests/conftest.py (pg_engine fixture runs alembic upgrade head)
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 5 (vchord_bm25 index DDL)
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-08, D-13
  </read_first>
  <behavior>
    - Test 1: `test_section_columns_exist` — after `alembic upgrade head`, information_schema.columns shows chunks.section_path (text, nullable), chunks.section_index (integer, nullable), chunks.bm25_tokens (ARRAY of int4, nullable)
    - Test 1b: `test_documents_corp_code_column_exists` — information_schema.columns shows documents.corp_code (character, length 8, nullable); pg_indexes shows ix_documents_corp_code btree index on documents(corp_code). Required by Plan 04 worker INSERT and Plan 05 hybrid_search WHERE clause.
    - Test 2: `test_hnsw_index_exists` — pg_indexes shows ix_chunks_embedding_hnsw using hnsw index method on (embedding vector_cosine_ops)
    - Test 3: `test_bm25_index_exists` — pg_indexes shows ix_chunks_bm25 using bm25 index method on (bm25_tokens bm25_catalog.bm25_ops)
    - Test 4: `test_iterative_scan_session_setting` — connection can `SET hnsw.iterative_scan = 'relaxed_order'` without error
    - Test 5: `test_downgrade_reverses_migration` — `alembic downgrade -1` removes the three columns and both indexes; `alembic upgrade head` restores them
  </behavior>
  <action>
    1. Add `sentence-transformers>=3.0`, `transformers>=4.44`, `python-mecab-ko>=1.3,<2`, `tenacity>=9.0` to the `[dependency-groups].ingest` list in pyproject.toml. Add `fastmcp>=2.11,<3.0` is already present; add `pydantic>=2.13` is already present. Add `[project.scripts]` section (if missing) with `stock-mcp = "stock_mcp.__main__:main"` and `stock = "cli.__main__:main"`.
    2. Remove `ollama` from the `[dependency-groups].ingest` list (CLAUDE.md 2026-04-17 quick task 260417-q3h mandates no Ollama). Remove `anthropic` from the `[dependency-groups].mcp` list (COLL-07 boundary enforcement — MCP server must not import anthropic either; only the external Claude Schedule agent may).
    3. Create `src/db/migrations/versions/0002_phase03_chunking_columns.py` with `revision = "0002"`, `down_revision = "0001"`, `branch_labels = None`, `depends_on = None`. Upgrade body:
       - `op.add_column("chunks", sa.Column("section_path", sa.Text, nullable=True))` (D-08)
       - `op.add_column("chunks", sa.Column("section_index", sa.Integer, nullable=True))` (D-08)
       - `op.execute("ALTER TABLE chunks ADD COLUMN bm25_tokens INT[] NULL")` (avoid SQLAlchemy ARRAY roundtrip; vchord_bm25 expects native INT[])
       - `op.add_column("documents", sa.Column("corp_code", sa.CHAR(8), nullable=True))` (RET-02 — Plan 05 hybrid_search filters documents.corp_code = :corp_code directly; Plan 04 worker writes it from fm.provenance.corp_code)
       - `op.create_index("ix_documents_corp_code", "documents", ["corp_code"])` (btree — selective for single-ticker queries)
       - `op.execute("CREATE INDEX ix_chunks_embedding_hnsw ON chunks USING hnsw (embedding vector_cosine_ops)")` (STORE-03)
       - `op.execute("CREATE INDEX ix_chunks_bm25 ON chunks USING bm25 (bm25_tokens bm25_catalog.bm25_ops)")` (STORE-04)
       - Include a module docstring citing D-08, STORE-03, STORE-04 and noting that `hnsw.iterative_scan` is a session GUC (not index-time).
    4. Downgrade body (reverse order): drop BM25 + HNSW indexes first (`DROP INDEX IF EXISTS ix_chunks_bm25; DROP INDEX IF EXISTS ix_chunks_embedding_hnsw`), then `op.drop_index("ix_documents_corp_code", table_name="documents")`, `op.drop_column("documents", "corp_code")`, `op.drop_column("chunks", "bm25_tokens")`, `op.drop_column("chunks", "section_index")`, `op.drop_column("chunks", "section_path")`.
    5. Extend `tests/conftest.py`: append `chunks` is already in `_PHASE2_TABLES`; no change to pg_clean. Add a new fixture `def pg_with_chunks_row(pg_clean)` that inserts one documents row (id='a'*64, body='x', source='dart', vault_path='vault/raw/dart/test.md') then one chunks row with `embedding=[0]*1024`, `bm25_tokens=ARRAY[1,2,3]::int[]` — use SQLAlchemy `text()` + binds; reuse in Task 2 probe.
    6. Create `tests/test_migration_0002.py` containing the five tests in <behavior>. Use `pg_engine` fixture (session-scoped, already runs upgrade head). For HNSW/BM25 index assertions query: `SELECT indexname, indexdef FROM pg_indexes WHERE schemaname='public' AND tablename='chunks'`. For downgrade test create a new fixture-scoped engine via `alembic.config.Config` and run `command.downgrade(cfg, "-1")` then `command.upgrade(cfg, "head")` — wrap in try/finally to ensure upgrade always restored.
    7. Run migration against live container: `uv run alembic -c src/db/alembic.ini upgrade head`. Verify via `docker exec stock-postgres psql -U stockwiki -d stockwiki -c "SELECT column_name,data_type FROM information_schema.columns WHERE table_name='chunks' ORDER BY ordinal_position;"` that the three new columns are present. [BLOCKING — quality_gate item]
  </action>
  <verify>
    <automated>uv sync --group collectors --group ingest --group mcp --group db --group dev &amp;&amp; uv run alembic -c src/db/alembic.ini upgrade head &amp;&amp; uv run pytest tests/test_migration_0002.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'revision = "0002"' src/db/migrations/versions/0002_phase03_chunking_columns.py` prints one line
    - `grep -c 'section_path\|section_index\|bm25_tokens' src/db/migrations/versions/0002_phase03_chunking_columns.py` >= 6 (upgrade + downgrade)
    - `grep -n 'USING hnsw (embedding vector_cosine_ops)' src/db/migrations/versions/0002_phase03_chunking_columns.py` prints one line
    - `grep -n 'USING bm25 (bm25_tokens bm25_catalog.bm25_ops)' src/db/migrations/versions/0002_phase03_chunking_columns.py` prints one line
    - `docker exec stock-postgres psql -U stockwiki -d stockwiki -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='chunks' AND column_name IN ('section_path','section_index','bm25_tokens')" | sort` prints three rows
    - `docker exec stock-postgres psql -U stockwiki -d stockwiki -tAc "SELECT column_name FROM information_schema.columns WHERE table_name='documents' AND column_name='corp_code'"` prints `corp_code`
    - `docker exec stock-postgres psql -U stockwiki -d stockwiki -tAc "SELECT indexname FROM pg_indexes WHERE tablename='chunks' AND indexname IN ('ix_chunks_embedding_hnsw','ix_chunks_bm25')" | sort` prints two rows
    - `docker exec stock-postgres psql -U stockwiki -d stockwiki -tAc "SELECT indexname FROM pg_indexes WHERE tablename='documents' AND indexname='ix_documents_corp_code'"` prints one row
    - `grep -n 'ollama' pyproject.toml` prints nothing
    - `grep -n 'anthropic' pyproject.toml` prints nothing
    - `grep -n 'sentence-transformers\|python-mecab-ko' pyproject.toml` >= 2 lines
    - All 6 tests in tests/test_migration_0002.py pass (includes test_documents_corp_code_column_exists)
  </acceptance_criteria>
  <done>
    Migration 0002 applied to the running stock-postgres container; chunks table has section_path/section_index/bm25_tokens columns, documents table has corp_code + index, both HNSW + BM25 indexes present. pyproject.toml reflects the final Phase 3 dependency set. All 6 tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dart-fss + vchord_bm25 API probes de-risk A4/A5 assumptions</name>
  <files>
    tests/test_api_probes.py,
    .env.example
  </files>
  <read_first>
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Assumptions Log A4, A5 (the probes exist to close these)
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 5 (verified vchord_bm25 API surface)
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-01 (pblntf_ty=A,B only)
  </read_first>
  <behavior>
    - Test 1: `test_dart_fss_report_body_shape` (marked `slow`, skips if DART_API_KEY env unset) — imports dart_fss, calls `set_api_key`, loads corp 00126380, calls `search_filings(bgn_de="20260101", pblntf_ty=["A","B"], last_reprt_at="Y")[:1]`, asserts returned filings has >=1 element with attributes `rcept_no` (str, 14 digits), `rcept_dt` (str, 8 digits or ISO), and that `Report` object yields non-empty text body via either `.to_dict().get("text")` or `Report.get_document()` — whichever works gets recorded in the test as the canonical accessor path for downstream plans
    - Test 2: `test_vchord_bm25_int_array_cast` — against pg_with_chunks_row fixture, executes `SELECT bm25_catalog._vchord_bm25_cast_array_to_bm25vector(ARRAY[101, 202, 303]::int[], -1, true)::text` via SQLAlchemy text() and asserts result is a non-null string starting with 'bm25vector' or matching vchord_bm25 documented format; documents the actual string in test assertion comment
    - Test 3: `test_bm25_end_to_end_scoring` — inserts 3 chunks with different bm25_tokens arrays; executes a `search_bm25query` call using the `ix_chunks_bm25` index; asserts chunks can be ranked by BM25 score without raising
  </behavior>
  <action>
    1. Add `DART_API_KEY=your-key-here  # register free at opendart.fss.or.kr` to .env.example (must not create or overwrite .env).
    2. Create `tests/test_api_probes.py` with three tests above. Use `pytest.mark.slow` on test 1 and `pytest.importorskip("dart_fss")` guard. Skip-if DART_API_KEY env var absent via `pytest.skip("DART_API_KEY not set — probe requires live DART API")`.
    3. For the vchord cast probe, use the `pg_with_chunks_row` fixture from Task 1; write the SQL as `sa.text("SELECT bm25_catalog._vchord_bm25_cast_array_to_bm25vector(CAST(:arr AS int[]), -1, true)::text AS result")` with bind param `arr=[101, 202, 303]`. Do NOT use f-string SQL (Phase 2 WR-03).
    4. For test 3, write one query using the pattern from RESEARCH.md §Pattern 5: `bm25_catalog.search_bm25query(c.bm25_tokens, bm25_catalog.to_bm25query('ix_chunks_bm25'::regclass, CAST(ARRAY[101, 202] AS bm25_catalog.bm25vector)))` — wrapped in a simple ORDER BY/LIMIT. If the cast fails, adapt to whatever signature the probe in test 2 revealed. Document actual working SQL as a docstring-block comment so Plan 05 can copy it verbatim.
    5. Record probe outcomes: at the end of each test, write a one-line finding to `.planning/phases/03-one-company-walking-skeleton/probe-findings.md` (append-only) with format `<timestamp> test=<name> finding=<string>`. Use an autouse module-level helper; do NOT fail the test if writing this log fails (best-effort).
  </action>
  <verify>
    <automated>DART_API_KEY="${DART_API_KEY:-}" uv run pytest tests/test_api_probes.py -xvs -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_' tests/test_api_probes.py` >= 3
    - `grep -n '_vchord_bm25_cast_array_to_bm25vector' tests/test_api_probes.py` prints at least one line
    - `grep -n 'pblntf_ty' tests/test_api_probes.py` prints at least one line (A+B filings only, D-01)
    - `grep -n "text(" tests/test_api_probes.py` prints at least one line (SQLAlchemy bind-param pattern)
    - `grep -E 'f"""|f"SELECT|f'"'"'SELECT' tests/test_api_probes.py` prints nothing (zero f-string SQL)
    - Test 2 and 3 (DB probes) pass; test 1 either passes (if DART_API_KEY set) or is skipped with a clear message
    - `cat .planning/phases/03-one-company-walking-skeleton/probe-findings.md` contains probe outcomes (even if truncated on partial skip)
  </acceptance_criteria>
  <done>
    Probe tests verify that dart-fss body accessor returns a usable shape (or the real shape is recorded) and VectorChord-BM25 INT[] casting + indexed scoring both work end-to-end. probe-findings.md provides Plan 04/05 with copy-paste SQL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml deps | Supply-chain surface — new deps trusted on pin + group isolation |
| stock-postgres | Local container, 127.0.0.1-only bound (Phase 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Information Disclosure | probe test Task 2 | mitigate | DART_API_KEY read from env only; test skips cleanly if unset — never committed to code; .env in .gitignore (Phase 1) |
| T-3-04 | Tampering | migration 0002 DDL | mitigate | DDL uses literal SQL in Alembic `op.execute` — no user input reaches the statement; column names static |
| T-3-11 | Supply chain | new deps (sentence-transformers, python-mecab-ko, tenacity) | accept | Version pinned; installed in isolated uv group; pure-pip wheels (no arbitrary build scripts) |
</threat_model>

<verification>
- `uv run alembic -c src/db/alembic.ini upgrade head` completes without error
- `docker exec stock-postgres psql -U stockwiki -d stockwiki -c "\d chunks"` shows new columns + indexes
- `uv run pytest tests/test_migration_0002.py tests/test_api_probes.py -xvs` green (api_probes test 1 allowed to SKIP if DART_API_KEY unset in CI)
- `probe-findings.md` captures actual API shapes so downstream plans can copy verbatim
</verification>

<success_criteria>
- Migration 0002 live on stock-postgres with three new chunks columns + HNSW index + BM25 index
- All 6 migration tests + at least 2 of 3 probe tests (DB probes required; DART probe optional) green
- pyproject.toml dependency groups updated; no ollama, no anthropic in mcp/ingest groups
- A4 and A5 assumptions documented in probe-findings.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-one-company-walking-skeleton/03-01-SUMMARY.md` using the summary.md template. Include the recorded dart-fss body accessor path and the exact vchord_bm25 cast SQL as canonical references downstream plans can copy.
</output>
