---
phase: 08-vault-dashboards-research-memo-templates
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .gitignore
autonomous: false
gap_closure: true
requirements: [DASH-01, DASH-04, NOTE-01]
must_haves:
  truths:
    - "CLAUDE.md Directory Layout 섹션이 vault_root vs notes_root 분리를 정확히 설명한다 (vault/notes/ 잔재 제거, repo-root notes/ 명시)"
    - "vault/notes/ 빈 디렉토리가 삭제된다 (git history 보존: tracked였다면 git rm)"
    - "repo-root ingested/ legacy 디렉토리가 정리된다 (실제 사용처는 vault/ingested/_status/)"
    - "tracked되어 있던 잔재 파일은 git rm으로 history에 보존되어 삭제된다"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Directory Layout + vault_root/notes_root 분리 멘탈 모델 명시 — GAP-01 재발 방지"
      contains: "notes_root"
    - path: ".gitignore"
      provides: "삭제된 디렉토리 / legacy 경로 정리"
  key_links:
    - from: "CLAUDE.md"
      to: "src/ingest/worker.py:ingest_run notes_root 자동 탐색"
      via: "디렉토리 도식 + 코드 동작 일치"
      pattern: "notes/private"
---

<objective>
GAP-06 (CLAUDE.md outdated) + GAP-08 (미사용 디렉토리 정리) 해소.

Purpose:
1. **GAP-06**: CLAUDE.md Directory Layout 섹션이 "vault/notes/ — 사람/LLM 노트"라고 적혀 있어, 새 협업자/agent가 GAP-01과 동일한 실수(`vault/notes/private/`로 메모 작성, 영원히 미인덱싱) 반복 가능. vault_root(`vault/`)와 notes_root(`notes/`) 분리를 명시적으로 문서화해야 함.
2. **GAP-08**: 빈 디렉토리(`vault/notes/`), legacy 디렉토리(`ingested/` at repo root) 정리. tracked되어 있다면 git rm으로 history 보존.

이 plan은 사용자 결정이 필요한 checkpoint를 1개 포함 (어떤 디렉토리를 삭제할지 최종 확인) — `autonomous: false`.

Output: CLAUDE.md 갱신 + 디렉토리 정리 + .gitignore 정합.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-vault-dashboards-research-memo-templates/08-VERIFICATION.md
@CLAUDE.md
@.gitignore
@src/ingest/worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 디렉토리 잔재 조사 + 사용처 검증</name>
  <files>.planning/phases/08-vault-dashboards-research-memo-templates/08-07-SURVEY.md</files>
  <read_first>
    - .planning/phases/08-vault-dashboards-research-memo-templates/08-VERIFICATION.md (GAP-08 list)
    - CLAUDE.md "Directory Layout" 섹션
    - .gitignore 전체
  </read_first>
  <action>
    1. 다음 명령으로 후보 식별:
       ```bash
       # 빈 디렉토리
       find . -type d -empty -not -path "./.git/*" -not -path "./.venv/*" -not -path "./node_modules/*"
       # ingested/ 두 곳 비교
       ls -la ingested/ 2>/dev/null
       ls -la vault/ingested/ 2>/dev/null
       # vault/notes 내용
       ls -la vault/notes/ 2>/dev/null
       # vault/.graphify-staging 내용
       ls -la vault/.graphify-staging/ 2>/dev/null
       # tracked 여부 확인
       git ls-files vault/notes/ ingested/ vault/.graphify-staging/
       ```

    2. 각 후보별로 **사용처 검증** (삭제 안전성):
       - `vault/notes/`: `grep -rn "vault/notes" src/ tests/ dashboards/ templates/ .planning/` — hits 분석. CLAUDE.md 외 코드 참조 0건이어야 안전 삭제.
       - `ingested/` (repo root): `grep -rn '"ingested/_status"' src/ tests/` 또는 `Path("ingested/")` — heartbeat 파일이 어디로 쓰이는지 확인. `src/ingest/heartbeat.py` 기본 경로가 vault/ingested/_status/heartbeat.md (worker.py:440)이면 repo-root ingested/ 는 legacy.
       - `vault/.graphify-staging/`: Phase 7 graphify 산출물. `.gitignore`에 이미 등록 여부 확인 (`vault/.graphify-staging/`).
       - 기타 `find -empty` 결과 각각 점검.

    3. SURVEY.md 작성:
       ```markdown
       # GAP-08 Directory Cleanup Survey

       ## 후보 디렉토리

       | Path | Tracked? | Empty? | Code references | Decision | Action |
       |------|----------|--------|-----------------|----------|--------|
       | vault/notes/ | yes/no | yes | grep hits ... | DELETE / KEEP / GITIGNORE | git rm -rf / rm -rf / .gitignore add |
       | ingested/ (repo root) | ... | ... | ... | ... | ... |
       | vault/.graphify-staging/ | ... | ... | ... | ... | ... |
       ```

    4. .gitignore 변경 사항 (필요시):
       - `ingested/`가 repo-root에 생성되지 않게 막을지 (legacy 재발 방지)
       - `vault/notes/` 재생성 방지

    Survey만 작성 — 실제 삭제는 Task 2의 checkpoint 후.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-vault-dashboards-research-memo-templates/08-07-SURVEY.md && grep -q "Decision" .planning/phases/08-vault-dashboards-research-memo-templates/08-07-SURVEY.md</automated>
  </verify>
  <acceptance_criteria>
    - SURVEY.md 존재, 각 후보별 Tracked/Empty/References/Decision/Action 4컬럼 채워짐
    - 코드 참조가 있는 디렉토리는 KEEP 결정
    - 삭제 결정된 항목 모두 grep 결과로 미사용 확인
  </acceptance_criteria>
  <done>SURVEY.md commit; Task 2 checkpoint가 결정 결과 사용</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 디렉토리 삭제 + .gitignore 변경 사용자 확인</name>
  <what-built>Task 1의 SURVEY.md가 식별한 디렉토리 삭제 계획.</what-built>
  <how-to-verify>
    1. `.planning/phases/08-vault-dashboards-research-memo-templates/08-07-SURVEY.md` 열기
    2. Decision 컬럼이 의도와 일치하는지 확인 (특히 git rm vs rm 차이)
    3. 다음 명령 검토:
       - `git status --porcelain` (untracked vs tracked 확인)
       - 실제 삭제될 파일 목록
    4. 삭제 계획 승인 또는 수정 요청
  </how-to-verify>
  <resume-signal>"approved" 또는 수정 요청 (예: "vault/notes/는 KEEP, 나머지 삭제")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md Directory Layout 갱신 + 디렉토리 정리 실행</name>
  <files>CLAUDE.md, .gitignore</files>
  <read_first>
    - CLAUDE.md "Directory Layout" 섹션 (현재 vault/notes/ 잔재 포함)
    - .planning/phases/08-vault-dashboards-research-memo-templates/08-07-SURVEY.md (Task 1 결과)
    - src/ingest/worker.py:362-369 (notes_root 자동 탐색 로직 — 문서화 대상)
  </read_first>
  <action>
    **Step 1 — CLAUDE.md Directory Layout 갱신:**

    현재 도식의 다음 부분 정정:
    ```
    ├── vault/                    # Obsidian vault — Markdown + YAML frontmatter (single source of truth)
    │   └── notes/                # 사람/LLM이 쓰는 노트 (portfolio.md 등)   ← REMOVE
    ```
    →
    ```
    ├── vault/                    # Obsidian vault — collectors가 쓰는 raw 데이터 + dashboards/graph 산출물
    │   ├── raw/                  # 수집기 산출물 (dart/, krx/, macro/, news/, kind/)
    │   ├── ingested/_status/     # 파이프라인 heartbeat
    │   └── graph/                # graphify 산출물 (gitignored)
    ├── notes/                    # 사람/LLM이 쓰는 노트 (vault와 분리된 별도 루트)
    │   └── private/              # 보유·평단·thesis (gitignored — D-03)
    ├── dashboards/               # Dataview 노트 (vault/와 분리; .obsidian이 dashboards/도 vault로 인식)
    ├── templates/notes/          # thesis/journal/portfolio 템플릿 (NOTE-01/02)
    ```

    그리고 새 섹션 추가 (Directory Layout 바로 아래):
    ```markdown
    ### vault_root vs notes_root (Phase 8 GAP-01 lesson)

    수집된 raw 데이터(`vault/raw/`)와 사용자 메모(`notes/private/`)는 별도 루트에 산다:

    - **vault_root** = `vault/` — collectors가 raw 파일을 작성하고 ingest worker가 스캔하는 루트
    - **notes_root** = `notes/private/` — 사용자 thesis/journal 메모 (gitignored)

    `stock ingest run`은 두 루트 모두 스캔한다:
    1. `<vault_root>/raw/**/*.md` — DART/KRX/news/macro/kind
    2. `<notes_root>/**/*.md` — 사용자 thesis/journal (자동 탐색: `vault_root/notes/private` → `vault_root.parent/notes/private` 순서)

    **WARNING**: `vault/notes/`에 메모를 쓰면 영원히 인덱싱되지 않는다. Obsidian sidebar에서 `notes/private/`로 작성할 것.
    ```

    **Step 2 — .gitignore 정합:**
    SURVEY.md 결정에 따라 추가 (예시):
    ```
    # Phase 8 GAP-08: legacy directories
    ingested/            # repo-root legacy — 실제 사용은 vault/ingested/_status/
    vault/notes/         # GAP-01 함정 방지 — 메모는 repo-root notes/private/
    ```

    **Step 3 — 디렉토리 삭제 실행 (SURVEY 결정대로):**
    Task 2에서 승인된 항목만:
    - tracked 디렉토리 → `git rm -rf <path>`
    - untracked 빈 디렉토리 → `rm -rf <path>`

    **Step 4 — Verify:**
    ```bash
    # vault/notes/ 부재 확인
    test ! -d vault/notes/ && echo OK
    # ingested/ at repo-root 부재 확인
    test ! -d ingested/ && echo OK
    # CLAUDE.md "notes_root" 키워드 등장 확인
    grep -q "notes_root" CLAUDE.md && echo OK
    # CLAUDE.md "vault/notes/ — 사람" 잔재 부재 확인
    ! grep -q "vault/notes/.*사람" CLAUDE.md && echo OK
    # .gitignore 갱신 확인
    grep -q "ingested/" .gitignore && echo OK
    ```
  </action>
  <verify>
    <automated>test ! -d vault/notes/ && test ! -d ingested/ && grep -q "notes_root" CLAUDE.md && grep -q "GAP-01 lesson\|GAP-01" CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `vault/notes/` 삭제됨 (또는 SURVEY 결정에 따라 KEEP)
    - `ingested/` (repo-root) 삭제됨 (또는 KEEP)
    - CLAUDE.md에 "notes_root" 키워드 등장 + GAP-01 재발 방지 문구
    - .gitignore에 정리된 디렉토리 추가 (재생성 방지)
    - 기존 코드/테스트 모두 동작 (디렉토리 의존성 0)
  </acceptance_criteria>
  <done>문서 + 정리 commit; 새로운 협업자/agent가 GAP-01 함정 회피 가능</done>
</task>

</tasks>

<verification>
- 전체 회귀 테스트: `uv run pytest -m "not slow and not e2e" --ignore=tests/perf -x` green
- `git status` 깨끗 (삭제 + .gitignore 변경 외 잔재 없음)
- CLAUDE.md 마크다운 렌더 정상 (visual check 가능)
</verification>

<success_criteria>
- CLAUDE.md outdated 잔재 0
- legacy 디렉토리 정리 완료
- 회귀 0건
</success_criteria>

<output>
After completion, create `.planning/phases/08-vault-dashboards-research-memo-templates/08-07-SUMMARY.md`
</output>
