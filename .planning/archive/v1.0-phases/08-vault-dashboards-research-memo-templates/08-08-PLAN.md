---
phase: 08-vault-dashboards-research-memo-templates
plan: 08
type: execute
wave: 2
depends_on: [08-05, 08-06]
files_modified:
  - tests/integration/test_production_layout_ingest.py
  - tests/integration/conftest.py
  - tests/integration/__init__.py
autonomous: true
gap_closure: true
requirements: [DASH-04, NOTE-03]
must_haves:
  truths:
    - "production-realistic integration test가 vault_root != notes_root 분리 layout에서 ingest를 실행하고 thesis 인덱싱 + hub 생성 + events_query 모두 검증한다"
    - "test fixture가 tmp_path를 vault_root와 notes_root로 동시 사용하지 않아 GAP-01 위양성 패턴이 재발 시 즉시 fail한다"
    - "GAP-02 entity_aliases 컬럼명 같은 silent SQL fail이 try/except 격리에 가려지지 않도록 hub_builder/edges_populate가 ingest_runs.failed에 기록되는지 검증한다"
  artifacts:
    - path: "tests/integration/test_production_layout_ingest.py"
      provides: "production-realistic E2E — vault/notes 분리 + 실제 SQL + hub + events"
      exports: ["test_full_ingest_with_split_roots", "test_silent_sql_fail_records_to_ingest_runs"]
    - path: "tests/integration/conftest.py"
      provides: "split-root fixture (vault_root != notes_root != repo_root)"
  key_links:
    - from: "tests/integration/test_production_layout_ingest.py"
      to: "src/ingest/worker.py:ingest_run"
      via: "actual ingest call with split roots"
      pattern: "vault_root=.*notes_root="
    - from: "tests/integration/test_production_layout_ingest.py"
      to: "src/ingest/hub_builder.py + events_query.py"
      via: "post-ingest 검증 단계"
      pattern: "hub_builder|events_this_week"
---

<objective>
GAP-07 해소 — Verifier 위양성 탐지 강화.

배경 (08-VERIFICATION.md GAP-07):
GAP-01 (notes_root 경로 분리 함정)과 GAP-02 (hub_builder entity_aliases 컬럼명 오류)는 모두 Phase 8 unit/E2E 테스트가 통과한 상태에서 production에서만 drop했음. 이유:
- `tests/ingest/test_note_e2e.py`가 `tmp_path`를 vault_root와 repo_root로 동시 사용 → notes_root 분리 가림
- `tests/ingest/test_hub_builder.py`가 mock entity 데이터 inject → 실제 entity_aliases.value 컬럼 join 안 함

Purpose: production-realistic integration test 추가로 동일 패턴 재발 즉시 catch. Plan 05/06 fix 후 실행되어야 의미 있음 → wave: 2.

Output: `tests/integration/` 신설, 2개 핵심 시나리오 — split-root ingest + silent SQL fail observability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-vault-dashboards-research-memo-templates/08-VERIFICATION.md
@src/ingest/worker.py
@src/ingest/hub_builder.py
@src/ingest/events_query.py
@tests/ingest/conftest.py
@tests/ingest/test_note_e2e.py

<interfaces>
<!-- Plan 05/06 fix 적용 후 worker.ingest_run 시그니처 -->

```python
def ingest_run(
    vault_root: Path,
    engine: Engine,
    *,
    force_reembed: bool = False,
    embedder: Embedder | None = None,
    notes_root: Path | None = None,  # Phase 8 GAP-01 fix
    repo_root: Path | None = None,    # Phase 8 GAP-05 fix (Plan 06)
) -> dict[str, Any]:
    ...
```

worker post-cycle hooks (이미 존재, GAP-02 fix 후):
- `price_snapshot.run(engine, repo_root=...)` — try/except 격리
- `hub_builder.run(engine, vault_root=..., repo_root=...)` — try/except 격리
- `edges_populate(committed_doc_ids, conn)` — ingest_runs row에 기록됨 (kind='edges')

ingest_runs 스키마 (Phase 2 0001 + Phase 7 0004):
- started_at, finished_at, kind, source, stats (JSONB), error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: split-root production layout integration test</name>
  <files>
    tests/integration/__init__.py,
    tests/integration/conftest.py,
    tests/integration/test_production_layout_ingest.py
  </files>
  <behavior>
    - **test_full_ingest_with_split_roots**:
      - tmp_path/repo/vault/raw/dart/x.md (실제 frontmatter; Plan 05의 EventType drift 해소된 상태 가정)
      - tmp_path/repo/notes/private/005930/thesis.md
      - vault_root != notes_root != repo_root (3개 모두 다른 Path)
      - `ingest_run(vault_root, engine, notes_root=..., repo_root=...)` 호출
      - 검증:
        1. documents 행 2개 (raw + private_note) 모두 생성
        2. documents.vault_path 모두 relative ("raw/dart/x.md", "notes/private/005930/thesis.md")
        3. chunks 행 ≥ 2
        4. vault/ingested/by-ticker/00126380.md hub 파일 생성됨 (hub_builder가 entity_aliases join 정상)
        5. events_this_week가 dart row 반환 (vault_root join 정상)
    - **test_split_roots_no_tmp_path_conflation**:
      - 명시적으로 vault_root와 notes_root가 동일 tmp_path가 아니어야 함 (assertion으로 enforce)
      - GAP-01 패턴 (`vault_root == repo_root == tmp_path`) 사용 시 명확한 에러 메시지 (test infrastructure 가드)
  </behavior>
  <read_first>
    - tests/ingest/test_note_e2e.py (현재 conflated 패턴)
    - tests/ingest/conftest.py (pg_engine, embedder fixtures)
    - src/ingest/worker.py:ingest_run 시그니처 (Plan 05/06 적용 후)
    - src/ingest/hub_builder.py (entity_aliases join 부분)
  </read_first>
  <action>
    1. `tests/integration/__init__.py` 빈 파일 생성

    2. `tests/integration/conftest.py`:
    ```python
    """Production-layout fixtures for integration tests.

    핵심 원칙: vault_root, notes_root, repo_root 3개를 명확히 분리하여
    GAP-01 (tmp_path conflation) 함정을 fixture 차원에서 차단.
    """
    from pathlib import Path
    import pytest

    @pytest.fixture
    def split_roots(tmp_path: Path) -> dict[str, Path]:
        """Production layout: repo_root contains vault/ and notes/ separately."""
        repo = tmp_path / "repo"
        vault = repo / "vault"
        notes_private = repo / "notes" / "private"
        (vault / "raw" / "dart" / "2026").mkdir(parents=True)
        (vault / "raw" / "macro" / "ecos").mkdir(parents=True)
        (vault / "ingested" / "by-ticker").mkdir(parents=True)
        (vault / "ingested" / "_status").mkdir(parents=True)
        notes_private.mkdir(parents=True)
        # Sanity gate: 3 roots must be distinct
        assert vault != notes_private and vault != repo
        return {"repo_root": repo, "vault_root": vault, "notes_root": notes_private}

    @pytest.fixture
    def seeded_entity(pg_engine):
        """Seed entities + entity_aliases so hub_builder.collect_inputs_for_corp succeeds."""
        import sqlalchemy as sa
        from db.entity import upsert_entity
        upsert_entity(pg_engine, "00126380", "삼성전자", "005930")
        # entity_aliases (value column — GAP-02 was alias)
        with pg_engine.begin() as conn:
            conn.execute(sa.text(
                "INSERT INTO entity_aliases (corp_code, kind, value) "
                "VALUES (:cc, 'krx_ticker', :v) ON CONFLICT DO NOTHING"
            ), {"cc": "00126380", "v": "005930"})
        return "00126380"
    ```

    3. `tests/integration/test_production_layout_ingest.py`:
    ```python
    """Phase 8 GAP-07 — production-layout integration tests.

    GAP-01 (notes_root) and GAP-02 (entity_aliases.value) both passed
    unit/E2E tests in Plan 04 but failed production. These tests force
    split roots + real SQL joins to catch the same class of regression.
    """
    from pathlib import Path
    import sqlalchemy as sa
    from ingest.worker import ingest_run


    DART_FM = """\
    ---
    provenance:
      source: dart
      date: '2026-04-26'
      corp_code: '00126380'
      ticker: '005930'
      company_name: '삼성전자'
      fetched_at: 2026-04-26 00:00:00+00:00
      lang: ko
      content_hash: ''
    ingest_state:
      injection_flags: []
      processed: false
    _derived:
      tickers: ['005930']
      event_type: earnings_release
      catalysts: []
      numeric_facts: []
    ---
    # 본문 — 삼성전자 실적 발표
    """

    THESIS_FM = """\
    ---
    type: thesis
    tickers: ['005930']
    tags: ['memory']
    created: 2026-05-08T00:00:00+09:00
    updated: 2026-05-08T00:00:00+09:00
    author: y
    kill_criteria: ['HBM 점유율 30% 미만으로 하락']
    conviction: medium
    target_price: null
    ---
    # 005930 Thesis
    HBM 사이클 + AI 투자 지속 가정
    """


    def test_full_ingest_with_split_roots(pg_engine, split_roots, seeded_entity, embedder_stub):
        v = split_roots["vault_root"]
        n = split_roots["notes_root"]
        repo = split_roots["repo_root"]

        # Write fixtures
        (v / "raw" / "dart" / "2026" / "x.md").write_text(DART_FM, encoding="utf-8")
        (n / "005930" / "thesis.md").parent.mkdir(parents=True, exist_ok=True)
        (n / "005930" / "thesis.md").write_text(THESIS_FM, encoding="utf-8")

        # Real ingest call with split roots
        stats = ingest_run(
            vault_root=v,
            engine=pg_engine,
            notes_root=n,
            repo_root=repo,
            embedder=embedder_stub,
        )
        assert stats["failed"] == [], stats["failed"]
        assert stats["succeeded"] >= 2

        with pg_engine.begin() as conn:
            rows = conn.execute(sa.text(
                "SELECT vault_path, source FROM documents ORDER BY source"
            )).fetchall()
        sources = {r.source for r in rows}
        assert sources == {"dart", "private_note"}, sources

        # GAP-05: relative paths
        for r in rows:
            assert not Path(r.vault_path).is_absolute(), f"absolute path leaked: {r.vault_path}"

        # GAP-02: hub file generated (entity_aliases.value join must succeed)
        hub_path = v / "ingested" / "by-ticker" / "00126380.md"
        assert hub_path.exists(), "hub_builder failed silently — likely entity_aliases regression"

        # DASH-03: events_this_week reads vault_path correctly
        from ingest.events_query import events_this_week
        events = events_this_week(
            pg_engine,
            holdings_tickers=["005930"],
            today=__import__("datetime").date(2026, 4, 28),  # Monday of that week
            vault_root=v,
            limit=10,
        )
        assert len(events) >= 1, "events_this_week returned empty — vault_path resolution broken"


    def test_split_roots_distinct_assertion(split_roots):
        """GAP-01 guard — fixture must enforce distinct roots."""
        v, n, r = split_roots["vault_root"], split_roots["notes_root"], split_roots["repo_root"]
        assert v != n
        assert v != r
        assert n != r
        # 추가: notes_root는 vault_root 하위가 아님 (production layout)
        try:
            n.relative_to(v)
            raise AssertionError("notes_root must NOT be under vault_root in production")
        except ValueError:
            pass  # expected
    ```

    4. Marker 등록 (필요 시): `tests/integration/`이 pytest collection에 포함되도록 `pyproject.toml`의 `tool.pytest.ini_options.testpaths` 확인 — 만약 `["tests"]`이면 자동 포함.

    5. Run:
    ```bash
    uv run pytest tests/integration/ -x -v
    ```
    Plan 05 + Plan 06이 적용된 상태에서 모두 GREEN이어야 함.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 2개 테스트 모두 통과
    - test가 vault_root, notes_root, repo_root를 분리하여 fixture 차원에서 GAP-01 패턴 차단
    - hub 파일 생성 검증으로 GAP-02 silent fail 패턴 차단
    - relative path 검증으로 GAP-05 회귀 차단
    - events_this_week 호출로 DASH-03 + vault_path resolution 검증
  </acceptance_criteria>
  <done>tests/integration/ 도입 완료; production-realistic E2E 신설</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: silent SQL fail observability test (GAP-02 클래스 가드)</name>
  <files>
    tests/integration/test_production_layout_ingest.py (append),
    src/ingest/worker.py (필요 시 best-effort hook이 ingest_runs에 fail 기록하도록 보강)
  </files>
  <behavior>
    - **test_silent_sql_fail_records_to_ingest_runs**:
      - hub_builder.run 또는 price_snapshot.run을 monkeypatch로 강제 raise
      - ingest_run 호출 → stats는 정상 반환 (try/except 격리 유지)
      - **그러나** ingest_runs 테이블 또는 heartbeat에 hub_builder/price_snapshot 실패가 기록되어야 함
      - 현재 worker.py:458-468은 logger.exception만 호출 — DB/heartbeat에 기록 없음. 이게 GAP-02가 silent하게 production drop한 근본 원인.
      - 테스트가 fail이면 worker.py 보강 필요 (Step 2 참조).
  </behavior>
  <read_first>
    - src/ingest/worker.py:452-468 (Phase 8 D-01 hooks — try/except)
    - src/ingest/heartbeat.py:record_source_run 시그니처
  </read_first>
  <action>
    **Step 1 — RED:**
    ```python
    def test_silent_sql_fail_records_to_ingest_runs(pg_engine, split_roots, monkeypatch, embedder_stub):
        """GAP-02-class regression: best-effort hooks must surface failures."""
        v = split_roots["vault_root"]
        n = split_roots["notes_root"]
        repo = split_roots["repo_root"]
        (v / "raw" / "dart" / "2026" / "x.md").write_text(DART_FM, encoding="utf-8")

        # Force hub_builder to raise — simulating GAP-02 SQL column name regression
        from ingest import hub_builder
        def _broken_run(*a, **kw):
            raise RuntimeError("simulated entity_aliases.alias column does not exist")
        monkeypatch.setattr(hub_builder, "run", _broken_run)

        stats = ingest_run(vault_root=v, engine=pg_engine, notes_root=n, repo_root=repo, embedder=embedder_stub)

        # ingest itself succeeds (D-01 isolation)
        assert stats["succeeded"] >= 1

        # But the failure must be visible somewhere
        with pg_engine.begin() as conn:
            hook_failures = conn.execute(sa.text(
                "SELECT source, error FROM ingest_runs "
                "WHERE source IN ('hub_builder', 'price_snapshot') AND error IS NOT NULL"
            )).fetchall()
        # Or, alternatively, heartbeat
        heartbeat = (v / "ingested" / "_status" / "heartbeat.md").read_text(encoding="utf-8") \
            if (v / "ingested" / "_status" / "heartbeat.md").exists() else ""

        assert hook_failures or "hub_builder" in heartbeat, (
            "hub_builder failure not recorded — GAP-02 silent-fail pattern still possible"
        )
    ```

    **Step 2 — GREEN (worker.py 보강 필요할 가능성 높음):**

    현 worker.py:458-468 hook 블록을 다음으로 보강:
    ```python
    def _record_hook_failure(engine, source: str, exc: Exception, vault_root: Path) -> None:
        """Surface best-effort hook failures via ingest_runs + heartbeat."""
        try:
            with engine.begin() as conn:
                conn.execute(sa.text(
                    "INSERT INTO ingest_runs (started_at, finished_at, kind, source, stats, error) "
                    "VALUES (now(), now(), 'hook', :s, CAST('{}' AS JSONB), :e)"
                ), {"s": source, "e": str(exc)[:500]})
        except Exception:
            logger.exception("failed to record hook failure for %s", source)
        try:
            record_source_run(
                source,
                {"total": 1, "succeeded": 0, "skipped": 0, "failed": [str(exc)[:200]]},
                heartbeat_path=vault_root / "ingested/_status/heartbeat.md",
            )
        except Exception:
            logger.exception("failed to heartbeat hook failure for %s", source)

    # Phase 8 D-01 hooks 블록:
    try:
        from ingest import price_snapshot
        price_snapshot.run(engine, repo_root=vault_root)
    except Exception as exc:  # noqa: BLE001
        logger.exception("Phase 8 price_snapshot hook failed")
        _record_hook_failure(engine, "price_snapshot", exc, vault_root)

    try:
        from ingest import hub_builder
        hub_builder.run(engine, vault_root=vault_root, repo_root=vault_root)
    except Exception as exc:  # noqa: BLE001
        logger.exception("Phase 8 hub_builder hook failed")
        _record_hook_failure(engine, "hub_builder", exc, vault_root)
    ```

    `kind='hook'`은 기존 ingest_runs CHECK constraint 허용 여부 점검 필요 — 만약 enum 제약이면 CHECK constraint를 'hook'/'edges'/'ingest' 등 허용으로 확장하는 작은 ALTER 마이그레이션 필요. 우선 `grep -n "kind" src/db/migrations/versions/*.py | grep -i check`로 확인하고, 제약 없거나 'edges'까지 허용된 상태면 그대로 사용 가능.

    **Step 3 — Verify:**
    ```bash
    uv run pytest tests/integration/test_production_layout_ingest.py::test_silent_sql_fail_records_to_ingest_runs -x
    ```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_production_layout_ingest.py -x -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test_silent_sql_fail_records_to_ingest_runs 통과
    - hub_builder/price_snapshot 실패 시 ingest_runs 또는 heartbeat에 가시화됨
    - D-01 isolation은 보존 (ingest stats.succeeded 정상)
    - tests/integration/ 전체 green
    - 전체 회귀: `uv run pytest -m "not slow and not e2e" --ignore=tests/perf -x` green
  </acceptance_criteria>
  <done>silent SQL fail observability 보강; verifier가 production drop 패턴 catch</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/integration/ -x -v` green (Plan 05/06 적용 전제)
- 전체 회귀 green
- COLL-07 guard 유지
</verification>

<success_criteria>
- GAP-01/02 패턴 재발 시 즉시 catch하는 production-realistic E2E 도입
- best-effort hook 실패가 더이상 silent하지 않음
- verifier 보강 — 향후 vault_root != notes_root 분리 검증 강제
</success_criteria>

<output>
After completion, create `.planning/phases/08-vault-dashboards-research-memo-templates/08-08-SUMMARY.md`
</output>
