---
phase: 02-decision-card-schema-storage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cards/__init__.py
  - src/cards/models.py
  - tests/cards/__init__.py
  - tests/cards/conftest.py
  - tests/cards/test_models.py
autonomous: true
requirements: [SC#3, SC#5]
tags: [pydantic, decision_card, validation]

must_haves:
  truths:
    - "DecisionCard.model_validate(redesign §3 YAML) succeeds and round-trips (SC#3)"
    - "A card with no expires_at raises ValidationError (SC#5 hard veto / Veto #2)"
    - "A card with empty assumptions[] raises ValidationError (SC#5 hard veto / Veto #2)"
    - "Unknown payload keys are rejected (extra='forbid', ASVS V5)"
    - "numeric_facts preserves int (market_cap_krw) and float (pe_ttm) without coercion drift"
  artifacts:
    - path: "src/cards/models.py"
      provides: "DecisionCard + nested Decision/KeyClaim/Contradiction Pydantic v2 models"
      contains: "assumptions: list[str] = Field(min_length=1)"
    - path: "tests/cards/conftest.py"
      provides: "decision_card_yaml fixture (SC#3 round-trip oracle) + seeded_engine fixture for Plan 03"
      contains: "card_005930_2026-05-28"
    - path: "tests/cards/test_models.py"
      provides: "round-trip + 2 hard-veto rejection tests"
      contains: "test_round_trip"
  key_links:
    - from: "tests/cards/test_models.py"
      to: "src/cards/models.py DecisionCard"
      via: "model_validate(decision_card_yaml)"
      pattern: "DecisionCard.model_validate"
    - from: "src/cards/models.py DecisionCard"
      to: "SC#5 hard veto"
      via: "non-Optional expires_at + assumptions min_length=1"
      pattern: "expires_at: datetime"
---

<objective>
Define the `DecisionCard` Pydantic v2 model (with nested `Decision`, `KeyClaim`,
`Contradiction` sub-models) in `src/cards/models.py`, matching the redesign §3 YAML field
names exactly so it round-trips, and enforce the SC#5 hard veto (no untimed thesis, no
empty assumptions) purely through field declarations. Create the `tests/cards/` package with
the `decision_card_yaml` round-trip oracle fixture and the `seeded_engine` fixture that Plan
03 consumes, plus the model unit tests.

Purpose: `DecisionCard` is the typed contract every downstream phase produces and consumes
(Veto #7 — typed Pydantic, never raw dicts). Locking it to the §3 schema and enforcing Veto
#2 at the model boundary means malformed cards never reach the DB.

Output: `src/cards/models.py`, `src/cards/__init__.py` (partial barrel), `tests/cards/`
package + `conftest.py` + `test_models.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-decision-card-schema-storage/02-RESEARCH.md
@.planning/phases/02-decision-card-schema-storage/02-PATTERNS.md
@.planning/phases/02-decision-card-schema-storage/02-VALIDATION.md
@.planning/research/redesign-2026-05.md

<interfaces>
<!-- The DecisionCard contract THIS plan authors. Plan 03 store.py imports DecisionCard and
     reconstructs instances from DB rows; field names MUST match redesign §3 YAML exactly. -->
DecisionCard fields (all from §3 YAML, redesign lines 236-294):
  card_id: str
  corp_code: str  (pattern ^[0-9]{8}$)
  ticker: str     (pattern ^[0-9]{6}$)
  generated_at: datetime
  as_of: datetime
  schema_version: int = 1
  decision: Decision
  key_claims: list[KeyClaim] = []
  contradictions: list[Contradiction] = []     # Veto #3: 1st-class output
  assumptions: list[str] = Field(min_length=1)  # SC#5 HARD VETO
  numeric_facts: dict[str, float | int] = {}    # shape-only; Phase 4 checksums digits
  evidence_weights: dict[str, str] = {}
  guards_passed: list[str] = []
  expires_at: datetime                           # SC#5 HARD VETO (no default, no Optional)
  body_md: str

Decision: stance Literal[BUY,ADD,HOLD,TRIM,SELL,AVOID]; conviction float ge0 le1;
          horizon_days int gt0; price_ref float|None=None; invalidation_triggers list[str]=[]
KeyClaim: id str; text str; evidence_refs list[str]=[]; weight Literal[HIGH,MEDIUM,LOW,CONTEXT];
          confidence float ge0 le1
Contradiction: bull str; bear_evidence str; bear_claim str; resolution str

All models: model_config = ConfigDict(extra="forbid")
Round-trip semantics (SC#3, OQ-3): DecisionCard.model_validate(card.model_dump(mode="json")) == card
</interfaces>

<!-- House Pydantic v2 conventions + the round-trip oracle -->
@src/shared/portfolio.py
@src/shared/frontmatter.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define DecisionCard Pydantic models + package barrel</name>
  <read_first>
    - src/shared/portfolio.py (lines 28-63) — house Pydantic v2 convention: `model_config = ConfigDict(extra="forbid")`, `Field(ge=0)`, `@field_validator @classmethod _validate_ticker` raising `ValueError`.
    - src/shared/frontmatter.py (lines 27-67) — `Literal[...]` enums, `Field(pattern=...)`, ISO-8601 datetime auto-coercion.
    - .planning/research/redesign-2026-05.md lines 236-294 — the §3 decision_card YAML. This is the field-name source of truth. Match `stance`/`conviction`/`horizon_days`/`price_ref`/`invalidation_triggers`/`key_claims`(id,text,evidence_refs,weight,confidence)/`contradictions`(bull,bear_evidence,bear_claim,resolution)/`assumptions`/`numeric_facts`/`evidence_weights`/`guards_passed`/`expires_at` exactly.
    - .planning/phases/02-decision-card-schema-storage/02-RESEARCH.md § Code Examples "Pydantic — DecisionCard with hard-veto fields" + Pattern 2 (hard-veto via non-Optional + min_length) + Pitfall #4 (round-trip tz/int-vs-float) + A2 (numeric_facts is permissive dict).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "src/cards/models.py" + § "src/cards/__init__.py".
  </read_first>
  <behavior>
    - DecisionCard.model_validate(decision_card_yaml) succeeds (all §3 fields accepted).
    - Round-trip: DecisionCard.model_validate(card.model_dump(mode="json")) == card.
    - numeric_facts['market_cap_krw'] stays int 425000000000000; numeric_facts['pe_ttm'] stays float 17.2.
    - Decision.conviction outside [0,1] → ValidationError; horizon_days <= 0 → ValidationError.
    - Unknown top-level key in payload → ValidationError (extra='forbid').
  </behavior>
  <files>src/cards/models.py, src/cards/__init__.py</files>
  <action>
    Create `src/cards/models.py` with Pydantic v2 models matching the `<interfaces>` block. Define nested `Decision`, `KeyClaim`, `Contradiction` first, then `DecisionCard`. Every model gets `model_config = ConfigDict(extra="forbid")` (ASVS V5 — reject unknown keys).

    Enforce SC#5 hard veto via FIELD DECLARATIONS ONLY (no `@model_validator`): `expires_at: datetime` with no default and no `| None` (omission → ValidationError), and `assumptions: list[str] = Field(min_length=1)` (empty list → ValidationError). This matches CONTEXT's "leave shape to the type system" and Veto #2.

    Use `corp_code: str = Field(pattern=r"^[0-9]{8}$")` and `ticker: str = Field(pattern=r"^[0-9]{6}$")`. Type `numeric_facts: dict[str, float | int] = Field(default_factory=dict)` (permissive — Phase 4 does the digit checksum; Phase 2 validates shape only). `decision: Decision` is required; `key_claims`/`contradictions`/`guards_passed` default to empty lists; `evidence_weights` defaults to empty dict. `schema_version: int = 1`. `body_md: str` required.

    Create `src/cards/__init__.py` re-exporting `DecisionCard` from `.models` with `__all__ = ["DecisionCard"]`. (Plan 03 extends this barrel with the store helpers — author it so adding the store imports is additive.) The `src/cards/__init__.py` file makes `src/cards/` an importable package.
  </action>
  <verify>
    <automated>uv run python -c "from cards.models import DecisionCard, Decision, KeyClaim, Contradiction; from cards import DecisionCard as DC; assert DC is DecisionCard" &amp;&amp; uv run mypy src/cards/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/cards/models.py` exists with classes `DecisionCard`, `Decision`, `KeyClaim`, `Contradiction`.
    - File contains literal `assumptions: list[str] = Field(min_length=1)`.
    - File contains `expires_at: datetime` with no `= ` default and no `| None`.
    - Every model class declares `model_config = ConfigDict(extra="forbid")`.
    - `numeric_facts` is typed `dict[str, float | int]`.
    - `src/cards/__init__.py` re-exports `DecisionCard`.
    - `uv run mypy src/cards/models.py` passes (strict).
  </acceptance_criteria>
  <done>`DecisionCard` and its nested models are importable, mypy-clean, with SC#5 enforced purely by field declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/cards package + conftest fixtures (round-trip oracle + seeded_engine)</name>
  <read_first>
    - tests/conftest.py (lines 19-41) — `SAMPLE_YAML` + `sample_yaml` fixture: the house pattern for an inline dict/yaml fixture.
    - tests/db/conftest.py (lines 44-62) — the `seeded_engine` fixture: pre-inserts 삼성전자/00126380/005930 into `entities` + `entity_aliases` so the corp_code FK is satisfiable. This must be RE-DECLARED under tests/cards/ (conftest scoping is per-directory).
    - .planning/research/redesign-2026-05.md lines 236-294 — the §3 YAML to transcribe verbatim into the `decision_card_yaml` fixture (card_id `card_005930_2026-05-28`, corp_code `00126380`, ticker `005930`).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "tests/cards/conftest.py" — both analog blocks (seeded_engine + decision_card_yaml) and the note that body_md is not in §3 YAML so add a minimal non-empty string.
  </read_first>
  <files>tests/cards/__init__.py, tests/cards/conftest.py</files>
  <action>
    Create `tests/cards/__init__.py` (empty package marker, mirror the other tests subpackages).

    Create `tests/cards/conftest.py` with TWO fixtures:
    (a) `decision_card_yaml() -> dict` — transcribe the redesign §3 YAML (lines 236-294) verbatim as a Python dict: `card_id="card_005930_2026-05-28"`, `corp_code="00126380"`, `ticker="005930"`, `generated_at="2026-05-28T17:42+09:00"`, `as_of="2026-05-28T16:00+09:00"`, `schema_version=1`, the full `decision`/`key_claims`/`contradictions`/`assumptions`/`numeric_facts` (with `market_cap_krw=425000000000000` int and `pe_ttm=17.2` float)/`evidence_weights`/`guards_passed`/`expires_at="2026-06-15T00:00+09:00"`, plus `body_md` set to a minimal non-empty human-render string (the §3 YAML has no body_md — add one, e.g. a one-line markdown header). This dict IS the SC#3 round-trip oracle.
    (b) `seeded_engine(pg_clean)` — copy the block from `tests/db/conftest.py:44-62` verbatim (it seeds exactly 00126380/삼성전자/005930, which matches the YAML's corp_code/ticker). Re-declare it here because conftest scoping is per-directory; Plan 03's `test_store.py` consumes it.
  </action>
  <verify>
    <automated>uv run python -c "import importlib.util; spec=importlib.util.spec_from_file_location('cf','tests/cards/conftest.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert hasattr(m,'decision_card_yaml') and hasattr(m,'seeded_engine')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/cards/__init__.py` exists (package marker).
    - `tests/cards/conftest.py` defines fixtures `decision_card_yaml` and `seeded_engine`.
    - `decision_card_yaml` contains the literal `card_005930_2026-05-28` and corp_code `00126380`.
    - `numeric_facts` in the fixture has `market_cap_krw` as an int literal (no decimal) and `pe_ttm` as `17.2`.
    - `decision_card_yaml` includes a non-empty `body_md` string.
    - The verify command (importing the module and asserting both fixtures exist) exits 0.
  </acceptance_criteria>
  <done>The `tests/cards/` package exists with the §3 round-trip oracle fixture and a re-declared `seeded_engine` ready for Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Round-trip + hard-veto rejection tests (SC#3, SC#5)</name>
  <read_first>
    - .planning/phases/02-decision-card-schema-storage/02-RESEARCH.md § Code Examples "Round-trip test (SC#3)" — the exact three test bodies (`test_round_trip`, `test_missing_expiry_rejected`, `test_empty_assumptions_rejected`).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "tests/cards/test_models.py" — round-trip semantics `model_validate(card.model_dump(mode="json")) == card` (NOT byte-identical YAML; OQ-3 / Pitfall #4).
    - .planning/phases/02-decision-card-schema-storage/02-VALIDATION.md § Per-Task Verification Map — exact test names SC#3=`test_round_trip`, SC#5=`test_missing_expiry_rejected`.
    - src/cards/models.py (Task 1 output) — the model under test.
  </read_first>
  <behavior>
    - test_round_trip: DecisionCard.model_validate(decision_card_yaml) then model_validate(model_dump(mode="json")) equals the original card.
    - test_missing_expiry_rejected: popping expires_at from the fixture → pytest.raises(ValidationError).
    - test_empty_assumptions_rejected: assumptions=[] → pytest.raises(ValidationError).
    - test_extra_key_rejected: adding an unknown top-level key → pytest.raises(ValidationError).
    - test_numeric_facts_types_preserved: after round-trip, market_cap_krw is int and pe_ttm is float.
  </behavior>
  <files>tests/cards/test_models.py</files>
  <action>
    Create `tests/cards/test_models.py` consuming the `decision_card_yaml` fixture. Write the five tests above. Round-trip is semantic equality: `card = DecisionCard.model_validate(decision_card_yaml); assert DecisionCard.model_validate(card.model_dump(mode="json")) == card`. For the two SC#5 rejections, build a mutated copy of the fixture dict (`{**decision_card_yaml}` then `.pop("expires_at")` / set `assumptions=[]`) and wrap `DecisionCard.model_validate(bad)` in `with pytest.raises(ValidationError)`. Import `from cards.models import DecisionCard` and `from pydantic import ValidationError`.
  </action>
  <verify>
    <automated>uv run pytest tests/cards/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/cards/test_models.py::test_round_trip -x` exits 0 (SC#3).
    - `uv run pytest tests/cards/test_models.py::test_missing_expiry_rejected -x` exits 0 (SC#5).
    - `uv run pytest tests/cards/test_models.py::test_empty_assumptions_rejected -x` exits 0 (SC#5).
    - `uv run pytest tests/cards/test_models.py::test_extra_key_rejected -x` exits 0 (ASVS V5).
    - Round-trip assertion uses `model_dump(mode="json")` semantic equality, not byte-identical YAML.
  </acceptance_criteria>
  <done>SC#3 round-trip and both SC#5 hard-veto rejections pass; `extra="forbid"` rejection verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card input (dict/YAML) → DecisionCard model | Untrusted card payload (eventually from Phase 4 analysis runner) crosses into the typed model here. This is the primary input-validation boundary for the whole card pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | malformed/extra-key card payload reaching storage | mitigate | `model_config = ConfigDict(extra="forbid")` on every model rejects unknown keys before INSERT (ASVS V5 Input Validation); shape enforced by Pydantic types |
| T-02-05 | Repudiation | untimed thesis (no expiry / no assumptions) stored as if valid | mitigate | SC#5 hard veto via non-Optional `expires_at` + `assumptions Field(min_length=1)` — ValidationError at model boundary (Veto #2). Card cannot exist without an expiry ledger. |
| T-02-06 | Information Disclosure | numeric fact coercion drift (int→float) corrupting source-faithful values | accept | `dict[str, float | int]` preserves both; digit-level verbatim checksum is Phase 4's responsibility (CONTEXT lock). Phase 2 only validates shape. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs NO new packages — Pydantic 2.13.1 already pinned in uv.lock (RESEARCH Package Legitimacy Audit: N/A). No slopcheck surface. |
</threat_model>

<verification>
- `uv run mypy src/cards/models.py` passes (strict typing).
- `uv run pytest tests/cards/test_models.py -x -q` — all model tests green.
- `uv run pytest -m "not slow and not e2e"` — full suite still green.
</verification>

<success_criteria>
- SC#3: `DecisionCard` round-trips the redesign §3 YAML (`test_round_trip`).
- SC#5: missing `expires_at` OR empty `assumptions` → ValidationError (`test_missing_expiry_rejected`, `test_empty_assumptions_rejected`).
- ASVS V5: unknown payload keys rejected (`test_extra_key_rejected`).
</success_criteria>

<output>
Create `.planning/phases/02-decision-card-schema-storage/02-02-SUMMARY.md` when done.
</output>
