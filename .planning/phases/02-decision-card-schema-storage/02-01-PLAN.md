---
phase: 02-decision-card-schema-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/versions/0007_decision_cards.py
  - src/db/entity_models.py
  - tests/conftest.py
  - tests/db/test_migration_0007.py
autonomous: true
requirements: [SC#1, SC#2, SC#6]
tags: [postgres, alembic, schema, decision_card]

must_haves:
  truths:
    - "Migration 0007 creates decision_cards with all 12 locked columns (SC#1)"
    - "Self-referential supersedes/superseded_by FKs resolve in one create_table (SC#1)"
    - "Three mandatory indexes exist with generated_at DESC ordering on the composite (SC#2)"
    - "body_tsv is a GENERATED ALWAYS AS (to_tsvector('simple',...)) STORED column with a GIN index (SC#6)"
    - "DecisionCard ORM column set matches the live DB column set (drift guard)"
  artifacts:
    - path: "src/db/migrations/versions/0007_decision_cards.py"
      provides: "decision_cards DDL: table, CheckConstraint, self-ref FKs, body_tsv GENERATED, 4 indexes"
      contains: "to_tsvector('simple'"
    - path: "src/db/entity_models.py"
      provides: "DecisionCard ORM class on existing Base"
      contains: "class DecisionCard(Base)"
    - path: "tests/db/test_migration_0007.py"
      provides: "schema-shape + index + body_tsv + ORM round-trip tests"
      contains: "ck_decision_cards_status"
  key_links:
    - from: "src/db/migrations/versions/0007_decision_cards.py"
      to: "entities.corp_code"
      via: "ForeignKey ondelete=CASCADE"
      pattern: "ForeignKey\\(\"entities.corp_code\""
    - from: "src/db/migrations/versions/0007_decision_cards.py"
      to: "decision_cards.card_id"
      via: "self-referential FK ondelete=SET NULL (supersedes + superseded_by)"
      pattern: "ForeignKey\\(\"decision_cards.card_id\""
    - from: "src/db/entity_models.py"
      to: "decision_cards live table"
      via: "test_orm_round_trip column-set parity"
      pattern: "set\\(Base.metadata.tables\\)"
---

<objective>
Author Alembic migration 0007 creating the `decision_cards` table (12 locked columns,
TEXT+CheckConstraint status, two self-referential FKs, a GENERATED `body_tsv` tsvector
column, and four indexes), declare the matching `DecisionCard` ORM class on the existing
`entity_models.Base`, register the table for TRUNCATE hygiene, and prove the schema with a
clone of `test_migration_0006.py`.

Purpose: `decision_cards` is the canonical storage for all v2.0 analysis output (Phase 3 MCP
reads, Phase 4 analysis writes, Phase 5 briefing rows). The schema must be locked exactly to
ROADMAP SC#1/#2/#6 before any CRUD or model code consumes it.

Output: migration 0007, `DecisionCard` ORM class, `decision_cards` in `_LIVE_TABLES`,
`tests/db/test_migration_0007.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-decision-card-schema-storage/02-RESEARCH.md
@.planning/phases/02-decision-card-schema-storage/02-PATTERNS.md
@.planning/phases/02-decision-card-schema-storage/02-VALIDATION.md

<interfaces>
<!-- Locked column set (SC#1). Authored by THIS migration; downstream plans bind to these names. -->
decision_cards columns (12 + generated body_tsv):
  card_id        TEXT  PRIMARY KEY
  corp_code      CHAR(8)  FK→entities.corp_code  ON DELETE CASCADE  NOT NULL
  ticker         CHAR(6)  NULLABLE
  generated_at   TIMESTAMPTZ  NOT NULL  (NO server_default — data-meaningful, OQ-2)
  as_of          TIMESTAMPTZ  NOT NULL  (NO server_default)
  payload        JSONB  NOT NULL
  body_md        TEXT  NOT NULL
  status         TEXT  NOT NULL  server_default 'active'  CHECK IN ('active','superseded','invalidated')
  supersedes     TEXT  FK→decision_cards.card_id  ON DELETE SET NULL  NULLABLE
  superseded_by  TEXT  FK→decision_cards.card_id  ON DELETE SET NULL  NULLABLE
  expires_at     TIMESTAMPTZ  NOT NULL  (NO server_default — SC#5)
  schema_version SMALLINT  NOT NULL  server_default 1
  body_tsv       TSVECTOR  GENERATED ALWAYS AS (to_tsvector('simple', coalesce(body_md,''))) STORED

Index names (SC#2 + SC#6):
  ix_decision_cards_corp_status_gen  ON (corp_code, status, generated_at DESC)
  ix_decision_cards_supersedes       ON (supersedes)
  ix_decision_cards_expires          ON (expires_at)
  ix_decision_cards_body_tsv         ON (body_tsv) USING gin
CheckConstraint name: ck_decision_cards_status
</interfaces>

<!-- Analogs to copy from (the executor reads these directly — no exploration needed) -->
@src/db/migrations/versions/0006_phase01_domain_tables.py
@src/db/entity_models.py
@tests/db/test_migration_0006.py
@tests/conftest.py
@scripts/init-extensions.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 0007 (decision_cards DDL)</name>
  <read_first>
    - src/db/migrations/versions/0006_phase01_domain_tables.py — primary analog. The `filings` block (lines 73-125) for corp_code FK + body_md; the `events` block (lines 290-322) for soft FK `events.filing_rcept_no` ondelete=SET NULL and the `ck_events_*` CheckConstraint shape; lines 348-351 `ck_collector_runs_source` and 346 `collector_runs.stats` JSONB; the halfvec `op.execute("ALTER TABLE ... ADD COLUMN ...")` technique (lines ~113, ~176) — reuse the TECHNIQUE for body_tsv, NOT the halfvec type.
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "src/db/migrations/versions/0007_decision_cards.py" — exact revision header, create_table, CheckConstraint, self-ref FK, body_tsv ALTER, index, and downgrade snippets.
    - .planning/phases/02-decision-card-schema-storage/02-RESEARCH.md § Code Examples "DDL — migration 0007 core table" + Pitfall #1 (no 'korean' config), Pitfall #5 (single-col self-ref FK works inline), Pitfall #6 (GIN on generated column).
    - scripts/init-extensions.sql — proves no `korean` tsearch config installed; `simple` is the only safe built-in.
  </read_first>
  <files>src/db/migrations/versions/0007_decision_cards.py</files>
  <action>
    Create migration `0007_decision_cards.py` with header `revision = "0007"`, `down_revision = "0006"`, `branch_labels = None`, `depends_on = None`, importing `sqlalchemy as sa`, `from alembic import op`, `from sqlalchemy.dialects import postgresql`.

    In `upgrade()`, `op.create_table("decision_cards", ...)` with the 12 locked columns from the `<interfaces>` block above: `card_id` `sa.Text` `primary_key=True`; `corp_code` `sa.CHAR(8)` `sa.ForeignKey("entities.corp_code", ondelete="CASCADE")` `nullable=False`; `ticker` `sa.CHAR(6)` `nullable=True`; `generated_at`/`as_of`/`expires_at` `sa.DateTime(timezone=True)` `nullable=False` with NO server_default (data-meaningful per OQ-2); `payload` `postgresql.JSONB` `nullable=False`; `body_md` `sa.Text` `nullable=False`; `status` `sa.Text` `nullable=False` `server_default=sa.text("'active'")`; `supersedes` and `superseded_by` each `sa.Text` `sa.ForeignKey("decision_cards.card_id", ondelete="SET NULL")` `nullable=True`; `schema_version` `sa.SmallInteger` `nullable=False` `server_default=sa.text("1")`. Add inline `sa.CheckConstraint("status IN ('active','superseded','invalidated')", name="ck_decision_cards_status")`.

    After `create_table`, add the GENERATED tsvector column via raw SQL (mirrors 0006's halfvec ALTER habit): `op.execute("ALTER TABLE decision_cards ADD COLUMN body_tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', coalesce(body_md, ''))) STORED")`. Use `'simple'` — `'korean'` does NOT exist and HARD-FAILS the migration (Pitfall #1).

    Create the four indexes: `ix_decision_cards_corp_status_gen` on `["corp_code", "status", sa.text("generated_at DESC")]`; `ix_decision_cards_supersedes` on `["supersedes"]`; `ix_decision_cards_expires` on `["expires_at"]`; `ix_decision_cards_body_tsv` on `["body_tsv"]` with `postgresql_using="gin"`.

    In `downgrade()`, drop the four indexes (body_tsv → expires → supersedes → corp_status_gen) then `op.drop_table("decision_cards")`.
  </action>
  <verify>
    <automated>uv run alembic -c src/db/alembic.ini upgrade head &amp;&amp; uv run alembic -c src/db/alembic.ini downgrade -1 &amp;&amp; uv run alembic -c src/db/alembic.ini upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - File `src/db/migrations/versions/0007_decision_cards.py` exists with `revision = "0007"` and `down_revision = "0006"`.
    - File contains the literal `to_tsvector('simple'` (NOT `'korean'`).
    - File contains `CheckConstraint("status IN ('active','superseded','invalidated')"` named `ck_decision_cards_status`.
    - File contains two `ForeignKey("decision_cards.card_id", ondelete="SET NULL")` declarations (supersedes + superseded_by).
    - File contains all four index names: `ix_decision_cards_corp_status_gen`, `ix_decision_cards_supersedes`, `ix_decision_cards_expires`, `ix_decision_cards_body_tsv` (the last with `postgresql_using="gin"`).
    - `generated_at`, `as_of`, `expires_at` columns have NO `server_default` argument.
    - The upgrade→downgrade→upgrade cycle in the verify command exits 0 (downgrade reversibility proven).
  </acceptance_criteria>
  <done>Migration 0007 applies cleanly on a fresh Postgres 17 + vchord container, is reversible, and uses `to_tsvector('simple', ...)` for the GENERATED body_tsv column.</done>
</task>

<task type="auto">
  <name>Task 2: Add DecisionCard ORM class + register for TRUNCATE hygiene</name>
  <read_first>
    - src/db/entity_models.py — the existing `Filing` class (lines 72-123) is the in-file sibling to mirror: corp_code FK, CHAR types, `__table_args__` with `sa.Index(..., sa.text("... DESC"))`. The `__all__` barrel (lines 35-43). The `_HalfVec` UserDefinedType (lines 46-65) is IRRELEVANT here — decision_cards has no embedding column (CONTEXT lock).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "src/db/entity_models.py (MODIFY)" — ORM class shape, CheckConstraint in `__table_args__`, the critical note that the body_tsv GENERATED column MUST be declared in the ORM (`postgresql.TSVECTOR, nullable=True`) so column-set parity holds.
    - tests/conftest.py lines 89-119 — the `_LIVE_TABLES` tuple (TRUNCATE-safe ordering: corp_code-FK children before `entities`).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "tests/conftest.py (MODIFY)" — placement guidance for `decision_cards`.
  </read_first>
  <files>src/db/entity_models.py, tests/conftest.py</files>
  <action>
    In `src/db/entity_models.py`, add `class DecisionCard(Base)` with `__tablename__ = "decision_cards"` declaring every column from Task 1 with matching SQLAlchemy types: `card_id` `sa.Text` primary_key; `corp_code` `sa.CHAR(8)` FK to entities ondelete CASCADE; `ticker` `sa.CHAR(6)` nullable; `generated_at`/`as_of`/`expires_at` `sa.DateTime(timezone=True)` nullable=False; `payload` `postgresql.JSONB` nullable=False; `body_md` `sa.Text` nullable=False; `status` `sa.Text` nullable=False server_default `sa.text("'active'")`; `supersedes`/`superseded_by` `sa.Text` self-ref FK ondelete SET NULL nullable; `schema_version` `sa.SmallInteger` nullable=False server_default `sa.text("1")`; and `body_tsv` `postgresql.TSVECTOR, nullable=True` (declared so `sa.inspect` column-set parity with the live generated column holds — do NOT use `_HalfVec`). Put `ck_decision_cards_status` CheckConstraint and the four `sa.Index` declarations in `__table_args__` (use `sa.text("generated_at DESC")` for the composite, `postgresql_using="gin"` for body_tsv).

    Add `"DecisionCard"` to the `__all__` list.

    In `tests/conftest.py`, add `"decision_cards"` to the `_LIVE_TABLES` tuple among the body-bearing tables BEFORE `entities` (e.g. right after `"events"`, before `"news"`). Its self-ref FK is handled by the existing `TRUNCATE ... CASCADE`. The `_SAFE_TABLE_RE` guard already passes `decision_cards`.
  </action>
  <verify>
    <automated>uv run python -c "from db.entity_models import DecisionCard, Base; assert 'decision_cards' in Base.metadata.tables; assert {c.name for c in DecisionCard.__table__.columns} >= {'card_id','corp_code','ticker','generated_at','as_of','payload','body_md','status','supersedes','superseded_by','expires_at','schema_version','body_tsv'}"</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/entity_models.py` contains `class DecisionCard(Base)` with `__tablename__ = "decision_cards"`.
    - `DecisionCard.__table__.columns` includes all 13 column names (12 locked + `body_tsv`).
    - `"DecisionCard"` appears in the `__all__` list.
    - `Base.metadata.tables` contains `"decision_cards"`.
    - `tests/conftest.py` `_LIVE_TABLES` contains `"decision_cards"` positioned before `"entities"`.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>`DecisionCard` ORM class declares all 13 columns matching the migration, is exported in `__all__`, and `decision_cards` is registered in `_LIVE_TABLES`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clone test_migration_0006.py → test_migration_0007.py (schema regression)</name>
  <read_first>
    - tests/db/test_migration_0006.py — the WHOLE file is the structure to clone. Column/PK/FK introspection via `sa.inspect` (lines 32-53); index presence + partial-WHERE via `pg_indexes` (lines 68-82); CheckConstraint via `pg_get_constraintdef` (lines 213-224, 261-279); column type via `format_type(atttypid, atttypmod)` (lines 56-66) and JSONB (lines 297-309); ORM round-trip parity loop + `set(Base.metadata.tables)` assertion (lines 376-414). Import style is `from db.entity_models import ...` (NOT `src.db...`).
    - .planning/phases/02-decision-card-schema-storage/02-PATTERNS.md § "tests/db/test_migration_0007.py" — per-assertion mapping for SC#1/#2/#6 + ORM round-trip; the table-set is now 7 tables (add `decision_cards`).
    - .planning/phases/02-decision-card-schema-storage/02-VALIDATION.md § Per-Task Verification Map — the exact test names: `test_decision_cards_shape`, `test_decision_cards_indexes`, `test_body_tsv_generated`, `test_orm_round_trip`.
  </read_first>
  <behavior>
    - test_decision_cards_shape: introspecting `decision_cards` returns all 12 locked columns; PK = ['card_id']; corp_code FK → entities; supersedes + superseded_by FK → decision_cards; `payload` is jsonb; `ck_decision_cards_status` constraintdef contains active/superseded/invalidated.
    - test_decision_cards_indexes: the 3 SC#2 index names exist; the corp_status_gen indexdef contains `generated_at DESC`.
    - test_body_tsv_generated: `body_tsv` column type is `tsvector`; `pg_attribute.attgenerated = 's'` (stored generated); `ix_decision_cards_body_tsv` indexdef contains `gin`; inserting a row with body_md then `body_tsv @@ to_tsquery('simple', <word>)` matches.
    - test_orm_round_trip: DecisionCard ORM column set == live DB column set; `set(Base.metadata.tables)` == the full 7-table set including `decision_cards`.
  </behavior>
  <files>tests/db/test_migration_0007.py</files>
  <action>
    Create `tests/db/test_migration_0007.py` cloning the structure of `test_migration_0006.py`. Use the session `pg_engine` fixture (migration already applied by `alembic upgrade head`). Write four tests named exactly `test_decision_cards_shape`, `test_decision_cards_indexes`, `test_body_tsv_generated`, `test_orm_round_trip` with the behaviors above.

    For the body_tsv generation check, prefer asserting `attgenerated = 's'` via `SELECT attgenerated FROM pg_attribute WHERE attrelid='decision_cards'::regclass AND attname='body_tsv'`, AND insert one row using the `seeded_engine`-style entities seed inline (or use `pg_clean` + a minimal `INSERT INTO entities` so the corp_code FK is satisfiable) then assert a `to_tsquery('simple', ...)` match returns the row. Use bind-param SQL only (never f-string) for inserted values.

    For `test_orm_round_trip`, import `from db.entity_models import Base, DecisionCard, ...`, extend the per-model loop to include `DecisionCard`, and assert `set(Base.metadata.tables) == {"filings","news","ohlcv","macro_series","events","collector_runs","decision_cards"}` (the full 7-table set — author this fresh in the NEW test; do NOT widen the 0006 assertion).
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_migration_0007.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/db/test_migration_0007.py::test_decision_cards_shape -x` exits 0.
    - `uv run pytest tests/db/test_migration_0007.py::test_decision_cards_indexes -x` exits 0.
    - `uv run pytest tests/db/test_migration_0007.py::test_body_tsv_generated -x` exits 0.
    - `uv run pytest tests/db/test_migration_0007.py::test_orm_round_trip -x` exits 0.
    - Test file uses `from db.entity_models import ...` import style and bind-param SQL only.
    - `test_orm_round_trip` asserts the 7-table `Base.metadata.tables` set including `decision_cards`.
  </acceptance_criteria>
  <done>All four schema-regression tests pass against the live Postgres container, proving SC#1, SC#2, SC#6, and ORM↔DB parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic DDL → Postgres | Migration text is authored by us; only risk is a non-existent tsearch config (`'korean'`) hard-failing the migration. Mitigated by using `'simple'`. |
| Test inserts → Postgres | Inserted seed/probe values cross into SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | test row inserts in `test_migration_0007.py` (corp_code, body_md, to_tsquery word) | mitigate | All inserts use SQLAlchemy bind params; never f-string interpolate (RULES Safety; mirror krx/db_writer.py) |
| T-02-02 | Denial of Service | migration uses a non-existent `'korean'` tsearch config → hard fail at deploy | mitigate | Use `to_tsvector('simple', coalesce(body_md,''))` — the only safe built-in (RESEARCH Pitfall #1; init-extensions.sql proof) |
| T-02-03 | Tampering | self-referential FK chain integrity (supersedes/superseded_by) | accept | FK referential integrity enforced by Postgres; `ondelete=SET NULL` prevents dangling-pointer hard failures; chain-walk safety is Plan 03's concern |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 installs NO new packages — all deps pinned in uv.lock (RESEARCH Package Legitimacy Audit: N/A). No slopcheck surface. |
</threat_model>

<verification>
- `uv run alembic -c src/db/alembic.ini upgrade head` then `downgrade -1` then `upgrade head` all exit 0 (reversibility).
- `uv run pytest tests/db/test_migration_0007.py -x -q` — all 4 tests green.
- `uv run pytest -m "not slow and not e2e"` — full suite still green (no regression from the new `_LIVE_TABLES` entry or ORM class).
</verification>

<success_criteria>
- SC#1: `decision_cards` exists with all 12 locked columns, correct PK/FKs (`test_decision_cards_shape`).
- SC#2: three mandatory indexes present with `generated_at DESC` ordering (`test_decision_cards_indexes`).
- SC#6: `body_tsv` GENERATED `simple` tsvector column + GIN index, query-matchable (`test_body_tsv_generated`).
- Drift guard: `DecisionCard` ORM column set == live DB; `Base.metadata.tables` includes `decision_cards` (`test_orm_round_trip`).
</success_criteria>

<output>
Create `.planning/phases/02-decision-card-schema-storage/02-01-SUMMARY.md` when done.
</output>
