---
phase: 03-mcp-tool-surface-read-side
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .mcp.json
  - src/db/migrations/versions/0008_phase03_mcp_surface.py
  - src/db/entity_models.py
  - tests/db/test_migration_0008.py
autonomous: true
requirements: [SC#1, SC#4, D-05, D-06]
must_haves:
  truths:
    - "fastmcp/sentence-transformers/python-mecab-ko install via `uv sync` (mcp + ingest groups exist)"
    - "CI `uv sync --group ingest --group mcp ...` no longer fails on missing groups"
    - "`notes` and `fundamentals` tables exist after `alembic upgrade head`"
    - "filings/news/notes carry a `bm25_tokens INT[]` column + a VectorChord-BM25 index + an HNSW index on the halfvec embedding column"
    - "`.mcp.json` registers a stdio server spawning `.venv` python `-m mcp_v2` with DATABASE_URL in env"
  artifacts:
    - path: "src/db/migrations/versions/0008_phase03_mcp_surface.py"
      provides: "notes + fundamentals tables, bm25_tokens columns, BM25/HNSW indexes"
      contains: "revision = \"0008\""
    - path: ".mcp.json"
      provides: "Claude Code stdio server registration for stock-mcp-v2"
    - path: "src/db/entity_models.py"
      provides: "Note + Fundamentals ORM classes + bm25_tokens columns on Filing/News"
      contains: "class Note"
    - path: "pyproject.toml"
      provides: "re-added mcp + ingest dependency groups; src/mcp_v2 in wheel packages"
      contains: "fastmcp>=2.11,<3.0"
  key_links:
    - from: "src/db/migrations/versions/0008_phase03_mcp_surface.py"
      to: "filings/news/notes (BM25 + HNSW indexes)"
      via: "op.execute CREATE INDEX ... USING bm25 / USING hnsw (halfvec_cosine_ops)"
      pattern: "halfvec_cosine_ops"
    - from: "tests/db/test_migration_0008.py"
      to: "Base.metadata (Note, Fundamentals)"
      via: "sa.inspect round-trip"
      pattern: "test_orm_round_trip"
---

<objective>
Wave-0 infrastructure for the entire phase: re-add the deleted `mcp` + `ingest` dependency
groups, write migration 0008 (the `notes` + `fundamentals` tables, the `bm25_tokens INT[]`
columns + VectorChord-BM25 indexes + HNSW indexes on the existing `halfvec(1024)` embedding
columns of filings/news/notes), add the `Note` + `Fundamentals` ORM classes, create `.mcp.json`,
and apply the migration to the test database.

Purpose: hybrid_search (Plan 06) and peer_view (Plan 05) cannot return anything until these
tables/columns/indexes exist; every other plan needs the dependency groups installed and the
package registered for the wheel build / pytest import path.

Output: installable deps, applied schema 0008, ORM parity, registered MCP server config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md
@.planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified against the codebase — executor uses these directly, no exploration. -->

From src/db/entity_models.py (the in-file analogs to mirror):
- `_HalfVec(types.UserDefinedType)` — `__init__(self, dim)`, `get_col_spec -> "halfvec({dim})"`, `cache_ok = True`.
- `class Filing(Base)`: cols `rcept_no CHAR(14) PK`, `corp_code CHAR(8) FK entities`, `ticker CHAR(6)`,
  `filed_at`, `report_nm`, `pblntf_ty`, `event_type`, `source_url`, `content_hash CHAR(64)`,
  `body_md TEXT NOT NULL`, `body_tsv TSVECTOR NULL`, `body_embedding _HalfVec(1024) NULL`.
- `class News(Base)`: `id BIGINT PK`, `url_hash CHAR(64) UNIQUE`, `corp_code FK SET NULL`,
  `published_at`, `body_md TEXT NOT NULL`, `body_tsv TSVECTOR NULL`, `body_embedding _HalfVec(1024) NULL`.
- `class OHLCV(Base)`: pure-numeric typed columns (NO body/embedding) — the Veto #6 shape to mirror for Fundamentals.
- `__all__` currently lists Base, Filing, News, OHLCV, MacroSeries, Event, CollectorRun, DecisionCard.

From src/db/migrations/versions/0007_decision_cards.py (structure analog):
- `revision = "0007"; down_revision = "0006"; branch_labels = None; depends_on = None`.
- `op.create_table(...)` + raw `op.execute("ALTER TABLE ... ADD COLUMN ... STORED")` for GENERATED tsvector.
- tsearch config is `'simple'` (NEVER `'korean'` — PG17 has no korean config; Pitfall 1).

From archive migration 0002 (git show archive/llm-wiki-2026-04:src/db/migrations/versions/0002_phase03_chunking_columns.py) — the IMAGE-TESTED BM25/HNSW SQL:
- `op.execute("CREATE EXTENSION IF NOT EXISTS vchord_bm25")`
- `op.execute("ALTER TABLE <t> ADD COLUMN bm25_tokens INT[] NULL")`  (raw SQL keeps the implicit bm25vector cast)
- `op.execute("CREATE INDEX ix_chunks_bm25 ON chunks USING bm25 (((bm25_tokens)::bm25_catalog.bm25vector) bm25_catalog.bm25_ops)")`
- archive HNSW used `vector_cosine_ops` on a `vector(1024)` column — Phase 1 columns are `halfvec(1024)`,
  so use `halfvec_cosine_ops` instead.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-add mcp + ingest dependency groups, register mcp_v2 package, create .mcp.json</name>
  <files>pyproject.toml, .mcp.json</files>
  <read_first>
    - pyproject.toml (current `[dependency-groups]`, `[tool.hatch.build.targets.wheel].packages`)
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ Standard Stack "Installation" block + Runtime State Inventory + A5)
    - .github/workflows/ci.yml (line ~43 already references `--group ingest --group mcp`)
  </read_first>
  <action>
    Add two dependency groups to `[dependency-groups]` in pyproject.toml exactly as RESEARCH § Standard Stack Installation specifies:
    `mcp = ["fastmcp>=2.11,<3.0", "psycopg[binary]>=3.2", "pgvector>=0.4", "sqlalchemy>=2.0,<3"]`
    and `ingest = ["sentence-transformers>=3.0", "transformers>=4.44", "python-mecab-ko>=1.3,<2", "pgvector>=0.4"]`.
    Pin fastmcp to the 2.x range (`>=2.11,<3.0`) per ROADMAP SC#1 + CONTEXT lock — do NOT use 3.x.
    Add `src/mcp_v2` to `[tool.hatch.build.targets.wheel].packages` (the fundamentals collector lives under the
    already-listed `src/collectors`, so no new package entry is needed for it).
    Create `.mcp.json` at repo root: a Claude Code stdio MCP server entry named `stock-mcp-v2` whose `command`
    spawns the project venv python (`.venv/Scripts/python.exe` on win32) with `args` `["-m", "mcp_v2"]`, `cwd`
    the repo root, and `env` passing `DATABASE_URL` (read by `db.engine.get_engine()`). This is the only
    no-analog file — follow RESEARCH A5's standard `command`/`args`/`env` shape. All three new packages are
    slopcheck-OK per RESEARCH § Package Legitimacy Audit (no [ASSUMED]/[SUS]/[SLOP]) — no legitimacy checkpoint needed.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import tomllib,pathlib,json; d=tomllib.loads(pathlib.Path('pyproject.toml').read_text()); g=d['dependency-groups']; assert 'mcp' in g and 'ingest' in g; assert any('fastmcp' in x for x in g['mcp']); assert 'src/mcp_v2' in d['tool']['hatch']['build']['targets']['wheel']['packages']; c=json.loads(pathlib.Path('.mcp.json').read_text()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `[dependency-groups].mcp` contains `fastmcp>=2.11,<3.0` (NOT a 3.x pin) and `[dependency-groups].ingest` contains `sentence-transformers`, `transformers`, `python-mecab-ko`.
    - `src/mcp_v2` appears in `[tool.hatch.build.targets.wheel].packages`.
    - `.mcp.json` is valid JSON with a server entry whose args invoke `-m mcp_v2` and whose env carries `DATABASE_URL`.
    - `uv sync --group mcp --group ingest --group db --group dev` (or `.venv/Scripts/python.exe -m pip install` of the pins) installs `fastmcp`, `sentence_transformers`, and `mecab` importable in `.venv` (run once during execution; SUMMARY records the install).
  </acceptance_criteria>
  <done>pyproject.toml has both groups + mcp_v2 wheel package; `.mcp.json` exists and is valid; deps install in .venv.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0008 — notes + fundamentals tables, bm25_tokens columns, BM25/HNSW indexes</name>
  <files>src/db/migrations/versions/0008_phase03_mcp_surface.py</files>
  <read_first>
    - src/db/migrations/versions/0007_decision_cards.py (revision skeleton, create_table, raw ALTER for special types, 'simple' tsvector)
    - archive 0002 via `git show archive/llm-wiki-2026-04:src/db/migrations/versions/0002_phase03_chunking_columns.py` (the image-tested BM25 + HNSW SQL)
    - src/db/entity_models.py Filing/News/OHLCV (column shapes the new tables/columns mirror)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ migration 0008 — exact table column lists)
    - CLAUDE.md Hard Vetoes #6 (fundamentals = typed NUMERIC, never embedded) + #8 (notes.content_md whole TEXT, no chunking)
  </read_first>
  <action>
    Create `0008_phase03_mcp_surface.py` with `revision = "0008"; down_revision = "0007"`. In `upgrade()`:
    (a) `op.execute("CREATE EXTENSION IF NOT EXISTS vchord_bm25")` (idempotent; safe in testcontainers).
    (b) `op.create_table("notes", ...)`: `path TEXT PK`, `corp_code CHAR(8) FK entities ondelete SET NULL nullable`,
        `content_md TEXT NOT NULL` (Veto #8 whole text), `content_hash CHAR(64) NOT NULL`,
        `updated_at TIMESTAMPTZ`, `body_tsv TSVECTOR NULL`. Then raw `op.execute("ALTER TABLE notes ADD COLUMN content_emb halfvec(1024)")`
        and `op.execute("ALTER TABLE notes ADD COLUMN bm25_tokens INT[] NULL")` (raw SQL preserves the implicit bm25vector cast).
    (c) `op.create_table("fundamentals", ...)` — Veto #6 PURE NUMERIC, NO embedding column:
        `ticker CHAR(6)`, `fdate DATE`, `per NUMERIC(18,4) NULL`, `pbr NUMERIC(18,4) NULL`, `eps NUMERIC(20,4) NULL`,
        `bps NUMERIC(20,4) NULL`, `roe NUMERIC(10,6) NULL`, `corp_code CHAR(8) FK entities SET NULL nullable`,
        `source TEXT NOT NULL`, `fetched_at TIMESTAMPTZ server_default now()`, PK `(ticker, fdate)`.
    (d) `op.execute("ALTER TABLE filings ADD COLUMN bm25_tokens INT[] NULL")` and same for `news`.
    (e) BM25 indexes (port archive 0002 SQL, one per narrative table): e.g.
        `CREATE INDEX ix_filings_bm25 ON filings USING bm25 (((bm25_tokens)::bm25_catalog.bm25vector) bm25_catalog.bm25_ops)`
        and `ix_news_bm25`, `ix_notes_bm25`.
    (f) HNSW indexes on the halfvec embedding columns — use `halfvec_cosine_ops` (NOT `vector_cosine_ops`; the columns are halfvec):
        `CREATE INDEX ix_filings_embedding_hnsw ON filings USING hnsw (body_embedding halfvec_cosine_ops)`,
        and `ix_news_embedding_hnsw`, `ix_notes_content_emb_hnsw` (on notes.content_emb).
    Provide a complete `downgrade()` dropping indexes → columns → tables in reverse order (DROP INDEX IF EXISTS for the raw-SQL indexes).
    body_tsv columns stay `'simple'` config if generated; do NOT use `'korean'` (Pitfall 1).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/db/test_migration_0008.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Migration declares `revision = "0008"` and `down_revision = "0007"`.
    - HNSW index DDL uses `halfvec_cosine_ops` (NOT `vector_cosine_ops`) — grep the file: `halfvec_cosine_ops` present, `vector_cosine_ops` absent.
    - BM25 index DDL uses `bm25_catalog.bm25vector` + `bm25_catalog.bm25_ops` on `filings`, `news`, AND `notes`.
    - `fundamentals` table has NO embedding/halfvec column (Veto #6): grep the file shows no `halfvec` token inside the fundamentals create block.
    - `notes` table has `content_md TEXT NOT NULL`, `content_emb halfvec(1024)`, `bm25_tokens INT[]`.
    - `downgrade()` reverses all DDL.
  </acceptance_criteria>
  <done>Migration 0008 creates notes + fundamentals + bm25_tokens columns + BM25/HNSW indexes; test_migration_0008 green.</done>
</task>

<task type="auto">
  <name>Task 3: Add Note + Fundamentals ORM classes + bm25_tokens columns; ORM round-trip test; [BLOCKING] apply migration</name>
  <files>src/db/entity_models.py, tests/db/test_migration_0008.py</files>
  <read_first>
    - src/db/entity_models.py (Filing/News/OHLCV classes, `_HalfVec`, `__all__`, the docstring stating Alembic is the DDL authority)
    - tests/db/test_migration_0007.py if present, else tests/db/test_migration_0006.py (the `test_orm_round_trip` / `sa.inspect` pattern)
    - src/db/migrations/versions/0008_phase03_mcp_surface.py (the table/column shapes to mirror in ORM)
  </read_first>
  <action>
    In `src/db/entity_models.py`: add `bm25_tokens = sa.Column(postgresql.ARRAY(sa.Integer), nullable=True)` to BOTH `Filing` and `News`.
    Add `class Note(Base)` mirroring `Filing`'s narrative shape: `path TEXT PK`, `corp_code CHAR(8) FK entities SET NULL nullable`,
    `content_md TEXT NOT NULL`, `content_hash CHAR(64) NOT NULL`, `updated_at TIMESTAMPTZ`, `body_tsv TSVECTOR NULL`,
    `content_emb _HalfVec(1024) NULL`, `bm25_tokens postgresql.ARRAY(Integer) NULL` (Veto #8 whole content_md TEXT, no chunk column).
    Add `class Fundamentals(Base)` mirroring `OHLCV`'s PURE-NUMERIC shape (Veto #6 — NO embedding): `ticker CHAR(6)`, `fdate Date`,
    `per/pbr/eps/bps/roe` as `sa.Numeric(...)`, `corp_code CHAR(8) FK SET NULL`, `source Text`, `fetched_at` server_default now(),
    PK `(ticker, fdate)`. Add both class names to `__all__`. Keep DDL authority in the migration (this module is ORM parity only — match the docstring contract).
    Create `tests/db/test_migration_0008.py` (DB test, `@pytest.mark.db`) with: a `test_orm_round_trip` asserting `Base.metadata` table set now includes `notes` and `fundamentals` and that `sa.inspect(engine)` column sets match the ORM (analog: test_migration_0006/0007); a `test_indexes_present` asserting the BM25 + HNSW indexes exist on filings/news/notes via `pg_indexes`; a `test_fundamentals_no_embedding` asserting `fundamentals` has no halfvec column.
    [BLOCKING] After writing the migration + ORM, run `.venv/Scripts/python.exe -m alembic -c src/db/alembic.ini upgrade head` against the test/dev database BEFORE marking the plan complete (uv is NOT on Bash PATH — use the venv python directly). hybrid_search (Plan 06) and peer_view (Plan 05) data-dependent verification BLOCK on this apply succeeding.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/db/test_migration_0008.py -x -q -m db</automated>
  </verify>
  <acceptance_criteria>
    - `__all__` in entity_models.py includes `"Note"` and `"Fundamentals"`.
    - `Note` declares `content_md` (TEXT NOT NULL), `content_emb` (`_HalfVec(1024)`), `bm25_tokens` (ARRAY Integer); NO chunk column (Veto #8).
    - `Fundamentals` declares only typed numeric columns + keys (NO `_HalfVec`/embedding — Veto #6).
    - `Filing` and `News` each gain a `bm25_tokens` ARRAY(Integer) column.
    - `tests/db/test_migration_0008.py::test_orm_round_trip` passes; `test_indexes_present` passes; `test_fundamentals_no_embedding` passes.
    - `alembic upgrade head` ran successfully (SUMMARY records the [BLOCKING] apply step + final `alembic current` = 0008).
  </acceptance_criteria>
  <done>Note + Fundamentals ORM exist with parity; migration 0008 applied to the DB (alembic current == 0008); ORM/index tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → build env | re-added deps install from PyPI |
| migration DDL → live Postgres | new tables/indexes created from raw SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip installs (fastmcp, sentence-transformers, python-mecab-ko) | mitigate | All three are slopcheck-OK + prior project use (RESEARCH § Package Legitimacy Audit); no [ASSUMED]/[SUS]/[SLOP] → no blocking-human checkpoint required. Pins are exact ranges. |
| T-03-01 | Tampering | migration raw SQL (bm25/hnsw DDL) | mitigate | DDL is static module-level strings (no user input); ports the image-tested archive 0002 SQL; `downgrade()` reverses cleanly |
| T-03-02 | Information Disclosure | `.mcp.json` env (DATABASE_URL) | accept | DATABASE_URL is a local dev secret read from env, not hard-coded into `.mcp.json`; repo is single-user local; no network auth surface this phase |
| T-veto6 | Tampering | fundamentals table shape | mitigate | fundamentals declared pure-NUMERIC, NO embedding column (test_fundamentals_no_embedding asserts it) — Veto #6 |
</threat_model>

<verification>
- `tests/db/test_migration_0008.py` green (ORM round-trip + indexes present + fundamentals-no-embedding).
- `alembic current` reports `0008` after the [BLOCKING] apply.
- `uv sync` (or venv pip install of the pins) succeeds; `import fastmcp`, `import sentence_transformers`, `import mecab` work in `.venv`.
</verification>

<success_criteria>
- mcp + ingest dependency groups re-added; CI `uv sync` group references resolve.
- Migration 0008 written, ORM parity classes added, migration APPLIED to the DB.
- `.mcp.json` registers the stdio server.
- Schema prerequisites for hybrid_search (bm25_tokens + BM25/HNSW indexes + notes table) and peer_view (fundamentals table) exist.
</success_criteria>

<output>
Create `.planning/phases/03-mcp-tool-surface-read-side/03-01-SUMMARY.md` when done.
</output>
