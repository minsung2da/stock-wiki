---
phase: 03-mcp-tool-surface-read-side
plan: 05
type: execute
wave: 1
depends_on: ["03-01", "03-03"]
files_modified:
  - src/collectors/fundamentals/__init__.py
  - src/collectors/fundamentals/client.py
  - src/collectors/fundamentals/fetcher.py
  - src/collectors/fundamentals/db_writer.py
  - src/collectors/fundamentals/roe.py
  - src/shared/run_log.py
  - src/cli/commands.py
  - src/mcp_v2/tools/market.py
  - tests/collectors/test_fundamentals.py
  - tests/mcp_v2/test_market.py
autonomous: true
requirements: [SC#2, D-01, D-04, D-06, "Veto#6"]
must_haves:
  truths:
    - "A fundamentals collector fetches PER/PBR/EPS/BPS (pykrx) + ROE (dart-fss) and DB-direct upserts typed NUMERIC columns (never embedded, Veto #6)"
    - "ohlcv_range / flow_range require from_date+to_date, return pure numeric bars/rows, no injection wrap"
    - "peer_view computes a REAL same-sector percentile_cont(0.5) median over the fundamentals table (D-06)"
    - "no same-sector peers → PeerView(median=None, n=0) (D-01 empty model); bad metric → InvalidArgument"
    - "collectors carry NO LLM SDK imports (COLL-07)"
  artifacts:
    - path: "src/collectors/fundamentals/db_writer.py"
      provides: "upsert_fundamentals typed NUMERIC upsert (ON CONFLICT (ticker, fdate))"
      contains: "ON CONFLICT (ticker, fdate)"
    - path: "src/collectors/fundamentals/roe.py"
      provides: "ROE = 당기순이익 / 자본총계 from dart-fss structured financials"
    - path: "src/mcp_v2/tools/market.py"
      provides: "ohlcv_range + flow_range + peer_view tool callables"
      contains: "percentile_cont(0.5)"
  key_links:
    - from: "src/mcp_v2/tools/market.py peer_view"
      to: "fundamentals + entities.sector"
      via: "percentile_cont(0.5) WITHIN GROUP grouped by sector"
      pattern: "percentile_cont\\(0.5\\)"
    - from: "src/collectors/fundamentals/__init__.py"
      to: "fundamentals table + collector_runs"
      via: "upsert_fundamentals + record_collector_run(engine, 'fundamentals', ...)"
      pattern: "record_collector_run"
---

<objective>
Deliver the D-06 fundamentals data pipeline AND the three numeric MCP tools. Build a
`src/collectors/fundamentals/` collector (pykrx PER/PBR/EPS/BPS + dart-fss ROE) writing typed
NUMERIC columns to the `fundamentals` table (Veto #6 — never embedded), extend `run_log` +
`collector_runs` CHECK for the new source name, expose it via CLI, and implement `ohlcv_range`,
`flow_range`, `peer_view` (real same-sector median over `fundamentals`) in `tools/market.py`.

Purpose: peer_view's locked SC#2 contract ("동종업종 median PER/PBR/ROE") requires a real
fundamentals source (D-06 expanded scope); ohlcv_range/flow_range read the existing populated
`ohlcv` table.

Output: fundamentals collector (5 files) + run_log/CLI edits + market.py (3 tools) + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md
@.planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md
@.planning/phases/03-mcp-tool-surface-read-side/03-VALIDATION.md
@CLAUDE.md
@src/collectors/krx/db_writer.py
@src/collectors/krx/__init__.py
@src/shared/run_log.py

<interfaces>
<!-- Verified analogs + Plan-03 contracts. -->

From src/collectors/krx/client.py: `get_ohlcv`/`get_trading_value`/`get_shorting_balance` lazy `from pykrx import stock`.
  NEW pykrx call for fundamentals: `stock.get_market_fundamental_by_date(date_str, date_str, ticker)` → PER/PBR/EPS/BPS/DIV.
From src/collectors/krx/fetcher.py: `@retry(stop=stop_after_attempt(5), wait=wait_exponential(...), retry=retry_if_exception_type(_RETRYABLE_EXC), reraise=True)` + `_RETRYABLE_EXC` (ReqConnectionError, ChunkedEncodingError, ProtocolError).
From src/collectors/krx/db_writer.py: module-level `_UPSERT_SQL = text(...)` with `ON CONFLICT (...) DO UPDATE`, `_TICKER_RE = ^[0-9]{6}$`, `Literal["inserted","updated","skipped"]` return, COALESCE preservation pattern.
From src/collectors/dart/financials.py: `get_structured_financials(corp_code, bgn_de) -> list[NumericFact]`; `LINE_ITEM_SYNONYMS` maps canonical key → 당기순이익/자본총계 variants; lazy `import dart_fss`. NO LLM SDK imports (COLL-07).
From src/collectors/krx/__init__.py: orchestration — `Portfolio.load(Path(".")).scope_tickers()`, per-ticker try/except isolation, `stats={total,inserted,updated,skipped,failed}`, `resolve_entity` pre-write, `_log.info("collector_run_complete", extra=...)` + `record_collector_run`.
From src/shared/run_log.py: `_ALLOWED_SOURCES = frozenset({"dart","krx","news","macro","kind"})` + `record_collector_run(engine, source, stats, elapsed_ms, extra=None)`. The `collector_runs.source` CHECK mirrors this set (migration).
From src/db/entity.py: `resolve_entity`, `_TICKER_RE`, `_CORP_CODE_RE`.

From src/mcp_v2 (Plan 03): `mcp` (shared FastMCP), `OhlcvBar`/`OhlcvRange`/`FlowRow`/`FlowRange`/`PeerView` models,
`InvalidArgument`/`EntityNotFound`/`DataBackendError`.

ohlcv table columns (src/db/entity_models.py OHLCV): ticker, trade_date, open, high, low, close, volume,
trading_value, foreign_net, inst_net, retail_net, short_volume, short_balance, corp_code.
entities.sector exists (TEXT, nullable). fundamentals table (Plan 01 migration 0008): ticker, fdate, per, pbr, eps, bps, roe, corp_code, source, fetched_at.

CONFLICT NOTE: this plan ADDS `"fundamentals"` to run_log `_ALLOWED_SOURCES` + the `collector_runs.source` CHECK.
Plan 03-02 adds `"notes_ingest"`. Both edit run_log.py + (potentially) a CHECK in migration 0008 — coordinate so
the final `_ALLOWED_SOURCES` = {dart, krx, news, macro, kind, fundamentals, notes_ingest} and the 0008 CHECK matches.
Since 0008 is owned by Plan 01, this plan's CHECK addition goes via run_log only IF 0008 already widened the CHECK;
otherwise add a tiny follow-on `op.execute("ALTER TABLE collector_runs DROP CONSTRAINT ... ADD CONSTRAINT ... CHECK (source IN (...))")` — confirm against the applied 0008 during execution.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fundamentals collector (client / fetcher / db_writer / roe / __init__) + run_log/CHECK extension</name>
  <files>src/collectors/fundamentals/client.py, src/collectors/fundamentals/fetcher.py, src/collectors/fundamentals/db_writer.py, src/collectors/fundamentals/roe.py, src/collectors/fundamentals/__init__.py, src/shared/run_log.py, tests/collectors/test_fundamentals.py</files>
  <read_first>
    - src/collectors/krx/client.py, src/collectors/krx/fetcher.py, src/collectors/krx/db_writer.py, src/collectors/krx/__init__.py (port pattern-for-pattern)
    - src/collectors/dart/financials.py (get_structured_financials + LINE_ITEM_SYNONYMS for 당기순이익/자본총계)
    - src/shared/run_log.py (_ALLOWED_SOURCES + record_collector_run)
    - src/db/entity_models.py Fundamentals + the applied 0008 collector_runs CHECK
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ fundamentals collector)
    - CLAUDE.md Hard Veto #6 (typed NUMERIC, never embedded) + COLL-07 (no LLM SDK in collectors)
  </read_first>
  <action>
    `client.py`: replicate krx/client.py shape — `get_market_fundamental(ticker, date_str)` lazy `from pykrx import stock` →
    `stock.get_market_fundamental_by_date(date_str, date_str, ticker)` (PER/PBR/EPS/BPS). `fetcher.py`: replicate krx/fetcher.py
    `@retry(...)` decorator + `_RETRYABLE_EXC` verbatim, wrapping the client call. `roe.py`: replicate dart financials pattern —
    lazy `import dart_fss`, reuse/extract 당기순이익 + 자본총계 via `get_structured_financials`-style line-item synonyms, compute
    `roe = 당기순이익 / 자본총계` (guard divide-by-zero → None). NO LLM SDK imports (COLL-07). `db_writer.py`: module-level
    `_UPSERT_SQL = text(...)` INSERT INTO fundamentals (ticker, fdate, per, pbr, eps, bps, roe, corp_code, source, fetched_at)
    `ON CONFLICT (ticker, fdate) DO UPDATE` with `COALESCE(EXCLUDED.roe, fundamentals.roe)` so a later ROE-only fill-in doesn't
    NULL-clobber pykrx values; `_TICKER_RE` pre-filter → ValueError; `Literal["inserted","updated","skipped"]`. Veto #6: only typed
    NUMERIC columns, NO embedding. `__init__.py`: `collect_fundamentals(*, engine, since=None)` replicate krx orchestration —
    `Portfolio.load(Path(".")).scope_tickers()`, per-ticker try/except isolation, `resolve_entity` pre-write (missing → stats failed),
    `{total,inserted,updated,skipped,failed}` stats, `_log.info("collector_run_complete", extra={"source":"fundamentals",...})` +
    `record_collector_run(engine, "fundamentals", ...)`. Extend `src/shared/run_log.py` `_ALLOWED_SOURCES` to include `"fundamentals"`
    (and confirm `"notes_ingest"` from Plan 02 is present — final set = {dart,krx,news,macro,kind,fundamentals,notes_ingest}). If the
    applied 0008 `collector_runs.source` CHECK does not yet allow these, add the CHECK widening (coordinate with Plan 01's 0008).
    Create `tests/collectors/test_fundamentals.py` (`@pytest.mark.db`): monkeypatch the pykrx + dart-fss fetchers to return fixed
    DataFrames/values; run `collect_fundamentals`; assert a fundamentals row with typed per/pbr/eps/bps + roe; assert `record_collector_run(engine, "fundamentals", ...)` inserts a collector_runs row; assert missing-entity ticker lands in stats["failed"].
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/collectors/test_fundamentals.py tests/test_import_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/collectors/fundamentals/` has client/fetcher/db_writer/roe/__init__; `collect_fundamentals(*, engine, since=None)` is the public entry.
    - db_writer SQL is a module-level `text()` constant with `ON CONFLICT (ticker, fdate) DO UPDATE` and writes ONLY typed NUMERIC + key/source columns (Veto #6 — grep shows no `halfvec`/embedding).
    - `tests/test_import_guard.py` stays green (COLL-07 — no anthropic/openai import under collectors/).
    - `run_log._ALLOWED_SOURCES` includes `"fundamentals"`; `record_collector_run(engine, "fundamentals", ...)` succeeds (no ValueError, inserts a row).
    - `tests/collectors/test_fundamentals.py` green: fundamentals row written with per/pbr/eps/bps/roe; missing entity → stats["failed"].
  </acceptance_criteria>
  <done>fundamentals collector writes typed PER/PBR/EPS/BPS/ROE; run_log/CHECK accept the new source; COLL-07 + DB test green.</done>
</task>

<task type="auto">
  <name>Task 2: CLI wiring — stock collect fundamentals</name>
  <files>src/cli/commands.py</files>
  <read_first>
    - src/cli/commands.py (existing `cmd_collect_krx` / subcommand registration pattern)
    - src/collectors/fundamentals/__init__.py (collect_fundamentals signature from Task 1)
  </read_first>
  <action>
    Add a `cmd_collect_fundamentals` (mirror `cmd_collect_krx` in src/cli/commands.py): build the engine, call
    `collect_fundamentals(engine=engine, since=args.since)`, emit the stats as stdout JSON (per CLAUDE.md convention —
    structured logs to stderr, user output stdout JSON). Register the `fundamentals` subcommand under `stock collect` with the
    same `--since` option shape as the krx subcommand. Do NOT change other subcommands.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/cli -x -q -k "fundamental or collect" -m "not db"</automated>
  </verify>
  <acceptance_criteria>
    - `stock collect fundamentals --help` lists the subcommand (or the argparse parser registers `fundamentals` under collect — assert via a CLI parse test).
    - `cmd_collect_fundamentals` calls `collect_fundamentals(engine=..., since=...)` and prints stats as JSON to stdout.
    - No regression in existing collect subcommands (existing tests/cli still green).
  </acceptance_criteria>
  <done>`stock collect fundamentals` subcommand wired mirroring krx; CLI test green.</done>
</task>

<task type="auto">
  <name>Task 3: market.py — ohlcv_range, flow_range (numeric) + peer_view (real sector median)</name>
  <files>src/mcp_v2/tools/market.py, tests/mcp_v2/test_market.py</files>
  <read_first>
    - src/collectors/krx/db_writer.py (ohlcv column set + parameterized SQL)
    - src/db/entity.py (resolve_entity, _TICKER_RE, _CORP_CODE_RE)
    - src/mcp_v2/models.py (OhlcvBar/OhlcvRange/FlowRow/FlowRange/PeerView) + errors.py
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ peer_view median PER/PBR/ROE shape)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ market.py — peer_view full-scope query)
    - CLAUDE.md Hard Veto #6 (pure numeric — NO injection wrap)
  </read_first>
  <action>
    Create `src/mcp_v2/tools/market.py`, registering all three on shared `mcp` (readOnlyHint).
    `ohlcv_range(ticker, from_date, to_date) -> OhlcvRange`: validate ticker via `_TICKER_RE` (→ InvalidArgument); `resolve_entity`
    (→ EntityNotFound if None); D-04 — `from_date`/`to_date` REQUIRED (no hard cap). Module-level `text()`:
    `SELECT trade_date, open, high, low, close, volume, trading_value FROM ohlcv WHERE ticker=:t AND trade_date>=:f AND trade_date<=:to ORDER BY trade_date`.
    Pure numeric → NO injection wrap (Veto #6). Zero rows → `OhlcvRange(ticker, bars=[])`.
    `flow_range(ticker, from_date, to_date) -> FlowRange`: same validation/dates; SELECT `trade_date, foreign_net, inst_net, retail_net, short_volume, short_balance`. Zero rows → `rows=[]`.
    `peer_view(corp_code, metric) -> PeerView`: validate corp_code via `_CORP_CODE_RE` (→ InvalidArgument); `metric in {"per","pbr","roe"}` else InvalidArgument.
    Build the metric-column SELECT from a fixed `{"per":"per","pbr":"pbr","roe":"roe"}` map (NOT user-string interpolation — pick a
    pre-defined module-level `text()` constant per metric so the SC#3 AST guard stays satisfied; do NOT f-string the column name).
    Query: `SELECT percentile_cont(0.5) WITHIN GROUP (ORDER BY fnd.<metric>) AS median, count(*) AS n FROM fundamentals fnd JOIN entities e ON e.corp_code=fnd.corp_code WHERE e.sector = (SELECT sector FROM entities WHERE corp_code=:cc) AND fnd.<metric> IS NOT NULL`.
    No same-sector peers / metric NULL everywhere → `PeerView(metric, median=None, n=0)` (D-01 empty model). Return median (float) + n (sample size; future weighted-median extension space, D-06).
    Create `tests/mcp_v2/test_market.py` (`@pytest.mark.db`): seed ohlcv rows + assert ohlcv_range/flow_range return ordered bars/rows;
    assert ohlcv_range with bad ticker → InvalidArgument, unknown ticker → EntityNotFound; seed two same-sector entities + fundamentals
    and assert peer_view returns the correct median + n=2; assert a sector with no peers → median=None, n=0; assert bad metric → InvalidArgument.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_market.py -x -q -m db</automated>
  </verify>
  <acceptance_criteria>
    - ohlcv_range/flow_range require from_date+to_date and return pure-numeric models with NO injection wrap (no `wrap_untrusted` call in market.py — grep absent; Veto #6).
    - peer_view SQL uses `percentile_cont(0.5) WITHIN GROUP (ORDER BY ...)` grouped by `entities.sector` over `fundamentals` and returns `median` + `n`.
    - peer_view metric column is selected via a fixed allow-listed `text()` constant per metric — NOT f-string interpolation (SC#3 AST guard stays green).
    - No same-sector peers → `PeerView(median=None, n=0)` (D-01); bad metric / bad corp_code → `InvalidArgument`; unknown ticker (ohlcv/flow) → `EntityNotFound`.
    - `tests/mcp_v2/test_market.py` green (median computed over 2 seeded peers).
  </acceptance_criteria>
  <done>ohlcv_range + flow_range (numeric) + peer_view (real sector median over fundamentals) implemented; D-04/D-06/Veto#6/D-01 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pykrx/dart-fss vendor → collector | external scraped/API data crosses into DB |
| tool args (ticker/corp_code/metric) → SQL | untrusted; metric must not reach SQL as raw string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-veto6 | Tampering | fundamentals table + market tools | mitigate | fundamentals = typed NUMERIC only (no embedding); ohlcv/flow/peer pure numeric, no injection path |
| T-metric-sql | Tampering | peer_view metric arg | mitigate | metric validated against {per,pbr,roe} allow-list; column chosen via fixed per-metric `text()` constant, NEVER f-string (SC#3 AST guard) |
| T-SQL-arg | Tampering | ohlcv/flow/peer ticker+corp_code | mitigate | ASCII regex pre-filter before DB; parameterized binds |
| T-coll07 | Elevation of Privilege | fundamentals collector imports | mitigate | no anthropic/openai import (tests/test_import_guard.py) — COLL-07 |
| T-03-08 | Denial of Service | ohlcv_range unbounded range | accept | D-04 — caller specifies from/to; range is caller's responsibility (no hard cap, per locked decision) |
</threat_model>

<verification>
- `tests/collectors/test_fundamentals.py` + `tests/test_import_guard.py` + `tests/mcp_v2/test_market.py` green.
- peer_view returns a real same-sector median (not an empty stub) for seeded peers (D-06 full-scope).
</verification>

<success_criteria>
- D-06 fundamentals pipeline (collector + typed table + CLI) live; PER/PBR/EPS/BPS/ROE collected.
- 3 numeric tools (ohlcv_range, flow_range, peer_view) implemented and registered; peer_view computes real sector medians.
- Veto #6, COLL-07, D-01, D-04 all honored.
</success_criteria>

<output>
Create `.planning/phases/03-mcp-tool-surface-read-side/03-05-SUMMARY.md` when done.
</output>
