---
phase: 03-mcp-tool-surface-read-side
plan: 03
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified:
  - src/mcp_v2/__init__.py
  - src/mcp_v2/_mcp.py
  - src/mcp_v2/errors.py
  - src/mcp_v2/models.py
  - src/mcp_v2/injection.py
  - src/mcp_v2/paths.py
  - tests/mcp_v2/test_injection.py
  - tests/mcp_v2/test_paths.py
  - tests/mcp_v2/test_error_model.py
autonomous: true
requirements: [SC#2, SC#5, D-01, D-03]
must_haves:
  truths:
    - "Genuine faults raise a typed McpToolError subclass; zero-row results are normal empty models (D-01)"
    - "Every narrative body is wrapped in an XML `<untrusted>` delimiter and flagged on pattern match, never blocked/stripped (D-03)"
    - "get_note paths outside `notes/private/` (incl. `..` and symlink escapes) are rejected (V12)"
    - "Each tool return model can represent an empty state (empty list / found=False / median=None)"
  artifacts:
    - path: "src/mcp_v2/errors.py"
      provides: "McpToolError hierarchy (ToolError subclasses) — raised not returned"
      contains: "class McpToolError(ToolError)"
    - path: "src/mcp_v2/models.py"
      provides: "Pydantic return models for all 10 tools, each empty-able"
      contains: "class SearchHit"
    - path: "src/mcp_v2/injection.py"
      provides: "WRAP+FLAG: PATTERNS table + detect() + wrap_untrusted()"
      exports: ["PATTERNS", "detect", "wrap_untrusted"]
    - path: "src/mcp_v2/paths.py"
      provides: "safe_resolve read-only notes/private whitelist (symlink + .. safe)"
      exports: ["safe_resolve"]
  key_links:
    - from: "src/mcp_v2/injection.py"
      to: "narrative tool return models"
      via: "wrap_untrusted(body, source, ref_id) + detect(body) -> injection_suspected"
      pattern: "<untrusted source="
    - from: "src/mcp_v2/paths.py"
      to: "filesystem notes/private/"
      via: "Path.resolve() + is_relative_to(whitelist_root)"
      pattern: "is_relative_to"
---

<objective>
Build the dependency-free leaf layer of `src/mcp_v2/`: the typed exception hierarchy (D-01), the
Pydantic return models for all 10 tools (each able to represent an empty state, D-01), the
prompt-injection WRAP+FLAG module (D-03/SC#5), the read-only path-traversal defense for get_note
(V12), and the shared FastMCP instance module. No tool callables yet — those land in Plans 04/05/06,
which import these leaves.

Purpose: interface-first ordering — downstream tool plans receive the error types, return models,
injection helper, path helper, and the shared `mcp` instance as contracts, with no codebase exploration.

Output: errors.py, models.py, injection.py, paths.py, _mcp.py + unit tests (all no-DB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md
@.planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md
@.planning/phases/03-mcp-tool-surface-read-side/03-VALIDATION.md
@CLAUDE.md
@src/cards/models.py

<interfaces>
<!-- Verified analogs — port/replicate these. -->

RESEARCH §Pattern 2 — D-01 exception hierarchy (subclass FastMCP ToolError):
- `class McpToolError(ToolError)` base; subclasses `InvalidArgument`, `EntityNotFound`, `FilingNotFound`,
  `NotePathForbidden`, `NoteNotFound`, `DataBackendError`. FastMCP converts a raised ToolError to an MCP error.
- Set `FastMCP(..., mask_error_details=True)` so non-ToolError bugs show a generic message while McpToolError
  messages stay specific (VERIFY A6 against installed fastmcp during execution).

src/cards/models.py — Pydantic v2 discipline to replicate: `model_config = ConfigDict(extra="forbid")`,
`Field(...)`, regex `r"^[0-9]{8}$"` (corp_code) / `r"^[0-9]{6}$"` (ticker).

archive src/ingest/injection_defense.py (`git show archive/llm-wiki-2026-04:src/ingest/injection_defense.py`):
- `PATTERNS: list[tuple[str, re.Pattern]]` — EN_IGNORE_PREV, FAKE_SYSTEM_TAG, DAN_MODE, ROLEPLAY_ADMIN,
  KO_IGNORE_PREV, KO_ADMIN_MODE (port verbatim).
- `_SAFE_ATTR_RE = re.compile(r"^[A-Za-z0-9_-]+$")`; `wrap_untrusted(body, source, ...)` validates attrs, raises
  ValueError on bad attr (never echoes value).
- CHANGE for D-03: archive `is_adversarial()` GATED content; v2.0 NEVER blocks/strips. `wrap_untrusted` always wraps;
  `detect(body) -> list[dict{pattern_id, match, span}]` returns flags the tool attaches as `injection_suspected=True`.
  Drop `is_adversarial`/`trust_level`.

archive src/stock_mcp/paths.py `safe_join` (`git show archive/llm-wiki-2026-04:src/stock_mcp/paths.py`):
- `Path.resolve()` (collapses `..`, follows symlinks) + `is_relative_to(root)` whitelist check.
- CHANGE: whitelist becomes `("notes/private/",)` ONLY (drop vault/notes — vault deleted); raise `NotePathForbidden`;
  add `candidate.is_file()` → `NoteNotFound`; rename to `safe_resolve`; read-only (no write aliases).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: errors.py (D-01 hierarchy) + _mcp.py (shared FastMCP instance) + models.py (return models)</name>
  <files>src/mcp_v2/__init__.py, src/mcp_v2/_mcp.py, src/mcp_v2/errors.py, src/mcp_v2/models.py, tests/mcp_v2/test_error_model.py</files>
  <read_first>
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§Pattern 2 error model + §Tool-by-Tool table for each model's empty-case)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ errors.py + § models.py — "New models needed" list)
    - src/cards/models.py (ConfigDict(extra="forbid"), Field, regex patterns)
    - CLAUDE.md Hard Veto #13 (CardView payload default)
  </read_first>
  <action>
    Create `src/mcp_v2/errors.py` with the D-01 hierarchy subclassing `fastmcp.exceptions.ToolError`:
    `McpToolError(ToolError)` base + `InvalidArgument`, `EntityNotFound`, `FilingNotFound`, `NotePathForbidden`,
    `NoteNotFound`, `DataBackendError`. Do NOT port the archive `StructuredError`/`ErrorCode`/`to_error_response`
    dict pattern (D-01 raises typed exceptions). Create `src/mcp_v2/_mcp.py` holding the single shared instance:
    `mcp = FastMCP("stock-mcp-v2", mask_error_details=True)` — VERIFY the `mask_error_details` kwarg + behavior against
    the installed fastmcp during execution (RESEARCH A6); tool modules and `server.py` import this `mcp`.
    Create `src/mcp_v2/models.py` with Pydantic v2 models (all `ConfigDict(extra="forbid")`), each EMPTY-ABLE per D-01:
    `FilingDetail` (rcept_no, body_md (wrapped), injection_suspected: bool, injection_flags: list[str], + metadata),
    `FilingHit` + `SearchFilingsResult(hits: list[FilingHit] = [])`, `OhlcvBar` + `OhlcvRange(ticker, bars: list = [])`,
    `FlowRow` + `FlowRange(ticker, rows: list = [])`, `PeerView(metric, median: float|None = None, n: int = 0)`,
    `SearchHit(source_type, id_or_path, rrf_score, snippet, injection_suspected, injection_flags)` +
    `SearchResult(hits: list[SearchHit] = [])`, `NoteContent(path, content_md, injection_suspected, injection_flags)`,
    `CardView(corp_code, found: bool = False, card: dict|None = None)`, `Briefing(date, type, found: bool = False, entries: list = [])`,
    and a `PortfolioView` passthrough (holdings/watchlist). Reuse the `r"^[0-9]{6}$"`/`r"^[0-9]{8}$"` regex patterns.
    Create `src/mcp_v2/__init__.py` exporting `mcp` (from `_mcp`) and the error classes.
    Create `tests/mcp_v2/test_error_model.py` (no DB): assert each return model constructs in its empty state
    (e.g. `OhlcvRange(ticker="005930").bars == []`, `CardView(corp_code="00126380").found is False`,
    `PeerView(metric="per").median is None`), and assert `McpToolError` and each subclass are `ToolError` subclasses.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_error_model.py -x -q -m "not db"</automated>
  </verify>
  <acceptance_criteria>
    - `from mcp_v2.errors import McpToolError, InvalidArgument, EntityNotFound, FilingNotFound, NotePathForbidden, NoteNotFound, DataBackendError` all import and are `issubclass(..., ToolError)`.
    - `mcp_v2._mcp.mcp` is a `FastMCP` instance named `"stock-mcp-v2"` constructed with `mask_error_details=True` (or the verified-equivalent for the installed version).
    - Every return model in models.py uses `ConfigDict(extra="forbid")` and constructs in an empty state without args beyond identifiers.
    - No `to_error_response` / `ErrorCode` / dict-return error pattern exists in errors.py (grep absent).
    - `tests/mcp_v2/test_error_model.py` green.
  </acceptance_criteria>
  <done>errors.py + _mcp.py + models.py exist; all models empty-able; error classes are ToolError subclasses; test green.</done>
</task>

<task type="auto">
  <name>Task 2: injection.py WRAP+FLAG (D-03 / SC#5)</name>
  <files>src/mcp_v2/injection.py, tests/mcp_v2/test_injection.py</files>
  <read_first>
    - archive injection_defense.py via `git show archive/llm-wiki-2026-04:src/ingest/injection_defense.py` (PATTERNS table + wrap_untrusted attr guard)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ injection.py — gate→WRAP+FLAG change)
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ Prompt-injection WRAP+FLAG — XML delimiter recommendation `<untrusted source="..." ref="...">`)
    - 03-CONTEXT.md D-03 (always wrap + flag-on-match, NEVER block, NEVER strip)
  </read_first>
  <action>
    Create `src/mcp_v2/injection.py` porting the archive EN+KO `PATTERNS` table VERBATIM (EN_IGNORE_PREV,
    FAKE_SYSTEM_TAG, DAN_MODE, ROLEPLAY_ADMIN, KO_IGNORE_PREV, KO_ADMIN_MODE) as a stable, ordered
    `list[tuple[str, re.Pattern]]`. Implement `detect(body: str) -> list[dict]` returning `{pattern_id, match (≤80 chars), span}`
    sorted by span start (NEVER raises on content). Implement `wrap_untrusted(body: str, source: str, ref_id: str) -> str`
    returning `'<untrusted source="{source}" ref="{ref_id}">\n{body}\n</untrusted>'`, validating `source`/`ref_id` against
    `_SAFE_ATTR = re.compile(r"^[A-Za-z0-9_:.-]+$")` and raising `ValueError("invalid delimiter attribute")` on bad attr
    WITHOUT echoing the offending value (info-disclosure guard). D-03: NEVER block, NEVER strip — `wrap_untrusted` always
    wraps the full body unchanged; `detect` only flags. Drop the archive `is_adversarial`/`trust_level` gating entirely.
    Create `tests/mcp_v2/test_injection.py` (no DB): `test_flag_not_block` — a body containing "ignore previous instructions"
    returns non-empty `detect()` flags AND `wrap_untrusted` returns the FULL body inside `<untrusted>` delimiters (body
    bytes preserved, nothing stripped); a clean body returns `detect() == []`; a pattern-ID snapshot test asserting the
    exact stable ID set (so reordering/removal is caught); `wrap_untrusted(..., source="bad src")` raises ValueError and
    the message does NOT contain `"bad src"`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_injection.py -x -q -m "not db"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/mcp_v2/test_injection.py::test_flag_not_block` proves the body is preserved verbatim inside `<untrusted>` and `detect()` flags it (never blocked/stripped — D-03).
    - Pattern-ID snapshot test asserts the set `{EN_IGNORE_PREV, FAKE_SYSTEM_TAG, DAN_MODE, ROLEPLAY_ADMIN, KO_IGNORE_PREV, KO_ADMIN_MODE}` (or the documented superset) exactly.
    - `wrap_untrusted` raises `ValueError` on an invalid attribute and the exception message contains neither the offending source value nor the body.
    - The output of `wrap_untrusted` starts with `<untrusted source=` and ends with `</untrusted>`.
  </acceptance_criteria>
  <done>injection.py WRAP+FLAG implemented (no block/strip); pattern-ID snapshot + flag-not-block tests green.</done>
</task>

<task type="auto">
  <name>Task 3: paths.py read-only get_note whitelist (V12)</name>
  <files>src/mcp_v2/paths.py, tests/mcp_v2/test_paths.py</files>
  <read_first>
    - archive paths.py via `git show archive/llm-wiki-2026-04:src/stock_mcp/paths.py` (safe_join resolve()+is_relative_to)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ paths.py — read-only variant, NotePathForbidden, is_file check)
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ get_note path-traversal defense)
    - src/mcp_v2/errors.py (NotePathForbidden, NoteNotFound — from Task 1)
  </read_first>
  <action>
    Create `src/mcp_v2/paths.py` with `safe_resolve(repo_root: Path, user_path: str) -> Path`: `rr = repo_root.resolve()`,
    `candidate = (rr / user_path).resolve()` (collapses `..` AND follows symlinks), whitelist roots = resolved
    `("notes/private/",)` only (drop vault/notes — vault deleted). Reject when candidate is neither equal to nor
    `is_relative_to` any whitelist root → raise `NotePathForbidden(f"path outside whitelist: {user_path!r}")`. If the path
    is allowed but `not candidate.is_file()` → raise `NoteNotFound(f"note not found: {user_path!r}")`. Read-only — no write
    aliases, no `resolve_path_alias`. Create `tests/mcp_v2/test_paths.py` (no DB, uses tmp_path): a file under
    `notes/private/` resolves OK; `../etc/passwd` and an absolute path outside the whitelist raise `NotePathForbidden`;
    a symlink under notes/private pointing outside the whitelist raises `NotePathForbidden`; a whitelisted-but-missing
    file raises `NoteNotFound`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_paths.py -x -q -m "not db"</automated>
  </verify>
  <acceptance_criteria>
    - `safe_resolve` whitelist is `("notes/private/",)` only (no `vault/notes`).
    - `../`-traversal and outside-whitelist absolute paths raise `NotePathForbidden`.
    - A symlink inside notes/private pointing outside the whitelist is rejected (resolve() follows it before the check).
    - A whitelisted but nonexistent file raises `NoteNotFound`; an existing whitelisted file returns its resolved `Path`.
    - `tests/mcp_v2/test_paths.py` green.
  </acceptance_criteria>
  <done>paths.py read-only safe_resolve implemented; traversal + symlink + missing-file cases proven by test_paths.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool args / file paths → filesystem | get_note path may attempt traversal |
| narrative body → downstream LLM | DART/news/note text may contain injection |
| tool fault → MCP client | error surface must distinguish no-data vs fault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-path-traversal | Tampering / Information Disclosure | get_note path (paths.py) | mitigate | `Path.resolve()` + `is_relative_to(notes/private)`; symlink-safe; read-only; raises NotePathForbidden (test_paths.py) |
| T-prompt-inj | Tampering (downstream LLM) | narrative body (injection.py) | mitigate | XML `<untrusted>` WRAP + pattern FLAG, never block/strip (D-03); downstream treats delimiter contents as data (test_injection.py) |
| T-03-05 | Information Disclosure | error messages | mitigate | `mask_error_details=True` hides non-ToolError bug details; `wrap_untrusted` attr-guard never echoes offending value |
| T-03-06 | Spoofing | XML delimiter attribute injection | mitigate | `_SAFE_ATTR` regex on source/ref_id; ValueError on bad attr |
</threat_model>

<verification>
- `tests/mcp_v2/{test_error_model,test_injection,test_paths}.py` all green with `-m "not db"`.
- `mcp_v2` package imports cleanly (errors, models, injection, paths, _mcp).
- SC#3 AST guard (from Plan 02) still green over the new mcp_v2 files (all SQL-free here — trivially passes).
</verification>

<success_criteria>
- Typed exception hierarchy (D-01), empty-able return models (D-01/SC#2 shape), WRAP+FLAG injection (D-03/SC#5), read-only path defense (V12) all implemented and unit-tested.
- Shared `mcp` instance available for Plans 04/05/06 to register tools on.
</success_criteria>

<output>
Create `.planning/phases/03-mcp-tool-surface-read-side/03-03-SUMMARY.md` when done.
</output>
