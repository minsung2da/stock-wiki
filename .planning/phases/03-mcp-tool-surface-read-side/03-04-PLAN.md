---
phase: 03-mcp-tool-surface-read-side
plan: 04
type: execute
wave: 1
depends_on: ["03-01", "03-03"]
files_modified:
  - src/mcp_v2/tools/__init__.py
  - src/mcp_v2/tools/filing.py
  - src/mcp_v2/tools/card.py
  - src/mcp_v2/tools/note.py
  - src/mcp_v2/tools/portfolio.py
  - src/mcp_v2/tools/briefing.py
  - tests/mcp_v2/test_filing.py
  - tests/mcp_v2/test_card.py
  - tests/mcp_v2/test_note.py
  - tests/mcp_v2/test_portfolio_briefing.py
autonomous: true
requirements: [SC#2, SC#5, D-01, D-04, "Veto#13"]
must_haves:
  truths:
    - "get_filing returns the WHOLE body_md (no chunking, Veto #8), XML-wrapped + injection-flagged"
    - "search_filings returns metadata hits ordered filed_at DESC, default limit=50 (D-04), no body"
    - "get_decision_card default view='payload' EXCLUDES body_md; view='both' includes it wrapped (Veto #13)"
    - "get_note reads notes/private/ disk whitelist read-only, content wrapped + flagged"
    - "list_portfolio wraps Portfolio.load; get_briefing returns Briefing(found=False, entries=[]) — Phase-5 data deferred (positively asserted, not just report_type-absent)"
    - "missing entity/filing raises a typed McpToolError; zero rows return an empty model (D-01)"
  artifacts:
    - path: "src/mcp_v2/tools/filing.py"
      provides: "get_filing + search_filings tool callables registered on shared mcp"
      contains: "def get_filing"
    - path: "src/mcp_v2/tools/card.py"
      provides: "get_decision_card wrapping cards.store.get_active (serialize-time view exclude)"
      contains: "exclude={\"body_md\"}"
    - path: "src/mcp_v2/tools/note.py"
      provides: "get_note disk-whitelist read-only + injection wrap"
    - path: "src/mcp_v2/tools/briefing.py"
      provides: "get_briefing empty-model (Phase 5 wires data)"
  key_links:
    - from: "src/mcp_v2/tools/card.py"
      to: "src/cards/store.get_active"
      via: "get_active(engine, corp_code) -> DecisionCard | None then model_dump(exclude)"
      pattern: "get_active"
    - from: "src/mcp_v2/tools/filing.py"
      to: "filings.body_md"
      via: "module-level text() SELECT bound with :rcept_no + injection.wrap_untrusted"
      pattern: "wrap_untrusted"
---

<objective>
Implement the five non-numeric, non-search read tools and register them on the shared `mcp`
instance: `get_filing`, `search_filings` (filing.py), `get_decision_card` (card.py), `get_note`
(note.py), `list_portfolio` (portfolio.py), `get_briefing` (briefing.py). Each returns its
Pydantic model (Plan 03), distinguishes empty-model from typed exception (D-01), wraps any
narrative body via injection WRAP+FLAG (D-03/SC#5), and honors the D-04 limit defaults +
Veto #13 payload-default.

Purpose: deliver the bulk of the locked tool surface that reads already-populated tables (filings,
decision_cards) and disk (notes/private) — no new infra dependency.

Output: tools/filing.py, card.py, note.py, portfolio.py, briefing.py, tools/__init__ + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md
@.planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md
@.planning/phases/03-mcp-tool-surface-read-side/03-VALIDATION.md
@CLAUDE.md
@src/cards/store.py
@src/shared/portfolio.py
@src/db/entity.py

<interfaces>
<!-- Verified contracts — use directly. -->

From src/mcp_v2 (Plan 03): `mcp` (FastMCP instance, import `from .._mcp import mcp` inside tool modules),
`McpToolError`/`InvalidArgument`/`EntityNotFound`/`FilingNotFound`/`NotePathForbidden`/`NoteNotFound`/`DataBackendError`,
return models `FilingDetail`, `FilingHit`, `SearchFilingsResult`, `CardView`, `NoteContent`, `PortfolioView`, `Briefing`,
`injection.wrap_untrusted(body, source, ref_id)` + `injection.detect(body)`, `paths.safe_resolve(repo_root, user_path)`.

From src/cards/store.py: `get_active(engine, corp_code) -> DecisionCard | None` returns the FULL typed card
(payload + body_md + status). `_PAYLOAD_EXCLUDE = {"body_md","status","invalidation_reason"}` is the store convention.
View projection is a serialize-time `model_dump(mode="json", exclude={"body_md"})` — NO re-query (Veto #13).

From src/db/entity.py: `resolve_entity(engine, value, as_of=None) -> Entity | None`; `_CORP_CODE_RE = ^[0-9]{8}$`, `_TICKER_RE = ^[0-9]{6}$`.
From src/db/engine.py: `get_engine()` (reads `os.environ["DATABASE_URL"]`, fail-fast).
From src/shared/portfolio.py: `Portfolio.load(repo_root) -> Portfolio` (raises `PortfolioLoadError`); `scope_tickers()`.

filings table columns (src/db/entity_models.py): `rcept_no CHAR(14) PK`, `corp_code`, `ticker`, `filed_at`,
`report_nm`, `pblntf_ty`, `event_type`, `source_url`, `body_md`.

FastMCP read-only annotation: `@mcp.tool(annotations=ToolAnnotations(readOnlyHint=True))` (from mcp.types).
SQL discipline (Veto #7 / SC#3 AST guard): every SQL is a module-level `text()` constant + bind params; NO f-strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: filing.py — get_filing (whole body) + search_filings (metadata, filed_at DESC, limit=50)</name>
  <files>src/mcp_v2/tools/__init__.py, src/mcp_v2/tools/filing.py, tests/mcp_v2/test_filing.py</files>
  <read_first>
    - src/cards/store.py (module-level `text()` constants + bind-param discipline)
    - src/db/entity.py (regex pre-filter + resolve_entity)
    - src/db/entity_models.py Filing (column names)
    - src/mcp_v2/models.py + injection.py + errors.py (Plan 03 contracts)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ filing.py)
    - CLAUDE.md Hard Veto #8 (whole body_md, no slicing)
  </read_first>
  <action>
    Create `src/mcp_v2/tools/__init__.py` (empty package marker — the side-effect aggregation lives in server.py, Plan 06).
    Create `src/mcp_v2/tools/filing.py`. Register both callables with `@mcp.tool(annotations=ToolAnnotations(readOnlyHint=True))`
    on the shared `mcp` (import `from .._mcp import mcp`). `get_filing(rcept_no: str) -> FilingDetail`: validate rcept_no shape
    (14 digits → else `InvalidArgument`), `SELECT corp_code, ticker, filed_at, report_nm, event_type, source_url, body_md FROM filings WHERE rcept_no = :r`
    (module-level `text()` constant). No row → raise `FilingNotFound`. Return `FilingDetail` with the WHOLE body_md (Veto #8 — no
    slicing) wrapped via `injection.wrap_untrusted(body_md, "dart", rcept_no)` and `injection_suspected=bool(injection.detect(body_md))`
    + `injection_flags=[h["pattern_id"] ...]`. `search_filings(corp_code: str, event_type: str|None=None, since: str|None=None, until: str|None=None, limit: int=50) -> SearchFilingsResult`:
    validate corp_code via `_CORP_CODE_RE` (→ `InvalidArgument`); `resolve_entity` to confirm existence (→ `EntityNotFound` if None);
    SELECT metadata (rcept_no, filed_at, report_nm, event_type, pblntf_ty) `WHERE corp_code=:cc [AND event_type / filed_at>=:since / filed_at<:until via NULL-cast guards] ORDER BY filed_at DESC LIMIT :limit`
    (D-04 default 50). Metadata only — NO body, NO injection wrap. Zero rows → `SearchFilingsResult(hits=[])` (D-01 empty model).
    Create `tests/mcp_v2/test_filing.py` (`@pytest.mark.db`, uses seeded fixtures): get_filing returns whole body inside `<untrusted>`;
    unknown rcept_no raises FilingNotFound; bad rcept_no raises InvalidArgument; search_filings orders filed_at DESC and respects default limit; unknown corp_code raises EntityNotFound.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_filing.py -x -q -m db</automated>
  </verify>
  <acceptance_criteria>
    - get_filing returns the entire stored body_md (length equals the stored value — no truncation/slicing; Veto #8) wrapped in `<untrusted source="dart" ref="...">`.
    - Unknown rcept_no → `FilingNotFound`; malformed rcept_no → `InvalidArgument`; zero search results → `SearchFilingsResult(hits=[])` (D-01).
    - search_filings SQL contains `ORDER BY filed_at DESC` and binds `:limit` (default 50); returns metadata-only hits (no body field on FilingHit).
    - All SQL in filing.py is module-level `text()` constants (SC#3 AST guard from Plan 02 stays green).
    - `tests/mcp_v2/test_filing.py` green.
  </acceptance_criteria>
  <done>get_filing + search_filings implemented and registered; whole-body + DESC + limit + D-01 cases proven by test_filing.py.</done>
</task>

<task type="auto">
  <name>Task 2: card.py — get_decision_card (Veto #13 payload default)</name>
  <files>src/mcp_v2/tools/card.py, tests/mcp_v2/test_card.py</files>
  <read_first>
    - src/cards/store.py (get_active returns full typed DecisionCard; _PAYLOAD_EXCLUDE convention)
    - src/cards/models.py (DecisionCard fields; corp_code regex line 79)
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ get_decision_card view projection)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ card.py)
    - CLAUDE.md Hard Veto #13
  </read_first>
  <action>
    Create `src/mcp_v2/tools/card.py`. `get_decision_card(corp_code: str, latest: bool = True, view: str = "payload") -> CardView`
    registered on shared `mcp` (readOnlyHint). Validate corp_code via `_CORP_CODE_RE` → else `InvalidArgument`. Call
    `cards.store.get_active(get_engine(), corp_code)`. If `None` → `CardView(corp_code=corp_code, found=False)` (D-01 empty model,
    NOT an error). If `view == "payload"` → `data = card.model_dump(mode="json", exclude={"body_md"})` (Veto #13 default — body_md
    excluded at serialize time, NO re-query). If `view == "both"` → `data = card.model_dump(mode="json")` then replace
    `data["body_md"]` with `injection.wrap_untrusted(card.body_md, "card", card.card_id)` + set `injection_suspected` (D-03).
    Any other `view` → `InvalidArgument("view must be 'payload' or 'both'")`. Return `CardView(corp_code, found=True, card=data)`.
    `latest=True` is the only mode this phase (get_active already returns latest active). Create `tests/mcp_v2/test_card.py`
    (`@pytest.mark.db`): seed an active card; assert view='payload' result `card` dict has NO `body_md` key; view='both' includes
    `body_md` wrapped in `<untrusted>`; no active card → `found=False`; bad corp_code → InvalidArgument; bad view → InvalidArgument.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_card.py -x -q -m db</automated>
  </verify>
  <acceptance_criteria>
    - `view="payload"` (default) → `CardView.card` dict does NOT contain `body_md` (Veto #13).
    - `view="both"` → `CardView.card["body_md"]` is wrapped in `<untrusted source="card" ...>` (D-03).
    - No active card for the corp → `CardView(found=False, card=None)` (D-01); never raises.
    - Bad corp_code shape and bad `view` value each raise `InvalidArgument`.
    - card.py performs NO direct SQL — it delegates to `cards.store.get_active` (no `text()` in this file).
    - `tests/mcp_v2/test_card.py` green.
  </acceptance_criteria>
  <done>get_decision_card wraps get_active with serialize-time view exclude; payload-default + both + empty cases proven.</done>
</task>

<task type="auto">
  <name>Task 3: note.py + portfolio.py + briefing.py (disk read + passthrough + Phase-5 empty)</name>
  <files>src/mcp_v2/tools/note.py, src/mcp_v2/tools/portfolio.py, src/mcp_v2/tools/briefing.py, tests/mcp_v2/test_note.py, tests/mcp_v2/test_portfolio_briefing.py</files>
  <read_first>
    - src/mcp_v2/paths.py (safe_resolve — Plan 03)
    - src/shared/portfolio.py (Portfolio.load, PortfolioLoadError, scope_tickers)
    - src/mcp_v2/injection.py + models.py (NoteContent, PortfolioView, Briefing)
    - .planning/phases/03-mcp-tool-surface-read-side/03-RESEARCH.md (§ get_note path-traversal + § get_briefing partial impl)
    - .planning/phases/03-mcp-tool-surface-read-side/03-PATTERNS.md (§ note.py / portfolio.py / briefing.py)
    - 03-CONTEXT.md Discretion #9 (get_briefing returns empty model; Phase 5 wires data)
  </read_first>
  <action>
    Create `src/mcp_v2/tools/note.py`. `get_note(path: str) -> NoteContent` (readOnlyHint): `repo_root = Path(".")`,
    `resolved = paths.safe_resolve(repo_root, path)` (raises NotePathForbidden/NoteNotFound), `content = resolved.read_text(encoding="utf-8")`,
    return `NoteContent(path=path, content_md=injection.wrap_untrusted(content, "note", <safe ref derived from path>), injection_suspected=..., injection_flags=...)`.
    Keep get_note reading DISK notes/private/ read-only even though hybrid_search uses the notes DB table (D-05). Derive a
    `_SAFE_ATTR`-compatible `ref_id` from the path (e.g. sanitized basename) so wrap_untrusted's attr guard passes.
    Create `src/mcp_v2/tools/portfolio.py`. `list_portfolio() -> PortfolioView` (readOnlyHint): `p = Portfolio.load(Path("."))`
    wrapped in try/except `PortfolioLoadError` → raise `DataBackendError`. Return `PortfolioView` carrying holdings + watchlist
    (structured — NO injection wrap). Empty holdings/watchlist is a valid empty model.
    Create `src/mcp_v2/tools/briefing.py`. `get_briefing(date: str, type: str = "daily") -> Briefing` (readOnlyHint):
    validate `type in ("daily","weekly")` → else `InvalidArgument`. Return `Briefing(date=date, type=type, found=False, entries=[])`.
    Do NOT query or add a `report_type` column (Phase 5's migration) — honest empty model (D-01, Discretion #9).
    Create `tests/mcp_v2/test_note.py` (no DB; tmp_path notes/private file): get_note returns wrapped content; outside-whitelist path
    raises NotePathForbidden; missing file raises NoteNotFound. Create `tests/mcp_v2/test_portfolio_briefing.py`: list_portfolio returns
    holdings/watchlist from a seeded portfolio.md; missing portfolio.md → DataBackendError; get_briefing POSITIVELY returns a
    `Briefing` with `found is False` and `entries == []` (assert the model fields, not just report_type-absence); a `get_briefing`
    regression that started returning rows or omitting `entries` MUST fail this assertion; bad type → InvalidArgument.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/mcp_v2/test_note.py tests/mcp_v2/test_portfolio_briefing.py -x -q -m "not db"</automated>
  </verify>
  <acceptance_criteria>
    - get_note returns `NoteContent.content_md` wrapped in `<untrusted source="note" ...>`; outside-whitelist → NotePathForbidden; missing → NoteNotFound.
    - list_portfolio returns a `PortfolioView` (structured, no injection wrap); `PortfolioLoadError` is mapped to `DataBackendError`.
    - get_briefing POSITIVELY returns `Briefing(found=False, entries=[])` (assert `result.found is False and result.entries == []`) AND does NOT reference a `report_type` column (grep absent — both checks present); bad type → InvalidArgument.
    - Both test files green with `-m "not db"`.
  </acceptance_criteria>
  <done>
    get_note: disk-whitelist read-only content returned wrapped+flagged; NotePathForbidden/NoteNotFound on bad/missing paths (test_note.py green).
    list_portfolio: Portfolio.load passthrough returns structured PortfolioView; PortfolioLoadError→DataBackendError (test_portfolio_briefing.py green).
    get_briefing: returns Briefing(found=False, entries=[]) positively asserted (independent of the other two tools' criteria) and references no report_type column; bad type→InvalidArgument (test_portfolio_briefing.py green).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool args → DB / disk | rcept_no, corp_code, note path are untrusted input |
| filing/note/card body → downstream LLM | narrative may carry injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SQL-arg | Tampering | filing.py / search_filings SQL args | mitigate | ASCII regex pre-filter (rcept_no/corp_code) before DB; parameterized `text()` constants; NULL-cast filter guards |
| T-prompt-inj | Tampering (downstream LLM) | get_filing/get_note body + card view='both' | mitigate | `injection.wrap_untrusted` + `detect` flag (D-03) on every narrative body |
| T-path-traversal | Tampering / Info Disclosure | get_note path | mitigate | `paths.safe_resolve` whitelist (Plan 03) — read-only notes/private |
| T-veto13 | Information Disclosure | get_decision_card payload default | mitigate | serialize-time `exclude={"body_md"}`; body only on explicit view='both' |
| T-03-07 | Denial of Service | search_filings oversized response | mitigate | D-04 default `limit=50` |
</threat_model>

<verification>
- `tests/mcp_v2/{test_filing,test_card}.py` green with `-m db`; `tests/mcp_v2/{test_note,test_portfolio_briefing}.py` green with `-m "not db"`.
- SC#3 AST guard (Plan 02) green over the new tool files (all SQL is module-level `text()` constants).
</verification>

<success_criteria>
- 6 of the 10 locked tools (get_filing, search_filings, get_decision_card, get_note, list_portfolio, get_briefing) implemented, registered, and tested.
- Veto #8 (whole body), Veto #13 (payload default), D-01 (empty vs raise), D-03 (wrap+flag), D-04 (limit) all honored.
</success_criteria>

<output>
Create `.planning/phases/03-mcp-tool-surface-read-side/03-04-SUMMARY.md` when done.
</output>
