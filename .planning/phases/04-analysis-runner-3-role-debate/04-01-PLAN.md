---
phase: 04-analysis-runner-3-role-debate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analysis/__init__.py
  - src/analysis/checksum.py
  - src/cards/models.py
  - tests/test_import_guard.py
  - pyproject.toml
  - tests/analysis/__init__.py
  - tests/analysis/conftest.py
  - tests/analysis/test_checksum.py
autonomous: true
requirements: ["SC#3"]

must_haves:
  truths:
    - "A numeric fact whose value appears (after Korean-unit normalization) in the source body_md is KEPT; a fact not derivable from source is DROPPED."
    - "Every dropped fact is recorded as a human-readable string in the card's warnings list (no silent loss)."
    - "42.5조원, 42,500,000,000,000, and 425000억 all verify against the same source value (value-equivalence, not exact string)."
    - "A freshly built DecisionCard can carry warnings and still round-trips through store (payload JSONB, no DB column change)."
    - "src/analysis can never import anthropic/openai (CI guard fails the build if it does)."
  artifacts:
    - path: "src/analysis/checksum.py"
      provides: "D-03 Korean-unit value-equivalence numeric checksum (pure Python)"
      contains: "def checksum_facts"
    - path: "src/cards/models.py"
      provides: "DecisionCard.warnings optional payload field"
      contains: "warnings"
    - path: "tests/analysis/conftest.py"
      provides: "Base test fixtures (seeded engine, real-filing body, card oracle) — Wave 0 scaffold"
    - path: "tests/analysis/test_checksum.py"
      provides: "SC#3 value-equivalence + drop→warnings tests"
  key_links:
    - from: "src/analysis/checksum.py"
      to: "src/shared/units.normalize_to_krw + src/shared/number_extraction.extract_numeric_candidates"
      via: "import + call (reuse, do not re-implement)"
      pattern: "normalize_to_krw|extract_numeric_candidates"
    - from: "tests/test_import_guard.py"
      to: "src/analysis"
      via: "GUARDED_DIRS entry"
      pattern: "src/analysis"
---

<objective>
Establish the `src/analysis/` package foundation and deliver the SC#3 numeric
checksum — the deterministic Python gate that drops any LLM-emitted numeric fact
not derivable from source (Veto #1/#4: the brain compresses evidence, it never
self-certifies its own numbers).

Purpose: SC#3 is the one Hard-Veto guarantee Phase 4 must add on top of the
Phase-2 `DecisionCard` contract. This plan also lays the package + test scaffold
(Wave 0) every later Phase-4 plan builds on, and the CI guard that enforces D-01's
"Max-only, no cloud-LLM SDK" rule for the whole module.

Output: `src/analysis/__init__.py`, `src/analysis/checksum.py`, the `warnings`
field on `DecisionCard`, the extended import guard, the `live` pytest marker, and
the `tests/analysis/` package with base fixtures + `test_checksum.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analysis-runner-3-role-debate/04-CONTEXT.md
@.planning/phases/04-analysis-runner-3-role-debate/04-RESEARCH.md
@.planning/phases/04-analysis-runner-3-role-debate/04-PATTERNS.md

<interfaces>
<!-- Exact contracts to reuse. Do NOT re-implement these primitives (RESEARCH "Don't Hand-Roll"). -->

From src/shared/units.py:
```python
KRW_MULTIPLIERS  # {"KRW원":1.0,"KRW백만":1e6,"KRW억":1e8,"KRW조":1e12}
def normalize_to_krw(value: float, unit: str) -> float | None  # None for non-KRW units
```

From src/shared/number_extraction.py:
```python
@dataclass(frozen=True)
class NumericCandidate:
    raw_text: str; offset: int; length: int; guessed_unit: str
    sentence_text: str; pre_context: str; post_context: str; section_hint: str | None
def extract_numeric_candidates(body: str, section_hint: str | None = None) -> list[NumericCandidate]
# guessed_unit ∈ {"KRW원","KRW백만","KRW억","KRW조","USD","EUR","JPY","pct","bps","multiplier","shares","other"}
```

From src/shared/number_sanity.py (optional secondary magnitude gate):
```python
SANITY_RULES: dict[str, SanityRule]   # 24 curated keys
# check_sanity/check_echo_back operate on shared.frontmatter.NumericFact — do NOT reuse the
# echo-back gate (D-03 chose value-equivalence over exact at-offset; PATTERNS lines 87-90)
```

From src/cards/models.py (Phase 2 — the card contract; round-trip target):
```python
class DecisionCard(BaseModel):  # ConfigDict(extra="forbid")
    numeric_facts: dict[str, float | int]   # shape-only; Phase 4 checksums digits
    invalidation_reason: str | None = None   # existing optional payload-field precedent
    status: str | None = None                # existing optional precedent
```

From src/cards/store.py (no change here — confirms warnings round-trips):
```python
_PAYLOAD_EXCLUDE = {"body_md", "status", "invalidation_reason"}  # warnings NOT excluded → persists in payload
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package + card.warnings + CI guards + live marker (Wave 0 scaffold)</name>
  <read_first>
    - src/cards/models.py (whole file — add field next to invalidation_reason/status at lines 93-104; copy their optional-payload-field docstring style)
    - src/cards/store.py lines 57-64 (_PAYLOAD_EXCLUDE — confirm warnings is intentionally NOT excluded so it rides payload JSONB with no DB column; PATTERNS lines 337-343)
    - tests/test_import_guard.py lines 19-20 (BANNED_MODULES / GUARDED_DIRS)
    - pyproject.toml lines 71-78 ([tool.pytest.ini_options] markers)
    - src/cards/__init__.py (barrel style analog for the new src/analysis/__init__.py)
    - tests/mcp_v2/conftest.py (seeded_engine / seeded_narrative_engine pattern — copy the monkeypatch of db.engine.get_engine for the base conftest)
    - tests/cards/conftest.py lines 24-98 (decision_card_yaml round-trip oracle dict to copy as card_oracle fixture)
  </read_first>
  <action>
    Add `warnings: list[str] = Field(default_factory=list)` to `DecisionCard`
    (src/cards/models.py) directly beside the existing `invalidation_reason`/`status`
    optional fields, with a docstring matching theirs: rides in payload JSONB, adds
    NO DB column (RESEARCH Open Q1; the §3 round-trip is unaffected because the field
    defaults empty). This is the typed home for D-03 dropped-fact records.

    Extend `GUARDED_DIRS` in tests/test_import_guard.py to include `"src/analysis"`
    so the cloud-LLM import guard (BANNED_MODULES = anthropic/openai) covers the new
    module — this is the CI enforcement of D-01 "no API direct calls; the brain is
    the claude CLI subprocess only" (RESEARCH Security; PATTERNS lines 349-353).

    Register a `live` marker in pyproject.toml `[tool.pytest.ini_options].markers`
    (alongside slow/e2e/db): `"live: opt-in tests that spawn the real claude CLI and
    spend Max quota (deselect by default)"`. The default suite must never select it.

    Create `src/analysis/__init__.py` (docstring only for now — a later plan adds the
    `analyze_ticker` re-export) and `tests/analysis/__init__.py` (empty package marker).

    Create `tests/analysis/conftest.py` with the BASE fixtures every Phase-4 plan
    reuses (do NOT add the sub-agent fake here — that arrives in the subagents plan):
    (a) a `seeded_engine` fixture that builds a testcontainers Postgres engine and
    monkeypatches `db.engine.get_engine` to it (copy tests/mcp_v2/conftest.py exactly,
    re-declaring what is needed since conftest scope is per-directory); seed entities +
    a couple of filings with real `body_md` (target corp `00126380` / ticker `005930`),
    (b) a `real_filing_body` fixture returning a body_md string containing at least one
    Korean-unit numeric (e.g. a 조/억 amount and a % value) for checksum tests,
    (c) a `card_oracle` fixture = the §3 round-trip dict from tests/cards/conftest.py.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_import_guard.py -x -q</automated>
    <automated>.venv/Scripts/python.exe -c "import pathlib,sys; sys.path.insert(0,'src'); from cards.models import DecisionCard; assert 'warnings' in DecisionCard.model_fields"</automated>
  </verify>
  <acceptance_criteria>
    - `DecisionCard.model_fields` contains `warnings`; building a card without warnings still validates (default empty list).
    - A card with a non-empty `warnings` list survives `DecisionCard.model_validate(card.model_dump(mode="json"))` (round-trip).
    - `tests/test_import_guard.py` passes AND `GUARDED_DIRS` contains `"src/analysis"`.
    - `pyproject.toml` registers a `live` marker; `pytest --markers` lists it (no "unknown marker" warning when used).
    - `src/analysis/__init__.py` and `tests/analysis/__init__.py` exist and import cleanly.
    - `tests/analysis/conftest.py` exposes `seeded_engine`, `real_filing_body`, `card_oracle` (no FakeDebateBackend yet).
  </acceptance_criteria>
  <done>warnings field added (payload-only, no migration); import guard covers src/analysis; live marker registered; analysis package + base test fixtures exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-03 numeric checksum (Korean-unit value-equivalence)</name>
  <read_first>
    - src/shared/units.py (whole — KRW_MULTIPLIERS, normalize_to_krw)
    - src/shared/number_extraction.py lines 34-46, 117-162 (NumericCandidate + extract_numeric_candidates)
    - src/shared/number_sanity.py lines 30-63 (SANITY_RULES — optional magnitude gate)
    - 04-RESEARCH.md lines 283-286 (Discretion #4: normalization pipeline + tolerance) and lines 355-372 (checksum core shape)
    - 04-PATTERNS.md lines 43-94 (checksum.py strongest analog + divergence-from-v1.0 note)
  </read_first>
  <behavior>
    - `42.5조원` claimed vs body containing `42,500,000,000,000` → supported (within tol).
    - `425000억` claimed vs body containing `42.5조원` → supported (both normalize to 4.25e13).
    - `17.2` (pct/multiplier) claimed vs body containing `17.2%` → supported.
    - A value with no corresponding span anywhere in body → NOT supported.
    - `checksum_facts([...], body)` returns kept facts + a warnings string per dropped fact.
  </behavior>
  <action>
    Create `src/analysis/checksum.py` (module docstring citing D-03/SC#3, "Pure
    Python. No LLM, no I/O." — copy number_extraction.py:1-8 style). Implement:

    `fact_supported(value: float, unit: str, body_md: str, tol: float = 0.005) -> bool`
    — normalize the claimed (value, unit) to a canonical number (via `normalize_to_krw`
    for KRW-family units; keep raw value otherwise), then scan `body_md` with
    `extract_numeric_candidates`, normalizing each candidate the SAME way, and return
    True if any candidate matches within RELATIVE tolerance
    `abs(c-target)/max(|c|,|target|,ε) <= tol`. Strip commas and trailing unit glyphs
    from candidate raw_text before float()-ing (the analog in PATTERNS lines 76-85 is
    the template; refine the unit stripping using each candidate's `guessed_unit`).
    Use a default `tol=0.005` (Discretion #4, [ASSUMED] — absorbs rounded display).

    `checksum_facts(facts: list[dict], body_md: str, *, tol: float = 0.005) -> tuple[dict[str, float | int], list[str]]`
    — `facts` are the Judge's rich facts `{key, value, unit, source_ref}`. For each:
    call `fact_supported`; if supported keep `{key: value}` in the projected dict
    (Open Q2 projection to the stored `dict[str,float|int]` shape); if NOT supported,
    DROP it and append `f"{key}={value}{unit}: not verifiable in source"` to the
    returned warnings list (Veto: "실패 = drop the fact"). Optionally run the
    `number_sanity.SANITY_RULES` magnitude check as a second gate for known keys and
    drop+warn on a sanity violation too. Scan the WHOLE body_md (Veto #8); the
    `<untrusted>` wrapper only adds delimiter lines and does not alter inner bytes, so
    matching still works (strip the wrapper first if convenient — either is safe).

    Do NOT use the v1.0 exact `check_echo_back` at-offset gate (PATTERNS lines 87-90).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/analysis/test_checksum.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `fact_supported(42.5, "KRW조", body_with_42_5e12)` is True.
    - `fact_supported(99.9, "KRW조", body_without_that_value)` is False.
    - `checksum_facts` returns a `(kept_dict, warnings)` tuple; kept_dict is `dict[str, float|int]` (the stored shape); warnings has one entry per dropped fact, naming the key.
    - Module imports nothing from anthropic/openai and performs no I/O (pure).
  </acceptance_criteria>
  <done>checksum.py drops unverifiable facts into warnings via Korean-unit value-equivalence, reusing shared/units + number_extraction.</done>
</task>

<task type="auto">
  <name>Task 3: SC#3 checksum tests</name>
  <read_first>
    - tests/test_number_extraction.py, tests/test_number_sanity.py, tests/test_units.py (pure-fn table-of-cases style — the exact analog, PATTERNS lines 308-310)
    - src/analysis/checksum.py (the module under test, from Task 2)
    - tests/analysis/conftest.py (real_filing_body fixture from Task 1)
  </read_first>
  <action>
    Create `tests/analysis/test_checksum.py` — pure-function table tests (no DB).
    Cover: the three-way value-equivalence (`42.5조원` ≡ `42,500,000,000,000` ≡
    `425000억` all verify against a body holding any one form); a pct/multiplier case
    (`17.2` vs `17.2%`); a clear non-match (drop) case; the `checksum_facts` contract
    (kept dict shape + one warning string per dropped fact, warning text names the
    dropped key); and a tolerance boundary case (a value just inside vs just outside
    `tol`). Use `real_filing_body` for at least one case against a realistic body.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/analysis/test_checksum.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/analysis/test_checksum.py` exits 0.
    - A test asserts a dropped fact produces exactly one warnings entry containing its key.
    - A test asserts the three Korean-unit formats of the same magnitude all verify.
  </acceptance_criteria>
  <done>SC#3 behavior locked by passing pure-fn tests; default suite runs them with no live CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → DecisionCard.numeric_facts | sub-agent-emitted numbers are untrusted until checksummed |
| narrative body_md → checksum scanner | filing/news bodies are untrusted input (already `<untrusted>`-wrapped upstream) |
| src/analysis package → cloud-LLM SDKs | the module must never reach a model except via the claude CLI (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | unverified numeric claim reaching a card | mitigate | `checksum_facts` drops any fact not value-equivalent to a source span; dropped facts recorded in `warnings` (Veto #1/#4) |
| T-04-02 | Information disclosure / EoP | cloud-LLM SDK import in src/analysis | mitigate | extend `tests/test_import_guard.py` GUARDED_DIRS to `src/analysis` (CI blocks anthropic/openai) |
| T-04-03 | Tampering | malformed payload reaching storage | mitigate | `warnings` added under `ConfigDict(extra="forbid")`; Pydantic validates before `store.save_card` |
| T-04-SC | Tampering | npm/pip installs | accept | No package installs in Phase 4 (RESEARCH Package Legitimacy Audit: zero new deps) — slopcheck N/A |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/analysis/test_checksum.py tests/test_import_guard.py -x -q` green.
- `DecisionCard` round-trips with a populated `warnings` list (no DB migration).
</verification>

<success_criteria>
SC#3 numeric-checksum primitive exists and is tested; the analysis package, base
fixtures, CI import guard, and `live` marker are in place for all downstream plans.
</success_criteria>

<output>
Create `.planning/phases/04-analysis-runner-3-role-debate/04-01-SUMMARY.md` when done.
</output>
