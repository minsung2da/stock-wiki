---
phase: 04-analysis-runner-3-role-debate
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - src/analysis/runner.py
  - src/analysis/__init__.py
  - tests/analysis/test_runner.py
  - tests/analysis/test_live.py
autonomous: false
requirements: ["SC#1", "SC#2", "SC#3", "SC#4", "SC#5", "SC#6", "SC#7"]

must_haves:
  truths:
    - "`analyze_ticker(corp_code, as_of)` returns ONE DecisionCard and saves it via store.save_card with atomic supersession of the prior active card (SC#1)."
    - "On a FULL run: build bundle → Bull/Bear parallel-blind → Judge synthesizes → checksum → rubric → card (SC#2)."
    - "Every numeric fact on the saved card was verified against source; unverifiable facts are dropped into card.warnings (SC#3)."
    - "The card always has expires_at + assumptions[] + invalidation_triggers[] or Pydantic rejects it (SC#4)."
    - "An empty contradictions[] logs a warning (SC#5)."
    - "A same-stance, no-trigger prior card takes the lightweight refresh path and the sub-agent backend is NEVER called (SC#6)."
    - "Per-stage cost/time is emitted for bundle build + each sub-agent call (SC#7)."
  artifacts:
    - path: "src/analysis/runner.py"
      provides: "analyze_ticker orchestrator (gate → debate|refresh → checksum → rubric → save)"
      contains: "def analyze_ticker"
    - path: "src/analysis/__init__.py"
      provides: "analyze_ticker re-export"
      contains: "analyze_ticker"
    - path: "tests/analysis/test_runner.py"
      provides: "SC#1-7 integration tests with the FakeDebateBackend"
    - path: "tests/analysis/test_live.py"
      provides: "@pytest.mark.live opt-in real-CLI smoke (kept out of default run)"
  key_links:
    - from: "src/analysis/runner.py"
      to: "store.get_active / store.save_card (supersedes=prior.card_id)"
      via: "prior-card load + atomic supersession"
      pattern: "get_active|save_card"
    - from: "src/analysis/runner.py"
      to: "gate.decide / gate.lightweight_refresh / bundle.build_bundle / subagents backend / checksum / rubric / cost"
      via: "orchestration"
      pattern: "decide|build_bundle|checksum_facts|score_to_conviction"
    - from: "src/analysis/runner.py"
      to: "DebateBackend (injected; default ClaudeCliBackend)"
      via: "constructor/param seam so tests inject FakeDebateBackend"
      pattern: "backend"
---

<objective>
Wire `analyze_ticker(corp_code, as_of)` — the composite orchestrator that turns a
ticker into one saved, checksummed, time-boxed `decision_card` via the D-04 gate and
(on a full run) the 3-role debate. This is the phase deliverable: SC#1-7 all close
here.

Purpose: assemble the Wave-1/2 leaves (gate, bundle, subagents, checksum, rubric,
cost, roles, store, models) into the single pure-Python callable Phase 8 (eval) and
Phase 9 (daily routine) will drive unattended. The debate runs only through the
injected `DebateBackend` seam, so the default suite uses the FakeDebateBackend and
spends zero Max quota; one opt-in `live` smoke proves the real CLI path.

Output: `src/analysis/runner.py`, the `analyze_ticker` re-export, the SC#1-7
integration tests, the opt-in live smoke test, and a human-verified real run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-analysis-runner-3-role-debate/04-CONTEXT.md
@.planning/phases/04-analysis-runner-3-role-debate/04-RESEARCH.md
@.planning/phases/04-analysis-runner-3-role-debate/04-PATTERNS.md

<interfaces>
<!-- Everything the runner composes. -->

From src/cards/store.py:
```python
def get_active(engine, corp_code) -> DecisionCard | None
def save_card(engine, card, *, supersedes=None) -> str   # flips prior to 'superseded' in the same txn
```
From src/analysis/gate.py (Plan 04):
```python
def decide(engine, prior, as_of, *, safety_net_days=14) -> GateDecision        # FULL | REFRESH + reasons
def lightweight_refresh(engine, prior, as_of) -> DecisionCard | Escalate
```
From src/analysis/bundle.py (Plan 03):
```python
def build_bundle(engine, corp_code, as_of) -> EvidenceBundle    # .to_stdin() -> str
```
From src/analysis/subagents.py (Plan 05):
```python
class DebateBackend(Protocol): async def run(role, instruction, system_prompt, schema, evidence_stdin, *, timeout_s=180.0) -> RoleResult
class ClaudeCliBackend  # default backend
```
From src/analysis/roles.py (Plan 02): ROLE_SYSTEM_PROMPTS, ROLE_SCHEMAS / prompt_for / schema_for
From src/analysis/rubric.py (Plan 02): score_to_conviction(...), derive_stance(...)
From src/analysis/checksum.py (Plan 01): checksum_facts(facts, body_md) -> (kept_dict, warnings)
From src/analysis/cost.py (Plan 05): capture_cost(role, env), emit_cost(stage_cost)
From src/cards/models.py: DecisionCard (Veto #2 enforced) + Decision/KeyClaim/Contradiction + warnings field
```
as_of (Discretion #9): caller-supplied for determinism; default None → last KST trading-day close ≤ now (16:00 KST).
guards_passed: Phase 4 fills the FIELD only (actual gate evaluation is Phase 6 — deferred).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: runner.py — analyze_ticker orchestrator + __init__ export</name>
  <read_first>
    - src/cards/store.py lines 147-216 (save_card supersession + get_active — PATTERNS lines 190-202)
    - src/cards/models.py lines 33-104 (DecisionCard construction + Veto #2 fields + warnings)
    - src/analysis/{gate,bundle,subagents,roles,rubric,checksum,cost}.py (the leaves to compose — read each module's public surface)
    - src/cards/__init__.py (barrel style for the __init__ export — PATTERNS lines 269-275)
    - 04-RESEARCH.md lines 144-192 (system architecture diagram — the exact flow), lines 300-308 (Discretion #7/#8/#9)
    - 04-PATTERNS.md lines 190-217 (runner.py composite assembly)
  </read_first>
  <action>
    Create `src/analysis/runner.py` (docstring citing SC#1-7 + "evidence compression,
    never price prediction" Veto #1). Implement
    `analyze_ticker(corp_code: str, as_of=None, *, engine=None, backend: DebateBackend
    | None = None) -> DecisionCard`:

    1. Resolve `engine` (default `db.engine.get_engine()`) and `backend` (default
       `ClaudeCliBackend()`). Derive `as_of` if None → last KST trading-day close ≤ now
       via a small pure helper (`_default_as_of()`), else use the caller's value
       (determinism for Phase 8 — Discretion #9). Resolve `ticker` via `resolve_entity`.
    2. `prior = store.get_active(engine, corp_code)`.
    3. `decision = gate.decide(engine, prior, as_of)`. If `REFRESH`:
       `result = gate.lightweight_refresh(engine, prior, as_of)`; if it returns the
       Escalate sentinel, fall through to the FULL path; otherwise
       `store.save_card(engine, result, supersedes=prior.card_id)` and return it. On
       the REFRESH path the `backend` is NEVER called (SC#6).
    4. FULL path: `bundle = build_bundle(engine, corp_code, as_of)`; `stdin =
       bundle.to_stdin()`; emit bundle-build cost/time (SC#7). Run Bull + Bear in
       parallel-blind via `asyncio.gather(backend.run("bull", ..., schema_for("bull"),
       stdin), backend.run("bear", ..., schema_for("bear"), stdin))` (each gets the
       SAME stdin, neither sees the other). Then `backend.run("judge", ...,
       schema_for("judge"), stdin + bull_out + bear_out)` (Judge synthesizes both —
       SC#2c). Emit per-call cost via `capture_cost`/`emit_cost` (SC#7).
    5. Re-validate each role's `structured_output` shape; on a Pydantic failure retry
       the Judge ONCE then fail loudly (RESEARCH Discretion #2).
    6. `kept_facts, warnings = checksum_facts(judge.numeric_facts, <source bodies from
       bundle>)` (SC#3 — drop unverifiable into warnings).
    7. `conviction = score_to_conviction(judge.rubric subscores, evidence_refs=...)`
       (Veto #4/#5 cap) and `stance = derive_stance(...)`.
    8. Build the `DecisionCard`: new `card_id`, `generated_at`, `as_of`, the Judge's
       `key_claims`/`contradictions`/`assumptions`/`invalidation_triggers`, the
       checksummed `numeric_facts` (projected `dict[str,value]`), `warnings`, the
       `guards_passed` FIELD (Phase-4 fills only; actual evaluation is Phase 6 —
       deferred), `body_md`, and `expires_at`. The non-Optional `expires_at` +
       `assumptions min_length=1` enforce SC#4 (Veto #2) at construction.
    9. SC#5: `if not card.contradictions: logging.warning("card has no contradictions
       — suspect", extra={...})`.
    10. `store.save_card(engine, card, supersedes=prior.card_id if prior else None)`
        (atomic supersession — Discretion #7). Return the card.

    EDIT `src/analysis/__init__.py` to `from .runner import analyze_ticker` +
    `__all__ = ["analyze_ticker"]`. The runner imports no anthropic/openai (the only
    model path is the injected backend → claude CLI).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/analysis/test_runner.py -x -q</automated>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_import_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `from analysis import analyze_ticker` works.
    - With an injected FakeDebateBackend + seeded engine, `analyze_ticker("00126380", as_of=<fixed>)` returns a `DecisionCard` and a row is persisted as the active card (SC#1) superseding any prior.
    - On the FULL path the backend is called for bull, bear, and judge; bull/bear receive identical stdin and neither receives the other's output (SC#2).
    - A fabricated numeric fact (absent from bundle bodies) does NOT appear in `card.numeric_facts` and IS listed in `card.warnings` (SC#3).
    - Per-stage cost/time is emitted (assertable via caplog) for bundle + each sub-agent (SC#7).
    - `tests/test_import_guard.py` green (runner has no cloud-LLM import).
  </acceptance_criteria>
  <done>analyze_ticker composes gate → (debate|refresh) → checksum → rubric → save, closing SC#1/#2/#3/#5/#6/#7 wiring; card construction enforces SC#4.</done>
</task>

<task type="auto">
  <name>Task 2: SC#1-7 integration tests + opt-in live smoke file</name>
  <read_first>
    - tests/cards/test_store.py lines 1-60 (seeded_engine + _make_card + round-trip/supersession — PATTERNS lines 304-306)
    - tests/analysis/conftest.py (seeded_engine + fake_debate_backend + card_oracle)
    - tests/test_api_probes.py line 37 (@pytest.mark.slow opt-in analog — PATTERNS lines 316-318)
    - src/analysis/runner.py (module under test)
  </read_first>
  <action>
    Create `tests/analysis/test_runner.py` (uses `fake_debate_backend` + `seeded_engine`
    + a fixed `as_of`):
    - `test_returns_and_saves_card` — SC#1: returns a DecisionCard; `store.get_active`
      afterwards yields the same card_id; a prior card is superseded.
    - `test_blind_parallel` — SC#2: backend `.calls` shows bull + bear got identical
      stdin and neither received the other's output; judge received both.
    - `test_numeric_checksum_drops` — SC#3: a canned Judge output with a fabricated
      numeric fact → that fact is absent from `card.numeric_facts` and present in
      `card.warnings`.
    - `test_veto2_rejects_untimed` — SC#4: a canned Judge output missing assumptions
      (or expires_at) raises `ValidationError` (Veto #2).
    - `test_empty_contradictions_warns` — SC#5: empty contradictions logs a warning
      (caplog).
    - `test_same_stance_refresh_skips_debate` — SC#6: with a fresh prior card and no
      triggers, the backend is NEVER called and a refreshed card is saved.
    - `test_cost_logged` — SC#7: per-stage cost lines are emitted (caplog).
    Create `tests/analysis/test_live.py`: a single `@pytest.mark.live` test that calls
    the REAL `ClaudeCliBackend` against corp `00126380` and asserts a saved active card
    with expires_at + assumptions + checksummed numeric_facts + captured cost. It is
    deselected by default (the marker is registered in Plan 01); document the run
    command in the test docstring.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/analysis/test_runner.py -x -q</automated>
    <automated>.venv/Scripts/python.exe -m pytest tests/analysis -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/analysis/test_runner.py` exits 0 and spawns NO real `claude` process.
    - There is one test per SC (SC#1-7) named for the criterion it locks.
    - `pytest tests/analysis -m "not live"` runs the full suite without selecting `test_live.py`.
    - `test_live.py` is collected only under `-m live` (default run skips it — zero quota).
  </acceptance_criteria>
  <done>SC#1-7 are each locked by a named, quota-free integration test; the live smoke exists as opt-in only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `analyze_ticker` end-to-end via the REAL headless `claude` CLI (Max OAuth) — the
    one verification that cannot run in CI because it spends Max quota and needs a
    logged-in `claude` (VALIDATION.md Manual-Only). The default suite is fully green
    and quota-free; this confirms the live D-01 path actually produces a valid card.
  </what-built>
  <how-to-verify>
    1. Confirm `claude` is logged in (Max OAuth) and `ANTHROPIC_API_KEY` is UNSET in
       the shell: `echo "$ANTHROPIC_API_KEY"` must print empty.
    2. Run the opt-in live smoke against the real DB (578 filings present):
       `.venv/Scripts/python.exe -m pytest tests/analysis/test_live.py -m live -x -q`
    3. Expect: the test passes — one active `decision_card` saved for corp `00126380`
       with a non-empty `assumptions[]`, an `expires_at`, `numeric_facts` that survived
       the checksum, and a captured `total_cost_usd`/`duration_ms` (printed/logged).
    4. Sanity-check the saved card via `get_active` (or the printed card): stance is one
       of BUY/ADD/HOLD/TRIM/SELL/AVOID, conviction ∈ [0,1], contradictions surfaced if
       any, and NO price prediction text in body_md (Veto #1).
    5. Note the per-call cost/time figures — they are the Phase 9 Open-Q4 quota input.
  </how-to-verify>
  <resume-signal>Type "approved" (with the observed cost/time), or describe the failure (auth, schema-invalid output, checksum over-drop, timeout).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-agent `structured_output` → DecisionCard → DB | LLM output is untrusted until Pydantic-validated + checksummed |
| untrusted narrative → sub-agent prompts | injection risk end-to-end (bundle wrapping + role prompts) |
| runner → model | the only model path is the injected DebateBackend → claude CLI (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | untrusted LLM output reaching the DB | mitigate | re-validate every `structured_output` through `DecisionCard` (extra="forbid") + parameterized `store` SQL (Veto #7); checksum drops fabricated numbers |
| T-04-18 | Tampering / EoP | end-to-end prompt injection | mitigate | bundle keeps `<untrusted>` wrapping (Plan 03) + role prompts treat it as data (Plan 02) + numeric checksum re-verifies (Plan 01) |
| T-04-19 | Spoofing / Info disclosure | runner reaching a cloud LLM API | mitigate | model only via injected backend → claude CLI; `tests/test_import_guard.py` covers src/analysis (no anthropic/openai) |
| T-04-20 | Tampering | untimed/assumption-less thesis persisted | mitigate | `DecisionCard` non-Optional `expires_at` + `assumptions min_length=1` reject at construction (SC#4 / Veto #2) |
| T-04-SC | Tampering | npm/pip installs | accept | No installs in Phase 4 (RESEARCH audit) — N/A; live smoke is a blocking human checkpoint, not auto-approvable |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/analysis -q -m "not live"` fully green (no live CLI).
- Full suite gate: `.venv/Scripts/python.exe -m pytest tests/analysis tests/cards tests/shared -q -m "not live"` green before `/gsd:verify-work`.
- Live smoke human-verified once (checkpoint) — real card saved with cost/time captured.
</verification>

<success_criteria>
SC#1-7 all close: `analyze_ticker` returns + saves one checksummed, time-boxed
decision_card via the gated 3-role debate (or cheap refresh), with per-stage cost
logging, proven both in the quota-free suite and once against the live claude CLI.
</success_criteria>

<output>
Create `.planning/phases/04-analysis-runner-3-role-debate/04-06-SUMMARY.md` when done.
</output>
