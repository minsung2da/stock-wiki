---
phase: 07.1-sql-driven-graph-and-mcp-acceleration
verified: 2026-05-06T01:01:13Z
status: passed
score: 4/4
overrides_applied: 0
human_verification:
  - test: "uv run stock graph snapshot --open (또는 out_dir/index.html 브라우저에서 열기)"
    expected: "인터랙티브 그래프가 렌더링됨. communities 색상 구분, god-node 강조, 엣지에 EXTRACTED/INFERRED 태그 표시됨"
    why_human: "index.html의 시각적 렌더링 품질은 코드 grep으로 검증 불가. graphify.export.to_html 호출과 tag 보존은 자동 검증됨(test_graph_json_link_carries_tag PASS), 브라우저 렌더링 결과는 사람이 직접 확인 필요"
---

# Phase 7.1: SQL-Driven Graph + MCP Graph Traversal Acceleration — Verification Report

**Phase Goal:** SQL `edges` + `entities` + `documents` 테이블에서 직접 `vault/graph/<DATE>/{index.html, graph.json, GRAPH_REPORT.md}` 를 생성하고, stock-mcp에 `graph_query` 도구(BFS/community/god_nodes)를 추가하여 2-hop in-memory 탐색이 SQL recursive CTE보다 빠름을 입증
**Verified:** 2026-05-06T01:01:13Z
**Status:** human_needed (3/4 자동 검증 완료, SC-2 브라우저 렌더링 1건 사람 확인 필요)
**Re-verification:** No — 초기 검증

## Goal Achievement

### ROADMAP.md Success Criteria (비교 기준)

| # | Success Criteria (ROADMAP.md) | Status | Evidence |
|---|-------------------------------|--------|----------|
| SC-1 | `uv run stock graph snapshot`이 SQL edges+entities에서 `vault/graph/<KST_DATE>/graph.json`을 non-empty nodes/links로 채운다 | VERIFIED | `test_snapshot_produces_non_empty_graph_json` PASS (07.1-02-SUMMARY); `build_extraction(get_engine())` → `build_from_json` 경로 코드 확인 (snapshot.py:124) |
| SC-2 | `index.html`이 communities, god-nodes, edges.tag(EXTRACTED/INFERRED)를 인터랙티브 그래프로 표시한다 | HUMAN NEEDED | 코드: `to_html(G, communities, ...)` 호출 확인 (snapshot.py:145), `test_graph_json_link_carries_tag` PASS → tag가 graph.json까지 보존됨. 브라우저 렌더링 품질은 사람이 확인 필요 |
| SC-3 | stock-mcp가 최신 `graph.json`을 lazy load하고 `graph_query`(BFS/community/god_nodes)를 노출; 2-hop in-memory가 SQL recursive CTE보다 빠름 | VERIFIED | `graph_cache.GraphCache` lazy-load 구현 확인; `test_graph_traversal_perf.py` PASS — graph_query 0.22ms vs SQL 46.53ms (N=30, ~211× speedup) |
| SC-4 | graphifyy의 detect/extract/collect_files가 snapshot 경로에서 더 이상 호출되지 않음 | VERIFIED | `grep -n "graphify\.detect\|graphify\.extract" src/graph/snapshot.py` → 0건 (docstring 언급만); `test_snapshot_does_not_import_graphify_detect_or_extract` PASS |

**Score:** 3/4 Success Criteria 자동 검증 완료 (SC-2는 코드 레벨 PASS + 브라우저 렌더링 사람 확인 대기)

### Observable Truths (Plan mustΘhaves 종합)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| T-1 | SQL edges+entities+documents에서 graphify build_from_json 호환 dict 생성 | VERIFIED | `src/graph/sql_to_graph.py:build_extraction` 170줄, `{"nodes":[...], "edges":[...], "input_tokens":0, "output_tokens":0}` 반환; `test_sql_to_graph.py` 5/5 PASS |
| T-2 | 각 노드에 (id, type, label, source_file) 키 존재 | VERIFIED | `sql_to_graph.py:144-148` 4개 키 명시적 생성; `test_seed_edges_full_extraction`에서 assert |
| T-3 | 각 엣지에 (source, target, edge_type, tag, weight) 키 존재 (graphify v0.7.5 계약 준수) | VERIFIED | `sql_to_graph.py:155-162`; SUMMARY의 계약 확인 — `src`/`dst` 대신 `source`/`target` 사용 |
| T-4 | EXTRACTED/INFERRED 6개 edge_type 모두 변환, 정보 손실 없음 | VERIFIED | `test_seed_edges_full_extraction`: `edge_types >= {mentions_ticker, note_ticker, ticker_sector, filing_event, event_event}` assert PASS |
| T-5 | 빈 DB에서 `{nodes:[], edges:[]}` 반환, 예외 없음 | VERIFIED | `test_empty_db_returns_empty_extraction` PASS |
| T-6 | `uv run stock graph snapshot`이 SQL 기반으로 비어있지 않은 graph.json 생성 (gap-1 해소) | VERIFIED | `test_snapshot_produces_non_empty_graph_json` PASS (gap-1 regression guard) |
| T-7 | graphify.detect/extract.collect_files 미호출 | VERIFIED | `grep` → snapshot.py에서 실제 import 0건; `test_snapshot_does_not_import_graphify_detect_or_extract` PASS |
| T-8 | index.html이 communities+god-nodes+edge.tag 표시 | HUMAN NEEDED | `to_html` 호출 코드 존재; tag 보존 자동 검증됨. 브라우저 렌더링은 사람 확인 필요 |
| T-9 | GRAPH_REPORT.md 사람이 읽을 수 있는 형태로 생성 | VERIFIED | `snapshot.py:181` — `generate()` 결과를 `GRAPH_REPORT.md`에 기록; 14-day prune/KST/dry-run 보존 확인 |
| T-10 | stock-mcp가 첫 graph-tool 호출 시 graph.json을 networkx.DiGraph로 lazy 로드 | VERIFIED | `GraphCache.get()` 구현: `self._G is None` 조건부 lazy load + lock; `test_get_returns_digraph` PASS |
| T-11 | graph_query 도구가 bfs/community/god_nodes 3가지 모드 노출 | VERIFIED | `graph_query.py:100-106` mode Literal 파라미터; 각 분기 구현; `test_graph_query_tool.py` 7/7(+1 추가) PASS |
| T-12 | 2-hop in-memory BFS가 SQL recursive CTE보다 빠름 (측정 가능) | VERIFIED | `test_graph_query_2hop_faster_than_sql` PASS — 0.22ms vs 46.53ms (~211×) |
| T-13 | graph_cache가 TTL(기본 6h) 후 stale 처리 + 다음 호출 시 재로드 | VERIFIED | `GraphCache.get()` TTL 로직; `test_stale_after_ttl_reloads` PASS |
| T-14 | graph_query는 예외를 던지지 않음 — StructuredError로 매핑 (D-21) | VERIFIED | graph_query.py에 `except Exception as e` catch-all + `to_error_response`; 8개 에러 경로 테스트 PASS |
| T-15 | 기존 get_related(SQL recursive CTE) 도구 그대로 작동 | VERIFIED | Phase 6 regression sweep: 22 passed, `test_get_related_regression.py` 2 PASS |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/graph/sql_to_graph.py` | build_extraction() — SQL→graphify dict 어댑터 (≥80줄) | VERIFIED | 170줄, `build_extraction` export, `__all__` 선언 |
| `tests/graph/test_sql_to_graph.py` | TDD 단위 테스트 ≥100줄 | VERIFIED | 142줄, 5 PASS |
| `src/graph/snapshot.py` | `_run_graphify`가 sql_to_graph 경로 사용, detect/extract 제거 | VERIFIED | 198줄, `build_extraction(get_engine())` 호출; detect/extract import 0건 |
| `tests/graph/test_snapshot_integration.py` | gap-1 회귀 가드 (≥60줄) | VERIFIED | 207줄, 4 PASS |
| `tests/graph/conftest.py` | graphify_stub가 build/cluster/analyze/export/report만 stub | VERIFIED | detect/extract stub 제거됨 (SUMMARY 02 확인) |
| `src/stock_mcp/graph_cache.py` | GraphCache + get_graph_cache export (≥80줄) | VERIFIED | 203줄, `__all__ = ["GraphCache", "get_graph_cache", "reset_graph_cache_singleton_for_tests"]` |
| `src/stock_mcp/tools/graph_query.py` | graph_query MCP tool bfs/community/god_nodes (≥100줄) | VERIFIED | 215줄, `mcp.tool()(graph_query)` 등록 |
| `src/stock_mcp/models.py` | GraphQueryResult, GraphNode, GraphCommunity Pydantic 모델 | VERIFIED | models.py:281-306에 3개 클래스 확인 |
| `tests/stock_mcp/test_graph_traversal_perf.py` | 2-hop BFS vs SQL CTE 벤치마크 (≥80줄) | VERIFIED | 218줄, 2 PASS, graph_query 0.22ms < SQL 46.53ms |
| `tests/stock_mcp/test_graph_cache.py` | GraphCache 단위 테스트 5개 | VERIFIED | 존재, 5 PASS (SUMMARY 03) |
| `tests/stock_mcp/test_graph_query_tool.py` | graph_query 도구 테스트 | VERIFIED | 존재, 8 PASS (SUMMARY 03) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/graph/sql_to_graph.py:build_extraction` | `edges + entities + documents` 테이블 | SELECT (no INSERT) | VERIFIED | sql_to_graph.py:94-121 — 3개 SELECT; INSERT 0건 |
| `build_extraction` 반환값 | `graphify.build.build_from_json` | `{nodes:[...], edges:[...]}` 계약 | VERIFIED | `test_empty_extraction_round_trips_through_build_from_json` + `test_seed_edges_full_extraction` PASS |
| `src/graph/snapshot.py:_run_graphify` | `src/graph/sql_to_graph.py:build_extraction` | import + 함수 호출 | VERIFIED | snapshot.py:116 `from src.graph.sql_to_graph import build_extraction`; :124 `extraction = build_extraction(get_engine())` |
| `src/graph/snapshot.py:_run_graphify` | `graphify.build.build_from_json` | extraction dict 전달 | VERIFIED | snapshot.py:131 `G = build_from_json(extraction, directed=True)` |
| `graph.json output` | `edges.tag 컬럼` | build_extraction → graphify.export.to_json | VERIFIED | sql_to_graph.py:160 `"tag": r["tag"]`; `test_graph_json_link_carries_tag` PASS |
| `src/stock_mcp/graph_cache.py` | `vault/graph/<latest>/graph.json` | Path.glob + 정렬 + json.loads + node_link_graph | VERIFIED | graph_cache.py:69-96 `_latest_graph_json()`; :193 `repo_root / "vault" / "graph"` |
| `src/stock_mcp/tools/graph_query.py` | `graph_cache.get_graph_cache()` | import + get() 호출 | VERIFIED | graph_query.py:26 `from ..graph_cache import get_graph_cache`; :147 `G = get_graph_cache().get()` |
| `src/stock_mcp/server.py` | `tools.graph_query` | side-effect import | VERIFIED | server.py:24 `graph_query,` in import list |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `snapshot._run_graphify` → `graph.json` | `extraction` | `build_extraction(get_engine())` → SQL 3 SELECTs | YES — edges/entities/documents 테이블 쿼리 | FLOWING |
| `graph_cache.GraphCache.get()` → `nx.DiGraph` | `self._G` | `json.loads(graph.json)` + `node_link_graph` | YES — 파일시스템에서 실제 JSON 파싱 | FLOWING |
| `graph_query(mode="bfs")` → `GraphQueryResult` | `nodes` | `nx.single_source_shortest_path_length(G, ...)` | YES — 캐시된 DiGraph에서 실제 BFS | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command / Method | Result | Status |
|----------|-----------------|--------|--------|
| `build_extraction` callable | `python3 -c "from src.graph.sql_to_graph import build_extraction; print(callable(build_extraction))"` | True | PASS |
| `GraphCache` exports | `python3 -c "from src.stock_mcp.graph_cache import GraphCache, get_graph_cache, reset_graph_cache_singleton_for_tests"` | No error | PASS |
| `graph_query` callable + MAX_DEPTH=5 | `python3 -c "from src.stock_mcp.tools.graph_query import graph_query, MAX_DEPTH; print(MAX_DEPTH)"` | 5 | PASS |
| `server.py` imports graph_query | `grep -n "graph_query" src/stock_mcp/server.py` | line 24 확인 | PASS |
| `snapshot.py`에 detect/extract import 없음 | `grep "graphify\.detect\|graphify\.extract" src/graph/snapshot.py` | 0건 (docstring만) | PASS |
| COLL-07 준수 | `grep -rn "anthropic\|openai" src/graph/sql_to_graph.py src/stock_mcp/graph_cache.py src/stock_mcp/tools/graph_query.py` | 0건 | PASS |
| Test suite (user-confirmed) | `pytest tests/graph/ tests/stock_mcp/` | 155 passed, 1 xfailed, 0 failures | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GRAPH-01 | Plans 01, 03 | `edges` 테이블에 ticker→filing, filing→event, note→ticker, event→event, ticker→sector 엣지 구축 | VERIFIED | Phase 7 기존 완료; Plan 01 `test_seed_edges_full_extraction`이 6개 edge_type 변환 검증 |
| GRAPH-02 | Plans 01, 02, 03 | graphify가 `vault/graph/{YYYY-MM-DD}/index.html·graph.json·GRAPH_REPORT.md` 생성 | VERIFIED (+ human for visual) | snapshot.py가 to_json/to_html/generate 모두 호출; 파일 3개 쓰기 코드 확인; 브라우저 렌더링은 human 확인 |

**GRAPH-03** (canonical subgraph queries documentation)은 Phase 7.1 Plan에 포함되지 않으며, Traceability에 "Phase 7 Complete"로 매핑됨 — Phase 7.1 스코프 밖.

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| 없음 | - | - | - |

TODO/FIXME/placeholder 0건, hardcoded empty returns 없음 (build_extraction은 의도적으로 빈 DB에서 빈 리스트 반환 — 이는 stub이 아닌 명시적 설계), anthropic/openai import 0건.

### Human Verification Required

#### 1. index.html 브라우저 렌더링 (SC-2)

**Test:** 실제 DB 데이터가 있는 환경에서 `uv run stock graph snapshot` 실행 후 생성된 `vault/graph/<YYYY-MM-DD>/index.html`을 브라우저에서 열기

**Expected:**
- 노드들이 communities 별로 색상 구분되어 표시됨
- God-node(고차수 허브 노드)가 시각적으로 강조됨
- 엣지에 EXTRACTED/INFERRED 태그가 구분되어 표시됨 (레이블 또는 색상)
- 노드를 클릭/호버 시 label, type, source_file 정보가 표시됨

**Why human:** 브라우저 렌더링 품질, 커뮤니티 레이아웃, 노드 레이블 가독성은 코드 정적 분석으로 검증 불가. `to_html` 호출 자체와 tag 보존은 자동 검증됨(PASS). 시각적 표현 품질만 사람이 확인 필요.

---

## Gaps Summary

자동 검증 가능한 항목은 모두 PASS. 사람 검증이 필요한 항목 1건:

- **SC-2 브라우저 렌더링:** `index.html`의 시각적 품질 — communities 색상, god-node 강조, edge tag 시각화. 코드 레벨(to_html 호출, tag 보존)은 자동 검증됨. 브라우저에서 직접 열어 품질 확인 필요.

이 항목은 갭이라기보다 자동화 불가능한 UI 품질 검증이며, 코드 구현 자체는 완료됨.

---

_Verified: 2026-05-06T01:01:13Z_
_Verifier: Claude (gsd-verifier)_
