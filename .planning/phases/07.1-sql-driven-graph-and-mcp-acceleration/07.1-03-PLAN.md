---
phase: 07.1-sql-driven-graph-and-mcp-acceleration
plan: 03
type: tdd
wave: 3
depends_on: ["07.1-01", "07.1-02"]
files_modified:
  - src/stock_mcp/graph_cache.py
  - src/stock_mcp/tools/graph_query.py
  - src/stock_mcp/server.py
  - src/stock_mcp/models.py
  - tests/stock_mcp/test_graph_cache.py
  - tests/stock_mcp/test_graph_query_tool.py
  - tests/stock_mcp/test_graph_traversal_perf.py
autonomous: true
gap_closure: true
requirements: [GRAPH-01, GRAPH-02]

must_haves:
  truths:
    - "stock-mcp는 첫 graph-tool 호출 시 vault/graph/<latest>/graph.json을 networkx.DiGraph로 lazy 로드한다"
    - "graph_query 도구는 BFS(depth), community lookup, god-node listing 3가지 모드를 노출한다"
    - "라벨링된 fixture에서 2-hop in-memory BFS가 동등 SQL recursive CTE보다 측정 가능하게 빠르다"
    - "graph_cache는 N시간(설정값) 이후 stale 처리하고 다음 호출 시 재로드한다"
    - "graph_query는 NEVER raises — StructuredError로 매핑 (D-21)"
    - "기존 get_related (recursive SQL) 도구는 그대로 작동 — graph_query는 추가 도구"
  artifacts:
    - path: "src/stock_mcp/graph_cache.py"
      provides: "GraphCache 클래스 — lazy loader + stale-after-N-hours invalidation"
      exports: ["GraphCache", "get_graph_cache"]
      min_lines: 80
    - path: "src/stock_mcp/tools/graph_query.py"
      provides: "graph_query MCP tool (mode: bfs|community|god_nodes)"
      exports: ["graph_query"]
      min_lines: 100
    - path: "src/stock_mcp/models.py"
      provides: "GraphQueryResult, GraphNode, GraphCommunity Pydantic 응답 모델"
      contains: "class GraphQueryResult"
    - path: "tests/stock_mcp/test_graph_traversal_perf.py"
      provides: "2-hop in-memory BFS vs SQL recursive CTE 벤치마크 — 100-node fixture"
      min_lines: 80
  key_links:
    - from: "src/stock_mcp/graph_cache.py"
      to: "vault/graph/<latest>/graph.json"
      via: "Path.glob + 정렬 + json.loads + networkx.readwrite.json_graph.node_link_graph"
      pattern: "vault/graph"
    - from: "src/stock_mcp/tools/graph_query.py"
      to: "graph_cache.get_graph_cache()"
      via: "lazy import + get() call"
      pattern: "get_graph_cache"
    - from: "src/stock_mcp/server.py"
      to: "tools.graph_query"
      via: "side-effect import"
      pattern: "from .tools import.*graph_query"
---

<objective>
Phase 7.1 SC-3 달성: stock-mcp가 Plan 02가 생성한 vault/graph/<DATE>/graph.json을
in-memory networkx.DiGraph로 lazy-load하고, 새로운 `graph_query` 도구를 통해
BFS(depth ≥ 2), community lookup, god-node listing을 제공한다. 라벨링된
fixture에서 2-hop in-memory traversal이 동등 SQL recursive CTE보다
측정 가능하게 빠름을 자동 벤치마크로 입증한다.

기존 `get_related` 도구(SQL recursive CTE)는 변경하지 않는다 — D-22 회귀
테스트가 보호. graph_query는 더 빠른 보조 도구로 병존.

Purpose: gap-2 (multi-hop SQL traversal 느림) 해소 + SC-3 자동 검증.
Output: graph_cache 모듈 + graph_query MCP 도구 + 성능 벤치마크 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-sql-driven-graph-and-mcp-acceleration/07.1-01-SUMMARY.md
@.planning/phases/07.1-sql-driven-graph-and-mcp-acceleration/07.1-02-SUMMARY.md

@/mnt/c/Users/minsu/workspace/stock/src/stock_mcp/server.py
@/mnt/c/Users/minsu/workspace/stock/src/stock_mcp/tools/related.py
@/mnt/c/Users/minsu/workspace/stock/src/stock_mcp/tools/search.py
@/mnt/c/Users/minsu/workspace/stock/src/stock_mcp/models.py
@/mnt/c/Users/minsu/workspace/stock/src/stock_mcp/errors.py

<interfaces>
graph.json 형식 (Plan 02 Task 2 검증된 출력):
```json
{
  "nodes": [{"id": "...", "type": "...", "label": "...", "source_file": "..."}],
  "links": [{"src": "...", "dst": "...", "edge_type": "...", "tag": "EXTRACTED|INFERRED", "weight": 1.0}]
}
```
graphify.export.to_json이 networkx.readwrite.json_graph.node_link_data와 호환되는
출력을 만드는지 Plan 02 SUMMARY에서 확인 — 만약 다르면 graph_cache가 직접 파싱.

기존 MCP 도구 등록 패턴 (search.py / related.py 참조):
```python
from .search import mcp  # shared FastMCP instance
def graph_query(...): ...
mcp.tool()(graph_query)
```

기존 에러 매핑 패턴 (errors.py):
- ErrorCode.NOT_FOUND, DB_UNAVAILABLE, INTERNAL
- StructuredError, to_error_response

기존 응답 모델 (models.py):
- BaseModel 상속 + 명시적 필드. exclude_none=True가 기본.

related.py의 깊이 클램프 / 응답 row cap 패턴 (참고만, 본 plan은 별도 도구):
- MAX_DEPTH = 2 (DoS 가드 — graph_query는 더 깊게 허용해도 무방하나 5 정도로 cap)
- 응답 노드 수 cap: 200 정도

networkx 사용 예시 (graph_cache 구현):
```python
import networkx as nx
from networkx.readwrite import json_graph
G = json_graph.node_link_graph(data, directed=True, multigraph=False)
neighbors = nx.single_source_shortest_path_length(G, source=node_id, cutoff=depth)
```
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-03-01 | D (DoS) | graph_query bfs depth | mitigate | depth 하드 cap = 5; 응답 노드 cap = 200 |
| T-7.1-03-02 | T (Tampering) | graph.json 파일 변조 | accept | vault는 git 추적; 외부 입력 아님 |
| T-7.1-03-03 | I (Info disclosure) | graph_cache가 stale graph.json을 무한 보유 | mitigate | TTL = 6시간 (config); 만료 시 재로드 |
| T-7.1-03-04 | D (DoS) | graph.json 거대 corpus 메모리 점유 | mitigate | 첫 로드 시 노드 수 로깅; > 50k 시 warning |
| T-7.1-03-05 | E (Elevation) | graph_query 예외가 stdout MCP 프로토콜 손상 | mitigate | D-21 — 모든 예외 to_error_response로 매핑 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphCache 모듈 — lazy loader + stale-after-N-hours invalidation</name>
  <files>src/stock_mcp/graph_cache.py, tests/stock_mcp/test_graph_cache.py</files>
  <behavior>
    - Test 1 (lazy load): GraphCache 인스턴스 생성 시점에는 파일 로드 안 함. `.get()` 첫 호출에서 vault/graph/<latest>/graph.json 발견 → networkx.DiGraph 반환.
    - Test 2 (latest 선택): 여러 날짜 디렉토리(vault/graph/2026-05-01, 05-03, 05-05) 중 가장 최신 디렉토리의 graph.json을 사용 — KST ISO 정렬.
    - Test 3 (stale invalidation): TTL = 1초로 강제 설정 → 첫 호출 후 sleep(1.5) → 두 번째 호출은 mtime 다시 체크 후 재로드 (파일 변경 시 새 그래프 반환).
    - Test 4 (missing graph): vault/graph/ 비어 있으면 .get()이 StructuredError(NOT_FOUND, "no graph snapshot found") 발생. raise하지 않고 None 반환은 도구 레이어에서 처리.
    - Test 5 (formats): graphify.export.to_json이 출력하는 형식이 networkx.readwrite.json_graph.node_link_data와 약간 다르면 (links 키가 edges인 등) 어댑터 함수가 두 형식 모두 처리.
  </behavior>
  <action>
1. **graph.json 형식 사전 검증**: Plan 02 SUMMARY를 읽어 정확한 output 형식 확인. 알 수 없으면:
   `cd /mnt/c/Users/minsu/workspace/stock && python -c "import inspect, graphify.export as e; print(inspect.getsource(e.to_json))"` 실행하여 키 이름 확정. 결과를 graph_cache.py docstring에 기록.

2. `tests/stock_mcp/test_graph_cache.py` 신규 (RED 우선):
   - test_get_returns_digraph (lazy + 최소 graph.json fixture in tmp_path)
   - test_picks_latest_dated_directory (3개 날짜 디렉토리 만들고 가장 최신 선택)
   - test_stale_after_ttl_reloads (TTL=1초; mtime 변경 후 재호출)
   - test_no_graph_dir_raises_not_found (vault/graph 비어 있을 때 StructuredError)
   - test_handles_link_or_edges_key (`{"links": [...]}` 와 `{"edges": [...]}` 두 형식 모두)

3. RED 확인: `pytest tests/stock_mcp/test_graph_cache.py -x` → 5개 ImportError.

4. `src/stock_mcp/graph_cache.py` 작성:
   ```python
   """Phase 7.1 SC-3: in-memory graph cache for stock-mcp.

   Lazy-loads vault/graph/<latest>/graph.json into networkx.DiGraph on the first
   graph_query call. TTL-based invalidation (default 6h, override via env
   STOCK_MCP_GRAPH_TTL_SECONDS). Reloads when graph.json mtime changes after
   TTL expiry.

   No LLM dependencies. Reads only the file system.
   """
   from __future__ import annotations
   import json, os, threading, time, logging
   from pathlib import Path
   from typing import Any
   import networkx as nx
   from networkx.readwrite import json_graph
   from .errors import ErrorCode, StructuredError

   logger = logging.getLogger(__name__)
   _DEFAULT_TTL_SECONDS = 6 * 3600

   class GraphCache:
       def __init__(self, vault_graph_dir: Path, *, ttl_seconds: int | None = None) -> None:
           self._dir = vault_graph_dir
           self._ttl = ttl_seconds if ttl_seconds is not None else _DEFAULT_TTL_SECONDS
           self._G: nx.DiGraph | None = None
           self._loaded_path: Path | None = None
           self._loaded_mtime: float | None = None
           self._loaded_at: float | None = None
           self._lock = threading.Lock()

       def _latest_graph_json(self) -> Path:
           if not self._dir.exists():
               raise StructuredError(ErrorCode.NOT_FOUND, f"graph dir missing: {self._dir}")
           dated = sorted(
               (d for d in self._dir.iterdir() if d.is_dir() and not d.name.startswith(".")),
               key=lambda p: p.name,
               reverse=True,
           )
           for d in dated:
               gj = d / "graph.json"
               if gj.exists():
                   return gj
           raise StructuredError(ErrorCode.NOT_FOUND, "no graph.json found under vault/graph/")

       def _load(self, path: Path) -> nx.DiGraph:
           data = json.loads(path.read_text(encoding="utf-8"))
           # Normalize: graphify.export.to_json may write 'links' or 'edges'.
           if "edges" in data and "links" not in data:
               data = {**data, "links": [
                   {**e,
                    "source": e.get("src", e.get("source")),
                    "target": e.get("dst", e.get("target"))}
                   for e in data["edges"]
               ]}
           # node_link_graph expects 'source'/'target' on each link.
           # If our graph.json uses 'src'/'dst', remap defensively.
           normalized_links = []
           for L in data.get("links", []):
               nl = dict(L)
               if "source" not in nl:
                   nl["source"] = nl.get("src")
               if "target" not in nl:
                   nl["target"] = nl.get("dst")
               normalized_links.append(nl)
           data = {**data, "links": normalized_links}
           G = json_graph.node_link_graph(data, directed=True, multigraph=False)
           logger.info("graph_cache: loaded %s (nodes=%d, edges=%d)",
                       path, G.number_of_nodes(), G.number_of_edges())
           if G.number_of_nodes() > 50_000:
               logger.warning("graph_cache: %d nodes — consider tightening snapshot scope",
                              G.number_of_nodes())
           return G

       def get(self) -> nx.DiGraph:
           with self._lock:
               now = time.time()
               needs_reload = (
                   self._G is None
                   or self._loaded_at is None
                   or (now - self._loaded_at) >= self._ttl
               )
               if needs_reload:
                   path = self._latest_graph_json()
                   mtime = path.stat().st_mtime
                   if (
                       self._G is None
                       or self._loaded_path != path
                       or self._loaded_mtime != mtime
                   ):
                       self._G = self._load(path)
                       self._loaded_path = path
                       self._loaded_mtime = mtime
                   self._loaded_at = now
               return self._G

   _SINGLETON: GraphCache | None = None

   def get_graph_cache(repo_root: Path | None = None) -> GraphCache:
       global _SINGLETON
       if _SINGLETON is None:
           if repo_root is None:
               from .repo_root import repo_root as _rr
               repo_root = _rr()
           ttl = int(os.getenv("STOCK_MCP_GRAPH_TTL_SECONDS", str(_DEFAULT_TTL_SECONDS)))
           _SINGLETON = GraphCache(repo_root / "vault" / "graph", ttl_seconds=ttl)
       return _SINGLETON

   def reset_graph_cache_singleton_for_tests() -> None:
       global _SINGLETON
       _SINGLETON = None
   ```

5. `pytest tests/stock_mcp/test_graph_cache.py -v` → 5 PASS GREEN.

6. **테스트 격리**: 싱글톤 사용으로 테스트 간 오염 방지를 위해 각 테스트는 `reset_graph_cache_singleton_for_tests()`를 호출하거나 GraphCache 직접 인스턴스화. test_graph_cache.py에서는 직접 인스턴스화 권장 (singleton 우회).

7. PROJECT.md 가드 재검증: `grep -E "anthropic|openai" src/stock_mcp/graph_cache.py` → 0건.

8. 함수 < 50 lines, 파일 < 800 lines, 변수 mutation 최소 (state는 인스턴스 변수만 — race condition 가드는 _lock).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/stock_mcp/test_graph_cache.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/stock_mcp/test_graph_cache.py 5개 테스트 모두 PASS
    - GraphCache 클래스 + get_graph_cache 싱글톤 헬퍼 export
    - TTL 만료 후 mtime 변경 감지 로직 검증
    - 'links' / 'edges' 두 형식 모두 처리 (graphify.export 호환성 확보)
    - 50k 이상 노드 시 warning 로그 (DoS 미티게이션 T-7.1-03-04)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: graph_query MCP 도구 + 응답 모델 + server 등록</name>
  <files>src/stock_mcp/tools/graph_query.py, src/stock_mcp/models.py, src/stock_mcp/server.py, tests/stock_mcp/test_graph_query_tool.py</files>
  <behavior>
    - Test 1 (bfs mode): seed graph + graph_cache stub → graph_query(mode="bfs", start_id="doc1", depth=2) → 1-hop, 2-hop neighbor 모두 반환, depth 필드 포함, 200-row cap.
    - Test 2 (community mode): graph_query(mode="community", node_id="doc1") → 노드가 속한 community id + 멤버 노드 리스트 반환 (cluster 정보가 graph.json node attribute에 들어있다는 가정 — 없으면 networkx.community 알고리즘으로 즉석 계산).
    - Test 3 (god_nodes mode): graph_query(mode="god_nodes", top_k=5) → degree centrality 기준 top 5 노드 반환.
    - Test 4 (depth clamp): depth > 5 → 5로 clamp (DoS).
    - Test 5 (NOT_FOUND): 존재하지 않는 start_id → {"error": {"code": "NOT_FOUND", ...}} 반환, raise 안 함.
    - Test 6 (graph missing): graph_cache가 NOT_FOUND raise → 도구가 NOT_FOUND error response로 매핑.
    - Test 7 (D-21 catch-all): 알 수 없는 mode 등 invalid 인자 → INTERNAL 또는 INVALID_INPUT error response.
  </behavior>
  <action>
1. `src/stock_mcp/models.py`에 응답 모델 추가 (immutable, BaseModel):
   ```python
   class GraphNode(BaseModel):
       id: str
       type: str | None = None
       label: str | None = None
       depth: int | None = None  # bfs mode only

   class GraphCommunity(BaseModel):
       community_id: int | str
       members: list[str]

   class GraphQueryResult(BaseModel):
       mode: Literal["bfs", "community", "god_nodes"]
       nodes: list[GraphNode] = Field(default_factory=list)
       communities: list[GraphCommunity] = Field(default_factory=list)
       truncation_applied: list[str] = Field(default_factory=list)
   ```

2. `tests/stock_mcp/test_graph_query_tool.py` 신규 (RED 먼저):
   - test_bfs_returns_2hop_neighbors
   - test_community_lookup
   - test_god_nodes_top_k
   - test_depth_clamped_to_5
   - test_unknown_start_id_returns_not_found
   - test_graph_missing_returns_not_found
   - test_invalid_mode_returns_error
   - 각 테스트는 `monkeypatch`로 `get_graph_cache`를 stub하여 testing-only DiGraph 주입.

3. RED 확인.

4. `src/stock_mcp/tools/graph_query.py` 작성:
   ```python
   """Phase 7.1 SC-3: graph_query MCP tool — BFS / community / god-nodes
   over the in-memory networkx.DiGraph cached from vault/graph/<latest>/graph.json.

   Faster path for multi-hop neighborhoods than the SQL recursive CTE in
   tools/related.py. The two tools coexist: get_related is the SQL fallback
   used when the graph snapshot is stale or missing; graph_query is the
   accelerated path used when the cache is fresh.
   """
   from __future__ import annotations
   import time
   from typing import Literal
   import networkx as nx

   from ..errors import ErrorCode, StructuredError, to_error_response
   from ..logging import log_tool_call
   from ..models import GraphNode, GraphCommunity, GraphQueryResult
   from ..graph_cache import get_graph_cache
   from .search import mcp

   __all__ = ["MAX_DEPTH", "MAX_NODES", "graph_query"]
   MAX_DEPTH = 5
   MAX_NODES = 200

   def _bfs(G: nx.DiGraph, start_id: str, depth: int) -> list[GraphNode]:
       if start_id not in G:
           raise StructuredError(ErrorCode.NOT_FOUND, f"node not in graph: {start_id}")
       lengths = nx.single_source_shortest_path_length(G, source=start_id, cutoff=depth)
       out: list[GraphNode] = []
       for nid, dist in lengths.items():
           if nid == start_id:
               continue
           attrs = G.nodes[nid]
           out.append(GraphNode(id=nid, type=attrs.get("type"),
                                label=attrs.get("label"), depth=dist))
       return out

   def _community_of(G: nx.DiGraph, node_id: str) -> list[GraphCommunity]:
       if node_id not in G:
           raise StructuredError(ErrorCode.NOT_FOUND, f"node not in graph: {node_id}")
       cid = G.nodes[node_id].get("community")
       if cid is None:
           # Fallback: weakly connected component as a coarse community.
           comp = nx.node_connected_component(G.to_undirected(), node_id)
           return [GraphCommunity(community_id="cc", members=sorted(comp)[:MAX_NODES])]
       members = [n for n, a in G.nodes(data=True) if a.get("community") == cid]
       return [GraphCommunity(community_id=cid, members=members[:MAX_NODES])]

   def _god_nodes(G: nx.DiGraph, top_k: int) -> list[GraphNode]:
       if G.number_of_nodes() == 0:
           return []
       deg = sorted(G.degree(), key=lambda x: x[1], reverse=True)[:top_k]
       return [GraphNode(id=nid, type=G.nodes[nid].get("type"),
                         label=G.nodes[nid].get("label")) for nid, _ in deg]

   def graph_query(
       mode: Literal["bfs", "community", "god_nodes"],
       start_id: str | None = None,
       node_id: str | None = None,
       depth: int = 2,
       top_k: int = 10,
   ) -> GraphQueryResult | dict:
       """In-memory graph traversal over the latest vault/graph/<DATE>/graph.json.

       ### Modes
       - bfs(start_id, depth): up to ``depth``-hop reachable nodes (depth ≤ 5).
       - community(node_id): community label + member list for the node's cluster.
       - god_nodes(top_k): top-k highest-degree nodes (centrality proxy).

       ### Performance
       2-hop traversal is measurably faster than tools/related.py SQL recursive CTE
       (verified by tests/stock_mcp/test_graph_traversal_perf.py).

       ### Errors
       Returns ``{"error": {...}}`` — never raises (D-21).
       """
       t0 = time.perf_counter()
       args_log = {"mode": mode, "start_id": start_id, "node_id": node_id,
                   "depth": depth, "top_k": top_k}
       truncation: list[str] = []
       try:
           clamped = max(1, min(depth, MAX_DEPTH))
           if depth > MAX_DEPTH:
               truncation.append(f"depth-clamped to {MAX_DEPTH}")
           G = get_graph_cache().get()
           if mode == "bfs":
               if not start_id:
                   raise StructuredError(ErrorCode.INTERNAL,
                                         "bfs mode requires start_id")
               nodes = _bfs(G, start_id, clamped)
               if len(nodes) > MAX_NODES:
                   nodes = nodes[:MAX_NODES]
                   truncation.append(f"{MAX_NODES}-node cap")
               result = GraphQueryResult(mode=mode, nodes=nodes,
                                         truncation_applied=truncation)
           elif mode == "community":
               if not node_id:
                   raise StructuredError(ErrorCode.INTERNAL,
                                         "community mode requires node_id")
               comms = _community_of(G, node_id)
               result = GraphQueryResult(mode=mode, communities=comms,
                                         truncation_applied=truncation)
           elif mode == "god_nodes":
               nodes = _god_nodes(G, max(1, min(top_k, 50)))
               result = GraphQueryResult(mode=mode, nodes=nodes,
                                         truncation_applied=truncation)
           else:
               raise StructuredError(ErrorCode.INTERNAL,
                                     f"unknown mode: {mode}")
           latency = int((time.perf_counter() - t0) * 1000)
           log_tool_call("graph_query", args_log, latency,
                         len(result.model_dump_json()) // 4)
           return result
       except StructuredError as e:
           latency = int((time.perf_counter() - t0) * 1000)
           err = to_error_response(e)
           log_tool_call("graph_query", args_log, latency, 0, error=err["error"])
           return err
       except Exception as e:  # noqa: BLE001 — D-21 catch-all
           wrapped = StructuredError(ErrorCode.INTERNAL, str(e)[:200])
           latency = int((time.perf_counter() - t0) * 1000)
           err = to_error_response(wrapped)
           log_tool_call("graph_query", args_log, latency, 0, error=err["error"])
           return err

   mcp.tool()(graph_query)
   ```

5. `src/stock_mcp/server.py`에 side-effect import 추가:
   ```python
   from .tools import (  # noqa: F401, E402
       events, filing, graph_query,  # NEW
       health, notes, overview, portfolio, related,
   )
   ```

6. RED→GREEN: `pytest tests/stock_mcp/test_graph_query_tool.py -v` → 7 PASS.

7. **Phase 6 회귀 보호**: `pytest tests/stock_mcp/ -v` → 기존 도구(search/related/filing/etc) 회귀 0건. 특히 docstring contract test가 graph_query에 대해서도 통과해야 함 — Behavior contract / Errors / Performance budget 섹션을 docstring에 모두 포함시켜둠.

8. PROJECT.md COLL-07 재검증: `grep -E "anthropic|openai" src/stock_mcp/tools/graph_query.py src/stock_mcp/graph_cache.py` → 0건.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/stock_mcp/test_graph_query_tool.py tests/stock_mcp/test_graph_cache.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    - tests/stock_mcp/test_graph_query_tool.py 7개 테스트 모두 PASS
    - server.py가 graph_query를 side-effect import로 등록
    - GraphQueryResult/GraphNode/GraphCommunity Pydantic 모델 추가
    - 모든 예외 경로가 to_error_response로 매핑되어 raise 0건 (D-21)
    - depth/노드 수 cap이 동작 (DoS 미티게이션)
    - tests/stock_mcp/ 전체 회귀 PASS (기존 도구 영향 없음)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 2-hop 성능 벤치마크 — in-memory BFS vs SQL recursive CTE</name>
  <files>tests/stock_mcp/test_graph_traversal_perf.py</files>
  <behavior>
    - Test 1 (correctness): 100-노드 라벨링된 fixture에서 graph_query(bfs, depth=2)와 SQL recursive CTE (related.get_related, depth=2)가 같은 set의 reachable document id를 반환 (단, related는 doc만 따라가므로 비교 대상은 도큐먼트 노드만).
    - Test 2 (latency): 100-노드 fixture × 100회 반복 측정. graph_query bfs 평균 latency < SQL recursive CTE 평균 latency. (또는 측정값 고정 임계: graph_query < 50ms, SQL > 100ms — 환경에 따라 변동 가능하므로 ratio assertion 권장: `graph_latency * 1.2 < sql_latency` 또는 명시적 차이 0이상 + 측정 값 출력.)
    - Test 3 (budget gate): graph_query 2-hop 단일 호출 latency < 500ms (절대 가드).
  </behavior>
  <action>
1. `tests/stock_mcp/test_graph_traversal_perf.py` 신규 작성:
   ```python
   """Phase 7.1 SC-3 자동 검증: 2-hop in-memory BFS vs SQL recursive CTE.

   라벨링된 100-노드 fixture에서 graph_query(bfs, depth=2)가 동등 SQL traversal
   (tools/related.get_related, depth=2)보다 측정 가능하게 빠름을 입증한다.

   Phase 7 verification gap-2의 자동 가드. 환경 변동을 고려해 ratio 기반
   assertion을 1차로, 절대 budget gate(<500ms)를 2차로 사용한다.
   """
   from __future__ import annotations
   import json, time
   from pathlib import Path
   from statistics import median

   import networkx as nx
   import sqlalchemy as sa
   import pytest

   def _build_100_node_fixture(pg_engine, vault_graph_dir: Path) -> dict:
       """Insert 100 documents + 200 edges (chain + star + cross) into pg + write
       matching graph.json into vault_graph_dir/<today>/graph.json.

       Returns {"start_doc_id": ..., "expected_2hop_docs": set[str]}.
       """
       # Truncate.
       with pg_engine.begin() as conn:
           conn.execute(sa.text("TRUNCATE edges, chunks, documents, entity_aliases, entities RESTART IDENTITY CASCADE"))

       import hashlib
       from datetime import datetime, timezone
       doc_ids = [hashlib.sha256(f"perf-doc-{i}".encode()).hexdigest() for i in range(100)]
       now = datetime.now(timezone.utc)
       with pg_engine.begin() as conn:
           for i, did in enumerate(doc_ids):
               conn.execute(sa.text(
                   "INSERT INTO documents (id, body, source, vault_path, first_seen_at, last_seen_at) "
                   "VALUES (:id, 'b', 'dart', :vp, :now, :now)"
               ), {"id": did, "vp": f"vault/raw/dart/2026/perf-{i}.md", "now": now})
           # Chain: doc[i] -- mentions_ticker --> doc[i+1] (events table simulation via doc-doc)
           edges_to_insert = []
           for i in range(99):
               edges_to_insert.append({
                   "st": "document", "si": doc_ids[i],
                   "dt": "document", "di": doc_ids[i+1],
                   "et": "filing_event", "tag": "INFERRED",
               })
           # Add a few cross-edges so 2-hop set > 1-hop set.
           for i in (0, 10, 20):
               edges_to_insert.append({
                   "st": "document", "si": doc_ids[i],
                   "dt": "document", "di": doc_ids[i+5],
                   "et": "event_event", "tag": "INFERRED",
               })
           # Note: edges table CHECK requires actual valid edge_types — use ones in EDGE_TAG_POLICY.
           for e in edges_to_insert:
               conn.execute(sa.text(
                   "INSERT INTO edges (src_type, src_id, dst_type, dst_id, edge_type, tag) "
                   "VALUES (:st, :si, :dt, :di, :et, :tag) ON CONFLICT DO NOTHING"
               ), e)

       # Build graph.json mirroring the SQL state.
       nodes = [{"id": did, "type": "document", "label": f"d{i}", "source_file": ""}
                for i, did in enumerate(doc_ids)]
       links = []
       for i in range(99):
           links.append({"src": doc_ids[i], "dst": doc_ids[i+1],
                         "edge_type": "filing_event", "tag": "INFERRED",
                         "source": doc_ids[i], "target": doc_ids[i+1]})
       for i in (0, 10, 20):
           links.append({"src": doc_ids[i], "dst": doc_ids[i+5],
                         "edge_type": "event_event", "tag": "INFERRED",
                         "source": doc_ids[i], "target": doc_ids[i+5]})

       today_dir = vault_graph_dir / "2026-05-05"
       today_dir.mkdir(parents=True, exist_ok=True)
       (today_dir / "graph.json").write_text(
           json.dumps({"nodes": nodes, "links": links}), encoding="utf-8")

       # Expected 2-hop set from doc[0]: doc[1] (1-hop), doc[2] + doc[5] (2-hop via chain & cross).
       return {
           "start_doc_id": doc_ids[0],
           "expected_2hop_doc_ids": {doc_ids[1], doc_ids[2], doc_ids[5]},
       }

   def test_graph_query_bfs_matches_sql_recursive(tmp_path, pg_engine, monkeypatch):
       from src.stock_mcp.graph_cache import GraphCache, reset_graph_cache_singleton_for_tests
       reset_graph_cache_singleton_for_tests()
       fixture = _build_100_node_fixture(pg_engine, tmp_path / "vault" / "graph")

       # Inject GraphCache pointing to tmp_path.
       cache = GraphCache(tmp_path / "vault" / "graph", ttl_seconds=3600)
       import src.stock_mcp.tools.graph_query as gq_mod
       monkeypatch.setattr(gq_mod, "get_graph_cache", lambda: cache)

       from src.stock_mcp.tools.graph_query import graph_query
       gq_result = graph_query(mode="bfs", start_id=fixture["start_doc_id"], depth=2)
       gq_doc_ids = {n.id for n in gq_result.nodes if n.type == "document"}

       from src.stock_mcp.tools.related import get_related
       related_result = get_related(fixture["start_doc_id"], depth=2)
       sql_doc_ids = {r.id for r in related_result.related if r.vault_path is not None}

       # Both should at least include the expected 2-hop doc set.
       assert fixture["expected_2hop_doc_ids"] <= gq_doc_ids, \
           f"graph_query missed: {fixture['expected_2hop_doc_ids'] - gq_doc_ids}"
       assert fixture["expected_2hop_doc_ids"] <= sql_doc_ids, \
           f"SQL get_related missed: {fixture['expected_2hop_doc_ids'] - sql_doc_ids}"

   def test_graph_query_2hop_faster_than_sql(tmp_path, pg_engine, monkeypatch, capsys):
       from src.stock_mcp.graph_cache import GraphCache, reset_graph_cache_singleton_for_tests
       reset_graph_cache_singleton_for_tests()
       fixture = _build_100_node_fixture(pg_engine, tmp_path / "vault" / "graph")
       cache = GraphCache(tmp_path / "vault" / "graph", ttl_seconds=3600)
       import src.stock_mcp.tools.graph_query as gq_mod
       monkeypatch.setattr(gq_mod, "get_graph_cache", lambda: cache)

       from src.stock_mcp.tools.graph_query import graph_query
       from src.stock_mcp.tools.related import get_related

       # Warm the cache.
       graph_query(mode="bfs", start_id=fixture["start_doc_id"], depth=2)

       N = 30
       gq_times: list[float] = []
       sql_times: list[float] = []
       for _ in range(N):
           t0 = time.perf_counter()
           graph_query(mode="bfs", start_id=fixture["start_doc_id"], depth=2)
           gq_times.append(time.perf_counter() - t0)
           t0 = time.perf_counter()
           get_related(fixture["start_doc_id"], depth=2)
           sql_times.append(time.perf_counter() - t0)

       gq_med = median(gq_times) * 1000
       sql_med = median(sql_times) * 1000
       print(f"\nperf: graph_query 2-hop median = {gq_med:.2f}ms, "
             f"SQL recursive 2-hop median = {sql_med:.2f}ms (N={N})")

       # Assertion: graph_query meaningfully faster than SQL.
       # Hard absolute gate: graph_query 2-hop < 500ms in this fixture size.
       assert gq_med < 500, f"graph_query 2-hop too slow: {gq_med:.2f}ms"
       # Ratio gate: in-memory should be at least as fast (allow small overhead margin).
       assert gq_med < sql_med, (
           f"in-memory NOT faster than SQL: gq={gq_med:.2f}ms sql={sql_med:.2f}ms — "
           f"investigate (perhaps SQL is small-data fast in this fixture size; "
           f"document the observation but do not silently weaken this gate)"
       )
   ```

2. RED 확인 (graph_query 미존재 시 ImportError; Task 2 후 GREEN).

3. **belt-and-suspenders 보호**: 만약 SQL이 100-노드 케이스에서 더 빨라 ratio assertion 실패 → 절대 임계로 후퇴하지 말고:
   - fixture 크기 키워서 (500-1000 노드) 다시 측정
   - 그래도 SQL이 빠르면 SC-3 가정 자체가 깨진 것 — phase 7.1 디자인 재고. 본 테스트가 fail로 그 시그널을 정확히 발산해야 함 (verification.md "verification before completion" 원칙).
   - 측정값을 stdout으로 print하여 SUMMARY에 포함.

4. `cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/stock_mcp/test_graph_traversal_perf.py -v -s` → 2 PASS, 측정값 캡처.

5. 전체 회귀: `uv run pytest tests/ -v 2>&1 | tail -10` → 누적 회귀 0건.

6. 코드 스타일: 파일 < 800 lines, mutation 없음 (list append은 불가피, dict 변형은 spread 사용).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/stock_mcp/test_graph_traversal_perf.py -v -s 2>&1 | tail -25</automated>
  </verify>
  <done>
    - tests/stock_mcp/test_graph_traversal_perf.py 2개 테스트 모두 PASS
    - graph_query 2-hop median latency < SQL recursive 2-hop median latency (assert 통과)
    - graph_query 2-hop median latency < 500ms (절대 budget gate)
    - 측정값 stdout 출력으로 SUMMARY에 포함 가능
    - 100-노드 fixture에서 두 traversal이 같은 expected 2-hop doc set 포함
    - tests/ 전체 회귀 0건
    - SC-3 자동 검증 완료 (gap-2 회귀 가드 확보)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v` → Plan 01/02/03 신규 + Phase 6/7 기존 테스트 전체 PASS
- `uv run pytest tests/stock_mcp/test_graph_traversal_perf.py -v -s` → 측정값 출력에 graph_query < SQL 명시
- `grep -rE "anthropic|openai" src/graph/ src/stock_mcp/graph_cache.py src/stock_mcp/tools/graph_query.py` → 0건 (COLL-07)
- `uv run python -c "from stock_mcp.server import mcp; print([t.name for t in mcp._tools.values()])"` → graph_query 포함 (등록 검증)
</verification>

<success_criteria>
- src/stock_mcp/graph_cache.py가 networkx.DiGraph를 lazy-load + TTL 기반 무효화
- graph_query MCP 도구가 bfs/community/god_nodes 3개 모드 모두 지원
- 모든 에러 경로가 StructuredError → to_error_response로 매핑됨 (D-21, raise 0건)
- 100-노드 라벨링된 fixture에서 graph_query 2-hop이 SQL recursive CTE 2-hop보다 빠름 (자동 측정)
- graph_query 2-hop 절대 latency < 500ms
- 기존 get_related (SQL) 도구 회귀 0건
- LLM API 의존성 0건
</success_criteria>

<output>
완료 후: `.planning/phases/07.1-sql-driven-graph-and-mcp-acceleration/07.1-03-SUMMARY.md` 생성.
포함: graph_cache TTL/lazy 동작 요약, graph_query 도구 시그니처/모드 목록,
2-hop 성능 측정값 (graph_query median ms vs SQL median ms — 실제 숫자 인용),
SC-3 자동 검증 증거, gap-2 회귀 가드 확보 선언. Phase 7.1 phase-gate에서 사람이
재실행해야 할 항목(visual graph 품질, live corpus Q1-Q5)은 phase-level
HUMAN-UAT.md로 라우팅.
</output>
