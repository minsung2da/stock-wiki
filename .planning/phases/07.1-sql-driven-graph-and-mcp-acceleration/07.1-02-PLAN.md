---
phase: 07.1-sql-driven-graph-and-mcp-acceleration
plan: 02
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - src/graph/snapshot.py
  - src/graph/window.py
  - tests/graph/conftest.py
  - tests/graph/test_snapshot_cli.py
  - tests/graph/test_snapshot_integration.py
autonomous: true
gap_closure: true
requirements: [GRAPH-02]

must_haves:
  truths:
    - "`uv run stock graph snapshot` 실행 시 vault/graph/<KST_DATE>/graph.json이 비어있지 않은 nodes/links로 채워진다 (gap-1 해소)"
    - "graphify.detect / graphify.extract.collect_files / graphify.extract.extract 가 더 이상 호출되지 않는다"
    - "vault/graph/<KST_DATE>/index.html이 communities + god-nodes + edge.tag(EXTRACTED/INFERRED)를 표시한다"
    - "GRAPH_REPORT.md가 사람이 읽을 수 있는 형태로 생성된다"
    - "기존 테스트(test_snapshot_cli.py 3개, test_get_related_regression.py 2개)가 계속 PASS"
    - "14일 prune + KST 디렉토리 네이밍 + dry-run 동작이 보존된다"
  artifacts:
    - path: "src/graph/snapshot.py"
      provides: "_run_graphify가 sql_to_graph.build_extraction → build_from_json 경로로 동작"
      contains: "build_extraction"
    - path: "tests/graph/conftest.py"
      provides: "graphify_stub fixture가 build/cluster/analyze/export/report만 stub (detect/extract 제거)"
      contains: "build_from_json"
    - path: "tests/graph/test_snapshot_integration.py"
      provides: "실제 SQL fixture에서 snapshot이 비어있지 않은 graph.json 생성 검증 (SC-1 자동화)"
      min_lines: 60
  key_links:
    - from: "src/graph/snapshot.py:_run_graphify"
      to: "src/graph/sql_to_graph.py:build_extraction"
      via: "import + 함수 호출"
      pattern: "from .*sql_to_graph import build_extraction"
    - from: "src/graph/snapshot.py:_run_graphify"
      to: "graphify.build.build_from_json"
      via: "extraction dict 전달"
      pattern: "build_from_json\\(extraction"
    - from: "graph.json output"
      to: "edges.tag column"
      via: "build_extraction → graphify.export.to_json"
      pattern: "tag.*EXTRACTED|INFERRED"
---

<objective>
src/graph/snapshot.py의 `_run_graphify`를 재작성하여, graphifyy의 마크다운 AST 추출
경로(`detect/collect_files/extract`)를 완전히 제거하고 Plan 01의
`sql_to_graph.build_extraction(engine)` → `graphify.build.build_from_json` 경로로
대체한다.

이는 gap-1 (빈 graph.json) 핵심 수정. window.py의 staging 로직은 더 이상
필요 없으나 (SQL이 데이터 소스), 기존 14-day prune / KST 네이밍 / dry-run /
test_snapshot_cli.py 3개 테스트는 계약 그대로 유지한다.

graphify_stub fixture는 detect/extract 항목을 제거하고 build/cluster/analyze/
export/report만 stub하도록 슬림화. 추가로 SQL fixture를 사용하는 통합
테스트(test_snapshot_integration.py)를 신설하여 graph.json이 비어있지 않은지
자동 검증 (지금까지 사람만 확인 가능했던 SC-2를 자동화).

Purpose: Phase 7 verification gap-1 완전 해소 + SC-1/SC-2 자동 검증 추가.
Output: 재작성된 snapshot.py + 신규 통합 테스트 + 슬림화된 fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-sql-driven-graph-and-mcp-acceleration/07.1-01-SUMMARY.md

@/mnt/c/Users/minsu/workspace/stock/src/graph/snapshot.py
@/mnt/c/Users/minsu/workspace/stock/src/graph/window.py
@/mnt/c/Users/minsu/workspace/stock/tests/graph/conftest.py
@/mnt/c/Users/minsu/workspace/stock/tests/graph/test_snapshot_cli.py
@/mnt/c/Users/minsu/workspace/stock/src/graph/sql_to_graph.py

<interfaces>
sql_to_graph.build_extraction (Plan 01에서 확정):
```python
from src.graph.sql_to_graph import build_extraction
extraction = build_extraction(engine)  # {nodes, edges, input_tokens, output_tokens}
G = graphify.build.build_from_json(extraction, directed=True)
```

graphify 0.7.5 KEEP 사용 심볼:
  graphify.build.build_from_json(extraction, directed=True) -> nx.DiGraph
  graphify.cluster.cluster(G) -> dict[int, list[node_id]]
  graphify.cluster.score_all(G, communities) -> dict
  graphify.analyze.god_nodes(G, top_n=10) -> list
  graphify.analyze.surprising_connections(G, communities, top_n=5) -> list
  graphify.analyze.suggest_questions(G, communities, labels, top_n=7) -> list
  graphify.export.to_json(G, communities, path, force=True)
  graphify.export.to_html(G, communities, path, community_labels=, member_counts=)
  graphify.report.generate(G, communities, cohesion, labels, gods, surprises,
                          detection, tokens, source_path, suggested_questions=)

graphify 0.7.5 DROP (이번 plan에서 import 제거):
  graphify.detect.detect
  graphify.extract.collect_files
  graphify.extract.extract

기존 snapshot.py 기존 동작 (보존):
  KST 디렉토리: vault/graph/<YYYY-MM-DD>/
  KEEP_DATED_DIRS = 14
  dry_run=True 시 _run_graphify 스킵
  finally에서 staging 정리
</interfaces>
</context>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.1-02-01 | D (DoS) | snapshot on huge corpus | mitigate | build_extraction LIMIT 없음; 메모리 사용량을 통합 테스트에서 측정·기록 |
| T-7.1-02-02 | T (Tampering) | edges.tag → graph.json | accept | tag는 Phase 7-02가 설정; 본 plan은 read-only 통과 |
| T-7.1-02-03 | I (Info) | vault_path 노드 source_file | accept | vault 자체가 git 추적 — vault_path 노출은 의도된 동작 |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: graphify_stub 재타겟 + test_snapshot_cli.py 호환성 유지 + snapshot.py 재작성</name>
  <files>tests/graph/conftest.py, tests/graph/test_snapshot_cli.py, src/graph/snapshot.py, src/graph/window.py</files>
  <behavior>
    - graphify_stub fixture: graphify.detect / graphify.extract 모듈 stub 제거. 새로 stub해야 할 핵심은 build_from_json (extraction dict 받아서 가짜 G 반환)
    - 추가 monkeypatch: `src.graph.sql_to_graph.build_extraction` (또는 `graph.sql_to_graph.build_extraction`)을 stub하여 deterministic dict 반환 — DB 의존 제거
    - test_snapshot_cli.py의 3개 테스트(writes_3_files, prune_keeps_14, staging_cleaned_on_failure)는 그대로 PASS 유지. should_raise=True 시점이 build_extraction 또는 build_from_json 단계로 변경되어야 finally cleanup 검증 가능.
    - snapshot.py의 _run_graphify는 detect/extract 호출을 제거하고 sql_to_graph.build_extraction → build_from_json 경로로 대체. build_extraction은 SQLAlchemy engine 인자가 필요하므로 `db.engine.get_engine()` 사용 (검증된 기존 패턴).
    - window.py의 build_staging 호출은 SQL 기반 path에서 더 이상 필요 없음. 다만 14-day prune은 vault/graph/ 디렉토리 정리에 여전히 사용. 결정: build_staging 호출 자체를 제거하고 staging 디렉토리 생성/cleanup도 제거. window.py 모듈은 일단 보존(다른 곳에서 import 시 ModuleNotFoundError 방지) — 하지만 snapshot.py에서 import 제거.
  </behavior>
  <action>
1. **변경 영향 사전 확인**: `cd /mnt/c/Users/minsu/workspace/stock && grep -rn "from.*window import\|import.*window\|build_staging" src/ tests/ 2>/dev/null` 실행. snapshot.py 외에서도 import하면 그 경로도 처리. 결과를 task SUMMARY에 기록.

2. **tests/graph/conftest.py의 graphify_stub fixture 재작성** (immutable replacement, 기존 항목 삭제 후 새 fixture):
   - `graphify.detect`, `graphify.extract` 모듈 stub 제거
   - `graphify.build.build_from_json(extraction, *, directed=False)` stub은 유지하되, extraction의 nodes/edges 개수를 stub 객체에 보존 (추후 to_json이 그걸 그대로 출력하도록):
     ```python
     def _build_from_json(extraction, *, directed=False):
         _maybe_raise()
         return {"_G_": True, "directed": directed,
                 "n_nodes": len(extraction.get("nodes", [])),
                 "n_edges": len(extraction.get("edges", []))}
     ```
   - `_to_json` stub은 G 객체에서 n_nodes/n_edges를 읽어 graph.json에 보존 — test_snapshot_integration.py에서 비어있지 않음을 검증할 때 사용:
     ```python
     def _to_json(g, c, output_path, *, force=False, built_at_commit=None):
         payload = {
             "nodes": [{"id": f"n{i}"} for i in range(g.get("n_nodes", 0))],
             "links": [{"src": "x", "dst": "y", "tag": "EXTRACTED"}
                      for _ in range(g.get("n_edges", 0))],
         }
         Path(output_path).write_text(json.dumps(payload), encoding="utf-8")
     ```
   - **추가 monkeypatch**: `monkeypatch.setattr("src.graph.sql_to_graph.build_extraction", lambda engine: state.get("extraction") or {"nodes": [], "edges": [], "input_tokens": 0, "output_tokens": 0})` (state dict에 setdefault). 호출자가 `state["extraction"] = {...}`로 주입 가능하게.
   - state dict에 `should_raise`, `extraction` 두 키 노출 — 이미 should_raise가 있으니 extraction만 추가.
   - get_engine도 stub: `monkeypatch.setattr("src.graph.snapshot.get_engine", lambda: object())` (engine 자체는 build_extraction stub이 무시하므로 placeholder OK). 만약 import 위치가 다르면 정확한 경로로 patch.

3. **src/graph/snapshot.py 재작성** (`_run_graphify` 함수만 변경, 다른 함수는 보존):
   ```python
   def _run_graphify(out_dir: Path) -> None:
       """SQL→graph snapshot. Phase 7.1: replaces 0.7.5 AST extraction path
       which produced empty graph on markdown vaults (gap-1, HUMAN-UAT.md).

       Reads SQL edges + entities + documents via sql_to_graph.build_extraction;
       graphifyy detect/extract/collect_files no longer called.
       """
       # Lazy imports — keep ``graph`` group optional.
       from graphify.analyze import god_nodes, suggest_questions, surprising_connections
       from graphify.build import build_from_json
       from graphify.cluster import cluster, score_all
       from graphify.export import to_html, to_json
       from graphify.report import generate

       try:
           from src.graph.sql_to_graph import build_extraction
       except ModuleNotFoundError:
           from graph.sql_to_graph import build_extraction
       try:
           from src.db.engine import get_engine
       except ModuleNotFoundError:
           from db.engine import get_engine

       extraction = build_extraction(get_engine())

       G = build_from_json(extraction, directed=True)
       communities = cluster(G)
       cohesion = score_all(G, communities)
       gods = god_nodes(G)
       surprises = surprising_connections(G, communities)
       labels = {cid: f"Community {cid}" for cid in communities}
       member_counts = {cid: len(members) for cid, members in communities.items()}
       questions = suggest_questions(G, communities, labels)

       to_json(G, communities, str(out_dir / "graph.json"), force=True)
       to_html(G, communities, str(out_dir / "index.html"),
               community_labels=labels, member_counts=member_counts)
       report_text = generate(
           G, communities, cohesion, labels, gods, surprises,
           {"sql": "edges+entities+documents"},  # detection placeholder (graphify.report needs a dict)
           {"input": 0, "output": 0},
           "db://stock",
           suggested_questions=questions,
       )
       (out_dir / "GRAPH_REPORT.md").write_text(report_text, encoding="utf-8")
   ```

4. **snapshot 함수 본체 정리** (window.py / staging 호출 제거):
   ```python
   def snapshot(repo_root: Path, config: dict, *, dry_run: bool = False) -> Path:
       today = _today_kst()
       out_dir = repo_root / "vault" / "graph" / today
       out_dir.mkdir(parents=True, exist_ok=True)
       if not dry_run:
           _run_graphify(out_dir)
       _prune_old(out_dir.parent, KEEP_DATED_DIRS)
       return out_dir
   ```
   - **CRITICAL**: 기존 `test_staging_cleaned_on_failure`는 `vault/.graphify-staging/<date>/`가 finally cleanup으로 사라지는지 검증. SQL path에서는 staging이 아예 생성되지 않으므로, 테스트는 "staging dir이 존재하지 않는다"를 그대로 PASS — 의미가 약간 변하지만 계약은 호환. 테스트 코드 자체는 수정 없이 통과해야 함.
   - 기존 build_staging import 제거. config 인자는 미래 확장을 위해 시그니처 유지.

5. **window.py 처리**: 모듈은 보존 (다른 import 사이트 영향 최소화). 단 snapshot.py에서 import 제거. window.py 자체 단위 테스트는 그대로 둠.

6. **기존 테스트 회귀 확인**:
   `cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/graph/test_snapshot_cli.py -v` → 3 passed (실패 시 graphify_stub 재작업).

7. **회귀 보호**: `uv run pytest tests/graph/test_get_related_regression.py -v` → 2 passed 유지.

8. PROJECT.md COLL-07 재검증: `grep -E "anthropic|openai" src/graph/snapshot.py` → 0건.

9. **AST extraction 코드 완전 제거 확인**:
   `grep -E "graphify\.detect|graphify\.extract\.collect_files|from graphify\.detect|from graphify\.extract" src/graph/snapshot.py` → 0건.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/graph/test_snapshot_cli.py tests/graph/test_get_related_regression.py -v 2>&1 | tail -25 && grep -cE "graphify\.detect|graphify\.extract" src/graph/snapshot.py</automated>
  </verify>
  <done>
    - tests/graph/test_snapshot_cli.py 3 tests PASS (회귀 없음)
    - tests/graph/test_get_related_regression.py 2 tests PASS
    - grep으로 graphify.detect/extract import가 src/graph/snapshot.py에서 0건
    - graphify_stub fixture는 build/cluster/analyze/export/report만 stub
    - get_engine + sql_to_graph.build_extraction stub이 conftest에 추가됨
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 신규 통합 테스트 — 실제 SQL fixture에서 graph.json이 비어있지 않음을 자동 검증</name>
  <files>tests/graph/test_snapshot_integration.py</files>
  <behavior>
    - Test 1 (gap-1 회귀 가드): seed_edges + edges.populate 실행 후 stock graph snapshot의 in-process 호출(snapshot 함수 직접) → vault/graph/<KST_DATE>/graph.json 파싱했을 때 nodes 비어있지 않음 + links 비어있지 않음 (graphify_stub 활용 시 build_extraction이 stub되지 않도록 별도 fixture 사용 또는 state["extraction"]에 실제 build_extraction 결과 주입).
    - Test 2 (tag 보존): graph.json의 link entry에 tag 필드가 EXTRACTED/INFERRED 중 하나로 존재 (stub 또는 실제 graphify export 둘 다 동작해야 함).
    - Test 3 (graphify.detect/extract 미호출 회귀 가드): test 시작 시 sys.modules에서 graphify.detect/graphify.extract를 None으로 setattr — snapshot 호출이 ImportError를 일으키지 않으면 의존성 제거 확인.
  </behavior>
  <action>
1. `/mnt/c/Users/minsu/workspace/stock/tests/graph/test_snapshot_integration.py` 신규 작성:
   ```python
   """Phase 7.1 SC-1/SC-2 자동 검증: snapshot이 비어있지 않은 graph.json을 만든다.

   gap-1 (HUMAN-UAT.md test_id 2): graphifyy AST 경로로는 markdown vault에서
   {nodes:[], links:[]} 빈 그래프가 생성됐다. 본 테스트는 SQL 기반 경로로
   변경 후 회귀를 막는 자동 가드.
   """
   from __future__ import annotations
   import json
   import sys
   from pathlib import Path

   import pytest
   import sqlalchemy as sa
   from src.graph.snapshot import snapshot, _today_kst
   from ingest.edges import populate

   _MIN_CONFIG = {"graphify": {"raw_windows_days": {}, "mode": "deep", "directed": True}}

   def _scaffold(repo_root: Path) -> None:
       (repo_root / "vault" / "graph").mkdir(parents=True, exist_ok=True)

   def test_snapshot_produces_non_empty_graph_json(
       tmp_path, graphify_stub, seed_edges, pg_engine, monkeypatch,
   ):
       _scaffold(tmp_path)
       seeded = seed_edges(with_event_chain=True)
       with pg_engine.begin() as conn:
           populate(seeded["doc_ids"], conn)

       # graphify_stub already monkeypatches sql_to_graph.build_extraction to
       # return state["extraction"]; inject the real one so the integration
       # path exercises the actual SQL adapter.
       try:
           from src.graph.sql_to_graph import build_extraction
       except ModuleNotFoundError:
           from graph.sql_to_graph import build_extraction
       graphify_stub["extraction"] = build_extraction(pg_engine)

       # snapshot.get_engine returns a placeholder; build_extraction stub
       # ignores it and returns the prepared extraction.
       out_dir = snapshot(tmp_path, _MIN_CONFIG)
       graph_path = out_dir / "graph.json"
       assert graph_path.exists()

       payload = json.loads(graph_path.read_text(encoding="utf-8"))
       nodes = payload.get("nodes", [])
       links = payload.get("links", []) or payload.get("edges", [])
       assert len(nodes) > 0, "Phase 7.1 gap-1 regression: graph.json has no nodes"
       assert len(links) > 0, "Phase 7.1 gap-1 regression: graph.json has no links"

   def test_snapshot_does_not_import_graphify_detect_or_extract(
       tmp_path, graphify_stub, monkeypatch,
   ):
       """SC-4: graphifyy.detect/extract no longer in the snapshot path."""
       _scaffold(tmp_path)
       # Strip graphify.detect/extract from sys.modules so any residual import
       # would raise ModuleNotFoundError immediately.
       for name in ["graphify.detect", "graphify.extract"]:
           sys.modules.pop(name, None)
           monkeypatch.setitem(sys.modules, name, None)
       graphify_stub["extraction"] = {"nodes": [{"id": "n1", "type": "ticker",
                                                 "label": "X", "source_file": ""}],
                                       "edges": [], "input_tokens": 0, "output_tokens": 0}
       # Should not raise — proves the new path is graphify.detect-free.
       snapshot(tmp_path, _MIN_CONFIG)

   def test_graph_json_link_carries_tag(tmp_path, graphify_stub):
       _scaffold(tmp_path)
       graphify_stub["extraction"] = {
           "nodes": [
               {"id": "doc1", "type": "document", "label": "d1", "source_file": ""},
               {"id": "005930", "type": "ticker", "label": "삼성전자", "source_file": ""},
           ],
           "edges": [
               {"src": "doc1", "dst": "005930",
                "edge_type": "mentions_ticker", "tag": "EXTRACTED", "weight": 1.0},
           ],
           "input_tokens": 0, "output_tokens": 0,
       }
       out_dir = snapshot(tmp_path, _MIN_CONFIG)
       payload = json.loads((out_dir / "graph.json").read_text(encoding="utf-8"))
       links = payload.get("links", []) or payload.get("edges", [])
       assert links, "no links written"
       assert any(L.get("tag") in {"EXTRACTED", "INFERRED"} for L in links), \
           f"tag not preserved through to_json; got: {links}"
   ```

2. **stub 호환성 확인**: Plan 02 Task 1에서 작성한 graphify_stub의 `_to_json`이 graph 안에 들어있던 노드/엣지를 graph.json으로 그대로 흘려보내야 위 테스트가 동작. 이미 Task 1 step 2에서 그렇게 stub함 — 만약 안 됐으면 stub을 수정.

3. **실제 graphify export 호환 옵션**: stub 없이 실제 graphify를 사용해 round-trip 검증을 추가하면 더 강한 가드가 됨. graphify가 설치돼 있는지 확인하고, 가능하면 별도 테스트(@pytest.mark.skipif(graphify 미설치))로 추가:
   ```python
   @pytest.mark.skipif("not _has_graphify()", reason="graphify not installed")
   def test_snapshot_with_real_graphify_round_trip(tmp_path, seed_edges, pg_engine, monkeypatch):
       # 실제 graphify를 사용 — stub 없음. seed → populate → snapshot → graph.json 검증
       ...
   ```
   `_has_graphify` 헬퍼: `def _has_graphify(): import importlib; return importlib.util.find_spec("graphify") is not None`.

4. RED 확인: 위 3개 테스트 추가 직후 `pytest tests/graph/test_snapshot_integration.py -v`. snapshot.py 재작성이 Task 1에서 끝났다면 GREEN. 만약 stub 호환성 문제로 실패하면 stub 조정.

5. **회귀 안전망 전체 실행**: `cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/graph/ -v` → 기존 24+ 테스트 + Plan 01의 5 + 본 task의 3+ 모두 PASS.

6. SC-1/SC-2 자동화 증거: integration test 출력을 본 plan SUMMARY에 기록. 사람의 인덱스.html 시각 검증은 별도 (HUMAN-UAT 단계로 다시 라우팅).

7. CLI dry-run 회귀 확인: `cd /mnt/c/Users/minsu/workspace/stock && uv run stock graph snapshot --dry-run` → exit 0, 출력 디렉토리 생성, graph.json 미생성 (dry_run이므로) — 기존 동작 그대로.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/minsu/workspace/stock && uv run pytest tests/graph/ -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tests/graph/test_snapshot_integration.py 3 테스트 모두 PASS
    - tests/graph/ 전체 테스트 모두 PASS (Plan 01 5개 + Plan 02 신규 3개 + 기존 24개 = 32 이상)
    - test_snapshot_does_not_import_graphify_detect_or_extract가 SC-4를 자동 가드
    - test_graph_json_link_carries_tag가 SC-2를 자동 가드
    - `uv run stock graph snapshot --dry-run` 회귀 없음 (exit 0)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/graph/ tests/db/test_migration_0004.py -v` → 전체 PASS (24+ 기존 + 5 Plan01 + 3 Plan02 신규)
- `uv run pytest tests/stock_mcp/ -v` → Phase 6 회귀 0건
- `uv run stock graph snapshot --dry-run` → exit 0
- `grep -rE "graphify\.(detect|extract)" src/graph/` → 0건 (gap-1 fix 확정)
- `grep -rE "anthropic|openai" src/graph/` → 0건 (COLL-07)
</verification>

<success_criteria>
- snapshot.py가 Plan 01의 build_extraction을 통해 SQL에서 직접 그래프 생성
- graphify.detect / graphify.extract.collect_files / graphify.extract.extract import가 src/graph/snapshot.py에서 완전 제거
- 통합 테스트가 graph.json의 nodes/links 비어있지 않음을 자동 검증 (gap-1 회귀 가드)
- edges.tag(EXTRACTED/INFERRED)가 graph.json의 link entry까지 보존됨을 자동 검증
- 기존 test_snapshot_cli.py 3 + test_get_related_regression.py 2 회귀 0건
- LLM API 0건 호출 (PROJECT.md "수집·인제스트에 LLM 토큰 0" 준수)
</success_criteria>

<output>
완료 후: `.planning/phases/07.1-sql-driven-graph-and-mcp-acceleration/07.1-02-SUMMARY.md` 생성.
포함: snapshot.py before/after diff 요약, gap-1 자동 가드 테스트 출력, graphify.detect/extract 제거 grep 증거,
SC-1/SC-2 자동 검증 결과. Plan 03(MCP graph_cache)이 의존할 graph.json 형식 명시.
사람 검증 항목(index.html 시각 품질)은 phase 7.1 phase-gate으로 라우팅 — HUMAN-UAT.md에 기록.
</output>
