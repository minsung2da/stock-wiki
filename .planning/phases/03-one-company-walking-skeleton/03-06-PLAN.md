---
phase: 03-one-company-walking-skeleton
plan: 06
type: execute
wave: 5
depends_on: [03-02, 03-04, 03-05]
files_modified:
  - src/cli/__init__.py
  - src/cli/__main__.py
  - src/cli/commands.py
  - src/ingest/rebuild.py
  - tests/test_cli.py
  - tests/test_ingest_rebuild.py
  - tests/e2e/test_search_citation_schema.py
autonomous: false
requirements: [STORE-05]
tags: [cli, rebuild, e2e, smoke, checkpoint]

must_haves:
  truths:
    - "uv run stock --help lists: collect dart, ingest run, ingest rebuild"
    - "stock collect dart --corp-code=... --since=... writes DART filings (delegates to Plan 02 collect_dart)"
    - "stock ingest run scans the vault and ingests (delegates to Plan 04 ingest_run)"
    - "stock ingest rebuild [--force-reembed] [--dry-run] [--yes] wipes the DB via alembic downgrade base → upgrade head, then reruns ingest (D-25)"
    - "rebuild is idempotent: ingest_run → snapshot → rebuild → snapshot yields identical row counts and key columns (embedding values allowed floating variance; D-29)"
    - "dry-run prints the plan without executing and exits 0"
    - "TTY without --yes prompts 'y/n' before wipe (D-28); --yes skips prompt for CI/cron"
    - "E2E test schema: one end-to-end real-data test asserts a search response hit contains vault_path matching ^vault/raw/dart/"
  artifacts:
    - path: "src/cli/__main__.py"
      provides: "stock CLI entry via Typer or argparse"
      exports: ["main"]
    - path: "src/ingest/rebuild.py"
      provides: "rebuild_from_vault(vault_root, engine, *, force_reembed, dry_run, assume_yes) -> dict"
      exports: ["rebuild_from_vault"]
  key_links:
    - from: "src/cli/commands.py"
      to: "src/collectors/dart/__init__.py::collect_dart"
      via: "collect dart subcommand"
      pattern: "collect_dart"
    - from: "src/cli/commands.py"
      to: "src/ingest/worker.py::ingest_run"
      via: "ingest run subcommand"
      pattern: "ingest_run"
    - from: "src/ingest/rebuild.py"
      to: "alembic.command.downgrade/upgrade"
      via: "downgrade base + upgrade head (D-25)"
      pattern: "command\\.(downgrade|upgrade)"
---

<objective>
Deliver the `stock` CLI entry and the `ingest rebuild` command that proves vault-as-SoT (STORE-05). End the phase with a human-verify checkpoint: run the full pipeline against real Samsung data and confirm Claude Code answers "삼성전자 최근 공시 알려줘" with a vault_path citation (JUDGE-04 smoke).

Purpose: Completes STORE-05 (ingest rebuild from vault alone) and gives the user the command-line surface for Phase 3 demo + E2E + the daily batch later in Phase 9. The checkpoint validates JUDGE-04 end-to-end.
Output: CLI module + rebuild module + idempotence test + E2E schema test + human-verify checkpoint for the real Claude Code query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md
@.planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md
@.planning/phases/03-one-company-walking-skeleton/03-02-SUMMARY.md
@.planning/phases/03-one-company-walking-skeleton/03-04-SUMMARY.md
@.planning/phases/03-one-company-walking-skeleton/03-05-SUMMARY.md
@src/db/alembic.ini

<interfaces>
```python
# From Plan 02:
from src.collectors.dart import collect_dart   # (corp_code, since, max_docs, vault_root) -> dict

# From Plan 04:
from src.ingest.worker import ingest_run       # (vault_root, engine, force_reembed) -> dict

# From Phase 1/2:
from alembic import command
from alembic.config import Config
config = Config("src/db/alembic.ini")
command.downgrade(config, "base")
command.upgrade(config, "head")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stock CLI (collect + ingest run) + ingest rebuild with idempotence test (RED→GREEN)</name>
  <files>
    src/cli/__init__.py,
    src/cli/__main__.py,
    src/cli/commands.py,
    src/ingest/rebuild.py,
    tests/test_cli.py,
    tests/test_ingest_rebuild.py
  </files>
  <read_first>
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-25/26/27/28/29
    - src/collectors/dart/__init__.py (collect_dart signature — from Plan 02)
    - src/ingest/worker.py (ingest_run signature — from Plan 04)
    - src/db/alembic.ini (configured Alembic)
  </read_first>
  <behavior>
    - C1 test_stock_help_lists_subcommands — `uv run stock --help` stdout contains "collect", "ingest"; and `stock ingest --help` lists "run" and "rebuild"
    - C2 test_collect_dart_delegates — subprocess invocation of `stock collect dart --corp-code=00126380 --since=2026-01-01 --max-docs=5 --vault-root=<tmp>` with DART_API_KEY mocked env + collect_dart monkeypatched returns exit 0; monkeypatch shows it was called with those args
    - C3 test_ingest_run_delegates — `stock ingest run --vault-root=<tmp>` invokes ingest_run(vault_root=<tmp>, engine=get_engine()); exit 0
    - C4 test_ingest_rebuild_wipes_and_reloads — seed 3 docs in vault, run ingest once → capture snapshot (counts + document ids); run `stock ingest rebuild --yes` → capture snapshot; assert document_ids match (same content_hashes), chunks count matches, embedding_model column is EMBEDDING_MODEL_VERSION (D-29)
    - C5 test_rebuild_dry_run — --dry-run prints "Would wipe:" with numbers and "Would process:" with vault-file count; does NOT touch the DB; exit 0
    - C6 test_rebuild_refuses_without_yes_on_tty — simulate TTY via monkeypatch sys.stdin.isatty=True; call rebuild without --yes; input returns "n"; function returns {"aborted": True} without running alembic
    - C7 test_rebuild_yes_skips_prompt — with --yes, rebuild runs regardless of TTY status (D-28)
    - C8 test_force_reembed_flag — rebuild --force-reembed passes force_reembed=True to ingest_run (assert via monkeypatch)
    - R1 test_rebuild_programmatic_matches_cli — invoking rebuild_from_vault(...) directly yields same result shape as the CLI path
    - R2 test_rebuild_alembic_reset — spy on alembic.command.downgrade/upgrade to confirm: downgrade("base") called before upgrade("head") (D-25)
  </behavior>
  <action>
    1. Create src/cli/__init__.py empty.
    2. Create src/cli/commands.py:
       - def cmd_collect_dart(args) -> int: call collect_dart(corp_code=args.corp_code, since=args.since, max_docs=args.max_docs, vault_root=Path(args.vault_root)); print stats JSON; return 0.
       - def cmd_ingest_run(args) -> int: call ingest_run(Path(args.vault_root), get_engine(), force_reembed=args.force_reembed); print stats; return 0.
       - def cmd_ingest_rebuild(args) -> int: call rebuild_from_vault(Path(args.vault_root), get_engine(), force_reembed=args.force_reembed, dry_run=args.dry_run, assume_yes=args.yes); print report; return 0 if not aborted else 2.
    3. Create src/cli/__main__.py:
       - Use argparse (no new dep; Typer is optional). Build parser with subparsers: collect (→dart), ingest (→run|rebuild).
       - Shared arg --vault-root (default "vault"). collect dart: --corp-code, --since, --max-docs (int, default 100). ingest run: --force-reembed (flag). ingest rebuild: --force-reembed, --dry-run, --yes (all flags).
       - def main(argv=None) -> int: dispatch to cmd_*; returns its exit code. `if __name__ == "__main__": sys.exit(main())`.
       - [project.scripts].stock was already added in Plan 01 Task 1.
    4. Create src/ingest/rebuild.py:
       - Module docstring citing STORE-05, D-25/26/27/28/29.
       - def _snapshot(engine) -> dict: return {"documents": int count, "chunks": int count, "distinct_embedding_models": sorted list}.
       - def rebuild_from_vault(vault_root, engine, *, force_reembed=False, dry_run=False, assume_yes=False) -> dict:
         - counts = _snapshot(engine); vault_files = list((vault_root/"raw").rglob("*.md")); n_files = len(vault_files)
         - If dry_run: print structured plan {would_wipe: counts, would_process_files: n_files, embedding_model: EMBEDDING_MODEL_VERSION}; return {"dry_run": True, ...}.
         - If sys.stdin.isatty() and not assume_yes: prompt "This will wipe the DB and reprocess {n_files} vault files. Continue? [y/N]: "; if response != "y" → return {"aborted": True}.
         - Load alembic Config("src/db/alembic.ini"). command.downgrade(cfg, "base"); command.upgrade(cfg, "head").
         - stats = ingest_run(vault_root, engine, force_reembed=True)  # always force when rebuilding — D-27 (hash-match reuse is moot post-wipe but force is explicit).
         - return {"wiped": counts, "rebuilt": _snapshot(engine), "ingest_stats": stats}.
    5. Create tests/test_cli.py (C1-C8) — use subprocess via `python -m src.cli ...` with PYTHONPATH or call main() directly with an argv list. Monkeypatch collect_dart, ingest_run, rebuild_from_vault to stubs that record arguments.
    6. Create tests/test_ingest_rebuild.py (R1, R2, plus overlap with C4 for idempotence):
       - Uses pg_clean fixture.
       - R1 creates 2 vault files, runs ingest_run, snapshots, calls rebuild_from_vault(assume_yes=True), snapshots; asserts equality of {document_ids sorted, chunks_count}.
       - R2 patches alembic.command.downgrade and upgrade with Mock; asserts call order.
       - Mark as slow if Embedder is used; prefer fake embedder monkeypatch for speed.
  </action>
  <verify>
    <automated>uv run stock --help &amp;&amp; uv run --group mcp --group ingest --group collectors --group db --group dev pytest tests/test_cli.py tests/test_ingest_rebuild.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - uv run stock --help exits 0 and stdout contains "collect" and "ingest"
    - uv run stock ingest --help stdout contains "run" and "rebuild"
    - grep -c 'def cmd_collect_dart\|def cmd_ingest_run\|def cmd_ingest_rebuild' src/cli/commands.py == 3
    - grep -n 'command.downgrade\|command.upgrade' src/ingest/rebuild.py >= 2 matches
    - grep -n 'isatty\|assume_yes' src/ingest/rebuild.py >= 2 matches (D-28 interactive + --yes)
    - grep -n '--dry-run\|dry_run' src/cli/__main__.py src/ingest/rebuild.py >= 2 matches
    - grep -c 'def test_' tests/test_cli.py tests/test_ingest_rebuild.py >= 10
    - All tests pass
  </acceptance_criteria>
  <done>
    CLI shipped with collect + ingest run + ingest rebuild. rebuild is idempotent end-to-end against the pg_clean fixture. --dry-run and --yes semantics tested. D-25/26/27/28/29 all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E schema test + tool output contract verification (automated part of JUDGE-04)</name>
  <files>
    tests/e2e/__init__.py,
    tests/e2e/test_search_citation_schema.py
  </files>
  <read_first>
    - src/stock_mcp/tools/search.py (search tool signature from Plan 05)
    - src/stock_mcp/search_core.py (hybrid_search)
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Open Questions Q3 (E2E split: pytest asserts schema, human asserts Claude answer)
  </read_first>
  <behavior>
    - E1 test_full_pipeline_collect_ingest_search (marked slow + e2e) — if DART_API_KEY env set: call collect_dart for 00126380 since 2026-01-01 max_docs=3 to tmp_vault; call ingest_run on it; call hybrid_search(query="삼성전자 최근 공시", top_k=3); assert >= 1 hit; assert hit.vault_path matches `^vault/raw/dart/` (actually starts with tmp_vault/raw/dart since we redirected vault_root — accept both patterns); assert hit.excerpt starts with `<vault_excerpt ` (D-17); assert every hit has doc_id 64 hex chars.
    - E2 test_schema_without_live_api (fast) — seed 2 fake DART-shaped vault files in tmp_vault, ingest, hybrid_search; assert vault_path in result starts with tmp_vault/raw/dart and `<vault_excerpt ` wrap is present (JUDGE-04 schema contract, no live API required).
  </behavior>
  <action>
    1. Create tests/e2e/__init__.py empty.
    2. Create tests/e2e/test_search_citation_schema.py with E1 (marked slow + e2e, skip if DART_API_KEY unset) and E2 (fast, required). Use existing pg_clean + vault seeding helpers.
    3. Register an `e2e` pytest marker via conftest.py or pyproject.toml [tool.pytest.ini_options] markers. Add "slow", "db", "e2e" if not already defined.
  </action>
  <verify>
    <automated>uv run --group mcp --group ingest --group collectors --group db --group dev pytest tests/e2e/test_search_citation_schema.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - [ -f tests/e2e/test_search_citation_schema.py ]
    - grep -c 'def test_' tests/e2e/test_search_citation_schema.py >= 2
    - grep -n '<vault_excerpt ' tests/e2e/test_search_citation_schema.py prints at least one line (JUDGE-04 + D-17)
    - Fast test E2 passes; E1 passes when DART_API_KEY set or is skipped cleanly
  </acceptance_criteria>
  <done>
    Schema-level E2E test proves the search tool returns vault_path + `<vault_excerpt>`-wrapped excerpt against seeded data without requiring DART_API_KEY. Live-API test available behind env-gate for manual smoke.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Claude Code answers "삼성전자 최근 공시 알려줘" with a vault path citation (JUDGE-04 final proof)</name>
  <what-built>
    End-to-end pipeline: DART collector → LLM-less ingest (bge-m3 + mecab-ko BM25) → hybrid search (pgvector + VectorChord-BM25 + RRF k=60) → FastMCP stdio `search` tool wrapped in `<vault_excerpt>` delimiters. Plans 01-06 have all delivered their automated tests green. The final human verification confirms Claude Code, using the registered `.mcp.json`, returns an answer that cites a real vault path.
  </what-built>
  <how-to-verify>
    Run these commands from the project root in order:

    1. Ensure `.env` contains a valid `DART_API_KEY` (register at https://opendart.fss.or.kr if needed).

    2. Confirm Postgres is running:
       ```
       docker compose ps postgres
       ```
       Expect: stock-postgres healthy.

    3. Pre-download the bge-m3 model (one-time, ~2.3GB):
       ```
       uv run --group ingest python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-m3'); print('ok')"
       ```
       Expect: "ok" after download.

    4. Collect real Samsung filings:
       ```
       uv run stock collect dart --corp-code=00126380 --since=2025-04-17 --max-docs=50
       ```
       Expect: stats dict with succeeded > 0; `ls vault/raw/dart/` shows new Markdown files.

    5. Run ingest:
       ```
       uv run stock ingest run
       ```
       Expect: stats with succeeded > 0; `docker exec stock-postgres psql -U stockwiki -d stockwiki -c "SELECT COUNT(*) FROM chunks"` returns > 0.

    6. Verify heartbeat:
       ```
       cat vault/ingested/_status/heartbeat.md
       ```
       Expect: sources.dart.last_success + sources.ingest.last_success both set to recent timestamps.

    7. Start a new Claude Code session in this project (the `.mcp.json` at repo root will auto-load `stock-mcp`). If the session was already open, restart it so MCP servers reload.

    8. In the Claude Code session, ask: `삼성전자 최근 공시 알려줘`

    9. Confirm the response includes at least one citation of the form `vault/raw/dart/YYYY/...md` and the excerpt shown is wrapped in `<vault_excerpt>` markers (or the content clearly traces to one of the collected DART filings). Copy the transcript into `.planning/phases/03-one-company-walking-skeleton/03-06-CLAUDE-TRANSCRIPT.md` for the SUMMARY.

    If step 9 fails (no citation, or Claude says "cannot find stock-mcp"): check `.planning/logs/stock-mcp-<today>.log` stderr output; the structured JSON log will indicate which layer (DB / embed / BM25 / response size) produced the error code. Do not mark the phase green until step 9 passes.
  </how-to-verify>
  <files>.planning/phases/03-one-company-walking-skeleton/03-06-CLAUDE-TRANSCRIPT.md</files>
  <action>
    Human operator performs the 9-step procedure in the how-to-verify block above. After Claude Code returns an answer, the operator saves the transcript to 03-06-CLAUDE-TRANSCRIPT.md and confirms via the resume-signal.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-one-company-walking-skeleton/03-06-CLAUDE-TRANSCRIPT.md &amp;&amp; grep -E 'vault/raw/dart/' .planning/phases/03-one-company-walking-skeleton/03-06-CLAUDE-TRANSCRIPT.md</automated>
  </verify>
  <done>
    Transcript file exists with a real `vault/raw/dart/YYYY/...md` citation captured from a live Claude Code session using the registered stock-mcp server. JUDGE-04 proven end-to-end.
  </done>
  <resume-signal>
    Reply with "approved — transcript saved" once the Claude Code response includes a `vault/raw/dart/...md` citation. Or describe what went wrong (attach stderr log snippet) so we can iterate.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → collectors / ingest | argparse validates types; paths resolved; no eval |
| rebuild → alembic DDL | Alembic commands are library calls, not shell; still destructive — gate via TTY prompt + --yes |
| E2E test → real DART API | Optional behind env var; network egress rate-limited by dart-fss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-18 | Tampering (accidental data wipe) | ingest rebuild | mitigate | D-28: TTY prompt requires explicit "y"; --yes flag documented; --dry-run preview |
| T-3-19 | DoS (long-running rebuild) | rebuild_from_vault | accept | Per-doc transactions isolate failure; stats captured for heartbeat; user can Ctrl-C between docs |
| T-3-03 | Info disclosure | CLI error path | mitigate | Exceptions print type + truncated message to stderr; DART_API_KEY never in argparse help or error |
| T-3-20 | Supply chain | alembic | accept | Already in Phase 2 deps; version pinned; bundled migration scripts trusted |
</threat_model>

<verification>
- All Task 1 + Task 2 automated tests green
- Task 3 checkpoint: user confirms Claude Code cites a vault/raw/dart/ path on the Samsung query
- Phase gate automated run: `uv run pytest tests/ -m 'not slow and not e2e' -xvs` green; `uv run pytest tests/ -m 'slow or e2e' -xvs` green when DART_API_KEY set and HF cache warm
</verification>

<success_criteria>
- STORE-05: `stock ingest rebuild --yes` wipes and rebuilds the DB from vault alone; row counts match the prior ingest run (D-29 idempotence)
- CLI `stock collect dart`, `stock ingest run`, `stock ingest rebuild` all functional
- E2E schema test proves `<vault_excerpt>` wrap + vault_path citation without requiring live API
- Human checkpoint confirms JUDGE-04 end-to-end: Claude Code answers with a vault_path citation
</success_criteria>

<output>
After completion, create `.planning/phases/03-one-company-walking-skeleton/03-06-SUMMARY.md` including: full `stock --help` output, rebuild idempotence evidence (snapshot diff), captured Claude Code transcript (the real question and real cited vault path), and a final phase-level checklist confirming all 20 requirement IDs met.
</output>
