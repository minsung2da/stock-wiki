---
phase: 03-one-company-walking-skeleton
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/collectors/__init__.py
  - src/collectors/dart/__init__.py
  - src/collectors/dart/client.py
  - src/collectors/dart/fetcher.py
  - src/collectors/dart/writer.py
  - src/ingest/heartbeat.py
  - src/ingest/__init__.py
  - src/shared/frontmatter.py
  - tests/test_collect_dart.py
  - tests/test_heartbeat.py
autonomous: true
requirements: [COLL-01, COLL-06, COLL-08, COLL-09]
tags: [collectors, dart, heartbeat, vault, provenance]

must_haves:
  truths:
    - "Running `collect_dart(corp_code='00126380', since='2026-01-01')` writes A+B filings to `vault/raw/dart/YYYY/{rcept_no}_{corp_code}.md`"
    - "Written files carry provenance-only frontmatter with trust_level='trusted' (D-19) and no `_derived` block with non-empty values"
    - "Re-running with the same args skips files whose content_hash is unchanged (idempotent, COLL-08)"
    - "Collector never imports `anthropic` or `openai` (COLL-06/COLL-07 respected)"
    - "`vault/ingested/_status/heartbeat.md` records last_run / last_success / last_failure / docs_processed per source after each collector run (COLL-09)"
  artifacts:
    - path: "src/collectors/dart/__init__.py"
      provides: "collect_dart(corp_code, since, max_docs, vault_root) -> dict"
      exports: ["collect_dart"]
    - path: "src/collectors/dart/client.py"
      provides: "dart-fss client wrapper with API-key init + retry"
    - path: "src/collectors/dart/fetcher.py"
      provides: "list_ab_filings(corp_code, since, max_docs) + fetch_body(filing) helpers"
    - path: "src/collectors/dart/writer.py"
      provides: "write_filing_to_vault(filing, body, vault_root) atomic Markdown write"
    - path: "src/ingest/heartbeat.py"
      provides: "record_source_run(source, stats) — atomic heartbeat update"
    - path: "src/shared/frontmatter.py"
      provides: "ProvenanceBlock extended with trust_level: Literal['trusted','semi_trusted','adversarial']"
      contains: "trust_level"
  key_links:
    - from: "src/collectors/dart/__init__.py"
      to: "src/ingest/heartbeat.py::record_source_run"
      via: "import at end of collect_dart to record stats"
      pattern: "record_source_run"
    - from: "src/collectors/dart/writer.py"
      to: "src/shared/frontmatter.py::write_frontmatter"
      via: "atomic vault write reuses Phase 1 primitive"
      pattern: "write_frontmatter"
    - from: "src/collectors/dart/__init__.py"
      to: "src/shared/content_hash.py::compute_content_hash"
      via: "skip already-written files whose body hash matches expected"
      pattern: "compute_content_hash"
---

<objective>
Ship the DART collector as the first and only Phase 3 source. It reads from the DART API via dart-fss, writes minimal-frontmatter Markdown to `vault/raw/dart/`, and records a heartbeat. No LLM, no DB writes — pure vault/network boundary (Pattern 1 Collector Boundary).

Purpose: Makes COLL-01/06/08/09 green and produces the real input data (삼성전자 A+B filings, ~40 docs) that Plans 04/05/06 will ingest and search.
Output: Working `collect_dart` function + 4 helper modules + heartbeat helper + extended ProvenanceBlock with trust_level + two passing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md
@.planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md
@.planning/phases/03-one-company-walking-skeleton/03-01-SUMMARY.md
@src/shared/frontmatter.py
@src/shared/content_hash.py

<interfaces>
<!-- From src/shared/frontmatter.py (existing): -->
```python
class ProvenanceBlock(BaseModel):
    source: str                   # dart | naver | news | macro | krx | note | kind
    source_id: str | None
    source_url: str | None
    date: str | None              # YYYY-MM-DD
    fetched_at: datetime | None
    content_hash: str | None
    corp_code: str | None         # DART 8-digit
    ticker: str | None            # KRX 6-digit
    lang: str = "ko"
    # TASK 1 ADDS: trust_level: Literal["trusted","semi_trusted","adversarial"] = "trusted"

class FrontMatter(BaseModel):
    provenance: ProvenanceBlock
    ingest_state: IngestStateBlock = Field(default_factory=IngestStateBlock)
    derived: DerivedBlock = Field(default_factory=DerivedBlock, alias="_derived")

def write_frontmatter(path: str, model: FrontMatter, body: str) -> None: ...
def read_frontmatter(path: str) -> tuple[FrontMatter, str]: ...
```

<!-- From src/shared/content_hash.py (existing): -->
```python
def compute_content_hash(path: str) -> str: ...       # sha256 hex of normalized body
def normalize_body(body: str) -> str: ...             # CRLF→LF, rstrip lines, trailing \n
```

<!-- Canonical probe findings from Plan 01 (READ probe-findings.md for actual shape): -->
<!-- Expected dart-fss accessor: filing.report.to_dict() OR dart.api.filings.get_document(rcept_no). -->
<!-- Use whichever Plan 01 probe recorded as working; fall back to BeautifulSoup-stripped HTML. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProvenanceBlock.trust_level + heartbeat helper (RED→GREEN)</name>
  <files>
    src/shared/frontmatter.py,
    src/ingest/__init__.py,
    src/ingest/heartbeat.py,
    tests/test_heartbeat.py
  </files>
  <read_first>
    - src/shared/frontmatter.py (extend ProvenanceBlock; preserve populate_by_name=True)
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-19 (trust_level enum values)
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 8 Heartbeat
  </read_first>
  <behavior>
    - Test 1: `test_provenance_trust_level_default_trusted` — ProvenanceBlock(source='dart') → trust_level == 'trusted'
    - Test 2: `test_provenance_trust_level_roundtrips_yaml` — a FrontMatter with provenance.trust_level='semi_trusted' serializes to YAML with `trust_level: semi_trusted` and round-trips back via read_frontmatter
    - Test 3: `test_provenance_trust_level_rejects_unknown` — ProvenanceBlock(source='dart', trust_level='bogus') raises ValidationError
    - Test 4: `test_heartbeat_creates_file_on_first_run` — tmp vault; record_source_run('dart', {'total': 3, 'succeeded': 3, 'skipped': 0, 'failed': []}) creates `vault/ingested/_status/heartbeat.md` with frontmatter.sources.dart = {last_run, last_success, docs_processed: 3}
    - Test 5: `test_heartbeat_preserves_other_sources_on_update` — record first for 'dart', then for 'krx' (stub); both sources remain present with distinct timestamps
    - Test 6: `test_heartbeat_records_failure_on_failed_docs` — record_source_run('dart', {'total': 2, 'succeeded': 1, 'skipped': 0, 'failed': [{'doc': 'x', 'error': 'boom'}]}) populates last_failure but keeps prior last_success from the previous run (when one existed)
    - Test 7: `test_heartbeat_atomic_write_survives_crash` — monkeypatch os.replace to raise; record_source_run raises; assert heartbeat.md either unchanged (if it existed) or absent (if first call), and NO `.tmp` leftovers in `_status/`
  </behavior>
  <action>
    1. Edit `src/shared/frontmatter.py`:
       - Change `from typing import ...` to import `Literal`.
       - Add `trust_level: Literal["trusted", "semi_trusted", "adversarial"] = "trusted"` to ProvenanceBlock as the final field. Update module docstring Zone 1 note: "trust_level gates prompt-injection defense (D-19)."
       - Do NOT touch write_frontmatter / read_frontmatter semantics.
    2. Create `src/ingest/__init__.py` as an empty module (only if missing).
    3. Create `src/ingest/heartbeat.py` with:
       - Module docstring citing D-15, COLL-09, Pattern 8, Pitfall 6 (atomic write).
       - `HEARTBEAT_PATH_DEFAULT = Path("vault/ingested/_status/heartbeat.md")` — parameterizable per test.
       - Function signature: `def record_source_run(source: str, stats: dict, heartbeat_path: Path | None = None) -> None`. Defaults to HEARTBEAT_PATH_DEFAULT.
       - Implementation: parse existing file with PyYAML directly (not FrontMatter model — the "sources" key is outside the Pydantic schema), update the `sources` dict, write via tempfile+os.replace in the SAME directory as target (atomic on POSIX — mirrors Phase 1 pattern). Use `datetime.now(timezone.utc).isoformat()`. Do NOT call write_frontmatter (the sources dict lives at frontmatter top-level, not under provenance — keep it a separate YAML key).
       - Heartbeat file body is literally `"<!-- auto-generated; do not edit -->\n"`.
       - If `stats.get("failed")` (truthy list/int): record last_failure=now; last_success stays as whatever it was (or absent).
       - If no failure: record last_success=now; last_failure stays as whatever it was.
       - Always: last_run=now, docs_processed=stats.get("succeeded", 0).
    4. Create `tests/test_heartbeat.py` with the 7 tests above. Use `tmp_path` fixture. Call `read_frontmatter` for round-trip tests; load heartbeat.md via `yaml.safe_load` for the sources-dict assertions (since `sources` is outside FrontMatter's Pydantic schema).
  </action>
  <verify>
    <automated>uv run pytest tests/test_heartbeat.py tests/test_frontmatter.py -xvs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'trust_level: Literal' src/shared/frontmatter.py` prints one line
    - `grep -n 'trusted' src/shared/frontmatter.py` >= 2 matches (Literal values)
    - `[ -f src/ingest/heartbeat.py ]` and `grep -c 'def record_source_run' src/ingest/heartbeat.py` == 1
    - `grep -c 'tempfile\|os.replace' src/ingest/heartbeat.py` >= 2 (atomic write)
    - `grep -c 'def test_' tests/test_heartbeat.py` >= 7
    - All 7 heartbeat tests pass; existing `tests/test_frontmatter.py` (Phase 1) still passes (backward compat: default trust_level='trusted')
  </acceptance_criteria>
  <done>
    ProvenanceBlock carries trust_level with default 'trusted' and three valid values. heartbeat.py provides atomic record_source_run. All tests green; Phase 1 frontmatter tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DART collector (client + fetcher + writer + orchestration) with idempotent upsert (RED→GREEN)</name>
  <files>
    src/collectors/__init__.py,
    src/collectors/dart/__init__.py,
    src/collectors/dart/client.py,
    src/collectors/dart/fetcher.py,
    src/collectors/dart/writer.py,
    tests/test_collect_dart.py
  </files>
  <read_first>
    - src/shared/frontmatter.py (updated ProvenanceBlock from Task 1)
    - src/shared/content_hash.py (compute_content_hash, normalize_body)
    - src/ingest/heartbeat.py (record_source_run)
    - .planning/phases/03-one-company-walking-skeleton/03-RESEARCH.md §Pattern 1 + Code Examples dart-fss
    - .planning/phases/03-one-company-walking-skeleton/03-CONTEXT.md D-01, D-02, D-03, D-04
    - .planning/phases/03-one-company-walking-skeleton/probe-findings.md (Plan 01 Task 2 recorded the working dart-fss body accessor — use that verbatim)
  </read_first>
  <behavior>
    - Test 1: `test_collects_ab_filings_writes_files` — mock dart_fss.get_corp_list() and filing list to return 2 fake filings (rcept_no='20260101000001', rcept_dt='20260115', pblntf_ty='A' and 'B'); run `collect_dart(corp_code='00126380', since='2026-01-01', vault_root=tmp_path)`; assert 2 files exist at `tmp_path/raw/dart/2026/20260101000001_00126380.md` and siblings; assert each file's frontmatter has source='dart', corp_code='00126380', trust_level='trusted', non-null content_hash
    - Test 2: `test_max_docs_cap_respected` — mock 200 filings; call with max_docs=100; assert exactly 100 files written (D-03)
    - Test 3: `test_only_ab_filing_types` — mock a 3-filing list (A, B, C); collect_dart uses `pblntf_ty=["A","B"]` in its search_filings call (assert via mock call args), NOT C/D (D-01)
    - Test 4: `test_idempotent_rerun_skips_unchanged` — run collect_dart twice with the same mocked filings; assert second run's returned stats['skipped'] equals count of first run's stats['succeeded'] and no new writes occurred (mtime unchanged) (COLL-08)
    - Test 5: `test_changed_body_is_rewritten` — run once, then mutate body and re-mock; second run's stats['succeeded'] >= 1 (not skipped) since content_hash differs
    - Test 6: `test_collector_records_heartbeat` — after a successful run, `vault/ingested/_status/heartbeat.md` has sources.dart.last_success set and docs_processed == stats['succeeded']
    - Test 7: `test_collector_records_heartbeat_on_failure` — mock fetch_body to raise for one filing out of 2; collect_dart catches per-filing, writes the good one, heartbeat records last_failure populated (COLL-08 per-source isolation)
    - Test 8: `test_ci_import_guard_passes` — grep `anthropic\|openai` in src/collectors/dart/ returns no matches (COLL-06, reuses Phase 1 CI discipline)
  </behavior>
  <action>
    1. Create `src/collectors/__init__.py` (empty marker).
    2. Create `src/collectors/dart/client.py`:
       - Module docstring citing COLL-06, D-01.
       - `def get_client() -> None` — calls `dart_fss.set_api_key(api_key=os.environ["DART_API_KEY"])`. Raises a typed `CollectorConfigError("DART_API_KEY not set")` if env var absent. Memoize via module-level `_initialized = False` flag so repeated calls are cheap. Import `dart_fss` lazily inside the function so test modules can monkeypatch.
       - `def find_corp(corp_code: str)` — thin wrapper around `dart_fss.get_corp_list().find_by_corp_code(corp_code)` for mocking.
    3. Create `src/collectors/dart/fetcher.py`:
       - `def list_ab_filings(corp_code: str, since: str, max_docs: int) -> list` — resolves corp via client.find_corp, calls `corp.search_filings(bgn_de=since.replace('-',''), pblntf_ty=["A","B"], last_reprt_at="Y")[:max_docs]`. since is 'YYYY-MM-DD'; dart-fss wants 'YYYYMMDD'.
       - `def fetch_body(filing) -> str` — uses the accessor path recorded in probe-findings.md (fallback: `filing.report.to_dict().get("text", "")`). Wrap in `tenacity.retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=0.3, max=2.0), reraise=True)` for transient network errors. Return empty string (not None) if body is genuinely empty.
    4. Create `src/collectors/dart/writer.py`:
       - `def vault_path_for(filing, corp_code: str, vault_root: Path) -> Path` — returns `vault_root/raw/dart/YYYY/{rcept_no}_{corp_code}.md` where YYYY is `filing.rcept_dt[:4]` (dart-fss returns 'YYYYMMDD' string).
       - `def write_filing(filing, body: str, corp_code: str, ticker: str | None, vault_root: Path) -> tuple[Path, str]`:
         - builds FrontMatter with ProvenanceBlock(source='dart', source_id=filing.rcept_no, source_url=f'https://dart.fss.or.kr/dsaf001/main.do?rcpNo={filing.rcept_no}', date=f'{filing.rcept_dt[:4]}-{filing.rcept_dt[4:6]}-{filing.rcept_dt[6:8]}', fetched_at=datetime.now(timezone.utc), corp_code=corp_code, ticker=ticker, lang='ko', trust_level='trusted') — **do NOT f-string into SQL** (this is URL construction, which is fine, but constant-template the URL to be explicit).
         - content_hash is set to None before write, then the writer computes `compute_content_hash(path)` after the atomic write and does a second write with the hash filled in. OR compute hash on `normalize_body(body)` directly before write (simpler) — pick the second approach; cite content_hash.py's normalize_body.
         - Ensures parent dir exists (`mkdir(parents=True, exist_ok=True)`).
         - Returns (path, content_hash).
    5. Create `src/collectors/dart/__init__.py` exporting `collect_dart`:
       ```python
       def collect_dart(
           corp_code: str,
           since: str,
           max_docs: int = 100,
           vault_root: Path = Path("vault"),
       ) -> dict:
           """Collect DART A+B filings for a single corp into vault/raw/dart/."""
       ```
       Implementation:
         - call client.get_client()
         - fetch corp via client.find_corp(corp_code); extract ticker via `corp.stock_code`
         - filings = fetcher.list_ab_filings(corp_code, since, max_docs)
         - stats = {"total": len(filings), "succeeded": 0, "skipped": 0, "failed": []}
         - For each filing:
           - path = writer.vault_path_for(filing, corp_code, vault_root)
           - if path.exists(): compute current hash; fetch body; compute new hash on normalize_body(body). If equal → stats["skipped"] += 1; continue
           - Otherwise: try fetcher.fetch_body(filing) + writer.write_filing(...). On exception, append {"doc": str(path), "error": str(e)} to stats["failed"] and continue to the next filing (per-source isolation, COLL-08).
         - At end: call record_source_run("dart", stats, heartbeat_path=vault_root/"ingested/_status/heartbeat.md")
         - Return stats
    6. Create `tests/test_collect_dart.py`:
       - Fixture `mock_dart_fss(monkeypatch)` that patches `src.collectors.dart.client.get_client` to no-op and `src.collectors.dart.client.find_corp` + `src.collectors.dart.fetcher.list_ab_filings` + `src.collectors.dart.fetcher.fetch_body` with test stubs.
       - Create a simple `_FakeFiling(rcept_no, rcept_dt, pblntf_ty)` dataclass for fixtures.
       - 8 tests from <behavior>. Keep each test under 30 lines.
    7. Run `grep -r "anthropic\|openai" src/collectors/` — must print nothing. Do NOT add a runtime check; this is a CI-guard responsibility already covered by Phase 1.
  </action>
  <verify>
    <automated>uv run --group collectors --group ingest --group dev pytest tests/test_collect_dart.py -xvs &amp;&amp; ! grep -rE 'import (anthropic|openai)|from (anthropic|openai)' src/collectors/ src/ingest/ 2&gt;&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def collect_dart' src/collectors/dart/__init__.py` == 1
    - `grep -n 'pblntf_ty=\["A", *"B"\]' src/collectors/dart/fetcher.py` prints at least one line (D-01)
    - `grep -n 'max_docs' src/collectors/dart/__init__.py` >= 2 matches (default + usage, D-03)
    - `grep -n 'record_source_run' src/collectors/dart/__init__.py` prints at least one line (COLL-09)
    - `grep -n "trust_level='trusted'\|trust_level=\"trusted\"" src/collectors/dart/writer.py` prints at least one line (D-19)
    - `grep -n 'compute_content_hash\|normalize_body' src/collectors/dart/writer.py` prints at least one line (COLL-08 dedup key)
    - `grep -rE '(import|from) (anthropic|openai)' src/collectors/ src/ingest/` prints nothing (COLL-06/07)
    - `grep -c 'def test_' tests/test_collect_dart.py` >= 8
    - All 8 collector tests + all 7 heartbeat tests pass
  </acceptance_criteria>
  <done>
    `collect_dart(corp_code, since, max_docs, vault_root)` writes A+B filings as minimal-frontmatter Markdown with trust_level='trusted', content-hash-keyed idempotency, per-filing isolation, and heartbeat recording. CI import guard green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DART API → collector | Untrusted text body crosses here; will reach MCP callers in Plan 05 |
| vault/raw/dart/ | Write boundary — path construction must resist traversal |
| Heartbeat file | Atomic write required — partial writes break Obsidian rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03 | Information Disclosure | client.get_client error path | mitigate | Env-var-only API key read; error raises CollectorConfigError without the key value; no log statement contains `os.environ["DART_API_KEY"]` |
| T-3-05 | Race / corruption | heartbeat.py atomic write | mitigate | tempfile+os.replace in same directory (POSIX atomic); tests include monkeypatched-os.replace failure + .tmp leftover assertion |
| T-3-12 | Path traversal | writer.vault_path_for | mitigate | rcept_no is 14 digits from dart-fss (asserted via _FakeFiling shape); corp_code is 8 digits (validated by CollectorConfigError → resolve_entity pattern); no user-supplied string reaches Path construction |
| T-3-13 | Supply-chain (dart-fss) | fetch_body | accept | dart-fss is the locked library (CLAUDE.md); pinned via dep group; response shape de-risked by Plan 01 probe |
</threat_model>

<verification>
- `uv run pytest tests/test_collect_dart.py tests/test_heartbeat.py tests/test_frontmatter.py -xvs` green
- Smoke test (manual, if DART_API_KEY set locally): `DART_API_KEY=$(cat ~/.secrets/dart_key) uv run python -c "from pathlib import Path; from src.collectors.dart import collect_dart; print(collect_dart('00126380', '2026-01-01', max_docs=5, vault_root=Path('vault')))"` writes 5 real filings and prints stats dict
- `ls vault/ingested/_status/heartbeat.md` exists after smoke
- Phase 1 CI import guard test still passes
</verification>

<success_criteria>
- COLL-01: DART A+B filings land in `vault/raw/dart/YYYY/` with minimal frontmatter
- COLL-06: provenance-only frontmatter; no LLM calls; zero anthropic/openai imports in collectors/
- COLL-08: Idempotent upsert on content-hash match; per-filing failures isolated
- COLL-09: Heartbeat file records last_run/success/failure/docs_processed per source atomically
- All 15 new tests green; Phase 1 tests still green
</success_criteria>

<output>
After completion, create `.planning/phases/03-one-company-walking-skeleton/03-02-SUMMARY.md` documenting: exact dart-fss body accessor used, heartbeat file schema (sources dict shape), and the collector's per-filing error-isolation behavior.
</output>
