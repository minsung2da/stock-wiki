---
phase: 05-briefing-renderer
plan: 05
type: execute
wave: 4
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - src/briefing/weekly.py
  - src/briefing/__init__.py
  - tests/briefing/test_weekly.py
autonomous: true
requirements: [SC#4]
must_haves:
  truths:
    - "generate_weekly_briefing writes a decision_cards row report_type='weekly_briefing' with source_reports:[{date,card_id} × <=7], PRE-MATERIALIZED (SC#4)"
    - "get_briefing(weekly) returns the stored row unchanged even after the 7 dailies are mutated/deleted — it only SELECTs, never recomputes (SC#4)"
    - "the weekly aggregates per-ticker NET change (start-of-week vs end-of-week stance+conviction); intra-week flip-flops (BUY->HOLD->BUY) net to no-change and are dropped (D-07)"
    - "when some dailies are missing the weekly rolls up whatever exists and records coverage: {present:N, expected:7}; missing != no-change (D-08)"
    - "weekly entries are <=10 and priority-sorted by reusing daily's DICT-keyed _priority_key over the flat net-entry dicts; the same 6-column body_md table is rendered"
  artifacts:
    - path: "src/briefing/weekly.py"
      provides: "generate_weekly_briefing — NET aggregation over <=7 daily payloads (flat entry-dicts), coverage, source_reports, pre-materialized row write"
      contains: "def generate_weekly_briefing"
      min_lines: 60
  key_links:
    - from: "src/briefing/weekly.py"
      to: "cards.store.get_daily_briefings_in_range"
      via: "load the <=7 daily payloads for the week"
      pattern: "get_daily_briefings_in_range"
    - from: "src/briefing/weekly.py"
      to: "cards.store.save_briefing"
      via: "pre-materialized weekly_briefing row (entries computed once)"
      pattern: "save_briefing"
---

<objective>
Build the weekly roll-up: aggregate the week's <=7 daily briefings into a per-ticker NET-change
digest, PRE-MATERIALIZE the entries into a `report_type='weekly_briefing'` row, and record
coverage. Reads never recompute — `get_briefing(weekly)` just SELECTs the stored row (SC#4). A
genuine digest, not a 7-day union: intra-week flip-flops net to no-change (D-07), and missing
dailies are rolled up best-effort with a coverage count (D-08).

Purpose: SC#4 + D-07/D-08. The weekly reuses the daily's locked FLAT entries[] schema (stance +
conviction per entry) and the DICT-keyed `_priority_key` — since it reads entries back from
`payload["entries"]` JSONB there is no DecisionCard object (that would violate SC#4 no-recompute),
so all aggregation and sorting operate on plain dicts.
Output: src/briefing/weekly.py::generate_weekly_briefing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-briefing-renderer/05-RESEARCH.md
@.planning/phases/05-briefing-renderer/05-PATTERNS.md
@.planning/phases/05-briefing-renderer/05-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The store source (added in 05-02) -->
```python
store.get_daily_briefings_in_range(engine, week_start, week_end) -> list[BriefingRow]  # ordered by report_date
# each BriefingRow.payload["entries"] is the LOCKED daily FLAT-dict schema:
#   {ticker, name|null, event_class, change, evidence, stance, conviction, why_now, why_not, card_id}
```

<!-- Reuse from daily.py (05-03) — do NOT reimplement. _priority_key is DICT-KEYED (operates on the
     flat entry-dicts above), so it applies unchanged to the net-entry dicts weekly builds. -->
```python
from .daily import render_body_md, _priority_key, load_held_tickers
# _priority_key(entry: dict, held_tickers) -> tuple  — reads entry["ticker"]/["event_class"]/["conviction"]
# NO .card / attribute access anywhere (there is no DecisionCard object at weekly-sort time)
```

<!-- source_reports + coverage payload shapes (05-RESEARCH §Weekly Roll-Up lines 269-292) -->
# payload = {
#   "entries": [...net per-ticker FLAT dicts (same schema as daily)...],
#   "source_reports": [{"date": "2026-07-07", "card_id": "brief_daily_2026-07-07"}, ...<=7],
#   "coverage": {"present": N, "expected": 7, "missing_dates": [...]},
# }

<!-- BriefingRow to build (05-02) — report_type='weekly_briefing', report_date=week_end, expires_at=week_end+7d -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Weekly NET aggregation — per-ticker start/end diff, flip-flop drop, coverage (D-07/D-08)</name>
  <files>src/briefing/weekly.py, tests/briefing/test_weekly.py</files>
  <behavior>
    - a ticker BUY(start)->HOLD->BUY(end) nets to no-change and is DROPPED (D-07)
    - a ticker HOLD(start)->SELL(end) is RETAINED with change='HOLD→SELL' and intra_week_events counted
    - 5 present / 2 missing dailies -> coverage {present:5, expected:7} and the 2 missing dates listed; a missing day is NOT treated as no-change (D-08)
    - conviction move beyond a small threshold with same stance is retained; within threshold is dropped
    - each surviving net entry is a FLAT dict matching the daily entries[] schema (so _priority_key sorts it unchanged)
  </behavior>
  <read_first>
    - src/briefing/daily.py (the LOCKED FLAT entries[] schema each daily payload carries; render_body_md + the DICT-keyed _priority_key to reuse)
    - src/cards/store.py (get_daily_briefings_in_range — after 05-02)
    - 05-RESEARCH.md §"Weekly Roll-Up" (lines 269-292 — the 6-step NET algorithm + coverage + source_reports shapes; A6 note that dailies carry stance+conviction)
    - 05-CONTEXT.md D-07/D-08
  </read_first>
  <action>
    Create src/briefing/weekly.py with `_KST = ZoneInfo("Asia/Seoul")`. Implement
    `aggregate_net(daily_rows, *, week_start, week_end) -> tuple[list[dict], dict]` returning the NET
    per-ticker entries as FLAT dicts (the SAME locked schema as daily entries[] — NOT ChangeEvent objects,
    so the reused _priority_key sorts them directly) plus a coverage dict. Group all `payload["entries"]`
    across the <=7 daily rows by ticker; order each ticker's entries by the owning row's report_date.
    start_state = earliest entry's (stance, conviction); end_state = latest entry's (stance, conviction).
    NET change = `start_stance != end_stance` OR `abs(end_conviction - start_conviction) >= _CONVICTION_DELTA`
    (define `_CONVICTION_DELTA = 0.10` as a module constant — a small, decomposable threshold, Veto #4
    spirit). DROP tickers where start_state == end_state within threshold (flip-flops net out — D-07); the
    daily rows preserve the detail. intra_week_events = count of that ticker's daily entries. Build each
    surviving entry as a FLAT dict reusing the daily schema keys: ticker, name, event_class (carried from
    the latest daily entry), change=f"{start_stance}→{end_stance}" (or a conviction-move note),
    stance/conviction = end_state, evidence/why_now/why_not carried from the latest daily entry, card_id,
    plus intra_week_events. coverage = `{"present": len(daily_rows), "expected": 7, "missing_dates": [d for
    d in week_dates if d not present]}` (D-08 — roll up whatever exists; missing != no-change; never block
    on <7).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/briefing/test_weekly.py::test_net_change -x` exits 0 (flip-flop dropped; genuine start!=end retained).
    - `uv run pytest tests/briefing/test_weekly.py::test_coverage -x` exits 0 (5/7 present -> coverage {present:5, expected:7}; 2 missing dates listed).
    - each surviving net entry is a flat dict with keys ticker/event_class/conviction (so `_priority_key(entry, held)` sorts it without attribute access).
    - a missing daily is NOT recorded as a no-change ticker.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/briefing/test_weekly.py::test_net_change tests/briefing/test_weekly.py::test_coverage -x</automated>
  </verify>
  <done>aggregate_net computes per-ticker NET change over the week as flat entry-dicts, drops intra-week flip-flops (D-07), and records best-effort coverage (D-08).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generate_weekly_briefing orchestrator + pre-materialize + no-recompute (SC#4)</name>
  <files>src/briefing/weekly.py, src/briefing/__init__.py, tests/briefing/test_weekly.py</files>
  <behavior>
    - generate_weekly_briefing writes a weekly_briefing row with payload.source_reports listing the <=7 dailies' {date, card_id}
    - the weekly entries are computed ONCE at generation (pre-materialized); the row's body_md is the 6-column table
    - get_briefing(weekly) returns the stored entries unchanged after the source dailies are deleted/mutated (proves no recompute — SC#4)
    - weekly entries are <=10 and priority-sorted by reusing daily's DICT-keyed _priority_key on the flat net-entry dicts
  </behavior>
  <read_first>
    - src/briefing/weekly.py (Task 1 aggregate_net)
    - src/briefing/daily.py (render_body_md, the DICT-keyed _priority_key, load_held_tickers, next-KST-close helpers to reuse)
    - src/mcp_v2/tools/briefing.py (the get_briefing tool wired in 05-04 — test_no_recompute calls it)
    - src/cards/store.py (get_daily_briefings_in_range, save_briefing, get_briefing_row)
    - src/briefing/models.py (BriefingRow)
    - 05-RESEARCH.md §"Weekly Roll-Up" (report_date=week_end, expires_at=week_end+7d) + §"Common Pitfalls" Pitfall 4 (recompute-on-read)
    - 05-PATTERNS.md §"src/briefing/weekly.py"
  </read_first>
  <action>
    Implement `generate_weekly_briefing(week_anchor, *, engine=None, held_tickers=None) -> BriefingRow`:
    derive week_start/week_end from week_anchor (week_end = the anchor the caller queries with; document the
    convention). Load `rows = store.get_daily_briefings_in_range(engine, week_start, week_end)`. Call
    aggregate_net(rows, week_start=..., week_end=...) -> (net_entries, coverage) where net_entries are FLAT
    dicts. Priority-sort net_entries by reusing daily's DICT-keyed `_priority_key`:
    `sorted(net_entries, key=lambda e: _priority_key(e, held))` (held via load_held_tickers) and truncate to
    10 — no `.card` access, since net_entries are plain dicts. Build payload = {"entries": net_entries,
    "source_reports": [{"date": r.report_date.isoformat(), "card_id": r.card_id} for r in rows],
    "coverage": coverage}. body_md = render_body_md(net_entries) (reuse the daily 6-column table; empty-week
    case writes a short line). Build BriefingRow(card_id=f"brief_weekly_{week_end.isoformat()}",
    report_type='weekly_briefing', report_date=week_end, generated_at=now(_KST), as_of=week_end,
    expires_at=week_end+7d, payload=payload, body_md=...). Persist via store.save_briefing. Append
    `from .weekly import generate_weekly_briefing` to src/briefing/__init__.py. Add the test_no_recompute
    test: generate a weekly, then DELETE/mutate the source daily rows, then call the 05-04-wired
    `get_briefing(week_end.isoformat(),'weekly')` MCP tool and assert the returned entries are byte-identical
    to what was stored (the read is a pure SELECT — SC#4). NO recomputation path exists on read.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/briefing/test_weekly.py -x` exits 0 (incl test_no_recompute + net_change + coverage).
    - test_no_recompute: after deleting the source dailies, `get_briefing(week_end,'weekly')` still returns the same entries (proves pre-materialization; requires 05-04's wired get_briefing).
    - payload.source_reports lists <=7 {date, card_id} pointers; payload.coverage present.
    - `uv run python -c "from briefing import generate_weekly_briefing; print('exported')"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/briefing/test_weekly.py -x</automated>
  </verify>
  <done>generate_weekly_briefing writes a pre-materialized weekly_briefing row (source_reports + coverage + <=10 dict-sorted NET entries); the 05-04-wired get_briefing(weekly) reads it without recomputing (SC#4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored daily payloads → weekly aggregation → weekly row | derived digest aggregated and re-persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | get_daily_briefings_in_range week-range binds | mitigate | parameterized store constant (`:s`/`:e` binds, Veto #7) — the weekly module emits no SQL of its own |
| T-05-05-02 | Tampering | recompute-on-read drift | mitigate | entries are PRE-MATERIALIZED at generation; `get_briefing(weekly)` is a pure SELECT (SC#4) — test_no_recompute enforces byte-stability after source deletion |
| T-05-05-03 | Info-disclosure | weekly entries content | mitigate | entries derived from already-wrapped daily entries (no raw narrative body); returned via the Briefing entries-only projection (Veto #13) |
| T-05-05-SC | Tampering | package installs | accept | zero new deps (05-RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `uv run pytest tests/briefing -x` green (weekly + daily + store + diff/priority/render).
- `uv run pytest` full suite green (testcontainers Postgres) before verify-work.
- test_no_recompute proves the weekly is pre-materialized and read through the 05-04-wired get_briefing (SC#4).
</verification>

<success_criteria>
generate_weekly_briefing produces a pre-materialized weekly_briefing row with per-ticker NET change
(flip-flops dropped, D-07), best-effort coverage (D-08), source_reports pointers, and <=10
dict-sorted entries; the read never recomputes (SC#4).
</success_criteria>

<output>
Create `.planning/phases/05-briefing-renderer/05-05-SUMMARY.md` when done.
</output>
