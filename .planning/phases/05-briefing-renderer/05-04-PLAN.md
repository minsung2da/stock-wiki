---
phase: 05-briefing-renderer
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/mcp_v2/tools/briefing.py
  - tests/mcp_v2/test_briefing_wired.py
  - tests/mcp_v2/test_portfolio_briefing.py
autonomous: true
requirements: [SC#5]
must_haves:
  truths:
    - "get_briefing('date','daily') reads the daily_briefing row and returns found=True with populated entries; an empty DB returns found=False (SC#5)"
    - "get_briefing delegates the SELECT to cards.store.get_briefing_row — it emits NO text() itself (SC#3 AST guard stays green)"
    - "get_briefing returns entries ONLY; the Briefing model has no body_md field, so the full table never reaches context (Veto #13)"
    - "an invalid type raises InvalidArgument; the date param is validated as ISO-8601 before it reaches the report_date bind (ASVS V5)"
  artifacts:
    - path: "src/mcp_v2/tools/briefing.py"
      provides: "wired get_briefing delegating to store.get_briefing_row; maps daily->daily_briefing, weekly->weekly_briefing"
      contains: "get_briefing_row"
    - path: "tests/mcp_v2/test_briefing_wired.py"
      provides: "DB-backed wired tests (found=True + entries; no body leak) replacing the Phase-3 honest-empty guard"
  key_links:
    - from: "src/mcp_v2/tools/briefing.py"
      to: "cards.store.get_briefing_row"
      via: "delegate (no inline text() — SC#3)"
      pattern: "store\\.get_briefing_row"
    - from: "get_briefing"
      to: "Briefing model"
      via: "entries-only projection (no body_md field)"
      pattern: "entries"
---

<objective>
Wire the Phase-3 honest-empty `get_briefing` MCP tool to actually read briefing rows (SC#5).
Map the public `type` to the stored `report_type`, validate the `date` param, delegate the SELECT
to `cards.store.get_briefing_row` (the tool emits no SQL of its own — the SC#3 AST guard forbids
non-constant `text()` in src/mcp_v2), and return the structured `entries` only (Veto #13).

Purpose: makes the daily/weekly rows readable through the locked 10-tool surface without adding a
`body_md` field to `Briefing` (filter-before-context). Swaps the now-obsolete honest-empty guard
test for a DB-backed wired test — an EXPECTED replacement, not a regression.
Output: wired src/mcp_v2/tools/briefing.py + tests/mcp_v2/test_briefing_wired.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-briefing-renderer/05-RESEARCH.md
@.planning/phases/05-briefing-renderer/05-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The delegate pattern to mirror (card.py:20-22 docstring contract + 30-31 imports + 72-90 body) -->
```python
# This module performs NO direct SQL — all DB access is delegated to cards.store.
from cards import store
from db.engine import get_engine
card = store.get_active(get_engine(), corp_code)
if card is None:
    return CardView(corp_code=corp_code, found=False, card=None)
```

<!-- Current honest-empty body to REPLACE (briefing.py:26-46). Keep the _VALID_TYPES guard. -->
```python
def get_briefing(date: str, type: str = "daily") -> Briefing:
    if type not in _VALID_TYPES:
        raise InvalidArgument("type must be 'daily' or 'weekly'")
    return Briefing(date=date, type=type, found=False, entries=[])
```

<!-- Briefing result model (mcp_v2/models.py:223-233) — do NOT add a body_md field (Veto #13) -->
```python
class Briefing(BaseModel):
    model_config = ConfigDict(extra="forbid")
    date: str
    type: str
    found: bool = False
    entries: list[dict] = Field(default_factory=list)
```

<!-- The store delegate this plan calls (added in 05-02) -->
```python
store.get_briefing_row(engine, report_type, report_date) -> BriefingRow | None   # BriefingRow.payload["entries"]
```

<!-- The SC#3 AST guard (test_no_run_sql_guard.py): NO text() in src/mcp_v2; registry == 10 locked names incl get_briefing -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire get_briefing to delegate to store.get_briefing_row</name>
  <files>src/mcp_v2/tools/briefing.py</files>
  <behavior>
    - type 'daily' maps to report_type 'daily_briefing'; 'weekly' -> 'weekly_briefing'
    - a malformed date (not ISO-8601) raises InvalidArgument before any DB access
    - an unknown type still raises InvalidArgument (guard preserved)
    - briefing.py contains NO text() call (all SQL stays in cards.store)
  </behavior>
  <read_first>
    - src/mcp_v2/tools/briefing.py:1-51 (the honest-empty scaffold to replace; keep _VALID_TYPES + registration line)
    - src/mcp_v2/tools/card.py:20-95 (the delegate-to-store pattern; imports `from cards import store` + `from db.engine import get_engine`)
    - src/mcp_v2/models.py:223-233 (Briefing — no body_md field, Veto #13)
    - src/mcp_v2/errors.py (InvalidArgument)
    - 05-RESEARCH.md §"get_briefing Wiring" (lines 253-267 — the 4 wiring steps + ASVS V5 date validation)
    - 05-PATTERNS.md §"src/mcp_v2/tools/briefing.py MODIFY" + §"SC#3 AST run-sql guard"
  </read_first>
  <action>
    Rewrite get_briefing keeping the `_VALID_TYPES` guard FIRST. Add `from cards import store` and
    `from db.engine import get_engine`. After the type guard, validate `date` as ISO-8601 via
    `date.fromisoformat(date)` inside try/except raising
    `InvalidArgument("date must be ISO-8601 (YYYY-MM-DD)")` (ASVS V5 — before the report_date bind). Map
    `report_type = "daily_briefing" if type == "daily" else "weekly_briefing"`. Call
    `row = store.get_briefing_row(get_engine(), report_type, parsed_date)`. Return
    `Briefing(date=date, type=type, found=row is not None, entries=(row.payload["entries"] if row is not None else []))`.
    The tool MUST contain NO `text()` (SC#3 AST guard). Do NOT add a body_md field to Briefing (Veto #13).
    Keep the `mcp.tool(annotations=ToolAnnotations(readOnlyHint=True))(get_briefing)` registration line so
    the tool count stays at the 10 locked names. Update the module docstring to reflect that Phase 5 now
    delegates (drop the "honest empty" prose so it is not misleading).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/mcp_v2/test_no_run_sql_guard.py -x` exits 0 (no text() in briefing.py; registry still exactly 10 names incl get_briefing).
    - briefing.py calls `store.get_briefing_row` and contains no `text(` substring.
    - a non-ISO date raises InvalidArgument before get_engine() is reached.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/mcp_v2/test_no_run_sql_guard.py -x</automated>
  </verify>
  <done>get_briefing delegates the SELECT to cards.store, maps type->report_type, validates the date, and returns entries only — no inline SQL, no body_md field.</done>
</task>

<task type="auto">
  <name>Task 2: DB-backed wired tests + retire the honest-empty guard</name>
  <files>tests/mcp_v2/test_briefing_wired.py, tests/mcp_v2/test_portfolio_briefing.py</files>
  <read_first>
    - tests/mcp_v2/test_portfolio_briefing.py:80-139 (the honest-empty get_briefing tests: DB-less found=False tests + test_get_briefing_does_not_reference_report_type:96-132 + the bad-type test:135-139 to KEEP)
    - src/cards/store.py (save_briefing / get_briefing_row — to seed and read)
    - src/briefing/models.py (BriefingRow — to construct a seed row)
    - tests/conftest.py:51-145 (pg_clean fixture) and tests/cards/conftest.py:101-124 (seeded_engine idiom)
    - 05-RESEARCH.md §"get_briefing Wiring" step 5 + §"State of the Art" (the EXPECTED test replacement)
  </read_first>
  <action>
    Create tests/mcp_v2/test_briefing_wired.py: seed a `daily_briefing` row via `store.save_briefing`
    (BriefingRow with payload={'entries':[{...ticker,stance,conviction,event_class...}]}, report_date=D),
    then patch/point `get_engine` at the test engine and assert `get_briefing(D.isoformat(),'daily')`
    returns found=True with the seeded entries; assert an empty DB (no row) returns found=False; assert an
    invalid type raises InvalidArgument; assert an invalid date string raises InvalidArgument. Add
    `test_no_body_leak` asserting the Briefing model has NO body_md field
    (`assert 'body_md' not in Briefing.model_fields`) so the full table never reaches context (Veto #13).
    In tests/mcp_v2/test_portfolio_briefing.py REMOVE the now-obsolete honest-empty get_briefing tests: the
    DB-less `found=False` daily/weekly tests AND `test_get_briefing_does_not_reference_report_type`
    (:96-132) — both guarded the Phase-3 no-data phase and are superseded by the wired tests (EXPECTED
    change, not a regression). KEEP `test_get_briefing_bad_type_raises_invalid_argument` (:135-139) — it
    needs no DB and still holds. If the engine cannot be injected via a fixture, use
    `monkeypatch.setattr("mcp_v2.tools.briefing.get_engine", lambda: test_engine)`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/mcp_v2/test_briefing_wired.py -x` exits 0 (found=True + entries; empty->found=False; bad type/date -> InvalidArgument; no body leak).
    - `uv run pytest tests/mcp_v2/test_portfolio_briefing.py -x` exits 0 (obsolete honest-empty get_briefing tests removed; bad-type test retained).
    - `test_no_body_leak` asserts `'body_md' not in Briefing.model_fields`.
    - `uv run pytest tests/mcp_v2 -x` green (no regression across the 10-tool surface).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/mcp_v2/test_briefing_wired.py tests/mcp_v2/test_portfolio_briefing.py -x</automated>
  </verify>
  <done>Wired DB-backed tests prove get_briefing reads a real daily_briefing row (found=True + entries) and leaks no body_md; the obsolete honest-empty guard is retired; the bad-type test stays.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client (date, type strings) → get_briefing → cards.store | two untrusted string params cross into a DB read |
| stored briefing row → MCP result | derived digest returned to the model context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | get_briefing date/type params | mitigate | `type ∈ {daily,weekly}` guard + `date.fromisoformat` validation (ASVS V5) before the report_date bind; the SELECT is a parameterized store constant (no inline text() — SC#3 AST guard) |
| T-05-04-02 | Info-disclosure | get_briefing result | mitigate | Briefing model has NO body_md field — returns `entries` only (Veto #13 filter-before-context); test_no_body_leak enforces it |
| T-05-04-03 | Tampering | prompt-injection via briefing content | mitigate | entries are DERIVED from already-wrapped card fields (no raw filing/narrative body) — no new untrusted surface |
| T-05-04-SC | Tampering | package installs | accept | zero new deps (05-RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `uv run pytest tests/mcp_v2 -x` green (wired tests + 10-tool registry + AST guard).
- briefing.py contains no `text(` (SQL stays in cards.store).
- Briefing model unchanged (no body_md field added).
</verification>

<success_criteria>
get_briefing is wired to read daily/weekly rows via the store delegate, validates its inputs, and
returns entries only (SC#5, Veto #13, SC#3 AST guard intact). Honest-empty guard retired for the
DB-backed wired test.
</success_criteria>

<output>
Create `.planning/phases/05-briefing-renderer/05-04-SUMMARY.md` when done.
</output>
