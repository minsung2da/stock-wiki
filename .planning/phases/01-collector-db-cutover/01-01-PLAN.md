---
phase: "01-collector-db-cutover"
plan: "01-01"
type: "migration"
wave: 0
depends_on: []
files_modified:
  - src/db/migrations/versions/0006_phase01_domain_tables.py
  - src/db/entity_models.py
  - tests/db/test_migration_0006.py
  - tests/conftest.py
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-5
must_haves:
  truths:
    - "Alembic head is 0006 after `uv run alembic upgrade head`"
    - "Tables `filings`, `news`, `ohlcv`, `macro_series`, `events`, `collector_runs` exist"
    - "Legacy `events` from migration 0001 is renamed to `events_legacy` (empty)"
    - "Legacy `documents`/`chunks`/`edges`/`ingest_runs` tables remain UNTOUCHED (Q3 Option B)"
    - "All five new tables enforce the dedup keys from RESEARCH.md Q2 (UPSERT-ready)"
    - "halfvec(1024) columns on filings/news are NULLable (populated Phase 3)"
  artifacts:
    - path: "src/db/migrations/versions/0006_phase01_domain_tables.py"
      provides: "Alembic migration 0006 creating 6 new tables + 1 rename"
    - path: "src/db/entity_models.py"
      provides: "SQLAlchemy ORM declarative classes for the 6 new tables"
    - path: "tests/db/test_migration_0006.py"
      provides: "Schema-shape assertions: columns, types, PK/UNIQUE/FK, indexes"
  key_links:
    - from: "filings.corp_code"
      to: "entities.corp_code"
      via: "REFERENCES entities(corp_code) ON DELETE CASCADE"
    - from: "events.filing_rcept_no"
      to: "filings.rcept_no"
      via: "REFERENCES filings(rcept_no) ON DELETE SET NULL"
    - from: "tests/conftest.py::pg_clean"
      to: "_LIVE_TABLES tuple"
      via: "TRUNCATE list updated to include the 6 new tables + events_legacy"
---

<objective>
Create Alembic migration `0006_phase01_domain_tables.py` that establishes the
v2.0 schema contract: five domain tables (`filings`, `news`, `ohlcv`,
`macro_series`, `events`) plus the observability table (`collector_runs`).
Rename the dormant `events` table from migration 0001 to `events_legacy` so the
new KIND-classifier `events` table can claim the name.

Add SQLAlchemy ORM declarative models so subsequent plans (01-03 ... 01-08)
can `INSERT` via SQLAlchemy Core/ORM with type-safe bind parameters instead of
hand-rolled SQL strings.

This plan is the **schema contract** every Wave 1/2 plan depends on. Land it
first, in isolation. No collector code changes here.

Purpose: SC-1, SC-2 cannot be satisfied without these tables existing; SC-5
requires the test fixture `_LIVE_TABLES` to know about them.

Output:
- New migration file (idempotent, testcontainer-safe)
- New ORM module (`src/db/entity_models.py`)
- Schema-shape test (`tests/db/test_migration_0006.py`)
- Updated `tests/conftest.py::_LIVE_TABLES` so `pg_clean` TRUNCATEs the new tables
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-collector-db-cutover/RESEARCH.md
@.planning/phases/01-collector-db-cutover/CONTEXT.md
@CLAUDE.md

# Existing schema baseline
@src/db/migrations/versions/0001_phase02_initial_schema.py
@src/db/entity.py
@tests/db/test_migration_0005.py
@tests/conftest.py

<interfaces>
<!-- Existing tables that the new schema interacts with — DO NOT MODIFY -->

From `src/db/migrations/versions/0001_phase02_initial_schema.py`:
```python
# entities.corp_code CHAR(8) PRIMARY KEY    -- referenced as FK from filings/news/ohlcv/events
# entity_aliases    (no UniqueConstraint on (corp_code,kind,value) — Pitfall 5: KRX ticker recycling)
# documents         (kept dormant per Q3 Option B; do NOT drop, rename, or alter)
# chunks            (kept dormant)
# edges             (kept dormant)
# events            (kept dormant — RENAMED to events_legacy in 0006)
# ingest_runs       (kept dormant)
```

From `src/db/entity.py`:
```python
@dataclass(frozen=True)
class Entity:
    corp_code: str
    canonical_name: str
    current_ticker: str | None

def resolve_entity(engine, value, as_of=None) -> Entity | None: ...
def resolve_entity_by_alias(engine, name, as_of=None) -> Entity | None: ...
def upsert_entity(engine, corp_code, canonical_name, ticker, market="KOSPI") -> None: ...
```

`resolve_entity` is the **only** entity lookup path collectors are allowed to
use (ENT-01/ENT-02). No collector in Wave 1/2 may bypass it.

From `tests/conftest.py` (line 91-99):
```python
_PHASE2_TABLES = (
    "ingest_runs", "events", "edges", "chunks", "documents",
    "entity_aliases", "entities",
)
```
This tuple drives `pg_clean` TRUNCATE; rename to `_LIVE_TABLES` and append the
six new tables plus `events_legacy`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 0006 — six new tables + events rename</name>
  <files>src/db/migrations/versions/0006_phase01_domain_tables.py</files>
  <behavior>
    The migration must, in upgrade(), perform these operations in this order:

    1. `ALTER TABLE events RENAME TO events_legacy` — preserves any FK paths
       even though the table is empty post-shutdown. Use `op.rename_table`.
       Do NOT DROP — Q3 Option B forbids destruction of legacy schema.
       Also rename any indexes on the old `events` table to keep `events_*`
       index names available for the NEW table (drop+recreate is acceptable).

    2. `CREATE TABLE filings` — per RESEARCH.md Q1 §`filings`:
       - `rcept_no CHAR(14) PRIMARY KEY`
       - `corp_code CHAR(8) NOT NULL REFERENCES entities(corp_code) ON DELETE CASCADE`
       - `ticker CHAR(6) NULL`
       - `filed_at TIMESTAMPTZ NOT NULL`
       - `report_nm TEXT NOT NULL`
       - `pblntf_ty CHAR(1) NOT NULL`
       - `event_type TEXT NULL`
       - `source_url TEXT NOT NULL`
       - `content_hash CHAR(64) NOT NULL`
       - `body_md TEXT NOT NULL` (Veto #8 — whole filing, NO pre-chunking)
       - `body_tsv tsvector NULL` (Phase 3 populates)
       - `body_embedding halfvec(1024) NULL` (Phase 3 populates; declared via
         `sa.dialects.postgresql.HALFVEC`-equivalent — if SQLAlchemy lacks
         halfvec, use `sa.Column("body_embedding", sa.dialects.postgresql.BIT)` is WRONG.
         Use `op.execute("ALTER TABLE filings ADD COLUMN body_embedding halfvec(1024)")`
         OR `sa.Column("body_embedding", sa.types.UserDefinedType)` — the
         cleanest path is a `sa.dialects.postgresql.ARRAY`-style raw SQL
         column. Confirm by mimicking how migration `0002_phase03_chunking_columns.py`
         declared `chunks.embedding vector(1024)`.)
       - `fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - `first_seen_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - `last_seen_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - Indexes: `ix_filings_corp_filed (corp_code, filed_at DESC)`,
         `ix_filings_pblntf_ty (pblntf_ty)`,
         `ix_filings_event_type (event_type) WHERE event_type IS NOT NULL`
         (partial index — use `postgresql_where=`)

    3. `CREATE TABLE news` — per RESEARCH.md Q1 §`news`:
       - `id BIGSERIAL PRIMARY KEY`
       - `url_hash CHAR(64) NOT NULL UNIQUE`
       - `url TEXT NOT NULL`
       - `outlet TEXT NOT NULL`
       - `corp_code CHAR(8) NULL REFERENCES entities(corp_code) ON DELETE SET NULL`
       - `tickers TEXT[] NOT NULL DEFAULT '{}'` — use `sa.dialects.postgresql.ARRAY(sa.Text)`
       - `published_at TIMESTAMPTZ NOT NULL`
       - `title TEXT NOT NULL`
       - `content_hash CHAR(64) NOT NULL`
       - `body_md TEXT NOT NULL` (2-paragraph cap retained — enforced at collector, not DB)
       - `body_tsv tsvector NULL`
       - `body_embedding halfvec(1024) NULL`
       - `license_flag TEXT NOT NULL DEFAULT 'summary_only'`
       - `fetched_at`, `first_seen_at`, `last_seen_at` (same pattern as filings)
       - Indexes: `ix_news_corp_pub (corp_code, published_at DESC)`,
         `ix_news_outlet (outlet, published_at DESC)`,
         `ix_news_tickers USING GIN (tickers)`
         (declare GIN via `postgresql_using="gin"`)

    4. `CREATE TABLE ohlcv` — per RESEARCH.md Q1 §`ohlcv`:
       - PK `(ticker, trade_date)`
       - `open/high/low/close NUMERIC(18,4) NOT NULL`
       - `volume BIGINT NOT NULL`
       - `trading_value BIGINT NULL` (sub-won precision; foreign_net / inst_net / retail_net likewise BIGINT)
       - `foreign_net/inst_net/retail_net BIGINT NULL`
       - `short_volume/short_balance BIGINT NULL` (T+2 lag → often NULL on fetch)
       - `corp_code CHAR(8) NULL REFERENCES entities(corp_code) ON DELETE SET NULL`
       - `fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - Indexes: `ix_ohlcv_date (trade_date DESC)`,
         `ix_ohlcv_corp (corp_code, trade_date DESC) WHERE corp_code IS NOT NULL`
       - NO body_md, NO body_embedding (Veto #6 — pure numeric, no embeddings)

    5. `CREATE TABLE macro_series` — per RESEARCH.md Q1 §`macro_series`:
       - Composite PK `(source, series_id, item_code, obs_date)`
       - `item_code TEXT NOT NULL DEFAULT ''` (FRED uses empty string; ECOS uses real value)
       - `value NUMERIC(20,6) NOT NULL`
       - `cycle CHAR(1) NOT NULL DEFAULT 'D'`
       - CHECK `source IN ('ecos','fred')`
       - Index `ix_macro_obs (obs_date DESC)`
       - NO body_md, NO embedding (Veto #6)

    6. `CREATE TABLE events` (fresh KIND-classifier table — the rename in step 1
       freed this name):
       - `id BIGSERIAL PRIMARY KEY`
       - `event_type TEXT NOT NULL`
       - `ticker CHAR(6) NOT NULL`
       - `event_date DATE NOT NULL`
       - `corp_code CHAR(8) NULL REFERENCES entities(corp_code) ON DELETE SET NULL`
       - `subtype TEXT NULL`
       - `reason TEXT NOT NULL DEFAULT ''`
       - `source TEXT NOT NULL` (CHECK `source IN ('dart','kind')`)
       - `source_id TEXT NULL` (DART rcept_no when source='dart')
       - `source_url TEXT NOT NULL`
       - `filing_rcept_no CHAR(14) NULL REFERENCES filings(rcept_no) ON DELETE SET NULL`
       - `fetched_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - UNIQUE `(event_type, ticker, event_date, source, source_id)` — note
         that `source_id` may be NULL; in Postgres NULL ≠ NULL in UNIQUE so
         duplicates with source_id=NULL won't collide. Acceptable per Q2
         (kind rows always have a source_id; only edge case is malformed
         input which the collector filters).
       - CHECK `event_type IN ('suspension','watchlist_designation','investment_caution','investment_risk','unfaithful_disclosure')`
       - Indexes: `ix_events_ticker_date (ticker, event_date DESC)`,
         `ix_events_type_date (event_type, event_date DESC)`

    7. `CREATE TABLE collector_runs` — per RESEARCH.md Q1 §`collector_runs`:
       - `id BIGSERIAL PRIMARY KEY`
       - `source TEXT NOT NULL` (CHECK `source IN ('dart','krx','news','macro','kind')`)
       - `run_at TIMESTAMPTZ NOT NULL DEFAULT now()`
       - `elapsed_ms INTEGER NOT NULL`
       - `stats JSONB NOT NULL`
       - `extra JSONB NULL`
       - Index `ix_collector_runs_source_time (source, run_at DESC)`

    downgrade(): reverse order — DROP all six new tables, then
    `ALTER TABLE events_legacy RENAME TO events`. Must be testcontainer-clean.
  </behavior>
  <action>
    Create `src/db/migrations/versions/0006_phase01_domain_tables.py` following the
    Alembic conventions used in migrations 0001-0005 (revision = "0006",
    down_revision = "0005", branch_labels = None, depends_on = None).

    For the `halfvec(1024)` columns: SQLAlchemy 2.0 has no native halfvec
    type. Two acceptable patterns (RESEARCH.md Pitfall #2 verified that
    `tensorchord/vchord-suite:pg17-latest` ships pgvector 0.8.x which
    supports halfvec):
    (a) Declare the column via `sa.Column("body_embedding", sa.types.UserDefinedType)`
        with a custom `compile_type` returning `"halfvec(1024)"`, OR
    (b) Emit raw DDL via `op.execute("ALTER TABLE filings ADD COLUMN body_embedding halfvec(1024)")`
        after the initial `create_table` that omits the column.

    Pick (b) — simpler, matches how 0001 uses `op.execute("CREATE EXTENSION IF NOT EXISTS vector")`
    pattern. Document the choice in a module docstring.

    The migration must NOT touch `documents`, `chunks`, `edges`, `ingest_runs`
    (Q3 Option B — leave dormant). Only `events → events_legacy` is renamed.

    For partial indexes use `postgresql_where=sa.text("event_type IS NOT NULL")`.

    For GIN index: `op.create_index("ix_news_tickers", "news", ["tickers"], postgresql_using="gin")`.

    The migration must be idempotent at the test container level — it will run
    against a fresh container in CI. No `IF EXISTS` shenanigans are needed for
    forward migration, but the `ALTER TABLE events RENAME` must succeed even
    if the table is empty (it will be — A1 from RESEARCH.md Assumptions Log).

    Pre-check (A1 verification): include a docstring comment that says
    "Assumption A1: legacy `events` table is empty in production. If the
    migration ever runs against a populated DB, the rename is still safe
    because `events_legacy` has no constraints pointing into the new
    `events` table."
  </action>
  <verify>
    <automated>uv run alembic upgrade head 2>&amp;1 | grep -E "(0006|head)"</automated>
    <automated>uv run pytest tests/db/test_migration_0006.py -x -v</automated>
  </verify>
  <done>
    Migration 0006 is the new Alembic head. `tests/db/test_migration_0006.py`
    (created in Task 3) passes, asserting every column + index + constraint
    matches the contract above. `alembic downgrade -1` followed by
    `alembic upgrade head` is clean (round-trip safe).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SQLAlchemy ORM models for the six new tables</name>
  <files>src/db/entity_models.py</files>
  <behavior>
    Create declarative ORM models so Wave 1/2 collectors can write
    type-safe inserts. The module exports:

    - `Base` — `sqlalchemy.orm.declarative_base()` instance
    - `Filing` (table='filings')
    - `News` (table='news')
    - `OHLCV` (table='ohlcv')
    - `MacroSeries` (table='macro_series')
    - `Event` (table='events')
    - `CollectorRun` (table='collector_runs')

    Each class declares every column from the migration with matching types.
    `__table_args__` includes the indexes and constraints (so `Base.metadata`
    is round-trippable with the migration, but DOES NOT actually create the
    tables — Alembic is the only DDL author).

    The `halfvec(1024)` columns use the same UserDefinedType trick as the
    migration; ORM rows can write NULL into them (Phase 1 never sets them).

    For `tickers TEXT[]` use `sa.Column("tickers", postgresql.ARRAY(sa.Text),
    nullable=False, server_default=sa.text("'{}'::text[]"))`.

    No `__init__` overrides; rely on SQLAlchemy default constructor.

    Existing `src/db/engine.py` and `src/db/entity.py` are not modified — this
    is purely additive.
  </behavior>
  <action>
    Write `src/db/entity_models.py`. Import pattern from the migration; use
    `sqlalchemy.orm.DeclarativeBase` (SQLAlchemy 2.0 style) and
    `sqlalchemy.dialects.postgresql` for ARRAY/JSONB.

    Define a private `_HalfVec` UserDefinedType:
    ```python
    class _HalfVec(types.UserDefinedType):
        cache_ok = True
        def __init__(self, dim: int) -> None:
            self.dim = dim
        def get_col_spec(self, **kw) -> str:
            return f"halfvec({self.dim})"
    ```
    Reuse this for both `Filing.body_embedding` and `News.body_embedding`.

    `Event.event_type`, `MacroSeries.source`, `CollectorRun.source` should be
    typed as `sa.Text` with a CheckConstraint in `__table_args__` mirroring the
    migration. This keeps Python-side validation simple while the DB still
    enforces the enum.

    Do NOT add behavior methods (no `to_dict`, no `from_pydantic`) — these
    are pure data carriers. Wave 1/2 plans can add helper functions in their
    own modules.

    Add `__all__ = ["Base", "Filing", "News", "OHLCV", "MacroSeries", "Event", "CollectorRun"]`.

    Verify no `anthropic` or `openai` import sneaks in (this file lives outside
    `src/collectors/` but the discipline is good hygiene).
  </action>
  <verify>
    <automated>uv run python -c "from db.entity_models import Base, Filing, News, OHLCV, MacroSeries, Event, CollectorRun; print(sorted(Base.metadata.tables.keys()))"</automated>
    <automated>uv run pytest tests/db/test_migration_0006.py -x -v -k orm</automated>
  </verify>
  <done>
    `src/db/entity_models.py` imports cleanly. `Base.metadata.tables` contains
    exactly the six new table names. ORM test (in Task 3) confirms each model
    column matches the live DB column type (introspection via
    `sa.inspect(engine).get_columns(table)`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write schema-shape regression test for migration 0006</name>
  <files>tests/db/test_migration_0006.py, tests/conftest.py</files>
  <behavior>
    Add `tests/db/test_migration_0006.py` modeled on
    `tests/db/test_migration_0005.py`. Tests use the session `pg_engine`
    fixture (testcontainer already runs `alembic upgrade head`) and
    introspect via `sqlalchemy.inspect`.

    Required assertions (each as a separate test function):

    `test_filings_table_shape`:
    - table exists
    - PK is `rcept_no` (CHAR(14))
    - `corp_code` is CHAR(8), NOT NULL, FK → entities.corp_code
    - `body_md` is TEXT, NOT NULL
    - `body_tsv` is tsvector, NULLABLE
    - `body_embedding` column exists; type matches "halfvec(1024)" via
      `conn.execute(text("SELECT format_type(atttypid, atttypmod) FROM pg_attribute
       WHERE attrelid = 'filings'::regclass AND attname = 'body_embedding'")).scalar()`
       must equal `"halfvec(1024)"`
    - Indexes exist: `ix_filings_corp_filed`, `ix_filings_pblntf_ty`,
      `ix_filings_event_type` (partial — verify via `pg_indexes.indexdef LIKE
       '%event_type IS NOT NULL%'`)

    `test_news_table_shape`:
    - `url_hash` has UNIQUE constraint (CHAR(64))
    - `tickers` is `_text` (Postgres array type code for TEXT[]) — assert
      via `pg_attribute.atttypid::regtype::text == '_text'` OR introspector
      reports `ARRAY` element type=Text
    - GIN index `ix_news_tickers` exists (verify `pg_indexes.indexdef LIKE
      '%USING gin%'`)
    - `license_flag` DEFAULT is `'summary_only'`

    `test_ohlcv_table_shape`:
    - composite PK `(ticker, trade_date)`
    - `open/high/low/close` are NUMERIC(18,4)
    - `volume` is BIGINT
    - NO `body_md` column (Veto #6 enforcement)
    - NO `body_embedding` column (Veto #6 enforcement)

    `test_macro_series_table_shape`:
    - composite PK `(source, series_id, item_code, obs_date)`
    - `item_code` has DEFAULT `''` (empty string)
    - CHECK constraint on `source IN ('ecos','fred')` — query
      `pg_constraint` for `conname LIKE '%source%' AND contype = 'c'`
    - NO body_md / body_embedding

    `test_events_table_shape`:
    - UNIQUE `(event_type, ticker, event_date, source, source_id)`
    - FK `filing_rcept_no → filings.rcept_no ON DELETE SET NULL`
    - CHECK on `event_type` includes all five enum values

    `test_collector_runs_shape`:
    - `stats` and `extra` are JSONB
    - Index `ix_collector_runs_source_time` exists

    `test_events_legacy_exists`:
    - The old `events` table is now `events_legacy` (verify via
      `information_schema.tables`)
    - Old `events` name is GONE from `information_schema.tables` (the new
      `events` table is the KIND classifier shape, NOT the old shape — verify
      via column set: `payload JSONB` column must NOT exist on `events`)

    `test_dormant_tables_untouched`:
    - `documents`, `chunks`, `edges`, `ingest_runs` still exist with original
      shape (introspect column count and one known column)

    `test_orm_round_trip`:
    - For each ORM model in `db.entity_models`, all column names in the model
      match column names from `inspect(engine).get_columns(table_name)`.

    Then update `tests/conftest.py`:
    - Rename `_PHASE2_TABLES` to `_LIVE_TABLES`.
    - Append the six new tables PLUS `events_legacy`:
      ```python
      _LIVE_TABLES = (
          # observability (truncate first to avoid FK cascades downstream)
          "collector_runs",
          # KIND classifier (FKs filings → must truncate before filings)
          "events",
          # body-bearing
          "news",
          "filings",
          # pure numeric
          "ohlcv",
          "macro_series",
          # legacy (Q3 Option B — empty, but keep truncate to be safe)
          "events_legacy",
          # original Phase 2 schema (kept dormant, but truncate for hygiene)
          "ingest_runs",
          "edges",
          "chunks",
          "documents",
          "entity_aliases",
          "entities",
      )
      ```
      Order matters: child tables (with FKs into the others) come first so
      `TRUNCATE ... CASCADE` doesn't blow up.
    - Keep the `_SAFE_TABLE_RE` guard. Update the `pg_clean` fixture's
      reference from `_PHASE2_TABLES` to `_LIVE_TABLES` (rename only).
  </behavior>
  <action>
    Mirror the structure of `tests/db/test_migration_0005.py`. Use the
    `pg_engine` session fixture (already runs migrations head). Each test
    opens a single read-only connection — do NOT use `pg_clean` here (these
    tests inspect schema, not data).

    For partial-index verification, query `pg_indexes`:
    ```sql
    SELECT indexdef FROM pg_indexes
    WHERE tablename = 'filings' AND indexname = 'ix_filings_event_type'
    ```
    and assert the result contains `WHERE (event_type IS NOT NULL)`.

    For halfvec verification, query `pg_attribute` joined to `pg_type` and
    assert the formatted type string equals `"halfvec(1024)"`.

    Do NOT mark these tests `slow` or `e2e`. They run in the default
    `pytest -m "not slow and not e2e"` pass.

    `tests/conftest.py` rename: this is a single-symbol rename. Find the
    only call site (the `pg_clean` fixture). No other code in the repo
    imports `_PHASE2_TABLES` directly.
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_migration_0006.py -x -v</automated>
    <automated>uv run pytest tests/db/ -x</automated>
  </verify>
  <done>
    All `test_migration_0006.py` tests pass against a fresh testcontainer.
    `pg_clean` correctly truncates the new tables — verifiable by inserting
    a row from a previous test session and checking it's gone in the next
    function-scoped test.
  </done>
</task>

</tasks>

<verification>
After all three tasks land:

1. `uv run alembic upgrade head` succeeds; head is `0006`.
2. `uv run pytest tests/db/ -x` passes all migration tests, including 0006.
3. `uv run pytest tests/db/test_migration_0006.py::test_dormant_tables_untouched`
   confirms `documents`/`chunks`/`edges`/`ingest_runs` are intact (Q3 Option B).
4. From a fresh `python -c`:
   ```python
   from db.entity_models import Base
   print(sorted(Base.metadata.tables))
   # ['collector_runs', 'events', 'filings', 'macro_series', 'news', 'ohlcv']
   ```
5. `grep -n "_PHASE2_TABLES" tests/conftest.py` returns zero matches; the
   rename is complete.
6. No `anthropic` / `openai` imports anywhere new (CI guard still passes).
</verification>

<success_criteria>
- Migration 0006 is Alembic head.
- Six new tables exist with exact column types from RESEARCH.md Q1.
- Legacy `events` → `events_legacy` rename is clean (table empty post-rename).
- Dormant `documents`/`chunks`/`edges`/`ingest_runs` untouched.
- `tests/conftest.py::_LIVE_TABLES` includes the six new tables + events_legacy,
  in TRUNCATE-safe order.
- Schema-shape regression test exists and passes.
- ORM models exist, importable, table set matches migration.
- Phase 1 Hard Vetoes baked into schema: Veto #6 (no embedding columns on
  ohlcv/macro_series/events), Veto #8 (filings.body_md is TEXT NOT NULL whole
  filing, no chunks table participation).
</success_criteria>

<output>
Create `.planning/phases/01-collector-db-cutover/01-01-SUMMARY.md` when done,
covering: migration file path, schema additions list, ORM module path, test
file path, any deviations from RESEARCH.md Q1 schema (halfvec implementation
choice, etc.), and any A1 verification result (events table population check).
</output>
