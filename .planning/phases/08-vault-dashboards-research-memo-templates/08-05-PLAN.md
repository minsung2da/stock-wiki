---
phase: 08-vault-dashboards-research-memo-templates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/frontmatter.py
  - src/ingest/parsers/__init__.py
  - src/ingest/parsers/macro.py
  - tests/ingest/parsers/test_parse_sections_dispatch.py
  - tests/ingest/parsers/test_macro.py
  - tests/shared/test_event_type_enum_drift.py
  - .planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md
autonomous: true
gap_closure: true
requirements: [DASH-03, DASH-04]
must_haves:
  truths:
    - "uv run stock ingest run 실행 시 vault/raw/dart/2026/*.md 4건이 모두 succeeded로 처리된다 (event_type validation error 0건)"
    - "uv run stock ingest run 실행 시 vault/raw/macro/{ecos,fred}/*.md 4건이 모두 succeeded로 처리된다 (unsupported source 0건)"
    - "EventType Literal과 vault/raw 실제 _derived.event_type 값 사이의 drift가 문서화되고 결정(A: enum 확장 / B: 데이터 마이그레이션)이 commit message에 기록된다"
  artifacts:
    - path: "src/ingest/parsers/macro.py"
      provides: "macro source 단일-section parser (ECOS/FRED 본문은 평문 메모 형태)"
      exports: ["parse_sections"]
    - path: "src/shared/frontmatter.py"
      provides: "EventType Literal 정렬 — 실제 vault 데이터와 드리프트 0"
      contains: "EventType = Literal["
    - path: ".planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md"
      provides: "vault/raw _derived.event_type 값 분포 조사 결과 + 결정 근거"
  key_links:
    - from: "src/ingest/parsers/__init__.py:parse_sections"
      to: "src/ingest/parsers/macro.py:parse_sections"
      via: "if source == 'macro' dispatch"
      pattern: "source == \"macro\""
    - from: "src/ingest/worker.py:process_document"
      to: "src/shared/frontmatter.py:read_frontmatter"
      via: "Pydantic FrontMatter validate"
      pattern: "DerivedBlock"
---

<objective>
GAP-03 (event_type Pydantic Literal drift) + GAP-04 (macro section parser 미지원) 해소.

Production `uv run stock ingest run` 실행 시 vault/raw 8건 모두 fail하던 문제 해결:
- DART/KRX 4건: `_derived.event_type` 값 (`disclosure`, `news_micro`)이 `EventType` Literal에 없어 Pydantic validation 실패
- macro 4건: `parse_sections`가 `dart`만 dispatch → `ValueError("unsupported source for section parsing")`

Purpose: ingest 진입 차단 해제 → 모든 다운스트림(검색, hub, dashboards events) 데이터 복구.
Output: macro parser 신설, EventType drift 결정 + 코드/테스트 정합, vault rebuild 시 8/8 succeeded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-vault-dashboards-research-memo-templates/08-VERIFICATION.md
@.planning/phases/08-vault-dashboards-research-memo-templates/08-CONTEXT.md
@src/shared/frontmatter.py
@src/ingest/parsers/__init__.py
@src/ingest/parsers/dart.py
@src/ingest/worker.py

<interfaces>
<!-- 핵심 컨트랙트 — 별도 코드 탐색 불필요 -->

src/ingest/parsers/__init__.py 현재 dispatch:
```python
def parse_sections(body: str, source: str) -> list[Section]:
    if source == "dart":
        from .dart import parse_sections as _parse
        return _parse(body, source)
    raise ValueError("unsupported source for section parsing")
```

src/shared/frontmatter.py 현재 EventType (D-08, 18 values + "other"):
```python
EventType = Literal[
    # DART 주요사항 (8): earnings_release, equity_issue, mergers_acquisitions,
    #   major_contract, board_change, ownership_change, buyback_announcement, dividend
    # DART 거래소공시 (4): suspension, watchlist_designation, unfaithful_disclosure, delisting
    # KIND (2): investment_caution, investment_risk
    # 뉴스·리포트 (4): analyst_upgrade, analyst_downgrade, macro_commentary, market_gossip
    # fallback: other
]
```

vault/raw 실제 데이터 (조사 결과 — Task 1):
- vault/raw/dart/2026/*.md (4건): event_type: "disclosure"  ← Literal 미포함
- vault/raw/krx/2026-04-24/*.md (~다수): event_type: "news_micro"  ← Literal 미포함
- vault/raw/macro/{ecos,fred}/*.md (4건): event_type 없음 (None)  ← OK, 다만 parse_sections에서 fail

src/ingest/parsers/dart.py:Section dataclass:
```python
@dataclass
class Section:
    title: str
    path: str
    text: str
    order: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vault/raw event_type 값 분포 조사 + 결정 기록 (FINDINGS.md)</name>
  <files>.planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md</files>
  <read_first>
    - vault/raw/ 전체 트리 (dart/, krx/, macro/, news/, kind/)
    - src/shared/frontmatter.py:149-175 (현재 EventType Literal)
    - src/collectors/kind/sources.py (KIND 측 event_type 정의 참고)
  </read_first>
  <action>
    1. 다음 명령으로 vault/raw 내 모든 event_type 값 추출:
       ```bash
       find vault/raw -name "*.md" -exec grep -h "^[[:space:]]*event_type:" {} \; \
         | sort | uniq -c | sort -rn
       ```
       출력을 FINDINGS.md `## 값 분포` 섹션에 그대로 기록.

    2. 각 값별로 **출처 collector** 식별:
       - `disclosure` → vault/raw/dart/ → DART 수집기 또는 Claude Schedule 산출 (확인: `grep -rn "disclosure" src/collectors/dart/` 및 `.claude/routines/enrich/`)
       - `news_micro` → vault/raw/krx/ → 의심: KRX writer 또는 Claude Schedule (확인: `grep -rn "news_micro" src/`)
       - 기타 발견값 동일 처리.

    3. **결정 (Decision A vs B):**
       - **A. EventType Literal 확장:** 실제 값을 `disclosure`, `news_micro` 등으로 추가. 장점: vault 데이터 그대로 유효. 단점: Phase 5 D-08 정규화된 enum 의도(18+1 카테고리)가 흐려짐.
       - **B. 데이터 정규화:** vault/raw의 `disclosure` → 적절한 D-08 enum (예: 일반 공시 → 가장 가까운 카테고리; news_micro → `market_gossip`)으로 마이그레이션 + collectors 출력 정규화.

       **D-08 (Phase 5 CONTEXT) 정합 우선 → 결정: B를 권고**하되, 다음 조건이면 A 채택:
       - `disclosure`/`news_micro`가 D-08의 18 카테고리 어디에도 의미적으로 매핑되지 않을 때
       - collectors가 enum을 정확히 추론하기 어려운 보수적 fallback일 때 → 새 enum 값 (예: `dart_misc`, `krx_micro_flow`) 추가가 더 정직함

    4. FINDINGS.md 결정 섹션에 다음 기록:
       - 채택 옵션 (A 또는 B)
       - 채택 이유 (D-08 정합 vs 데이터 보존 trade-off)
       - 변경 대상: EventType Literal에 추가할 값 목록 OR vault/raw + collectors 마이그레이션 스크립트 sketch
       - Task 2가 이를 그대로 구현할 수 있도록 명확한 값 목록 제공

    5. FINDINGS.md 헤더:
       ```markdown
       # GAP-03 + GAP-04 Findings

       **Investigated:** {YYYY-MM-DD}
       **Decision (GAP-03):** Option {A|B}
       **Files affected:** ...
       ```
  </action>
  <verify>
    <automated>test -f .planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md && grep -q "Decision (GAP-03): Option" .planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - FINDINGS.md 존재
    - "값 분포", "결정", "변경 대상" 3 섹션 모두 채워짐
    - 결정은 A 또는 B 중 하나로 명시 (양다리 금지)
    - Task 2 구현자가 추가/매핑할 값 목록을 코드 한 줄로 옮길 수 있는 형태
  </acceptance_criteria>
  <done>FINDINGS.md commit; Task 2가 결정에 따라 코드 변경 시작 가능</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: macro parser 신설 + EventType drift 해소 (Task 1 결정 적용)</name>
  <files>
    src/ingest/parsers/macro.py,
    src/ingest/parsers/__init__.py,
    src/shared/frontmatter.py,
    tests/ingest/parsers/test_parse_sections_dispatch.py,
    tests/ingest/parsers/test_macro.py,
    tests/shared/test_event_type_enum_drift.py
  </files>
  <behavior>
    - **test_macro.py**:
      - `parse_sections(body, "macro")` returns `[Section]` length 1 with `title="body"`, `path="body"`, `order=0`, `text=body.strip()`
      - 빈 body → `[]` (Phase 8 worker.process_private_note의 단일-section 패턴과 일관)
    - **test_parse_sections_dispatch.py**:
      - `parse_sections(body, "macro")` 정상 dispatch (no ValueError)
      - `parse_sections(body, "unknown_source")` 여전히 `ValueError("unsupported source for section parsing")` (T-3-15 info-disclosure 하드닝 보존, 정확한 메시지 일치)
      - `parse_sections(body, "dart")` 회귀 — 기존 동작 유지
    - **test_event_type_enum_drift.py** (regression guard):
      - vault/raw/**/*.md에서 모든 `_derived.event_type` 값을 추출
      - 각 값이 `EventType` Literal에 포함되는지 확인 (또는 None)
      - drift 발견 시 fail with 명확한 메시지 (어떤 파일의 어떤 값이 enum 외인지)
      - **단, Option B 채택 시:** 이 테스트는 vault rebuild 후 통과 (마이그레이션 적용 후)
  </behavior>
  <read_first>
    - .planning/phases/08-vault-dashboards-research-memo-templates/08-05-FINDINGS.md (Task 1 결정)
    - src/ingest/parsers/__init__.py (현재 dispatch)
    - src/ingest/parsers/dart.py (Section dataclass, parse_sections 시그니처 참고)
    - src/shared/frontmatter.py:149-175 (EventType 현재 정의)
    - vault/raw/macro/ecos/722Y001.md (실제 macro body 형태)
  </read_first>
  <action>
    **Step 1 — RED: 위 3개 테스트 파일 먼저 작성, fail 확인.**

    `tests/ingest/parsers/test_macro.py`:
    ```python
    from ingest.parsers import parse_sections

    def test_macro_returns_single_section():
        body = "# Macro: 기준금리\n\n2026-04-26: 2.75%\n"
        sections = parse_sections(body, "macro")
        assert len(sections) == 1
        s = sections[0]
        assert s.title == "body"
        assert s.path == "body"
        assert s.order == 0
        assert s.text == body.strip()

    def test_macro_empty_body():
        assert parse_sections("", "macro") == []
        assert parse_sections("   \n  ", "macro") == []
    ```

    `tests/ingest/parsers/test_parse_sections_dispatch.py`:
    ```python
    import pytest
    from ingest.parsers import parse_sections

    def test_macro_dispatched():
        sections = parse_sections("body text", "macro")
        assert len(sections) == 1

    def test_dart_dispatched_unchanged():
        # smoke — DART body must still parse without error
        body = "# 제목\n## 1. 본문\ncontent\n"
        sections = parse_sections(body, "dart")
        assert len(sections) >= 1

    def test_unknown_source_generic_error():
        with pytest.raises(ValueError, match=r"^unsupported source for section parsing$"):
            parse_sections("x", "unknown_source")
    ```

    `tests/shared/test_event_type_enum_drift.py`:
    ```python
    import pathlib
    import re
    import yaml
    from typing import get_args
    from shared.frontmatter import EventType

    REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]
    VAULT_RAW = REPO_ROOT / "vault" / "raw"
    ALLOWED = set(get_args(EventType)) | {None}

    def _extract_event_type(md_path: pathlib.Path) -> str | None:
        text = md_path.read_text(encoding="utf-8")
        m = re.match(r"^---\n(.*?)\n---", text, re.DOTALL)
        if not m:
            return None
        try:
            fm = yaml.safe_load(m.group(1)) or {}
        except yaml.YAMLError:
            return None
        return ((fm.get("_derived") or {}).get("event_type"))

    def test_no_event_type_drift_in_vault_raw():
        if not VAULT_RAW.exists():
            return  # vault unpopulated — CI/clone case
        offenders = []
        for p in VAULT_RAW.rglob("*.md"):
            v = _extract_event_type(p)
            if v not in ALLOWED:
                offenders.append((str(p.relative_to(REPO_ROOT)), v))
        assert not offenders, (
            f"event_type drift — values not in EventType Literal: {offenders}"
        )
    ```

    Run: `uv run pytest tests/ingest/parsers/test_macro.py tests/ingest/parsers/test_parse_sections_dispatch.py tests/shared/test_event_type_enum_drift.py -x` → 모두 RED.

    **Step 2 — GREEN (macro parser):**

    Create `src/ingest/parsers/macro.py`:
    ```python
    """Macro source parser (Phase 8 GAP-04).

    ECOS/FRED 본문은 numeric series 표 + 짧은 메모 형태. DART처럼 TOC를
    풀어내지 않고 단일 section으로 처리 (Phase 8 worker.process_private_note의
    동일한 단일-section 패턴 — chunking + embedding은 정상 동작).
    """
    from __future__ import annotations
    from .dart import Section

    __all__ = ["parse_sections"]

    def parse_sections(body: str, source: str) -> list[Section]:
        text = body.strip()
        if not text:
            return []
        return [Section(title="body", path="body", text=text, order=0)]
    ```

    Modify `src/ingest/parsers/__init__.py`:
    ```python
    def parse_sections(body: str, source: str) -> list[Section]:
        if source == "dart":
            from .dart import parse_sections as _parse
            return _parse(body, source)
        if source == "macro":
            from .macro import parse_sections as _parse
            return _parse(body, source)
        raise ValueError("unsupported source for section parsing")
    ```

    **Step 3 — GREEN (EventType drift, Task 1 결정 적용):**

    Option A 채택 시: `src/shared/frontmatter.py` EventType Literal에 Task 1이 식별한 값 추가 (예: `"disclosure"`, `"news_micro"` 등). 코멘트에 `# Phase 8 GAP-03 — vault/raw legacy values` 표기.

    Option B 채택 시:
    - EventType Literal 그대로
    - **별도 마이그레이션 스크립트 작성 금지** (이 plan 범위 외; FINDINGS.md에 후속 quick-task로 기록)
    - 대신 vault/raw 8건의 _derived.event_type 값을 직접 sed/python 스크립트로 매핑 (FINDINGS.md 매핑표 사용) — 단 1회성, commit 포함
    - collectors 측 출력 정규화: FINDINGS.md가 식별한 collector 파일에서 향후 같은 잘못된 값 안 나오도록 수정 (이 plan 범위 내, files_modified 추가 OK)

    **Step 4 — Verify:**
    ```bash
    uv run pytest tests/ingest/parsers/test_macro.py tests/ingest/parsers/test_parse_sections_dispatch.py tests/shared/test_event_type_enum_drift.py -x
    ```
    All green.

    추가로 production rebuild 시뮬레이션:
    ```bash
    uv run python -c "from src.shared.frontmatter import read_frontmatter; \
      from pathlib import Path; \
      [read_frontmatter(str(p)) for p in Path('vault/raw').rglob('*.md')]"
    ```
    → exception 없이 통과해야 함.
  </action>
  <verify>
    <automated>uv run pytest tests/ingest/parsers/test_macro.py tests/ingest/parsers/test_parse_sections_dispatch.py tests/shared/test_event_type_enum_drift.py tests/ingest/ -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 3개 신규 테스트 파일 모두 통과
    - 기존 `tests/ingest/` 회귀 0건
    - `find vault/raw -name "*.md" | xargs -I{} python -c "from src.shared.frontmatter import read_frontmatter; read_frontmatter('{}')"` 0 errors
    - `parse_sections(body, "macro")`가 ValueError 던지지 않음
    - Option A 채택 시: EventType에 추가된 값이 FINDINGS.md 명시 값과 정확히 일치
    - Option B 채택 시: vault/raw 모든 _derived.event_type이 D-08 enum 내에 매핑됨 (test_event_type_enum_drift 통과)
  </acceptance_criteria>
  <done>
    Plan 05 완료 — `uv run stock ingest run` 실행 시 vault/raw/dart/krx/macro 모든 문서가 Pydantic validation + parse_sections 양쪽 모두 통과 (실제 ingest 성공률은 Plan 06 vault_path 수정 전까지는 동일 머신에 한정).
  </done>
</task>

</tasks>

<verification>
- 3개 신규 테스트 파일 + 기존 tests/ingest/ + tests/shared/ 모두 green
- COLL-07 CI guard: `! grep -rE "^(import|from) (anthropic|openai)" src/ingest/ src/collectors/` (no match)
- Production-realistic check (수동 가능): `uv run stock ingest run --vault-root vault` 실행 시 ingest_runs.stats.failed에서 'unsupported source' 또는 'event_type' 메시지 0건
</verification>

<success_criteria>
- vault/raw 8건 (DART 4 + KRX는 분리 — Phase 4 KRX는 동일 issue 가능, FINDINGS.md에 기록 + macro 4) 모두 ingest succeeded
- EventType Literal과 vault/raw 데이터 사이 drift 0
- regression guard 테스트가 향후 동일 issue 재발 시 즉시 fail
</success_criteria>

<output>
After completion, create `.planning/phases/08-vault-dashboards-research-memo-templates/08-05-SUMMARY.md`
</output>
